# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32173452593

## Run Summary (32173452593)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-18 ~18:52Z (opencode-schedule, relay=false). Prior
relay 32109373135 was complete. This run:

1. Found the auto-updater run 32172190321 hung in "Install System
   Dependencies" (apt-get install step) for 50+ minutes (normally
   <2 min). Diagnosed as a stuck runner (no logs visible while
   in_progress). Since its plain `git push` (no pull/rebase) would
   race my own commits, and it never reached its update phase,
   I CANCELLED it (gh run cancel, confirmed cancelled) and performed
   the two pending -git package bumps MANUALLY via the repo's own
   update.sh scripts (which carry the pure-commit-SHA no-churn
   guards).
2. Bumped the two stale -git packages to current upstream main:
   - mangowm-git: a392724 -> **813acaf** (mango main, committer
     2026-08-18T15:50:33Z; gitdate 20260818155033; Version
     0.16.1+git20260818155033.813acaf)
   - niri-git: 6062844 -> **e9b215f** (niri main, committer
     2026-08-18T15:15:49Z; gitdate 20260818151549; Version
     26.04+git20260818151549.e9b215f)
3. Regenerated the niri vendor.tar.xz (38.9MB, cargo vendor) +
   cargo_config from the new commit, downloaded both new source
   tarballs, updated spec globals + .changes (one entry each),
   committed to main (f36c016, identity opencode-agent[bot]),
   pushed, synced to OBS (deleted stale tarballs, uploaded new
   tarballs/vendor/spec/changes), triggered rebuilds for both.
4. Verified OBS builds: mangowm-git + niri-git now `succeeded` on
   BOTH openSUSE_Tumbleweed and openSUSE_Slowroll (new binaries
   published). All 42 builds (21 pkgs x 2 codestreams) green.
5. Install-tested the NEW binaries in clean TW containers (twice,
   incl. independent fresh-checker pass): mangowm-git
   0.16.1+git20260818155033.813acaf-2.1.x86_64 and niri-git
   26.04+git20260818151549.e9b215f-2.1.x86_64 both install clean
   (rpm -V rc=0), mango --help exit 0, niri --version prints
   "26.04 (e9b215f)". ELF re-tear: mango links 71 libs / niri 51
   libs, 0 missing; all expected sonames present (libwlroots-0.20,
   libscenefx-0.5, libwayland-*, libseat, libdisplay-info, etc.).
6. Re-verified the OTHER 19 packages against upstream (21/21 full
   inventory sweep): all up-to-date (full table below). All 21 spec
   files still md5-MATCH local==OBS. _builddepinfo 0 error blocks.
7. Issues/PRs: 0 open. Issue #2 was closed by the prior run.
8. NOTE for next runs: the auto-updater's dependency-install step
   hung once (18:39Z 2026-08-18). If it hangs again, cancelling and
   doing the update.sh bumps manually is the working fallback - the
   update.sh scripts are pure-commit-guarded so they cannot churn.

### Version inventory sweep (21/21, THIS RUN)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api) | up-to-date |
| fluxer | 2026.818.31606 | X-Fluxer-Version header == spec | up-to-date |
| helium-browser | 0.15.5.1 | tag 0.15.5.1 | up-to-date |
| lazyvim-git | 459a4c3 | LazyVim main HEAD 459a4c3 | up-to-date |
| localsend | 1.18.0 | held (v1.18.1 = Android APKs only, no DEB) | up-to-date (hold) |
| mangowm-git | 0.16.1+git20260818155033.813acaf | mango main 813acaf | UPDATED (this run) |
| matugen | 4.2.0 | v4.2.0 | up-to-date |
| niri-git | 26.04+git20260818151549.e9b215f | niri main e9b215f | UPDATED (this run) |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 | up-to-date |
| obsidian | 1.13.7 | v1.13.7 | up-to-date |
| opencode-desktop | 1.18.18 | v1.18.18 | up-to-date |
| protonplus | 0.6.4 | v0.6.4 | up-to-date |
| rootapp | 0.9.126 | AppImage X-AppImage-Version 0.9.126 | up-to-date |
| scenefx | 0.5 | release 0.5 | up-to-date |
| stb | 20260802 | master commit 2c980bb (2026-08-02) | up-to-date |
| stoat-desktop | 1.5.2 | v1.5.2 | up-to-date |
| vesktop | 1.6.7 | v1.6.7 | up-to-date |
| wlroots | 0.20.2 | GitLab 0.20.2 (newest 0.20.x) | up-to-date |
| xwayland-satellite-git | 3bc915f | Supreeeme main 3bc915f | up-to-date |
| zen-browser | 1.21.14b | tag 1.21.14b | up-to-date |

## Bugs found and fixed this run

1. Auto-updater run 32172190321 hung in dependency install and never
   reached the update phase -> cancelled it and performed the
   mangowm-git (813acaf) + niri-git (e9b215f) bumps manually via the
   repo's update.sh scripts. The bumps are genuine upstream advances
   (both packages were behind upstream main at run start). No
   packaging bug; the two packages are now current and OBS-green.
2. No other bugs. All 21 versions correct, no churn, no missing deps.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (only the relay
   commit remains after the package commit; artifacts in /tmp/opencode).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (mangowm-git + niri-git new binaries installed from clean
   containers x2, incl. independent fresh-checker pass; ELF re-tear
   0 missing).
3. Install-test coverage ledger updated -> **PASS** (ledger below).
4. Dependency audit 100% coverage (21/21) -> **PASS** (mango/niri
   re-derived from NEW meson.build/Cargo + real-ELF ldd this run; other
   19 unchanged, local==OBS md5, builddepinfo 0 errors).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sIL,
   git log/diff, rpmspec -P, OBS _result/_builddepinfo/_binarylist,
   docker/zypper/rpm/ldd, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 32173452593 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: binaries changed for mangowm-git (813acaf) and niri-git
(e9b215f). Both re-tested from clean TW containers twice (original +
independent fresh-checker pass) using the freshly built OBS binaries
directly. All other 19 packages' binaries unchanged since their last
successful install test (prior runs tested all 21); upstream versions
re-verified this run.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| mangowm-git | 0.16.1+git20260818155033.813acaf-2.1.x86_64 | pass (direct OBS binary, clean TW; fresh-checker x2) | clean (rc=0) | mango --help exit 0 x2; ldd 71 libs, 0 missing |
| niri-git | 26.04+git20260818151549.e9b215f-2.1.x86_64 | pass (direct OBS binary, clean TW; fresh-checker x2) | clean (rc=0) | niri --version -> "26.04 (e9b215f)" x2; ldd 51 libs, 0 missing |

(All other 19 packages: last install-tested in prior runs from the
public nexus repo - binaries unchanged since, results stand; versions
re-verified this run.)

## Dependency audit (all 21)

Ground truth this run: (1) _builddepinfo (TW) covers all 21 packages,
ZERO <error> blocks; (2) mangowm-git + niri-git re-torn from the NEW
binaries this run (real-ELF ldd: 0 missing, expected sonames present);
(3) local spec == OBS for all 21 (md5); (4) upstream unchanged for the
other 19 since prior audits.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| helium-browser | ldd real ELF 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real ELF 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git @813acaf | meson.build 14 deps extracted THIS RUN; real-ELF ldd 71 libs 0 missing | all present | none | none | deps-verified |
| matugen (4.2.0) | Cargo.lock 382 crates == vendor 382 (verified this run) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git @e9b215f | Cargo.toml extracted THIS RUN; real-ELF ldd 51 libs 0 missing (libwayland, libseat, libdisplay-info, libpipewire, etc.) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson.build @5.0.0-beta.8 30 deps (prior) | all 44 BRs present | none | none | deps-verified |
| noctalia-greeter | meson.build @1.2.1 deps (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson.build @0.6.4 deps (prior) | all in spec | none | none | deps-verified |
| rootapp | AppImage ELFs ldd 0 missing (prior) | libXt6 | none | none | deps-verified |
| scenefx | meson.build @0.5 (prior) | all in spec | none | none | deps-verified |
| stb | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop | all /opt/Stoat ELFs ldd 0 missing (prior) | gtk3, mozilla-nss, libcups2, dbus-1, libsecret-1-0, xdg-utils | none | none | deps-verified |
| vesktop | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior) | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser | libxul/libtool deps; NSS symlinked (prior) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for mangowm-git + niri-git (fresh-container install x2
+ real-ELF re-tear this run). high for all others (upstream unchanged
since prior audits; binaries byte-identical; local==OBS md5).

## Commits this run

1. `f36c016` "auto-update: sync mangowm-git (813acaf) and niri-git
   (e9b215f) to latest upstream" - spec globals (commit/shortcommit/
   gitdate) + one .changes entry each. Author identity:
   opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>
   (verified after commit; no trailers).
2. Relay update (this commit).

## OBS build status

All 42 builds (21 pkgs x Tumbleweed + Slowroll) `succeeded`/`published`
at run end. mangowm-git 813acaf + niri-git e9b215f re-built and
published on BOTH codestreams after this run's sync.

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; binary
  execution + --version + ldd verified, launcher launch confirmed).
- obsidian/opencode-desktop/zen/rootapp/helium/localsend binary
  artifacts fetched by OBS `download_files` service at build time
  (verified via URL 200 + upstream release assets + spec checksums).
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- The auto-updater's hung dependency-install step was worked around
  by cancellation + manual update.sh runs; the root cause (runner
  apt-get hang) is infra-side and may recur. Next run should check
  `gh run list` for the newest auto-updater run and re-verify it
  completed rather than assuming the previous hang repeats.
- The download.opensuse.org mirror repodata lags the OBS API publish
  (old RPM filenames remained visible in the mirror listing for a
  while after publish). Install tests used the authoritative OBS API
  binaries; the mirror catches up on its own.
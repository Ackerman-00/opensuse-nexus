# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32256104161

## Run Summary (32256104161)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-19 ~13:05Z (opencode-schedule, relay=false). Prior
relay 32225920829 was complete. This run:

1. **OBS build check: all 42 builds green.** `_result?view=status`
   returned `succeeded` for every package on both openSUSE_Tumbleweed
   and openSUSE_Slowroll; `_result?view=status&code=failed&code=broken
   &code=unresolvable` returned zero packages. OBS package list ==
   local directories == README table (21/21, verified by diff). Zero
   hidden failures.
2. **Auto-updater bumps verified (commit 04e7257, 12:50Z).** The
   latest "Nexus Auto-Updater (OBS Pipeline)" run bumped fluxer to
   2026.819.105810 and niri-git to commit b0eb8ad (gitdate
   20260819083147). Both verified against upstream THIS RUN:
   - fluxer: `X-Fluxer-Version: 2026.819.105810` header from
     https://api.fluxer.app/dl/desktop/stable/linux/x64/latest/rpm
     (HTTP 200). == spec.
   - niri-git: `gh api repos/niri-wm/niri/commits/main` == b0eb8ad,
     committer date 2026-08-19T08:31:47Z == gitdate 20260819083147.
   - Both new binaries confirmed published on OBS (Tumbleweed +
     Slowroll): fluxer-2026.819.105810-3.1.x86_64.rpm and
     niri-git-26.04+git20260819083147.b0eb8ad-5.1.x86_64.rpm.
     Local spec files == OBS spec files (md5 verified).
3. **Version inventory sweep: all 21 packages up-to-date.** Evidence:
   `gh api .../releases/latest` for every GitHub release-based package
   (bibata v2.0.7, helium 0.15.5.1, matugen v4.2.0, noctalia
   v5.0.0-beta.8, protonplus v0.6.4, scenefx 0.5, stoat v1.5.2,
   vesktop v1.6.7, zen 1.21.14b, opencode v1.18.18, obsidian v1.13.7);
   `gh api .../commits/main` for the git packages (lazyvim 459a4c3,
   mango 813acaf, niri b0eb8ad, xwayland-satellite 3bc915f) with each
   gitdate cross-checked against the commit's committer date (all
   exact matches); stb master commit date 2026-08-02T06:42:09Z ->
   version 20260802 (UTC+8) matches; rootapp AppImage X-AppImage-
   Version=0.9.126 (extracted from squashfs at ELF offset 944632 this
   run) and sha256 0736926f... == spec comment (re-downloaded +
   re-extracted this run); wlroots GitLab release list -> 0.20.2
   (newest 0.20.x).
   - **localsend held at 1.18.0**: v1.18.1 release assets verified =
     Android APKs only (arm32v7/arm64v8/google-play/x64), no linux
     x86-64 .deb. Hold is correct.
   - **noctalia-greeter**: no GitHub "latest release" (404), but tag
     list confirms v1.2.1 is the newest tag == spec version; source
     URL v1.2.1.tar.gz returns 200.
   - All 13 versioned source URLs return HTTP 200 (curl -sL), incl.
     obsidian x86_64+arm64 tarballs in the v1.13.7 release assets.
   - wlroots Source0 (GitLab release download) verified: GET fetches a
     valid 701254-byte tarball (sha256 80c567d2...).
4. **No repo changes needed this run.** No spec, .changes, update.sh,
   or workflow file needed an edit. Local files == OBS files for all
   21 packages (md5 compared per file, excluding update.sh + hidden).
   The auto-updater's bumps were correct; no churn, nothing to fix.
5. **GitHub Actions health: healthy.** Latest "Nexus Auto-Updater
   (OBS Pipeline)" run 32254546607 (2026-08-19T12:47:57Z) succeeded;
   run 32224607256 (06:43Z) succeeded; all recent opencode-schedule
   runs succeeded; one cancelled run 32172190321 was superseded. The
   update-packages.yml cron is '0 */6 * * *' (every 6h), modular
   updaters with per-package failure isolation.
6. **Issues/PRs: none open** (`gh issue list` and `gh pr list` empty).
7. **Dependency audit: 100% coverage (21/21).** Changed packages this
   run (fluxer, niri-git) were RE-DERIVED from the new artifacts
   (fluxer rpm2cpio + ldd of the main ELF -> all sonames covered by
   spec Requires; niri b0eb8ad tree -> Fedora in-tree niri.spec.rpkg
   BuildRequires all present as pkgconfig() in spec, cross-checked
   against OBS `_builddepinfo` resolved list - all 21 deps resolve).
   All other upstreams unchanged (version sweep above), so prior
   audits carry forward. Ground truth: all 42 OBS builds green and
   the 18-package clean-container install below resolved every
   Requires from OSS + nexus (RC=0). See table.
8. **Install-verification sweep (fresh checker pass this run):**
   - Re-tested the two binaries that changed since the last test:
     niri-git b0eb8ad and fluxer 2026.819.105810 installed in a FRESH
     Tumbleweed container from the OBS-published RPMs (local file
     install, deps from OSS + nexus): RC=0, `rpm -V` clean, `niri
     --version` = "niri 26.04 (b0eb8ad)", fluxer launches (Electron
     starts, only a container dbus warning).
   - Fresh-checker pass over ALL 18 user-facing runtime packages: in
     a FRESH Tumbleweed container, installed from the public nexus
     repo by name (`zypper install --no-recommends <all 18>` ->
     RC=0, no "nothing provides") AND from the OBS-API-downloaded
     published binaries (all 18 RPMs -> RC=0, `rpm -V` clean for all
     18, all 16 x86_64 binaries + 2 noarch packages present).
     Smoke: niri 26.04 (b0eb8ad), matugen 4.2.0 run; GUI/Electron
     apps (fluxer, helium, obsidian, opencode-desktop, protonplus,
     rootapp, stoat-desktop, vesktop, zen-browser) present +
     executable (no display in container, documented).
   - MIRROR NOTE: one install attempt through download.opensuse.org
     redirect (mirror.umd.edu) hit transient 404s for a few Tumbleweed
     OSS files (qalculate-5.11.0-1.3, libjxl0_11, glycin-loaders)
     -> zypper RC=4. Root cause verified as mirror lag, NOT a repo
     bug: the files exist in the OSS repo index, are served by the
     primary and healthy mirrors (mirrors.ocf.berkeley.edu returns
     200), and a rerun pinned to a healthy mirror installed all 18
     packages with RC=0. The same transient lag hit the nexus repo
     mirror (aardsoft served the previous niri-git build); the OBS
     API + download.opensuse.org direct URLs serve the current
     builds. Documented as infra, not package.
9. **Validation:** `bash -n` on all update.sh scripts (previous runs;
   none changed this run). rootapp sha256 re-verified (0736926f...).
   Local==OBS md5 for all 21 packages this run.

### Version inventory sweep (21/21, THIS RUN)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api releases/latest) | up-to-date |
| fluxer | 2026.819.105810 | X-Fluxer-Version header 2026.819.105810 | up-to-date (auto-update bump verified) |
| helium-browser | 0.15.5.1 | 0.15.5.1 (gh api releases/latest) | up-to-date |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3 | LazyVim main HEAD 459a4c3 (gitdate matches) | up-to-date |
| localsend | 1.18.0 | held (v1.18.1 = Android APKs only, no linux .deb; verified assets) | up-to-date (hold) |
| mangowm-git | 0.16.1+git20260818155033.813acaf | mango main 813acaf (gitdate matches) | up-to-date |
| matugen | 4.2.0 | v4.2.0 (gh api) | up-to-date |
| niri-git | 26.04+git20260819083147.b0eb8ad | niri main b0eb8ad (gitdate matches) | up-to-date (auto-update bump verified) |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 (gh api) | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 (tag list; no gh "latest" release but newest tag) | up-to-date |
| obsidian | 1.13.7 | v1.13.7 (gh api + asset check) | up-to-date |
| opencode-desktop | 1.18.18 | v1.18.18 (gh api; asset opencode-desktop-linux-amd64.deb exists) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (gh api) | up-to-date |
| rootapp | 0.9.126 | X-AppImage-Version 0.9.126 (extracted this run); sha 0736926f == spec | up-to-date |
| scenefx | 0.5 | 0.5 (gh api tags) | up-to-date |
| stb | 20260802 | master 2026-08-02T06:42:09Z -> UTC+8 20260802 | up-to-date |
| stoat-desktop | 1.5.2 | v1.5.2 (gh api) | up-to-date |
| vesktop | 1.6.7 | v1.6.7 (gh api) | up-to-date |
| wlroots | 0.20.2 | GitLab releases 0.20.2 (newest 0.20.x) | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f | Supreeeme main 3bc915f (gitdate matches) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh api) | up-to-date |

## Bugs found and fixed this run

None in the repo. All 42 OBS builds green, all 21 versions current,
all source URLs 200, no missing deps, no open issues/PRs. The
auto-updater's fluxer/niri-git bumps (commit 04e7257) were verified
correct and already built + published on OBS. The only issue
encountered was transient download.opensuse.org mirror lag
(mirror.umd.edu 404 on a few Tumbleweed OSS files and a stale nexus
mirror) - verified as infrastructure, not a packaging bug; healthy
mirrors serve all files. No spec/.changes/update.sh/workflow edits
were needed (verified by local==OBS md5 for every package).

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (only the relay
   commit remains; artifacts in /tmp/opencode).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (the two changed binaries niri-git b0eb8ad + fluxer
   2026.819.105810 installed + smoke-tested in a fresh TW container;
   fresh-checker pass re-installed all 18 runtime packages from the
   published repo and from OBS-API binaries in fresh containers).
3. Install-test coverage ledger updated -> **PASS** (ledger below;
   every package whose binary changed since its last test - fluxer,
   niri-git - was re-tested this run).
4. Dependency audit 100% coverage (21/21) -> **PASS** (table below;
   changed packages re-derived from new artifacts, 18/18 runtime
   packages installed clean in fresh containers this run, all 42 OBS
   builds green).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL/
   -fsSL, sha256sum, unsquashfs, rpm2cpio+ldd, git log/diff/status,
   md5sum vs OBS, OBS _result, docker/zypper/rpm -V, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 32256104161 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: binaries changed for fluxer (2026.819.10042 ->
2026.819.105810) and niri-git (e9b215f -> b0eb8ad) via the
auto-updater. Both re-tested in a FRESH Tumbleweed container from the
published OBS RPMs (deps from OSS + nexus): RC=0, rpm -V clean, niri
--version = "niri 26.04 (b0eb8ad)". As a fresh-checker pass, ALL 18
user-facing runtime packages were then re-installed in a fresh
Tumbleweed container both (a) by name from the public nexus repo
(`zypper install --no-recommends <all 18>` -> RC=0, no "nothing
provides") and (b) from the OBS-API-downloaded published binaries
(all 18 RPMs -> RC=0, rpm -V clean, all binaries present). Smoke:
niri 26.04 (b0eb8ad), matugen 4.2.0 run; GUI/Electron apps present +
executable (no display in container, documented).

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| fluxer | 2026.819.105810-3.1 (published) | pass (fresh TW, RC=0) | clean | Electron launches (container dbus warning only) |
| niri-git | 26.04+git20260819083147.b0eb8ad-5.1 (published) | pass (fresh TW, RC=0) | clean | `niri 26.04 (b0eb8ad)` |
| 16 remaining runtime pkgs (bibata, helium, lazyvim-git, localsend, mangowm-git, matugen, noctalia, noctalia-greeter, obsidian, opencode-desktop, protonplus, rootapp, stoat-desktop, vesktop, xwayland-satellite, zen) | current published | pass (fresh TW, repo-by-name RC=0 + OBS-API binaries RC=0) | clean (all 18) | matugen 4.2.0 runs; GUI apps present + executable |
| scenefx, wlroots, stb | current published | n/a (library/devel packages) | n/a | consumed as BuildRequires by compositors; all 42 builds green |

## Dependency audit (all 21)

Ground truth this run: (1) all 42 OBS builds `succeeded`/`published`;
(2) 18 runtime packages installed together in fresh TW containers
(both by repo name and from OBS-API binaries) with every Requires
resolved by zypper (no missing providers, RC=0); (3) local spec ==
OBS for all 21 (md5 this run); (4) upstream unchanged for 19/21 since
prior audits (version sweep above); fluxer + niri-git changed and were
RE-DERIVED this run.

RE-DERIVATION (changed packages):
- fluxer 2026.819.105810 (prebuilt RPM): rpm2cpio + ldd of
  /usr/lib64/fluxer/fluxer -> linked sonames (glib, gobject, gio,
  nspr, nss, nssutil, smime, dbus, atk, atk-bridge, cups, cairo,
  gtk-3, pango, X11, Xcomposite, Xdamage, Xext, Xfixes, Xrandr, gbm,
  expat, xcb, xkbcommon, udev, asound, atspi + libffmpeg bundled) all
  covered by spec soname Requires; libXss/libXtst/libnotify/libuuid/
  xdg-utils/hicolor-icon-theme/at-spi2-core present for the Electron
  runtime. Install test passed -> deps complete.
- niri-git b0eb8ad (Rust/meson-free cargo build): Fedora in-tree
  niri.spec.rpkg BuildRequires (udev, gbm, xkbcommon, wayland, libinput,
  dbus-1, systemd, libseat, libdisplay-info, pipewire, pango,
  cairo-gobject, clang, mesa-libEGL) all present in spec as
  pkgconfig() BuildRequires; OBS `_builddepinfo` for niri-git resolves
  every one (glib2-devel, pango-devel, systemd-mini-devel, libudev,
  seatd-devel, etc.). Build green + install green.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer (2026.819.105810, re-derived) | ldd of main ELF this run | soname Requires | none | none | deps-verified |
| helium-browser | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real-ELF ldd 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git | meson.build deps (prior) | pkgconfig set incl. wlroots-0.20 + scenefx-0.5 | none | none | deps-verified |
| matugen (4.2.0) | Cargo.lock == vendor (prior) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (b0eb8ad, re-derived) | Fedora spec.rpkg + _builddepinfo this run | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson.build @5.0.0-beta.8 (prior) | all 44 BRs present | none | none | deps-verified |
| noctalia-greeter | meson.build @1.2.1 (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson.build @0.6.4 (prior) | all in spec | none | none | deps-verified |
| rootapp | AppImage ELFs ldd 0 missing (prior) | libXt6 + wl-clipboard etc. | none | none | deps-verified |
| scenefx | meson.build @0.5 (prior) | all in spec; provides libscenefx-0_5 | none | none | deps-verified |
| stb | header-only | c++_compiler; provides stb.pc | none | none | deps-verified |
| stoat-desktop | all /opt/Stoat ELFs ldd 0 missing (prior) | gtk3, mozilla-nss, libcups2, dbus-1, libsecret-1-0, xdg-utils | none | none | deps-verified |
| vesktop | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior); provides libwlroots-0_20 | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser | libxul/libtool deps; NSS symlinked (prior) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 21 (upstream verified per-package this run,
all 42 builds green, 18-package fresh-container install twice this
run, local==OBS md5 this run; fluxer/niri-git deps re-derived from the
new artifacts this run).

## Commits this run

1. Relay update (this commit) - run_id 32256104161, status complete.
   No package/workflow changes were needed this run; the auto-updater
   commit 04e7257 (fluxer 2026.819.105810, niri-git b0eb8ad) was
   verified correct and already built + published on OBS.

## OBS build status

All 42 builds (21 pkgs x Tumbleweed + Slowroll) `succeeded`/`published`
at run start and throughout. No failed/broken/unresolvable codes in
`_result?view=status&code=failed&code=broken&code=unresolvable`.
The auto-updater's two bumps are built + published on both codestreams
(verified binary filenames via the OBS API this run).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; binary
  presence + rpm -V + ldd verified, CLI-capable apps run).
- obsidian/opencode-desktop/zen/rootapp/helium/localsend/vesktop/
  fluxer binary artifacts fetched by OBS `download_files` service at
  build time (verified via URL 200 + upstream release assets + spec
  checksums + rootapp AppImage sha re-verified; OBS builds green).
- The transient download.opensuse.org mirror lag (mirror.umd.edu 404
  on a few Tumbleweed OSS files) is infra and out of repo control;
  confirmed healthy mirrors + the primary serve all files.
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- Dependency re-derivation for the 19 unchanged upstreams was carried
  forward from prior audits (upstream artifacts identical - verified
  by the version sweep showing no upstream change).
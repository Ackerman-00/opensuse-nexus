# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32206289058

## Run Summary (32206289058)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-19 ~01:58Z (opencode-schedule, relay=false). Prior
relay 32173452593 was complete. This run:

1. OBS build check found exactly one broken package: **fluxer** on
   BOTH openSUSE_Tumbleweed and openSUSE_Slowroll, code=broken,
   `service error: ERROR: Failed to download
   "https://api.fluxer.app/dl/desktop/stable/linux/x64/latest/rpm"`.
   All other 20 packages succeeded on both codestreams.
2. Diagnosed the fluxer failure. The api.fluxer.app CDN now answers
   the RPM URL with a **302 redirect to a short-lived signed Vultr
   object-storage URL** (X-Amz-Expires=900, host
   fluxer-downloads.ewr1.vultrobjects.com). OBS's `download_files`
   service (which runs server-side on runservice AND at build time)
   consistently fails on this fetch (5+ attempts incl. 4 x
   runservice+waitservice over ~25 min). The URL downloads fine from
   the runner via curl/wget/python, HTTP/1.1 + HTTP/2, IPv4 (IPv6
   fails on the runner too). rootapp's AppImage uses the same
   download_files service and succeeds because its URL does NOT
   redirect. Conclusion: fluxer's 302->signed-URL is not fetchable by
   OBS's download infrastructure; the fix must not depend on OBS
   fetching that URL.
3. Fixed fluxer using the repo's established binary-artifact pattern
   (same as lazyvim-git/mangowm-git/matugen source tarballs):
   - `update.sh` now downloads the RPM directly into the package dir
     (`fluxer.rpm`, gitignored), verifies it is a valid RPM
     (`rpm -qp`), and only then bumps Version/Release + writes a
     .changes entry. Pure version-guard (no churn).
   - `fluxer.spec` Source0 changed from the CDN URL to the local file
     `fluxer.rpm`.
   - `_service` removed entirely, so OBS now gets a plain rebuild
     (no runservice) and builds from the uploaded RPM.
   - `fluxer/.gitignore` added for `fluxer.rpm`.
   - Bumped Version 2026.818.230856 -> **2026.819.10042** (upstream
     X-Fluxer-Version header) with one .changes entry.
4. Committed (single clean commit 0f3360b, identity
   opencode-agent[bot], verified) and pushed to main.
5. Synced to OBS: deleted obsolete `_service`, uploaded fluxer.rpm
   (92,491,121 bytes) + spec + changes + rpmlintrc, triggered a plain
   rebuild. fluxer now `succeeded` on BOTH openSUSE_Tumbleweed and
   openSUSE_Slowroll. All 42 builds (21 pkgs x 2 codestreams) green.
6. Full 21/21 version inventory sweep vs upstream: **all packages
   up-to-date** (full table below). localsend correctly held at
   1.18.0: v1.18.1 is an Android-APKs-only release (no linux
   x86-64 .deb asset yet). All 21 specs still md5-MATCH local==OBS.
7. Dependency audit: 100% coverage (21/21, table below); all runtime
   deps resolvable. Install-verification sweep in a clean Tumbleweed
   container: **all 18 user-facing runtime packages installed
   successfully** with binaries present (zypper resolved every
   Requires from OSS + the nexus repo); scenefx/wlroots/stb are
   library packages (devel/lib variants) correctly consumed as build
   deps by mangowm-git/niri-git/noctalia/noctalia-greeter/scenefx.
8. Issues/PRs: 0 open. GitHub Actions: latest auto-updater run
   32204332240 succeeded (all phases incl. Check OBS Build Status +
   Smart Sync) at 2026-08-19T01:16:20Z.

### Version inventory sweep (21/21, THIS RUN)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api) | up-to-date |
| fluxer | 2026.819.10042 | X-Fluxer-Version header 2026.819.10042 | UPDATED (this run) |
| helium-browser | 0.15.5.1 | tag 0.15.5.1 | up-to-date |
| lazyvim-git | 459a4c3 | LazyVim main HEAD 459a4c3 | up-to-date |
| localsend | 1.18.0 | held (v1.18.1 = Android APKs only, no linux .deb) | up-to-date (hold) |
| mangowm-git | 0.16.1+git20260818155033.813acaf | mango main 813acaf | up-to-date |
| matugen | 4.2.0 | v4.2.0 | up-to-date |
| niri-git | 26.04+git20260818151549.e9b215f | niri main e9b215f | up-to-date |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 | up-to-date |
| obsidian | 1.13.7 | v1.13.7 | up-to-date |
| opencode-desktop | 1.18.18 | v1.18.18 | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (vysp3r tags/releases) | up-to-date |
| rootapp | 0.9.126 | AppImage sha 0736926f == spec sha | up-to-date |
| scenefx | 0.5 | tag 0.5 (wlrfx) | up-to-date |
| stb | 20260802 | master commit date 2026-08-02 (UTC+8) == 20260802 | up-to-date |
| stoat-desktop | 1.5.2 | v1.5.2 (stoatchat/for-desktop) | up-to-date |
| vesktop | 1.6.7 | v1.6.7 | up-to-date |
| wlroots | 0.20.2 | GitLab 0.20.2 (newest 0.20.x) | up-to-date |
| xwayland-satellite-git | 3bc915f | Supreeeme main 3bc915f | up-to-date |
| zen-browser | 1.21.14b | tag 1.21.14b | up-to-date |

## Bugs found and fixed this run

1. **fluxer OBS build broken (both codestreams)** - OBS `download_files`
   service could not fetch the RPM: api.fluxer.app now serves a
   302 redirect to a short-lived signed Vultr URL that OBS's download
   infrastructure cannot follow (works from the runner; rootapp's
   non-redirecting AppImage URL downloads fine via the same service,
   proving the redirect is the trigger). Fixed by switching fluxer to
   the repo's local-artifact pattern: update.sh downloads/verifies the
   RPM, spec Source0 = local file, `_service` removed, RPM uploaded to
   OBS directly. Version bumped to 2026.819.10042. Verified
   `succeeded` on Tumbleweed + Slowroll after rebuild.
2. No other bugs. All 21 versions correct, no churn, no missing deps.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (only the relay
   commit remains; artifacts in /tmp/opencode).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (fluxer new binary fluxer-2026.819.10042-3.1.x86_64
   installed clean in a fresh TW container this run, /usr/bin/fluxer
   present; see ledger).
3. Install-test coverage ledger updated -> **PASS** (ledger below).
4. Dependency audit 100% coverage (21/21) -> **PASS** (table below;
   all 18 runtime packages installed in one clean container this run;
   OBS _result all green).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sIL/
   -fSL, git log/diff/status, rpmspec -P, rpm -qp, OBS _result/
   _history/_meta/source listings, podman/zypper/rpm, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 32206289058 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: fluxer binary rebuilt (2026.819.10042) after the fix.
Install-tested in a fresh TW container from the public nexus repo:
fluxer-2026.819.10042-3.1.x86_64 installed clean, /usr/bin/fluxer
present. Also did a broad 18-package install sweep in one clean TW
container (all runtime packages) - all installed with binaries
present. All other packages' binaries unchanged since prior successful
install tests.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| fluxer | 2026.819.10042-3.1.x86_64 | pass (clean TW, nexus repo) | installed clean | /usr/bin/fluxer present |
| (broad sweep) bibata, helium-browser, lazyvim-git, localsend, mangowm-git, matugen, niri-git, noctalia, noctalia-greeter, obsidian, opencode-desktop, protonplus, rootapp, stoat-desktop, vesktop, xwayland-satellite-git, zen-browser | current | pass (all resolved + installed) | installed clean | binaries present (localsend -> /opt/localsend_app, stoat -> /usr/bin/stoat-desktop, zen -> /usr/bin/zen-browser) |

## Dependency audit (all 21)

Ground truth this run: (1) all 42 OBS builds `succeeded` at run end;
(2) 18 runtime packages installed together in one clean TW container
with every Requires resolved by zypper (no manual fixes, no missing
providers); (3) local spec == OBS for all 21 (md5); (4) upstream
unchanged for all 21 since prior audits (sweep above).

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer | bundled Electron libs excluded from auto-deps (spec guards) | soname Requires | none | none | deps-verified |
| helium-browser | ldd real ELF 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | ldd real ELF 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git | meson.build deps (prior run) | pkgconfig set incl. wlroots-0.20 + scenefx-0.5 | none | none | deps-verified |
| matugen (4.2.0) | Cargo.lock == vendor (prior) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git | Cargo.toml + ldd 51 libs 0 missing (prior) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson.build @5.0.0-beta.8 30 deps (prior) | all 44 BRs present | none | none | deps-verified |
| noctalia-greeter | meson.build @1.2.1 deps (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson.build @0.6.4 deps (prior) | all in spec | none | none | deps-verified |
| rootapp | AppImage ELFs ldd 0 missing (prior) | libXt6 + wl-clipboard etc. | none | none | deps-verified |
| scenefx | meson.build @0.5 (prior) | all in spec; provides libscenefx-0_5 | none | none | deps-verified |
| stb | header-only | c++_compiler; provides stb.pc | none | none | deps-verified |
| stoat-desktop | all /opt/Stoat ELFs ldd 0 missing (prior) | gtk3, mozilla-nss, libcups2, dbus-1, libsecret-1-0, xdg-utils | none | none | deps-verified |
| vesktop | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior); provides libwlroots-0_20 | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser | libxul/libtool deps; NSS symlinked (prior) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for fluxer (fresh-container install + verified RPM
content this run). high for all others (upstream unchanged since prior
audits; all 42 builds green; 18-package concurrent install this run).

## Commits this run

1. `0f3360b` "fluxer: fetch RPM locally in update.sh instead of OBS
   download_files service" - fluxer update.sh (download+verify RPM,
   version-guard), spec (Source0 -> fluxer.rpm, Version
   2026.819.10042), .changes entry, .gitignore added, `_service`
   removed. Author identity:
   opencode-agent[bot] <41898282+opencode-agent[bot]@users.noreply.github.com>
   (verified after commit; no trailers).
2. Relay update (this commit).

## OBS build status

All 42 builds (21 pkgs x Tumbleweed + Slowroll) `succeeded`/`published`
at run end. fluxer rebuilt from the uploaded RPM (no download_files
service) and published on BOTH codestreams after this run's sync.

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; binary
  execution + presence + ldd verified, launcher launch confirmed).
- obsidian/opencode-desktop/zen/rootapp/helium/localsend binary
  artifacts fetched by OBS `download_files` service at build time
  (verified via URL 200 + upstream release assets + spec checksums).
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- fluxer CDN behavior could change again; the local-RPM pattern is
  robust regardless (update.sh downloads on the runner, which CAN
  fetch the redirect, and the RPM is uploaded to OBS directly).
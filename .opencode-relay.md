# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31986193764

## Run Summary (31986193764)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-17 ~01:51Z (opencode-schedule, relay=false). Prior
relay 31965399411 was complete. The auto-updater (run 581) had bumped
stoat-desktop 1.4.2->1.5.1 and fluxer to 2026.816.204155 since the last
relay. This run re-verified all versions, install-tested all 21 packages
from the PUBLIC mirror as a fresh checker, and found + fixed TWO real
runtime-dependency bugs (helium-browser, rootapp) that had slipped
through prior install tests.

### Bugs found and fixed this run

1. **helium-browser (0.15.5.1, critical)** - The bundled Chromium ELF
   (/opt/helium/helium) could not launch after install:
   `/opt/helium/helium: error while loading shared libraries:
   libnspr4.so: cannot open shared object file`. Root cause: the spec
   excludes auto-requires for /opt/helium (`%global __requires_exclude_from
   ^/opt/helium/.*$`) but shipped NO manual Requires, so mozilla-nss /
   mozilla-nspr / libasound2 / libgbm1 were never pulled in. The previous
   run's "ldd 0 missing" was a false pass - ldd was run on the WRAPPER
   SCRIPT (/usr/bin/helium -> helium-wrapper), not the real ELF. Verified
   via zypper what-provides in a TW container: libnspr4.so->mozilla-nspr,
   libnss3/libnssutil3/libsmime3->mozilla-nss, libasound.so.2->libasound2,
   libgbm.so.1->libgbm1. Added 6 soname Requires, synced to OBS, rebuilt.
   VERIFIED: fresh container installs clean, ldd real ELF 0 missing,
   binary launches: "Helium 0.15.5.1 (Chromium 151.0.7922.137)" rc=0
   (was lib-not-found fatal before the fix). Commit d6b1dbf.

2. **rootapp (0.9.126)** - bundled libuiohook.so needs libXt.so.6 (not
   bundled, not required). Verified: `ldd /opt/rootapp/usr/bin/libuiohook.so`
   -> libXt.so.6 not found; zypper what-provides -> libXt6. Added
   `Requires: libXt6`, synced, rebuilt. VERIFIED: fresh container
   installs libXt6, ldd libuiohook.so 0 missing, AppRun launches rc=0.
   Commit d6b1dbf.

Both were fixed in one commit: d6b1dbf
"fix(autonomous): helium-browser, rootapp: add missing runtime deps".
OBS rebuilt both on Tumbleweed + Slowroll; all builds succeeded/published.
OBS binaries: helium-browser-0.15.5.1-7.1.x86_64, rootapp-0.9.126-5.1.x86_64.

### Version inventory sweep 21/21 (STEP B)

Verified every package against real upstream this run (gh api
releases/tags/commits, git ls-remote, GitLab API, X-Fluxer-Version
header, AppImage re-tear):
- bibata-cursor-theme 2.0.7 = tag v2.0.7 (up-to-date)
- fluxer 2026.816.204155 = X-Fluxer-Version header this run (up-to-date;
  auto-updater bumped 2026.815.214426 -> 2026.816.204155 at 01:17Z)
- helium-browser 0.15.5.1 = tag 0.15.5.1 + tarball 200 (up-to-date)
- lazyvim-git 459a4c3 = LazyVim main HEAD (up-to-date)
- localsend 1.18.0 = held (v1.18.1 ships ONLY 4 Android APKs, no desktop
  DEB - re-verified asset list this run; correct hold)
- mangowm-git c5dd504 = mango main HEAD (up-to-date)
- matugen 4.1.0 = upstream v4.1.0 (up-to-date)
- niri-git 6062844 = niri main HEAD (up-to-date)
- noctalia 5.0.0~beta8 = tag v5.0.0-beta.8; local tarball byte-identical
  to fresh GitHub download (sha256 a60e4ab8...)
- noctalia-greeter 1.2.1 = tag v1.2.1; tarball byte-identical
  (sha256 1d1c4349...)
- obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
- opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date)
- protonplus 0.6.4 = tag v0.6.4 (up-to-date)
- rootapp 0.9.126 = AppImage re-torn this run: X-AppImage-Version=0.9.126
  + sq.version <version>0.9.126</version>; sha256 0736926fde...d910 ==
  spec comment (up-to-date)
- scenefx 0.5 = wlrfx/scenefx release 0.5 (up-to-date)
- stb 20260802 = commit 2c980bb date 2026-08-02 == master HEAD (up-to-date)
- stoat-desktop 1.5.1 = tag v1.5.1 (auto-updater bumped 1.4.2 -> 1.5.1)
- vesktop 1.6.5 = upstream v1.6.5 (up-to-date)
- wlroots 0.20.2 = GitLab release 0.20.2; tarball GET 200 (up-to-date;
  HEAD 403 is a known GitLab HEAD-block, GET works)
- xwayland-satellite-git 3bc915f = Supreeeme main HEAD (up-to-date)
- zen-browser 1.21.14b = tag 1.21.14b + tarball 200 (up-to-date)

All versioned Source0 URLs verified 200 (or valid tarball download).
All -git gitdates == upstream committer dates exactly.
No stale/wrong versions found. README (21 rows) == local dirs (21) ==
OBS packages (21). Cross-repo check vs fedora-nexus done; no missing
packages that belong in this curated repo.

### OBS build health

All 44 builds (22 packages x Tumbleweed + Slowroll) succeeded/published
after the helium/rootapp fixes; 0 failed/broken/unresolvable.
_builddepinfo (openSUSE_Tumbleweed/x86_64) re-fetched this run: all 21
packages present, 0 <error> blocks. OBS spec + .changes byte-identical
to local for all 21 packages (drift check clean).

### GitHub Actions health

Recent auto-updater runs all success (run 581 = 31984540179 at 01:17Z
bumped stoat-desktop + fluxer, synced to OBS, builds green). 0 open
issues, 0 open PRs. All update.sh scripts pass bash -n; no-churn guards
(commit-SHA / version / sha256 comparisons) verified in all updaters -
no repeated .changes-only churn.

### Spec validation

rpmspec -P on all 21 specs: clean. helium-browser + rootapp re-validated
after the Requires fix (new Requires present in the built RPM metadata).

### Tumbleweed snapshot (discover every run)

Tumbleweed repo last regenerated 2026-08-16 09:36 UTC (snapshot ~20260814,
week 2026/33 review). Slowroll lags ~5-10 days. All packages built with
the current toolchain on both codestreams (OBS green). No Leap 16 usage.

## Pre-completion gate

1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp).
2. Every package you touched install-tested in a clean TW container ->
   **PASS** (helium + rootapp re-tested from scratch twice: once after the
   fix, once as the independent fresh checker; before/after evidence in
   the bug notes above).
3. Install-test coverage ledger updated -> **PASS** (all 21 rows THIS
   RUN, table below).
4. Dependency audit 100% coverage (21/21) -> **PASS** (real-ELF ldd of
   every bundled binary this run; 2 gaps found & fixed; builddepinfo 0
   errors).
5. Claims backed by command output -> **PASS** (gh api, git ls-remote,
   curl -sI/-sL, sha256sum, unsquashfs + X-AppImage-Version/sq.version,
   rpm2cpio-free `rpm -Kv`, docker/zypper/rpm/ldd logs, OBS
   _result/_builddepinfo XML, rpmspec -P, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 31986193764 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 21 packages install-tested THIS RUN in FRESH Tumbleweed containers
from the PUBLIC download.opensuse.org mirror (exact user flow: OSS +
home:ackerman repos). stoat-desktop 1.5.1 and fluxer 2026.816.204155
were re-tested because their binaries changed since the last test.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | clean | n/a (icons) |
| fluxer | 2026.816.204155-4.1 | pass | clean | ldd /usr/lib64/fluxer/fluxer 0 missing; electron root-fatal (Docker-only) |
| helium-browser | 0.15.5.1-7.1 | pass | clean | **launches: "Helium 0.15.5.1 (Chromium 151.0.7922.137)" rc=0** (was libnspr4 fatal before fix) |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass | clean | n/a (config package) |
| localsend | 1.18.0-15.1 | pass | clean | ldd 0 missing (bundled libflutter via rpath) |
| mangowm-git | 0.16.1+git20260816114232.c5dd504-1.1 | pass | clean | mango launches rc=0 (compositor needs display); config parse warnings = upstream bug at c5dd504 |
| matugen | 4.1.0-30.1 | pass | clean | "matugen 4.1.0" rc=0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | clean | "niri 26.04 (6062844)" rc=0 |
| noctalia | 5.0.0~beta8-4.1 | pass | clean | "noctalia v5.0.0" rc=0 |
| noctalia-greeter | 1.2.1-2.1 | pass | clean | "noctalia-greeter 1.2.1" rc=0 |
| obsidian | 1.13.7-6.1 | pass | clean | electron (system) root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | clean | ldd /opt/OpenCode/ai.opencode.desktop 0 missing; electron root-fatal (Docker-only) |
| protonplus | 0.6.4-8.1 | pass | clean | launches rc=0; rpm+metainfo show 0.6.4 |
| rootapp | 0.9.126-5.1 | pass | clean | **AppRun launches rc=0; ldd libuiohook.so 0 missing** (was libXt.so.6 gap before fix) |
| libscenefx-0_5 | 0.5-7.1 | pass | clean | n/a (lib); provides libscenefx-0.5.so |
| stb-devel | 20260802-1.1.noarch | pass | clean | n/a (headers) |
| stoat-desktop | 1.5.1-2.1 | pass | clean | ldd /opt/Stoat/stoat-desktop 0 missing; electron root-fatal (Docker-only) |
| vesktop | 1.6.5-35.1 | pass | clean | ldd /opt/Vesktop/vesktop 0 missing; electron root-fatal (Docker-only) |
| libwlroots-0_20 | 0.20.2-8.1 | pass | clean | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | clean | binary launches rc=0 |
| zen-browser | 1.21.14b-6.1 | pass | clean | "Mozilla Zen 1.21.14b" rc=0 (sandbox EPERM Docker-only); ldd 0 missing |

## Dependency audit (all 21)

Ground truth this run: (1) `_builddepinfo` (openSUSE_Tumbleweed/x86_64)
covers all 21 packages, ZERO <error> blocks; (2) REAL-ELF ldd of every
bundled binary/so in every binary-distributed package (not the wrapper
scripts - the prior run's blind spot). This caught the two gaps below.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer (2026.816.204155) | real-ELF ldd 0 missing + bundled libs scan clean | soname Requires | none | none | deps-verified |
| helium-browser (0.15.5.1) | ldd real ELF: libnspr4, libnss3, libnssutil3, libsmime3, libasound2, libgbm1 | **ADDED 6 soname Requires** | fixed: libasound/libgbm/libnss*/libnspr4 | none | deps-fixed |
| lazyvim-git (459a4c3) | config only | none | none | none | deps-verified |
| localsend (1.18.0) | real ELF + bundled plugins clean (rpath) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (c5dd504) | meson.build @c5dd504 (13 deps) | all present | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 + builddepinfo | pkgconfig set (21) + cargo-packaging | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (tarball byte-verified) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (tarball byte-verified) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron via system pkg; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | real-ELF ldd 0 missing | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | meson appstream + cairo; builddepinfo | pkgconfig(appstream), pkgconfig(cairo) | none | none | deps-verified |
| rootapp (0.9.126) | ldd libuiohook.so -> libXt.so.6; main ELF 0 missing | **ADDED libXt6** | fixed: libXt6 | none | deps-fixed |
| scenefx (0.5) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| stb (20260802) | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.5.1) | real ELF + bundled libs scan clean | soname/named Requires | none | none | deps-verified |
| vesktop (1.6.5) | real ELF 0 missing + bundled scan clean | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | libxul/libtool deps; NSS symlinked | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 21. Real-ELF ldd of every bundled binary this
run (the blind spot in prior audits) + clean builddepinfo + 2 gaps found
and fixed with before/after evidence.

## Commits this run
- d6b1dbf fix(autonomous): helium-browser, rootapp: add missing runtime
  deps (4 files: 2 specs + 2 .changes). Authored as opencode-agent[bot],
  no co-author trailer.

## OBS build status

All 44 builds succeeded/published. helium-browser rebuilt to
0.15.5.1-7.1 and rootapp to 0.9.126-5.1 on both Tumbleweed and Slowroll
after the Requires fix; both verified installed in fresh containers.

## Not Verified / Gaps
- Tumbleweed snapshot current ~20260814 (week 2026/33 review); Slowroll
  lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified). fluxer, helium-browser, obsidian,
  opencode-desktop, stoat-desktop, vesktop are Electron apps - the
  running-as-root sandbox fatal is Docker-only and documented.
- helium /opt/helium/libqt{5,6}_shim.so need Qt5/Qt6 - these are OPTIONAL
  dlopen'd shims (Qt integration features), not required for the browser
  to launch ("Helium 0.15.5.1" runs without them). Not added as hard
  Requires to avoid forcing a Qt5/Qt6 install; noted for future review.
- fluxer internal version: X-Fluxer-Version header (2026.816.204155) is
  canonical per the project's update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop DEB
  (re-verified asset list = 4 Android APKs only). Guard protects the repo.
- mangowm-git: mango config/parser mismatch at c5dd504 is an upstream
  bug (== master HEAD); faithfully shipped, launches rc=0. Guarded
  updater will pick up the fix when upstream lands it.
- lazyvim-git ships Lua config as plain files; end-to-end LazyVim startup
  requires its runtime-download on first launch (network dependent, not
  testable offline).
- The Qt shims and optional features above were verified as non-blocking;
  no other dlopen()-gated dependencies were found in the bundled scans.
# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31452593751

## Verification Summary (Run 31452593751, scheduled pass)

Independent maintenance pass over all 17 packages in home:ackerman.

### Run start note
This run started 02:30:40Z. A prior model attempt in this same run (before this
session) bumped mangowm-git to 30bc8ef and synced it to OBS (commit 5611cd4,
02:50:36Z, one .changes entry). This session independently re-verified that
work: upstream mango main HEAD == 30bc8ef (git ls-remote), gitdate
20260811015800 == upstream committer date (gh api), OBS spec matches, OBS
build started 02:51:23Z after the commit and published the 30bc8ef binary in
both openSUSE_Tumbleweed and openSUSE_Slowroll. No further churn.

### Inventory (17/17 match repo dirs = README = OBS)
bibata-cursor-theme, fluxer, localsend, mangowm-git, matugen, niri-git,
noctalia, noctalia-greeter, obsidian, opencode-desktop, rootapp, scenefx, stb,
vesktop, wlroots, xwayland-satellite-git, zen-browser. No missing/extra
packages on either side; README table matches.

### OBS build status
All 34 builds (17 pkgs x 2 repos = openSUSE_Tumbleweed + openSUSE_Slowroll)
report `succeeded`/`published`. Failure filter
`&code=failed&code=broken&code=unresolvable` => EMPTY.

### OBS vs local drift
All 17 spec files and all 17 .changes files content-MATCH OBS (md5 per package).
OBS file lists contain the expected build-time artifacts (source tarballs,
vendor.tar.xz, cargo_config uploaded during pipeline sync). No local file is
missing from OBS. No re-sync needed this run.

### Version sweep (independent, this process) - 17/17 current
- bibata-cursor-theme 2.0.7 = v2.0.7 (gh api releases/latest; source URL 200)
- fluxer 2026.731.153836 = X-Fluxer-Version header (curl -sI api.fluxer.app, 200, 92MB)
- localsend 1.18.0 = v1.18.0 (gh api releases/latest; DEB URL 200)
- mangowm-git 30bc8ef = upstream main HEAD (git ls-remote, twice)
- matugen 4.1.0 = v4.1.0 (gh api releases/latest)
- niri-git 464d45d = upstream main HEAD (git ls-remote); gitdate 20260810152026 ==
  upstream committer date 2026-08-10T15:20:26Z
- noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api tags); tarball URL 200; local
  tarball sha256 a60e4ab8... == upstream download (byte-identical)
- noctalia-greeter 1.2.1 = tag v1.2.1 (gh api tags); local tarball sha256
  1d1c4349... == upstream download (byte-identical)
- obsidian 1.13.6 = v1.13.6 (gh api releases/latest); tarball URL 200;
  app.asar/obsidian.asar present; version inside verified
- opencode-desktop 1.18.16 = v1.18.16 (gh api releases/latest); DEB control
  Version: 1.18.16, Depends mapped to spec Requires
- rootapp 0.9.126: AppImage re-downloaded (386MB, sha256 0736926f...d910 == spec
  comment, byte-identical) AND re-extracted via squashfs offset 944632:
  Root.desktop X-AppImage-Version=0.9.126 (independent version confirmation)
- scenefx 0.5 = tags 0.5 (gh api repos/wlrfx/scenefx); source URL 200
- stb 20260802 = master HEAD 2c980bb dated 2026-08-02T06:42:09Z; committed
  tar.xz content diff-matches upstream tarball (diff -rq, no differences)
- vesktop 1.6.5 = v1.6.5 (gh api releases/latest); RPM torn: app.asar
  package.json version 1.6.5; RPM URL 200
- wlroots 0.20.2 = GitLab release 0.20.2 (releases API). Direct download from
  this runner now returns 200 (previous anti-bot issue was transient)
- xwayland-satellite-git 3bc915f = main HEAD of the CURRENT upstream repo
  Supreeeme/xwayland-satellite (spec URL points there; git ls-remote + bare
  clone + merge-base confirm 3bc915f is main HEAD). NOTE: the old
  YaLTeR/xwayland-satellite location is abandoned (main stuck at 2025) and is
  NOT the upstream anymore - spec URL is correct.
- zen-browser 1.21.13b = releases/latest; tarball URL 200 (92MB); application.ini
  Version=1.21.13b, BuildID=20260809044209 inside

### Source URLs / checksums
All Source0 URLs reachable (curl -sL 200). rootapp sha256 + internal version
verified. noctalia/noctalia-greeter/stb tarballs content-verified against
upstream. No checksums needed updating.

### No-churn
Isolated-copy runs of niri-git, mangowm-git, xwayland-satellite-git, localsend
update.sh all exit 0 with "already at latest"/"already up to date" (pure
commit-SHA / version guards). No repeated auto-update commits for the same SHA.
No duplicate .changes entries. Last 40 commits audited - no churn pattern.

### Spec validity / updater health
- `rpmspec -P` all 17 specs OK
- `bash -n` all 16 update.sh OK
- all 12 _service files parse as valid XML (python xml parser)
- GitHub Actions: 5 most recent "Nexus Auto-Updater (OBS Pipeline)" runs all
  success (31447999711, 31429511028, 31381491220, 31346029617, 31334029842).
  No failed runs; updater logs show all packages "already on latest".

### Issues/PRs
`gh api issues?state=open` => 0 open; pulls => 0 open. Nothing to triage.

### Tumbleweed snapshot
Clean container /etc/os-release = 20260806 (current snapshot; Tumbleweed
Monthly Update June 2026 + Review of the week 2026/29 confirm rolling cadence;
Slowroll lags ~5-10 days per openSUSE News). GCC 16 is the TW default - all
builds green on current snapshot. Slowroll not container-tested (identical
binaries ~5-10 days behind TW; TW proof per policy).

### Install tests (fresh TW containers, snapshot 20260806)
All tested from the PUBLIC nexus repo (download.opensuse.org home:ackerman TW)
except mangowm which was tested from a local OBS-downloaded RPM (and separately
from the public repo). INSTALL_EXIT=0 for all 17 packages; smoke where
applicable:
- bibata-cursor-theme: INSTALL_EXIT=0 (icons, no smoke)
- fluxer: INSTALL_EXIT=0 (electron, needs display)
- localsend 1.18.0: INSTALL_EXIT=0; /usr/bin/localsend_app launches to GTK
  display init ("cannot open display" = headless Docker, rc=0); ldd 0 not-found
- mangowm-git 30bc8ef: INSTALL_EXIT=0 (local RPM AND public repo);
  `mango -v` => "mango 0.15.6(release)" rc=0; rpm -V clean
- matugen: INSTALL_EXIT=0; `matugen --version` => "matugen 4.1.0" rc=0
- niri-git 464d45d: INSTALL_EXIT=0; `niri --version` => "niri 26.04 (464d45d)" rc=0
- noctalia beta8: INSTALL_EXIT=0; `noctalia --version` => "noctalia v5.0.0" rc=0
- noctalia-greeter: INSTALL_EXIT=0 (greetd greeter, no headless smoke)
- obsidian 1.13.6: INSTALL_EXIT=0 (electron, needs display)
- opencode-desktop: INSTALL_EXIT=0 (electron, needs display); ldd of
  ai.opencode.desktop 0 not-found
- rootapp 0.9.126: INSTALL_EXIT=0; `rootapp --version` => "info: Program[0]" rc=0
- scenefx (libscenefx-0_5): INSTALL_EXIT=0 (lib, no smoke)
- stb (stb-devel): INSTALL_EXIT=0 (header lib, no smoke)
- vesktop: INSTALL_EXIT=0 (electron, needs display)
- wlroots (libwlroots-0_20): INSTALL_EXIT=0 (lib, no smoke)
- xwayland-satellite-git: INSTALL_EXIT=0; `xwayland-satellite -version` => 0.8.2 rc=0
- zen-browser 1.21.13b: INSTALL_EXIT=0; `zen-browser --version` => "Mozilla Zen
  1.21.13b" rc=0 (sandbox userns EPERM warning is Docker-only, not a package bug)

### Install-test coverage ledger (current binaries as of this run)
| package | last install-tested | result | smoke |
|---|---|---|---|
| bibata-cursor-theme | 2026-08-11 run 31452593751 (binary unchanged) | pass | n/a (icons) |
| fluxer | 2026-08-11 run 31452593751 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| localsend (1.18.0) | 2026-08-11 run 31452593751 (binary unchanged) | pass | launches; display needed |
| mangowm-git (30bc8ef) | 2026-08-11 run 31452593751 (binary CHANGED) | pass | mango 0.15.6(release) |
| matugen | 2026-08-11 run 31452593751 (binary unchanged) | pass | 4.1.0 |
| niri-git (464d45d) | 2026-08-11 run 31452593751 (binary unchanged) | pass | niri 26.04 (464d45d) |
| noctalia (5.0.0~beta8) | 2026-08-11 run 31452593751 (binary unchanged) | pass | v5.0.0 |
| noctalia-greeter | 2026-08-11 run 31452593751 (binary unchanged) | pass | --help usage |
| obsidian (1.13.6) | 2026-08-11 run 31452593751 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| opencode-desktop (1.18.16) | 2026-08-11 run 31452593751 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| rootapp | 2026-08-11 run 31452593751 (binary unchanged) | pass | --version info:Program[0] |
| scenefx (libscenefx-0_5) | 2026-08-11 run 31452593751 (binary unchanged) | pass | n/a (lib, no smoke) |
| stb (stb-devel) | 2026-08-11 run 31452593751 (binary unchanged) | pass | n/a (header lib) |
| vesktop | 2026-08-11 run 31452593751 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| wlroots (libwlroots-0_20) | 2026-08-11 run 31452593751 (binary unchanged) | pass | n/a (lib, no smoke) |
| xwayland-satellite-git | 2026-08-11 run 31452593751 (binary unchanged) | pass | -version => 0.8.2 |
| zen-browser (1.21.13b) | 2026-08-11 run 31452593751 (binary unchanged) | pass | Mozilla Zen 1.21.13b |

### Dependency audit (all 17, high confidence)
OBS `_builddepinfo` (TW/x86_64) for all 17 packages: 0 `<error>` entries.
Only mangowm-git changed since the last audit; its deps were re-derived from
the NEW tarball this run (meson.build: libdrm, xcb, xcb-icccm, wayland-server,
wlroots-0.20, xkbcommon, libinput, wayland-client, libpcre2-8, scenefx-0.5,
pixman-1, libcjson, pangocairo, libm) - all mapped in spec. All other
packages' upstream versions unchanged since last audit (verified above).

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (icons only) | none | none | deps-verified |
| fluxer | Electron bundled; ldd 0 not-found | soname Requires | none | deps-verified |
| localsend (1.18.0) | DEB control Depends; ldd 0 not-found | soname Requires + xdg-user-dirs | none | deps-verified |
| mangowm-git (30bc8ef) | meson: libcjson,libdrm,libinput,libpcre2-8,pangocairo,pixman-1,scenefx-0.5,wayland-client,wayland-server,wlroots-0.20,xcb,xcb-icccm,xkbcommon | all mapped | none | deps-verified |
| matugen | Cargo vendored; no system deps beyond cargo | cargo-packaging,gcc-c++ | none | deps-verified |
| niri-git (464d45d) | niri.spec.rpkg; Requires mesa-dri-drivers,mesa-libEGL,xwayland-satellite>=0.7 | all mapped | none | deps-verified |
| noctalia (5.0.0~beta8) | meson.build full dep list | all in spec incl pkgconfig(pam)+pkgconfig(stb) | none | deps-verified |
| noctalia-greeter | meson: wayland,xkb,pango/cairo,glib,libinput,wlroots,epoxy,tomlplusplus,json,stb,webp,rsvg | all in spec | none | deps-verified |
| obsidian (1.13.6) | Electron bundled; AutoReqProv:no; Requires electron | electron,bash | none | deps-verified |
| opencode-desktop (1.18.16) | DEB control Depends: libgtk-3-0, libnotify4, libnss3, libxss1, libxtst6, xdg-utils, libatspi2.0-0, libuuid1, libsecret-1-0; ldd 0 not-found | all mapped (incl libsecret-1-0, ripgrep) | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn (offset 944632): Root.desktop X-AppImage-Version=0.9.126 | all soname/named | none | deps-verified |
| scenefx (0.5) | meson: egl,gbm,glesv2,libdrm,pixman-1,xkbcommon,wlroots-0.20,wayland-server | all in spec | none | deps-verified |
| stb | header-only; c++ test in %check | c++_compiler | none | deps-verified |
| vesktop (1.6.5) | Electron bundled; app.asar package.json 1.6.5 | soname Requires | none | deps-verified |
| wlroots (0.20.2) | meson: glslang,egl,gbm,glesv2,hwdata,libdrm,libpng,libseat,libinput,vulkan,wayland,xkbcommon,x11/xcb/xwayland,ffmpeg,libdisplay-info,libliftoff,lcms2 | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson: fontconfig,wayland,xcb,xcb-cursor,clang/llvm,cargo | all in spec | none | deps-verified |
| zen-browser (1.21.13b) | Firefox tarball; application.ini 1.21.13b; ldd 0 not-found | mozilla-nss/nspr,hunspell,hyphen | none | deps-verified |

Confidence: high for all 17 (_builddepinfo + artifact extraction + clean
container installs agree). Package-specific verification notes: rootapp AppImage
torn twice (sha256 + X-AppImage-Version); obsidian/zen/vesktop/opencode-desktop
tarballs/RPM/DEB torn and versions read from inside; noctalia + noctalia-greeter
+ stb tarballs sha256/content-checked against upstream.

### Docs
README.md: fixed stale "every ~2h" to the actual "every 10h" maintenance
schedule. Package table verified correct (17/17, matches dirs and OBS).

### Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts kept in /tmp only).
2. Every package touched this run install-tested in clean TW container incl.
   fresh-checker re-runs -> **PASS** (all 17 install-tested in fresh containers
   this run; mangowm-git (changed binary) tested from local RPM AND public repo).
3. Install-test coverage ledger updated -> **PASS** (this file; all rows now
   reference current binaries).
4. Dependency audit 100% coverage (17/17) -> **PASS** (table above; mangowm
   re-derived from NEW artifact; all others upstream-unchanged and cross-checked
   against _builddepinfo).
5. Claims backed by command output -> **PASS** (gh api, git ls-remote, bare
   clones, curl -sL status/size, sha256sum, readelf/unsquashfs, ar/dpkg-deb,
   tar/asar, diff -rq, rpmspec -P, bash -n, python XML parse, OBS
   _result/_builddepinfo XML, docker/zypper/rpm -V/ldd logs).
6. Named gaps -> see below.
7. Relay updated with run_id 31452593751 + status complete -> **PASS** (this
   commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64 proof per
  policy).
- Slowroll container installs (identical binaries ~5-10 days behind TW; TW proof
  per policy).
- GUI apps' full graphical smoke (no display in Docker; launcher + binary
  execution verified instead).
- niri upstream is extremely active; pinned commit 464d45d may advance before
  the next auto-updater. Spec will be refreshed by the next schedule run; not a
  bug.
- matugen/niri/xwayland-satellite vendor tarballs not re-extracted this run
  (their upstream versions/commits are unchanged since the last full audit and
  OBS builds succeeded; vendor regeneration only needed when Cargo.lock changes).
- One transient OSS-mirror flake family from download.opensuse.org on this
  runner (timeouts/404s on dependency RPMs during zypper preloading) noted by
  previous runs; this run's downloads were clean.

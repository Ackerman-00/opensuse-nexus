# OpenCode Autonomous Run Relay State

## Run info
- Started: 2026-08-03 07:48 UTC
- RELAY=0 (fresh run, no prior relay state)

## Status: complete

## Completed
- [x] OBS build check: all 17 packages succeeded/published on Tumbleweed + Slowroll
- [x] Version sweep (100% inventory): all 17 packages verified against upstream (GitHub API,
      CDN headers, artifact tear-downs):
  bibata 2.0.7, fluxer 2026.731.153836 (CDN header), localsend 1.17.0,
  mangowm-git 0.15.6+git de9ee83 (= upstream HEAD, tag 0.15.6), matugen 4.1.0,
  niri-git 26.04+git feb3e43 (= upstream HEAD), noctalia 5.0.0~beta7,
  noctalia-greeter 1.1.0, obsidian 1.13.4 (asar tear-down), opencode-desktop 1.18.11
  (asar tear-down), rootapp 0.9.125 (AppImage sq.version + sha86ed0d76 match),
  scenefx 0.5, stb 20260802 (tracked tarball == upstream HEAD 2c980bb), vesktop 1.6.5,
  wlroots 0.20.2 (GitLab release), xwayland-satellite-git 0.8.2+git 8d135d3 (= HEAD),
  zen-browser 1.21.10b (application.ini)
- [x] OBS package list == local dirs == README (17/17); OBS spec content == local (all 17)
- [x] Dependency deep audit (all 17): see audit table. Only real finding:
  matugen had dead `BuildRequires: pkgconfig(openssl)` (rustls-only, no openssl-sys in
  Cargo.lock, vendor tarball complete 381/382). DROPPED (commit 3b19340), synced to OBS,
  rebuild SUCCEEDED on Tumbleweed + Slowroll, new rpm matugen-4.1.0-29.1 installs + runs
  (`matugen 4.1.0`). Verified: mangowm meson.build deps all in spec; noctalia/gnigreeter
  wayland-egl satisfied via wayland-devel; wlroots deps all in spec; xwayland-satellite
  vendor tarball complete (136 crates); all builds resolve per OBS builddepinfo.
- [x] Install-verification sweep: ALL 17 packages installed cleanly in fresh Tumbleweed
      containers (oss + nexus repos). Real installs + ldd + smoke tests all pass:
      matugen (--version), mangowm-git (Usage), noctalia-greeter (--version), niri
      (26.04 feb3e43), noctalia (v5.0.0), rootapp (xvfb, .NET runtime 10.0.10),
      obsidian (xvfb rc124 running), opencode-desktop (xvfb, version 1.18.11),
      vesktop/fluxer/localsend/zen (xvfb rc124 running), stb/scenefx/wlroots (libs/headers).
      xwayland-satellite: install+ldd OK, no --version flag (daemon, panics on args = expected).
- [x] Churn audit: no duplicate .changes entries; all update.sh have proper guards
      (pure commit-SHA for mangowm/niri/xws; date-version for stb; sha256 for rootapp).
- [x] GitHub Actions: all Nexus Auto-Updater runs healthy (latest #529 success).
- [x] No open issues, no open PRs.
- [x] Working tree clean (git status --porcelain empty); all artifacts in /tmp.

## Next steps
- Nothing pending.

## Traps found
- Local RPM install in container prompts on GPG signature (NOKEY); use
  `zypper --non-interactive --no-gpg-checks install` for the local .rpm file, then the
  app repo key covers real users.
- Repo is a shallow clone at workflow start: run `git fetch --unshallow origin` before
  relying on `git log` for churn checks.
- This runner checks out a feature branch `opencode/schedule-*`; commit then
  `git push origin HEAD:main` and `git checkout main && git pull --rebase` to stay on main.
- GUI/Electron apps need `xvfb-run -a <bin> --no-sandbox` for headless launch smoke tests;
  rc=124 (timeout) means the app was running.
- xwayland-satellite has no --version/--help; it is a daemon and panics on unknown args.

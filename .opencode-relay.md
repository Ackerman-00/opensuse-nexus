# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31536365363

## Run Summary (31536365363)

Independent maintenance pass over all 17 packages in home:ackerman.
Started 21:07Z. Found one real bug introduced by the 20:37Z auto-updater
(run 31533658725, success) and one new upstream commit:

1. **localsend: bogus 1.18.1 bump.** The auto-updater's update.sh picked
   tag v1.18.1 via `git ls-remote | sort -V | tail -1`. v1.18.1 is a bare
   git tag with NO GitHub release and NO DEB asset (`gh api releases/latest`
   = v1.18.0; DEB URL 404). OBS build broke with `download_files failed`.
   Fixed: reverted spec Version/Source0 to 1.18.0, dropped spurious
   .changes entry, hardened update.sh to resolve via the GitHub releases
   API (real releases only) + require the DEB asset URL to return HTTP 200
   before bumping. Committed 62b56ae. Synced to OBS, rebuilt: both repos
   built localsend-1.18.0-15.1, `succeeded`.

2. **niri-git new upstream commit.** The 20:37Z auto-updater pinned
   aac1551; upstream pushed fec411e at 20:42:58Z (post-autoupdater).
   Bumped spec to fec411e, regenerated vendor.tar.xz + cargo_config from
   the new Cargo.lock, deleted obsolete niri-aac1551.tar.gz on OBS, synced
   all files (revs 81-86), rebuilt. Committed 38989ba. First rebuild
   failed: my manual `cargo vendor > cargo_config 2>&1` had merged
   "Downloading crates..." progress into the TOML config (repo update.sh
   uses stdout-only `cargo vendor > cargo_config` and is correct - the
   fault was mine, not the script's). Regenerated the config cleanly,
   re-uploaded, rebuilt: both repos built
   26.04+git20260811204258.fec411e-5.1, `succeeded`.

3. **localsend dep audit (1.18.0 DEB re-torn):** control Depends =
   libappindicator3-1|libayatana-appindicator3-1, gir1.2-appindicator3-
   0.1|gir1.2-ayatanaappindicator3-0.1, libayatana-ido3-0.4-0, xdg-user-
   dirs. ldd of libtray_manager_plugin.so additionally links
   `libayatana-indicator3.so.7` (provider: openSUSE libayatana-indicator3-7,
   verified in OSS repo + rpm -qlp). Added explicit Requires
   libayatana-indicator3.so.7()(64bit). Committed c010e4b, synced, rebuilt
   (localsend-1.18.0-15.1), `succeeded`. Verified the built RPM's Requires
   contain all 4 ayatana/ido sonames via rpm -qpR.

### Inventory (17/17 match repo dirs = README = OBS)
bibata-cursor-theme, fluxer, localsend, mangowm-git, matugen, niri-git,
noctalia, noctalia-greeter, obsidian, opencode-desktop, rootapp, scenefx, stb,
vesktop, wlroots, xwayland-satellite-git, zen-browser. No missing/extra
packages on either side; README table matches.

### OBS build status
All 34 builds (17 pkgs x 2 repos = openSUSE_Tumbleweed + openSUSE_Slowroll)
report `succeeded`. Failure filter
`&code=failed&code=broken&code=unresolvable` => EMPTY (checked repeatedly
throughout the run). localsend/niri-git jobhistory confirm the latest
builds are `succeeded`; the only recent failures were the already-fixed
cargo_config (21:20Z) and pre-fix bogus-1.18.1 (08-10).

### Version sweep - 17/17 current (verified against upstream this run)
- bibata-cursor-theme 2.0.7 = v2.0.7 (gh api releases/latest; Bibata.tar.xz 200)
- fluxer 2026.810.212541 = X-Fluxer-Version header (curl -sIL, 200, 92MB rpm)
- localsend 1.18.0 = v1.18.0 (gh api releases/latest; DEB URL 200)
- mangowm-git 4a3bff4 = upstream main HEAD (git ls-remote mangowm/mango)
- matugen 4.1.0 = v4.1.0 (gh api releases/latest; archive URL 200
  via refs/tags/v4.1.0.tar.gz)
- niri-git fec411e = upstream main HEAD (git ls-remote niri-wm/niri);
  gitdate 20260811204258 == committer date 2026-08-11T20:42:58Z
- noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api tags)
- noctalia-greeter 1.2.1 = tag v1.2.1 (gh api tags)
- obsidian 1.13.6 = v1.13.6 (gh api releases/latest; AppImage URL 200)
- opencode-desktop 1.18.16 = v1.18.16 (gh api releases/latest)
- rootapp 0.9.126: AppImage downloaded (386MB) sha256
  0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910 == spec
  comment (byte-identical)
- scenefx 0.5 = tag 0.5 (gh api tags + releases/latest; tarball URL 200)
- stb 20260802 = master HEAD 2c980bb dated 2026-08-02T06:42:09Z (gh api)
- vesktop 1.6.5 = v1.6.5 (gh api releases/latest; RPM 200; app.asar
  package.json "version": "1.6.5" confirmed)
- wlroots 0.20.2 = GitLab releases API (0.20.2 newest); direct tarball
  download from this runner blocked by GitLab anti-bot (403 challenge) -
  OBS builds/publishes fine, same as previous runs
- xwayland-satellite-git 3bc915f = main HEAD (git ls-remote Supreeeme repo)
- zen-browser 1.21.13b = releases/latest; tarball URL 200

### No-churn
All 17 update.sh run in isolation with GITHUB_TOKEN: all EXIT=0, no changes
(mangowm, xwayland-satellite, etc. all "already current"). localsend
update.sh now exits 0 with current 1.18.0 and is hardened against
release-less tags. niri .changes has exactly one entry per distinct commit
(fec411e new entry added). No duplicate rebuilds: OBS history shows one
build per commit.

### Spec validity / updater health
- `rpmspec -P` all 17 specs OK (incl. niri-git post-bump, localsend spec)
- `bash -n` all 17 update.sh OK
- all 12 _service files parse as valid XML
- GitHub Actions: all 5 most recent Nexus Auto-Updater runs green. No
  failed runs.

### Issues/PRs
`gh api issues?state=open` => 0 open; pulls => 0 open. Nothing to triage.
Latest Actions runs all succeeded.

### Tumbleweed snapshot
Clean container /etc/os-release = 20260809 (current as of this run).
Slowroll lags ~5-10 days (not container-tested; identical binaries, TW
proof per policy).

### Install tests (fresh TW 20260809 containers)
Changed/uncovered binaries tested against BOTH OBS-downloaded RPMs and the
public download.opensuse.org mirror (fresh docker run each time):
- localsend 1.18.0-15.1: installs clean, all 4 ayatana/ido sonames resolve
  (incl. new libayatana-indicator3.so.7). GUI smoke skipped headless.
- niri-git fec411e: installs clean, `niri --version` => "niri 26.04 (fec411e)"
- mangowm-git 4a3bff4: installs clean, `mango -v` => "mango 0.15.6(release)"
- fluxer 2026.810.212541: installs clean, rpm -V clean; electron root-fatal
  (Docker-only, documented flake)
- Public-repo full sweep (bibata, matugen, noctalia, noctalia-greeter,
  obsidian, opencode-desktop, rootapp, scenefx, stb, vesktop, wlroots,
  xwayland-satellite-git, zen-browser) 13/13 install cleanly from the
  synced mirror; matugen/noctalia/xwayland-satellite smokes pass.
NOTE: the public download.opensuse.org mirror lags OBS during republish -
  one install batch hit a transient 404 for fluxer (metadata ahead of rpm
  files); retry immediately succeeded and the mirror then listed all the
  new binaries. Same flake family as prior runs' OSS 404 notes.
NOTE: local-RPM installs against a container with ONLY the OSS repo added
  fail with "Signature verification failed - key not available" (the OBS
  signing key is imported via the nexus repo at gpg-auto-import-keys);
  adding the nexus repo matches the documented user setup and resolves it.

### Install-test coverage ledger (current binaries as of this run)
| package | last install-tested | result | smoke |
|---|---|---|---|
| bibata-cursor-theme (2.0.7) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (icons) |
| fluxer (2026.810.212541) | 2026-08-11 run 31536365363 (binary CHANGED) | pass | electron root-fatal (Docker-only) |
| localsend (1.18.0-15.1) | 2026-08-11 run 31536365363 (binary CHANGED x2) | pass | launches; display needed |
| mangowm-git (4a3bff4) | 2026-08-11 run 31536365363 (binary unchanged) | pass | mango 0.15.6(release) |
| matugen (4.1.0) | 2026-08-11 run 31536365363 (binary unchanged) | pass | 4.1.0 |
| niri-git (fec411e) | 2026-08-11 run 31536365363 (binary CHANGED) | pass | niri 26.04 (fec411e) |
| noctalia (5.0.0~beta8) | 2026-08-11 run 31536365363 (binary unchanged) | pass | v5.0.0 |
| noctalia-greeter (1.2.1) | 2026-08-11 run 31536365363 (binary unchanged) | pass | --help usage |
| obsidian (1.13.6) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| opencode-desktop (1.18.16) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| rootapp (0.9.126) | 2026-08-11 run 31536365363 (binary unchanged) | pass | --version info:Program[0] |
| scenefx (libscenefx-0_5) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (lib) |
| stb (stb-devel) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (header lib) |
| vesktop (1.6.5) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| wlroots (libwlroots-0_20) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (lib) |
| xwayland-satellite-git (3bc915f) | 2026-08-11 run 31536365363 (binary unchanged) | pass | -version => 0.8.2 |
| zen-browser (1.21.13b) | 2026-08-11 run 31536365363 (binary unchanged) | pass | Mozilla Zen 1.21.13b |

### Dependency audit (all 17, high confidence)
OBS `_builddepinfo` (TW/x86_64) for all 17 packages: 0 `<error>` entries.
Re-derived/changed this run:
- localsend (1.18.0): DEB re-torn (ar x + tar --zstd -xf). control Depends
  mapped. ldd of bundled libtray_manager_plugin.so revealed
  libayatana-indicator3.so.7 -> ADDED Requires (openSUSE
  libayatana-indicator3-7 verified in OSS). libayatana-appindicator3,
  libayatana-ido3, xdg-user-dirs already present.
- niri-git (fec411e): vendor.tar.xz regenerated from NEW Cargo.lock; OBS
  offline build with the vendored set succeeded, so crates match the lock.
- fluxer (2026.810): RPM re-torn (rpm2cpio|cpio). ldd 0 not-found; all
  Requires soname/named deps verified real on openSUSE (libXtst6, libuuid1
  exist in OSS). vesktop/obsidian/opencode-desktop artifacts re-torn this
  run (rpm2cpio/ar x): 0 missing sonames; versions confirmed inside
  (vesktop app.asar "1.6.5", opencode control Version 1.18.16, obsidian
  AppImage download 200).

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (icons only) | none | none | deps-verified |
| fluxer (2026.810) | Electron bundled; ldd 0 not-found | soname Requires + named (all exist in OSS) | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd incl libayatana-indicator3.so.7 | 4 soname Requires + xdg-user-dirs | ADDED libayatana-indicator3.so.7 | deps-fixed |
| mangowm-git (4a3bff4) | meson deps (prior audit, unchanged upstream) | all mapped | none | deps-verified |
| matugen (4.1.0) | Cargo vendored; no system deps beyond cargo | cargo-packaging, gcc-c++ | none | deps-verified |
| niri-git (fec411e) | niri.spec.rpkg BR set + Requires | all mapped; vendor regenerated | none | deps-verified |
| noctalia (5.0.0~beta8) | meson.build full dep list | all in spec incl pkgconfig(pam)+pkgconfig(stb) | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| obsidian (1.13.6) | Electron bundled; AutoReqProv:no; Requires electron | electron, bash | none | deps-verified |
| opencode-desktop (1.18.16) | DEB control Depends; ldd 0 not-found | all mapped (named pkgs verified in OSS) | none | deps-verified |
| rootapp (0.9.126) | AppImage torn (prior audit); sha256 re-verified byte-identical | all soname/named | none | deps-verified |
| scenefx (0.5) | meson deps (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| stb (20260802) | header-only; c++ test in %check | c++_compiler | none | deps-verified |
| vesktop (1.6.5) | Electron bundled; app.asar 1.6.5; ldd 0 not-found | soname Requires | none | deps-verified |
| wlroots (0.20.2) | meson deps (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| zen-browser (1.21.13b) | Firefox tarball; application.ini 1.21.13b | mozilla-nss/nspr, hunspell, hyphen | none | deps-verified |

Confidence: high for all 17 (_builddepinfo + artifact re-torn this run for
changed packages + clean container installs agree).

### Commits this run
- 62b56ae fix(autonomous): localsend: revert bogus 1.18.1 bump, harden update.sh
- 38989ba fix(autonomous): niri-git: bump to upstream main fec411e
- c010e4b fix(autonomous): localsend: add missing Requires libayatana-indicator3.so.7
- (relay+run record commit)

### Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp only).
2. Every package touched this run install-tested in a clean TW container
   incl. fresh-checker re-runs -> **PASS** (localsend 1.18.0-15.1, niri
   fec411e, mangowm, fluxer; plus 13-package public-repo sweep).
3. Install-test coverage ledger updated -> **PASS** (this file; rows
   reference current binaries).
4. Dependency audit 100% coverage (17/17) -> **PASS** (table above;
   changed packages re-derived from NEW artifacts).
5. Claims backed by command output -> **PASS** (gh api, git ls-remote,
   curl -sIL, sha256sum, rpm -qpR/-qlp, rpm2cpio, ar x, rpmspec -P,
   bash -n, OBS _result/_history/_builddepinfo XML, docker/zypper/rpm -V
   logs).
6. Named gaps -> see below.
7. Relay updated with run_id 31536365363 + status complete -> **PASS**
   (this commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64
  proof per policy).
- Slowroll container installs (identical binaries ~5-10 days behind TW; TW
  proof per policy).
- GUI apps' full graphical smoke (no display in Docker; launcher + binary
  execution verified instead; electron apps show the documented
  running-as-root sandbox fatal, Docker-only).
- wlroots 0.20.2 direct tarball download from this runner blocked by
  GitLab anti-bot (403 challenge); version verified via the GitLab releases
  API and OBS builds/publishes it fine.
- niri upstream is extremely active; pinned commit fec411e may advance
  before the next auto-updater. Spec will be refreshed by the next schedule
  run; not a bug.
- matugen/xwayland-satellite vendor tarballs not independently re-extracted
  this run (upstream commits unchanged since the last full audit and OBS
  offline builds succeed).
- The download.opensuse.org mirror occasionally serves metadata slightly
  ahead of RPM files during republish (transient 404 on re-fetch; retry
  passes). Known infra flake, not a repo bug.
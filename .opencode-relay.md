# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32004791309

## Run Summary (32004791309)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-17 ~07:12Z (opencode-schedule, relay=false). Prior
relay 31986193764 was complete. This run acted as an INDEPENDENT
FRESH CHECKER for the previous run's work: no binaries changed since
the last full test sweep (auto-updater run 32003626254 at 06:55Z
found every package already current - no churn, no sync), so the
sweep below re-verifies the two packages with critical runtime-dep
fixes last run (helium-browser, rootapp) plus a fresh full install
sweep of all 21 from the public mirror.

### What this run verified (evidence-backed)

1. OBS build health: all 44 builds (21 packages x Tumbleweed +
   Slowroll) `succeeded`/`published`; 0 failed/broken/unresolvable.
   _builddepinfo (TW + Slowroll): all 21 packages present, 0 <error>
   blocks.
2. Version inventory sweep 21/21 (STEP B) - all up-to-date:
   - bibata-cursor-theme 2.0.7 = tag v2.0.7 (gh api)
   - fluxer 2026.816.204155 = X-Fluxer-Version header this run
   - helium-browser 0.15.5.1 = tag 0.15.5.1 (gh api; no newer
     pre-release)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (git ls-remote)
   - localsend 1.18.0 = held (v1.18.1 asset list re-verified = 4
     Android APKs ONLY, no desktop DEB -> correct hold)
   - mangowm-git c5dd504 = mango main HEAD (git ls-remote)
   - matugen 4.1.0 = tag v4.1.0 (gh api)
   - niri-git 6062844 = niri main HEAD; prefix 26.04 matches
     Cargo.toml workspace version 26.4.0 at that commit
   - noctalia 5.0.0~beta8 = tag v5.0.0-beta.8; local tarball
     byte-identical to fresh download (sha256 a60e4ab8...)
   - noctalia-greeter 1.2.1 = tag v1.2.1 (tags API); tarball
     byte-identical (sha256 1d1c4349...)
   - obsidian 1.13.7 = tag v1.13.7 (gh api)
   - opencode-desktop 1.18.18 = tag v1.18.18 (gh api)
   - protonplus 0.6.4 = tag v0.6.4 (gh api)
   - rootapp 0.9.126 = AppImage re-torn: X-AppImage-Version=0.9.126;
     sha256 0736926fde...d910 == spec comment == fedora-nexus
   - scenefx 0.5 = wlrfx/scenefx release 0.5 (gh api)
   - stb 20260802 = commit 2c980bb dated 2026-08-02 == master HEAD
   - stoat-desktop 1.5.1 = tag v1.5.1 (gh api)
   - vesktop 1.6.5 = tag v1.6.5 (gh api)
   - wlroots 0.20.2 = GitLab API release 0.20.2; tarball GET 200
     after transient GitLab 504 (retry; object-storage blip, not a
     repo problem)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD
   - zen-browser 1.21.14b = tag 1.21.14b (gh api)
   All gitdates == upstream committer dates. All versioned Source0
   URLs verified 200 (curl -sI). README (21 rows) == local dirs (21)
   == OBS packages (21).
3. Dependency deep audit (fresh checker): real-ELF ldd of every
   bundled binary in a clean TW container - 0 "not found" for
   helium /opt/helium/helium, rootapp main ELF, fluxer, opencode,
   stoat, vesktop, zen, localsend, obsidian. helium libqt5_shim.so
   still shows Qt5 not found = optional dlopen'd shim (documented,
   non-blocking, browser launches without it). mangowm meson.build
   @c5dd504 deps (13) all present in spec. niri Cargo.toml
   @6062844 deps all satisfied by vendored crates + pkgconfig set.
   xwayland-satellite Cargo.toml deps (wayland-client/server, xcb,
   xcb-cursor, fontconfig) all in spec. No missing/extra deps found.
4. Install-test sweep: all 21 packages installed from the PUBLIC
   mirror in FRESH TW containers; every `rpm -V` clean; smoke tests
   below. helium + rootapp (the two last-run fixes) re-verified
   with launch evidence.
5. Spec validation: `rpmspec -P` clean for 20/21. protonplus shows a
   `%files lang` error ONLY because Ubuntu's rpm lacks openSUSE's
   `%lang_package` macro (host false positive) - OBS built
   protonplus-0.6.4-8.1.x86_64.rpm AND
   protonplus-lang-0.6.4-8.1.noarch.rpm (verified via OBS API).
6. Drift check: all 21 specs + .changes byte-identical to OBS
   (SPEC-MATCH/CHG-MATCH everywhere). OBS carries all git-package
   tarballs (mango-c5dd504, lazyvim-459a4c3, niri-6062844,
   xwayland-satellite-3bc915f) + vendor tarballs.
7. update.sh health: `bash -n` PASS on all 15 update.sh scripts.
   No-churn guards verified (pure commit-SHA / version / sha256
   comparisons; tarball refresh is artifact-only and exits 0).
   localsend 404-guard correctly stays on 1.18.0.
8. Actions health: last auto-updater run 32003626254 success; no
   failed pipeline runs. 0 open issues, 0 open PRs.
9. Tumbleweed snapshot: TW OSS last refresh 2026-08-16 09:36 UTC
   (snapshot 20260815); Slowroll OSS last refresh 2026-08-03
   (~10 day lag). All builds green on both codestreams. No Leap 16.

### Bugs found and fixed this run

None. Repo was fully healthy; all packages up-to-date, all builds
green, all installs pass. No churn was introduced.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (artifacts in
   /tmp).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (nothing needed fixing; the full 21-package sweep below
   was run as the fresh checker, including helium + rootapp
   re-verified with launch evidence).
3. Install-test coverage ledger updated -> **PASS** (21/21 rows this
   run, table below; no binary changed since the last test, all
   re-confirmed).
4. Dependency audit 100% coverage (21/21) -> **PASS** (real-ELF ldd
   of every bundled binary this run; meson/Cargo re-derivation for
   mangowm/niri/xwayland-satellite; builddepinfo 0 errors).
5. Claims backed by command output -> **PASS** (gh api, git
   ls-remote, curl -sI/-sL, sha256sum, unsquashfs +
   X-AppImage-Version, docker/zypper/rpm/ldd logs, OBS _result/
   _builddepinfo/_binarylist XML, rpmspec -P, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 32004791309 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 21 packages install-tested THIS RUN in FRESH Tumbleweed
containers from the PUBLIC download.opensuse.org mirror (exact user
flow: OSS + home:ackerman repos).

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | clean | n/a (icons) |
| fluxer | 2026.816.204155-4.1 | pass | clean | ldd /usr/lib64/fluxer/fluxer 0 missing; electron root-fatal (Docker-only) |
| helium-browser | 0.15.5.1-7.1 | pass | clean | **launches: "Helium 0.15.5.1 (Chromium 151.0.7922.137)" rc=0**; ldd real ELF 0 missing |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass | clean | n/a (config package) |
| localsend | 1.18.0-15.1 | pass | clean | ldd 0 missing (bundled libflutter via rpath) |
| mangowm-git | 0.16.1+git20260816114232.c5dd504-1.1 | pass | clean | mango runs rc=0 (compositor needs display) |
| matugen | 4.1.0-30.1 | pass | clean | "matugen 4.1.0" rc=0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | clean | "niri 26.04 (6062844)" rc=0 |
| noctalia | 5.0.0~beta8-4.1 | pass | clean | "noctalia v5.0.0" rc=0 |
| noctalia-greeter | 1.2.1-2.1 | pass | clean | "noctalia-greeter 1.2.1" rc=0 |
| obsidian | 1.13.7-6.1 | pass | clean | electron (system) root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | clean | ldd /opt/OpenCode/ai.opencode.desktop 0 missing; electron root-fatal (Docker-only) |
| protonplus | 0.6.4-8.1 | pass | clean | launches rc=0; rpm+metainfo show 0.6.4; --version not a supported flag |
| rootapp | 0.9.126-5.1 | pass | clean | **AppRun launches rc=0; ldd libuiohook.so 0 missing (libXt6 present)** |
| libscenefx-0_5 | 0.5-7.1 | pass | clean | n/a (lib); provides libscenefx-0.5.so |
| stb-devel | 20260802-1.1.noarch | pass | clean | n/a (headers) |
| stoat-desktop | 1.5.1-2.1 | pass | clean | ldd /opt/Stoat/stoat-desktop 0 missing; electron root-fatal (Docker-only) |
| vesktop | 1.6.5-35.1 | pass | clean | ldd /opt/Vesktop/vesktop 0 missing; electron root-fatal (Docker-only) |
| libwlroots-0_20 | 0.20.2-8.1 | pass | clean | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | clean | starts "xwayland-satellite version 0.8.2"; XDG_RUNTIME_DIR panic = headless container only |
| zen-browser | 1.21.14b-6.1 | pass | clean | "Mozilla Zen 1.21.14b" rc=0 (sandbox EPERM Docker-only); ldd 0 missing |

## Dependency audit (all 21)

Ground truth this run: (1) `_builddepinfo` (TW + Slowroll) covers
all 21 packages, ZERO <error> blocks; (2) REAL-ELF ldd of every
bundled binary (not wrapper scripts) - 0 missing everywhere.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer (2026.816.204155) | real-ELF ldd 0 missing | soname Requires | none | none | deps-verified |
| helium-browser (0.15.5.1) | ldd real ELF 0 missing | 6 soname Requires (added last run) | none | none | deps-verified |
| lazyvim-git (459a4c3) | config only | none | none | none | deps-verified |
| localsend (1.18.0) | real ELF 0 missing | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (c5dd504) | meson.build @c5dd504 (13 deps) | all present | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 + builddepinfo | pkgconfig set (21) + cargo-packaging | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (tarball byte-verified) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (tarball byte-verified) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron via system pkg; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | real-ELF ldd 0 missing | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | meson appstream + cairo; builddepinfo | pkgconfig(appstream), pkgconfig(cairo) | none | none | deps-verified |
| rootapp (0.9.126) | ldd libuiohook.so 0 missing (libXt6 installed) | libXt6 (added last run) | none | none | deps-verified |
| scenefx (0.5) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| stb (20260802) | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.5.1) | real ELF 0 missing | soname/named Requires | none | none | deps-verified |
| vesktop (1.6.5) | real ELF 0 missing | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | libxul/libtool deps; NSS symlinked | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 21 (fresh-container real-ELF ldd this run
+ clean builddepinfo + meson/Cargo re-derivation for the
source-built packages).

## Commits this run

None (repo fully healthy - no fixes required, no churn introduced).

## OBS build status

All 44 builds succeeded/published (Tumbleweed + Slowroll). No
rebuilds triggered this run (nothing changed).

## Not Verified / Gaps
- Tumbleweed snapshot current ~20260815 (OSS refresh 2026-08-16
  09:36 UTC); Slowroll lags ~10 days (refresh 2026-08-03). TW proof
  per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified). fluxer, helium-browser, obsidian,
  opencode-desktop, stoat-desktop, vesktop are Electron apps - the
  running-as-root sandbox fatal is Docker-only and documented.
- helium /opt/helium/libqt{5,6}_shim.so need Qt5/Qt6 - OPTIONAL
  dlopen'd shims (Qt integration), not required for the browser to
  launch ("Helium 0.15.5.1" runs without them). Not hard Requires to
  avoid forcing Qt5/Qt6 install; noted for future review.
- fluxer internal version: X-Fluxer-Version header (2026.816.204155)
  is canonical per the project's update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB (re-verified asset list = 4 Android APKs only). Guard protects
  the repo.
- mangowm-git: mango config/parser mismatch at c5dd504 is an upstream
  bug (== master HEAD); faithfully shipped, runs rc=0. Guarded updater
  will pick up the fix when upstream lands it.
- lazyvim-git ships Lua config as plain files; end-to-end LazyVim
  startup requires its runtime-download on first launch (network
  dependent, not testable offline).
- wlroots source: GitLab tarball returned 504 twice then 200 on
  retry (object-storage blip); the OBS download_files _service
  handles it at build time. Not a repo problem.
- The Qt shims and optional features above were verified as
  non-blocking; no other dlopen()-gated dependencies were found in
  the bundled scans.

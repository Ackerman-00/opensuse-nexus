# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32225920829

## Run Summary (32225920829)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-19 ~07:01Z (opencode-schedule, relay=false). Prior
relay 32206289058 was complete. This run:

1. **OBS build check: all 42 builds green.** `_result?view=status`
   returned `succeeded` for every package on both openSUSE_Tumbleweed
   and openSUSE_Slowroll. No failed/broken/unresolvable codes. OBS
   package list == local directories == README table (21/21, verified
   by diffing all three). Zero hidden failures.
2. **Version inventory sweep: all 21 packages up-to-date.** Evidence:
   `gh api .../releases/latest` for every GitHub release-based package
   (bibata v2.0.7, helium 0.15.5.1, matugen v4.2.0, noctalia
   v5.0.0-beta.8, protonplus v0.6.4, scenefx 0.5, stoat v1.5.2,
   vesktop v1.6.7, zen 1.21.14b, opencode v1.18.18, obsidian v1.13.7);
   `gh api .../commits/main` for the git packages (lazyvim 459a4c3,
   mango 813acaf, niri e9b215f, xwayland-satellite 3bc915f) and each
   gitdate cross-checked against the commit's committer date (all
   exact matches); stb master commit date 2026-08-02T06:42:09Z ->
   version 20260802 (UTC+8) matches; fluxer X-Fluxer-Version header
   2026.819.10042 == spec; rootapp AppImage X-AppImage-Version=0.9.126
   (extracted from squashfs) and sha256 0736926f... == spec comment
   (re-downloaded + re-extracted this run); wlroots GitLab release
   list -> 0.20.2 (newest 0.20.x).
   - **localsend held at 1.18.0**: v1.18.1 release assets verified =
   Android APKs only (arm32v7/arm64v8/google-play/x64), no linux
   x86-64 .deb. Hold is correct.
   - **noctalia-greeter**: no GitHub "latest release" (404), but tag
   list confirms v1.2.1 is the newest tag == spec version; source
   URL v1.2.1.tar.gz returns 200.
   - All 14 versioned source URLs return HTTP 200 (curl -sIL).
   - wlroots Source0 (GitLab release download) 302-redirects to a
     signed object-storage URL; GET succeeds (701254-byte tarball,
     verified) - the earlier HEAD 403 is object-storage HEAD rejection,
     not a broken source. Builds already green.
3. **No churn / no changes needed this run.** No spec, .changes,
   update.sh, or workflow file needed a change. No auto-update commits
   existed to audit (repo history is a single flattened import commit
   4c652dd; the prior run's fluxer fix 0f3360b is already inside it).
   Local files == OBS files for all 21 packages (md5 compared via
   `?expand=1` for every non-gitignored file per package - all OK).
4. **GitHub Actions health: healthy.** Latest "Nexus Auto-Updater
   (OBS Pipeline)" run 32224607256 (2026-08-19T06:43:16Z) succeeded;
   run 32204332240 (01:16Z) succeeded; one cancelled run
   32172190321 (2026-08-18 18:39Z) was superseded by a newer run, no
   failures. update-packages.yml reviewed - cron '0 */6 * * *',
   modular updaters with per-package failure isolation, pure
   version/commit-guard update.sh scripts (no churn), Smart Sync
   only touches actually-changed packages.
5. **Issues/PRs: none open** (`gh issue list` and `gh pr list` empty).
6. **Dependency audit: 100% coverage (21/21).** All upstreams
   unchanged since the prior audits (version sweep above proves no
   upstream change), all 42 OBS builds green, and the clean-container
   install sweep below resolved every Requires from OSS + nexus repo.
   See table.
7. **Install-verification sweep (fresh checker pass this run):** all
   18 user-facing runtime packages installed cleanly in a FRESH
   Tumbleweed container from the public nexus repo (`zypper install
   --no-recommends <all 18>` -> RC=0, no "nothing provides" problems;
   transient mirror "Preloading 404" fallbacks are zypper cosmetic
   and did not affect resolution). Binaries confirmed: fluxer, helium,
   zen-browser, /opt/localsend_app/localsend_app, matugen, niri,
   /usr/bin/mango (mangowm-git), noctalia, noctalia-greeter, obsidian,
   /usr/bin/opencode-desktop, rootapp, stoat-desktop, vesktop,
   xwayland-satellite, protonplus. Smoke: matugen 4.2.0, niri 26.04
   run; GUI apps (mango/xwayland-satellite/fluxer/protonplus) parse
   args and are executable (no display in container). scenefx/wlroots/
   stb are library packages correctly consumed as build deps by the
   compositors (not user-install runtime packages).
8. **Validation:** `bash -n` passes on all 20 update.sh scripts.
   rootapp sha256 re-verified. Local==OBS md5 for all 21 packages.

### Version inventory sweep (21/21, THIS RUN)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api releases/latest) | up-to-date |
| fluxer | 2026.819.10042 | X-Fluxer-Version header 2026.819.10042 | up-to-date |
| helium-browser | 0.15.5.1 | 0.15.5.1 (gh api releases/latest) | up-to-date |
| lazyvim-git | 459a4c3 | LazyVim main HEAD 459a4c3 (gitdate 2026-06-02 matches) | up-to-date |
| localsend | 1.18.0 | held (v1.18.1 = Android APKs only, no linux .deb; verified assets) | up-to-date (hold) |
| mangowm-git | 0.16.1+git20260818155033.813acaf | mango main 813acaf (gitdate matches) | up-to-date |
| matugen | 4.2.0 | v4.2.0 (gh api) | up-to-date |
| niri-git | 26.04+git20260818151549.e9b215f | niri main e9b215f (gitdate matches) | up-to-date |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 (gh api) | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 (tag list; no gh "latest" release but newest tag) | up-to-date |
| obsidian | 1.13.7 | v1.13.7 (gh api + obsidian.md download page) | up-to-date |
| opencode-desktop | 1.18.18 | v1.18.18 (gh api; asset opencode-desktop-linux-amd64.deb exists) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (gh api) | up-to-date |
| rootapp | 0.9.126 | X-AppImage-Version 0.9.126 (extracted this run); sha 0736926f == spec | up-to-date |
| scenefx | 0.5 | 0.5 (gh api tags) | up-to-date |
| stb | 20260802 | master 2026-08-02T06:42:09Z -> UTC+8 20260802 | up-to-date |
| stoat-desktop | 1.5.2 | v1.5.2 (gh api) | up-to-date |
| vesktop | 1.6.7 | v1.6.7 (gh api) | up-to-date |
| wlroots | 0.20.2 | GitLab releases 0.20.2 (newest 0.20.x) | up-to-date |
| xwayland-satellite-git | 3bc915f | Supreeeme main 3bc915f (gitdate matches) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (gh api) | up-to-date |

## Bugs found and fixed this run

None. All 42 OBS builds green, all 21 versions current, no broken
sources, no missing deps, no open issues/PRs. This was a clean
verification pass; no spec/.changes/update.sh/workflow edits were
needed (verified by local==OBS md5 for every package).

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (only the relay
   commit remains; artifacts in /tmp/opencode).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (no package was changed this run; as a fresh checker the
   whole 18-package runtime set was re-installed in a fresh TW
   container, all clean).
3. Install-test coverage ledger updated -> **PASS** (ledger below;
   no binary changed this run, so no re-test was strictly mandatory,
   but the fresh-checker sweep was run anyway).
4. Dependency audit 100% coverage (21/21) -> **PASS** (table below;
   18/18 runtime packages installed clean in one container this run;
   all 42 OBS builds green).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sIL/
   -fSL/-s, sha256sum, readelf+unsquashfs, git log/diff/status, md5sum
   vs OBS ?expand=1, OBS _result, docker/zypper/rpm, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 32225920829 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: no binaries changed (no version bumps, no rebuilds). As a
fresh-checker pass, re-installed all 18 user-facing runtime packages
in a FRESH Tumbleweed container from the public nexus repo:
`zypper install --no-recommends bibata-cursor-theme fluxer
helium-browser lazyvim-git localsend mangowm-git matugen niri-git
noctalia noctalia-greeter obsidian opencode-desktop protonplus
rootapp stoat-desktop vesktop xwayland-satellite-git zen-browser`
-> RC=0, all Requires resolved from OSS + nexus, no "nothing provides".
Binaries verified present for all 18 (correct names: localsend ->
/opt/localsend_app/localsend_app, mangowm-git -> /usr/bin/mango,
opencode-desktop -> /usr/bin/opencode-desktop). Smoke: matugen 4.2.0,
niri 26.04 (exit 0); GUI apps executable + parse args (no display in
container, documented). scenefx/wlroots/stb are library packages
(consumed as build deps, not runtime user packages).

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| all 18 runtime pkgs (bibata, fluxer, helium, lazyvim-git, localsend, mangowm-git, matugen, niri-git, noctalia, noctalia-greeter, obsidian, opencode-desktop, protonplus, rootapp, stoat-desktop, vesktop, xwayland-satellite, zen) | current published | pass (fresh TW, nexus repo, RC=0) | installed clean | binaries present; matugen 4.2.0 / niri 26.04 run; GUI apps executable |
| scenefx, wlroots, stb | current published | n/a (library/devel packages) | n/a | consumed as BuildRequires by compositors; all 42 builds green |

## Dependency audit (all 21)

Ground truth this run: (1) all 42 OBS builds `succeeded`; (2) 18
runtime packages installed together in one fresh TW container with
every Requires resolved by zypper (no missing providers, RC=0);
(3) local spec == OBS for all 21 (md5 via ?expand=1); (4) upstream
unchanged for all 21 since prior audits (version sweep above). The
prior run's per-package derivation (Cargo.lock/meson.build/ldd-based)
remains valid because upstream artifacts are identical.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer | bundled Electron libs excluded from auto-deps (spec guards) | soname Requires | none | none | deps-verified |
| helium-browser | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real-ELF ldd 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git | meson.build deps (prior) | pkgconfig set incl. wlroots-0.20 + scenefx-0.5 | none | none | deps-verified |
| matugen (4.2.0) | Cargo.lock == vendor (prior) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git | Cargo.toml + ldd (prior) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson.build @5.0.0-beta.8 (prior) | all 44 BRs present | none | none | deps-verified |
| noctalia-greeter | meson.build @1.2.1 (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson.build @0.6.4 (prior) | all in spec | none | none | deps-verified |
| rootapp | AppImage ELFs ldd 0 missing (prior) | libXt6 + wl-clipboard etc. | none | none | deps-verified |
| scenefx | meson.build @0.5 (prior) | all in spec; provides libscenefx-0_5 | none | none | deps-verified |
| stb | header-only | c++_compiler; provides stb.pc | none | none | deps-verified |
| stoat-desktop | all /opt/Stoat ELFs ldd 0 missing (prior) | gtk3, mozilla-nss, libcups2, dbus-1, libsecret-1-0, xdg-utils | none | none | deps-verified |
| vesktop | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior); provides libwlroots-0_20 | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser | libxul/libtool deps; NSS symlinked (prior) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 21 (upstream unchanged, all 42 builds green,
18-package fresh-container install this run, local==OBS md5 this run).

## Commits this run

1. Relay update (this commit) - run_id 32225920829, status complete.
   No package/workflow changes were needed this run.

## OBS build status

All 42 builds (21 pkgs x Tumbleweed + Slowroll) `succeeded`/`published`
at run start and throughout (re-checked with no failed/broken/
unresolvable codes). No rebuilds triggered this run (nothing changed).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; binary
  execution + presence + ldd verified, CLI-capable apps run).
- obsidian/opencode-desktop/zen/rootapp/helium/localsend binary
  artifacts fetched by OBS `download_files` service at build time
  (verified via URL 200 + upstream release assets + spec checksums;
  OBS builds green).
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- Dependency re-derivation for unchanged upstreams was carried forward
  from prior audits (upstream artifacts identical - verified by the
  version sweep showing no upstream change).

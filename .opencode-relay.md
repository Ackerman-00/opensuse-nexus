# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31590795781

## Run Summary (31590795781)

Autonomous maintenance pass over all 17 packages in home:ackerman.
Started 11:12Z. Found and fixed a wrong-version bug on mangowm-git
(upstream bumped 0.15.6 -> 0.16.0) and hardened the three -git
updater scripts against the same latent flaw.

1. **mangowm-git: FIXED-WRONG-VERSION (0.15.6 -> 0.16.0).** The 10:52Z
   auto-updater (run 31589341936, commit ac6b0a0) bumped the pinned
   commit to 7bb3f7e ("bump version to 0.16.0") but the hardcoded
   `Version: 0.15.6+git...` prefix in the spec stayed stale while
   upstream's meson.build now declares `version: '0.16.0'`. Verified:
   `curl` meson.build at 7bb3f7e -> `version : '0.16.0'`; Cargo/meson
   at de691ce -> `0.15.6`. Fixed the spec Version prefix to 0.16.0
   (commit 70e59d6), added a .changes entry, and fixed the .changes
   ordering (48a90fe). update.sh now ALSO rewrites the Version prefix
   from the upstream meson.build on every bump, so this cannot recur;
   the up-to-date guard remains a pure commit-SHA comparison. OBS
   re-uploaded (rev 101->104, spec+changes), rebuilt both repos:
   `mangowm-git-0.16.0+git20260812063157.7bb3f7e-2.1` SUCCEEDED in
   openSUSE_Tumbleweed and openSUSE_Slowroll. Full update path tested
   in /tmp: simulated 4a3bff4->7bb3f7e bump correctly rewrote commit
   AND Version prefix AND appended changelog; guard returns exit 0
   with zero file changes when already current.

2. **niri-git + xwayland-satellite-git updater hardening (commit
   acea886).** Both update.sh scripts had the same latent flaw: they
   bump commit/shortcommit/gitdate but never reconcile the hardcoded
   Version prefix with upstream's own version when main moves. Added
   prefix sync: niri reads `[workspace.package] version` and converts
   the calendar scheme (26.4.0 -> 26.04, matching upstream tags v26.04
   / v25.05.1); xwayland-satellite reads `[package] version` directly
   (0.8.2). No spec edits needed today (current prefixes already
   correct). No-churn guards verified: both scripts exit 0 with no
   file modification @ fec411e / 3bc915f (md5-compared before/after).

3. **Version sweep 17/17** - every package's packaged version
   verified against true upstream this run (gh api releases/tags,
   git ls-remote clone, curl headers, artifact tear-down). Only
   mangowm needed a correction. No wrong or stale versions elsewhere.

4. **No other changes needed.** No failed OBS builds, no broken
   workflows, no open issues, no open PRs, README accurate.

### Inventory (17/17 match repo dirs = README = OBS)
bibata-cursor-theme, fluxer, localsend, mangowm-git, matugen, niri-git,
noctalia, noctalia-greeter, obsidian, opencode-desktop, rootapp, scenefx, stb,
vesktop, wlroots, xwayland-satellite-git, zen-browser. No missing/extra
packages on either side; README table matches.

### OBS build status
All 34 builds (17 pkgs x 2 repos = openSUSE_Tumbleweed + openSUSE_Slowroll)
report `succeeded`/`published` (queried twice; failure filter empty).
mangowm-git was rebuilt this run to `0.16.0+git20260812063157.7bb3f7e-2.1`.
OBS spec + changes byte-identical to git (diff confirmed). OBS tarball
md5 7f49746a... == locally-downloaded github archive (confirmed).

### Version sweep - 17/17 verified against upstream this run
| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api releases/latest) | up-to-date |
| fluxer | 2026.810.212541 | X-Fluxer-Version header on Source0 (200, 92MB) | up-to-date |
| localsend | 1.18.0 | v1.18.0 (gh api; DEB URL 200) | up-to-date |
| mangowm-git | 0.16.0+git20260812063157.7bb3f7e | 7bb3f7e = main HEAD; meson.build 0.16.0 | FIXED-WRONG-VERSION (70e59d6) |
| matugen | 4.1.0 | v4.1.0 (gh api; tar.gz 200) | up-to-date |
| niri-git | 26.04+git20260811204258.fec411e | fec411e = main HEAD; Cargo 26.4.0=26.04 | up-to-date |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 (tags+releases; URL 200) | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 (tags) | up-to-date |
| obsidian | 1.13.6 | v1.13.6 (releases/latest; tar.gz 200) | up-to-date |
| opencode-desktop | 1.18.16 | v1.18.16 (releases/latest; DEB 200) | up-to-date |
| rootapp | 0.9.126 | AppImage re-torn: X-AppImage-Version=0.9.126; sha256 0736926f... == spec; fedora-nexus 0.9.126 same sha | up-to-date |
| scenefx | 0.5 | tag+release 0.5 | up-to-date |
| stb | 20260802 | master HEAD 2c980bb dated 2026-08-02T06:42:09Z | up-to-date |
| vesktop | 1.6.5 | v1.6.5 (releases/latest; RPM URL 200) | up-to-date |
| wlroots | 0.20.2 | GitLab releases API 0.20.2 | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f | 3bc915f = main HEAD; Cargo 0.8.2 | up-to-date |
| zen-browser | 1.21.13b | 1.21.13b (releases/latest; tar.xz 200) | up-to-date |

### No-churn
mangowm is the ONLY package that changed upstream binary this run.
- git log before bumping confirmed the prior state (de691ce at c1ee11c,
  0.15.6); the new 7bb3f7e commit was NOT already in git log/OBS as
  the same build - the spec Version was stale against it, so a fix was
  genuinely required, not churn.
- OBS carried 7bb3f7e spec/changes from 10:52Z auto-updater; our re-sync
  changed only the Version prefix + changelog entry (rev 101->104),
  not a duplicate of an existing successful build.
- Exactly ONE "Commit: 7bb3f7e" .changes entry exists (grep count 1).
- niri/xwayland specs NOT bumped (upstream unchanged); only update.sh
  hardened. No OBS re-sync, no rebuild - zero churn.
- stb/matugen/others guards verified no-re-entry (version/date/sha
  comparisons, exit 0).

### Spec validity / updater health
- `bash -n` all 16 update.sh OK
- `rpmspec -P` all 17 specs OK
- GitHub Actions: last 30+ runs all success; no failed Nexus
  Auto-Updater runs. update-packages.yml reviewed: Phase 1/2/3 +
  Smart Sync logic sound; update.sh failures non-fatal; pure
  commit-SHA guards present everywhere.
- Auto-updater log for 31589341936 confirmed every package "no update
  needed" except mangowm (commit bump only - the version gap it missed,
  which this run fixed in the script).

### Issues/PRs
`gh issue list --state open` => 0 open; `gh pr list` => 0 open.
Nothing to triage.

### Tumbleweed snapshot
Fresh container /etc/os-release = 20260811 (current; web search
confirms week 2026/32 = snapshots 0731..0805 era + 0808.. continued,
no Leap 16 involvement). Slowroll lags ~5-10 days behind TW (identical
binaries; TW proof per policy).

### Install-test coverage ledger (current binaries as of this run)
| package | last install-tested | result | smoke |
|---|---|---|---|
| bibata-cursor-theme (2.0.7) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (icons) |
| fluxer (2026.810.212541) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| localsend (1.18.0-15.1) | 2026-08-12 run 31590795781 re-verified (mirror RPM) | pass | installs clean |
| mangowm-git (0.16.0+git20260812063157.7bb3f7e-2.1) | 2026-08-12 run 31590795781 (binary CHANGED 0.16.0), 2 fresh containers | pass | mango 0.16.0(release) |
| matugen (4.1.0-30.1) | 2026-08-12 run 31590795781 re-verified (mirror RPM) | pass | matugen 4.1.0 |
| niri-git (26.04+fec411e-5.1) | 2026-08-12 run 31590795781 re-verified (mirror RPM) | pass | niri 26.04 (fec411e) |
| noctalia (5.0.0~beta8) | 2026-08-11 run 31536365363 (binary unchanged) | pass | v5.0.0 |
| noctalia-greeter (1.2.1) | 2026-08-11 run 31536365363 (binary unchanged) | pass | --help usage |
| obsidian (1.13.6) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| opencode-desktop (1.18.16) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| rootapp (0.9.126-2.1) | 2026-08-12 run 31590795781 re-verified (mirror RPM) | pass | --version starts app, exit 0 |
| scenefx (libscenefx-0_5) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (lib) |
| stb (stb-devel) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (header lib) |
| vesktop (1.6.5) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| wlroots (libwlroots-0_20) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (lib) |
| xwayland-satellite-git (3bc915f) | 2026-08-11 run 31536365363 (binary unchanged) | pass | -version => 0.8.2 |
| zen-browser (1.21.13b) | 2026-08-11 run 31536365363 (binary unchanged) | pass | Mozilla Zen 1.21.13b |

Only mangowm's binary changed since the last ledger entry; it was
install-tested end-to-end in fresh containers (twice) plus fresh-checker
re-verifications of rootapp/localsend/niri/matugen from the public
mirror. Fresh-checker rule satisfied: independent tail pass ran in new
containers, not reusing prior state.

### Dependency audit (all 17)
Ground truth `_builddepinfo` (openSUSE_Tumbleweed/x86_64) covers all 17
packages with ZERO `<error>` entries (only deps that legitimately contain
"error" in their name: libgpg-error-devel, libxcb-errors0,
xcb-util-errors-devel). mangowm RE-DERIVED against the new 7bb3f7e
artifact: meson.build dependency() list = libdrm, xcb, xcb-icccm,
wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon,
libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0,
pixman-1, libcjson, pangocairo -> all 13 have matching BuildRequires;
OBS builddepinfo resolves exactly those (wlroots-devel, scenefx-devel,
libxcb-devel, xcb-util-wm-devel, cJSON-devel, etc.). Runtime soname
Requires satisfied (installs clean, `rpm -V` clean).

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.810) | Electron bundled; RPM torn via rpm2cpio (prior audit) | soname Requires + named (all exist in OSS) | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd (prior audit) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (0.16.0/7bb3f7e) | meson 13 deps (RE-DERIVED this run) | all 13 mapped | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored (unchanged upstream) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (fec411e) | niri.spec.rpkg BR set (unchanged) | all mapped | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (unchanged upstream) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (unchanged upstream) | all in spec | none | none | deps-verified |
| obsidian (1.13.6) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.16) | DEB control (prior audit) | all mapped (named pkgs verified in OSS) | none | none | deps-verified |
| rootapp (0.9.126) | AppImage torn this run; ldd of bundled Root/libSkiaSharp etc 0 not-found | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson deps (unchanged upstream) | all in spec | none | none | deps-verified |
| stb (20260802) | header-only (unchanged upstream) | c++_compiler | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled (unchanged) | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson deps (unchanged upstream) | all in spec | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson (unchanged upstream) | all in spec | none | none | deps-verified |
| zen-browser (1.21.13b) | Firefox tarball (unchanged) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 17 (_builddepinfo clean across the board,
re-derived where upstream moved, clean container installs agree).

### Commits this run
- 70e59d6 fix(autonomous): mangowm-git: sync Version prefix
  0.15.6->0.16.0, update.sh keeps prefix in sync
- acea886 fix(autonomous): niri-git, xwayland-satellite-git: keep
  Version prefix in sync with upstream
- 48a90fe chore(autonomous): mangowm-git: fix .changes entry ordering
  (newest first)
- (relay + run record commit)

### Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp only;
   verified clean after relay commit).
2. Every package touched this run install-tested in a clean TW
   container incl. fresh-checker re-runs -> **PASS** (mangowm 0.16.0:
   2 fresh containers; rootapp/localsend/niri/matugen re-verified from
   public mirror in fresh containers).
3. Install-test coverage ledger updated -> **PASS** (table above;
   mangowm row references the 0.16.0 binary; re-verified rows dated
   this run).
4. Dependency audit 100% coverage (17/17) -> **PASS** (table above;
   mangowm re-derived from new artifact, rootapp re-torn).
5. Claims backed by command output -> **PASS** (gh api, curl -sIL,
   sha256sum/md5sum, readelf+unsquashfs, rpmspec -P, bash -n, diff
   vs OBS files, OBS _result/_builddepinfo XML, docker/zypper/rpm -V
   logs).
6. Named gaps -> see below.
7. Relay updated with run_id 31590795781 + status complete -> **PASS**
   (this commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64
  proof per policy).
- Slowroll container installs (identical binaries ~5-10 days behind TW;
  TW proof per policy).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution verified instead; electron apps show the documented
  running-as-root sandbox fatal, Docker-only).
- mangowm/niri/xwayland upstreams are extremely active; pinned commits
  may advance before the next auto-updater. The version-prefix sync
  added this run guarantees the next bump cannot leave a stale Version
  again - but on upstream release-version bumps the scripts now track
  it correctly.
- matugen/xwayland-satellite vendor tarballs not independently
  re-extracted this run (upstream commits unchanged since the last
  full audit and OBS offline builds succeed).
- wlroots direct GitLab tarball download blocked by anti-bot from this
  runner (verified via releases API + OBS publishes fine).
- scenefx has an empty .changes on OBS and locally (library package;
  builds and functions fine; pre-existing, not introduced this run).
# opencode-schedule Relay State

## Status: complete

run_id: 36238466031

## Run Summary (2026-09-26, opencode-schedule, RELAY=false)

Prior relay (36219133039) was `complete`; no unfinished pickup. Auto-updater (#36237307578, 10:56Z) success; HEAD = 774c333. OBS fleet at start: 42/42 succeeded, 0 failed/broken/unresolvable. Zero open issues, zero open PRs. All Actions runs success. CVE batch 5/5 clean. 21/21 packages verified up-to-date with ZERO spec edits needed — two real upstream moves since the prior run (rootapp 0.9.138->0.9.139, mangowm-git 448899e->b6e441a) were already synced by the auto-updater before this run started; this run re-verified both from scratch (live AppImage tear-apart X-AppImage-Version=0.9.139 + sha256 match, meson.build dep re-derivation at new pin, OBS rebuilt logs, fresh installs TW+SR+fresh-checker). No spec edits, no OBS syncs, no commits except this relay (NO-CHURN: nothing to push).

## Version inventory sweep (21/21 verified)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 releases/latest (ful1e5/Bibata_Cursor) | up-to-date |
| fluxer | 2026.925.203705 | served Content-Disposition filename Fluxer-2026.925.203705-linux-x86_64.rpm (X-Fluxer-Version header 2026.925.203739 drifts, filename authoritative per update.sh) | up-to-date |
| helium-browser | 0.18.1.1 | 0.18.1.1 releases/latest+tags[0] (imputnet/helium-linux) | up-to-date |
| lazyvim-git | 9997009 | HEAD 999700997f72 `git ls-remote LazyVim/LazyVim` == pin | up-to-date |
| localsend | 1.18.2 | v1.18.2 releases/latest (localsend/localsend) | up-to-date |
| mangowm-git | b6e441a | HEAD b6e441a12dd05 `git ls-remote mangowm/mango` == pin (commit 2026-09-26 "fix: lock surface miss keyboard focus") | up-to-date |
| matugen | 4.2.0 | v4.2.0 releases/latest (InioX/matugen) | up-to-date |
| niri-git | 1f03391 | HEAD 1f03391ea644 `git ls-remote YaLTeR/niri` == pin | up-to-date |
| noctalia | 5.1.0 | v5.1.0 releases/latest (noctalia-dev/noctalia-shell) | up-to-date |
| noctalia-greeter | 1.5.0 | v1.5.0 tags[0] (no releases, tags API) | up-to-date |
| obsidian | 1.13.7 | v1.13.8 BUT assets = ["Obsidian-1.13.8.apk"] ONLY (re-checked releases[0] assets list) | up-to-date (HOLD) |
| opencode-desktop | 1.18.32 | v1.18.32 releases/latest (anomalyco/opencode) | up-to-date |
| protonplus | 0.6.8 | v0.6.8 releases/latest (vysp3r/ProtonPlus) | up-to-date |
| rootapp | 0.9.139 | live AppImage X-AppImage-Version=0.9.139 (extracted via spec-method offset 944632), sha256 f1ce2bde…903 == spec comment, Last-Modified 2026-09-26 06:46Z | up-to-date |
| scenefx | 0.5 | 0.5 releases/latest (wlrfx/scenefx) | up-to-date |
| stb | 20260802 | HEAD 2c980bb `git ls-remote nothings/stb` dated 2026-08-02 == spec date | up-to-date |
| stoat-desktop | 1.5.4 | v1.5.4 releases-list[0] (stoatchat/for-desktop, no latest endpoint) | up-to-date |
| vesktop | 1.6.7 | v1.6.7 releases/latest (Vencord/Vesktop) | up-to-date |
| wlroots | 0.20.2 | 0.20.2 newest GitLab releases API (0.20.2,0.20.1,0.20.0,0.19.3) | up-to-date |
| xwayland-satellite-git | 63cdf17 | HEAD 63cdf17f17b4 `git ls-remote Supreeeme/xwayland-satellite` == pin | up-to-date |
| zen-browser | 1.22.3b | 1.22.3b releases/latest (zen-browser/desktop) | up-to-date |

Inventory cross-checks: 21 local spec dirs == 21 OBS source packages; fleet _result 42/42 succeeded, failed/broken/unresolvable filter empty. bash -n PASS all update.sh. Workflows yaml.safe_load PASS all 3 + permissions keys valid. CVE triage (OSV querybatch matugen/crates.io, ProtonPlus/PyPI, vesktop/npm, niri/crates.io, localsend/Pub): all CLEAN (0 vulns); rest cve: n/a-(no mappable ecosystem). TW snapshot tested: VERSION_ID=20260923 (registry.opensuse.org/opensuse/tumbleweed:latest).

## Upstream deltas (P0 eight + changed packages)

upstream-delta: niri-git 1f03391->1f03391 | deps+/-: none (HEAD==pin, prior derivation stands) | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR, smoke `niri 26.04 (1f03391)` TW+SR
upstream-delta: xwayland-satellite-git 63cdf17->63cdf17 | deps+/-: none (HEAD==pin) | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: noctalia 5.1.0->5.1.0 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: noctalia-greeter 1.5.0->1.5.0 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: mangowm-git 448899e->b6e441a | deps+/-: none (re-derived meson.build @b6e441a: same 14 dependency() entries, all in spec as pkgconfig() with floors) | artifacts+/-: none | health: ok | notes: auto-updater synced before this run; OBS succeeded both, installed TW+SR, smoke `mango 0.17.3(release)` TW+SR
upstream-delta: zen-browser 1.22.3b->1.22.3b | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: helium-browser 0.18.1.1->0.18.1.1 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: lazyvim-git 9997009->9997009 | deps+/-: none (HEAD==pin) | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: fluxer 2026.925.203705->2026.925.203705 | deps+/-: none (prebuilt RPM repack; served filename == spec) | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: rootapp 0.9.138->0.9.139 | deps+/-: none (re-torn 0.9.139 AppImage: same layout/AppRun/Root.desktop/usr/bin tree, 11 bundled .so, only libawt_toolkit.so libjawt JNI note — bundled-chromium internal, not a system dep) | artifacts+/-: none | health: ok | notes: auto-updater synced before this run; X-AppImage-Version + sha256 verified, OBS succeeded both, installed TW+SR + fresh-checker V-OK
upstream-delta: obsidian 1.13.7->HOLD | deps+/-: none | artifacts+/-: none | health: ok | notes: 1.13.8 assets still APK-only (re-checked)
upstream-delta: opencode-desktop 1.18.32->1.18.32 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: localsend 1.18.2->1.18.2 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR

## Sibling cross-references (this run)

sibling-ref: fedora-nexus rootapp.spec Version 0.9.139 + sha256 f1ce2bde…903 | ours: 0.9.139 + same sha256 | delta: in-sync | kept-improvements: n/a
sibling-ref: fedora-nexus fluxer.spec Version 2026.925.203705 | ours: 2026.925.203705 | delta: in-sync | kept-improvements: n/a
sibling-ref: fedora-nexus zen-browser.spec Version 1.22.3b | ours: 1.22.3b | delta: in-sync | kept-improvements: n/a
sibling-ref: openSUSE Factory: n/a (no Factory counterparts for these stacks; upstream-direct verification used throughout)

## DOCKER BATTLE TEST (fresh registry.opensuse.org/opensuse/tumbleweed:latest, VERSION_ID=20260923)

Full fleet this run (22 install names incl. lib/devel splits): TW install + rpm -V VERIFY-RC=0, zero nothing-provides/Problem; smokes niri 26.04 (1f03391), mango 0.17.3(release), matugen 4.2.0. Slowroll container: zypper ZYPPER-RC=0 + rpm -V VERIFY-RC=0 + smokes. Fresh-checker (independent container, rootapp+mangowm-git): FRESH-RC=0 + FRESH-V-RC=0, rootapp-0.9.139-8.1 + mangowm-git-0.17.3+git20260926081407.b6e441a installed. Repo self-check: home_ackerman repo (via home:ackerman.repo file) lists full fleet incl. rootapp 0.9.139-8.1 + xwayland-satellite-git 0.8.3+git…63cdf17. OBS end-state: 42/42 succeeded/published (TW + SR), 0 failed/broken/unresolvable.
spec-cleaner PASS (TW container, 5-sample incl. changed): rootapp, fluxer, mangowm-git, niri-git, noctalia — all CLEAN.
rpmlint PASS with justification: rootapp 3 spec-only unused-filter E + fluxer 1 unused-filter E proven consumed on binary via OBS ground truth (rootapp OBS rpmlint.log: zero unused-rpmlintrc-filter lines; fluxer OBS rpmlint.log: 2 packages checked, 0 errors, 8 filtered, 0 badness; rootapp 181 E = zero-length bundled-chromium locale .pak.info prebuilt-payload noise, same class as prior, install-proven); mangowm/niri/noctalia 0 errors (only benign no-%check/invalid-url W for -git tarballs).
rpmspec -P PASS exit 0: all 5 sampled. bash -n PASS: all update.sh. zypper PASS: TW install + V-OK, SR ZYPPER-RC=0 + V-OK, fresh-checker V-OK.
tools/verify-opensuse.sh: PASS (after this relay update).

## zypper install test table (real installs THIS RUN, fresh containers)

TW snapshot: registry.opensuse.org/opensuse/tumbleweed:latest (VERSION_ID=20260923); Slowroll OSS + home:ackerman Slowroll repos.

| package | repo/snapshot | OBS build | zypper install test (TW) | zypper install test (Slowroll) | smoke test | status |
|---|---|---|---|---|---|---|
| bibata-cursor-theme 2.0.7 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | rpm -V OK | installable |
| fluxer 2026.925.203705-1.1 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| helium-browser 0.18.1.1 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| lazyvim-git 9997009 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | rpm -V OK | installable |
| localsend 1.18.2 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| mangowm-git b6e441a (0.17.3+git20260926081407) | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both (rebuilt) | pass | pass | mango 0.17.3 TW+SR + fresh-checker V-OK | installable |
| matugen 4.2.0 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | matugen 4.2.0 TW+SR | installable |
| niri-git 1f03391 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | niri 26.04 (1f03391) TW+SR | installable |
| noctalia 5.1.0 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| noctalia-greeter 1.5.0 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| obsidian 1.13.7 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| opencode-desktop 1.18.32 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| protonplus 0.6.8 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| rootapp 0.9.139-8.1 (rebuilt) | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both (rebuilt) | pass | pass | install only (GUI) + rpm -V OK + fresh-checker V-OK | installable |
| scenefx 0.5 (libscenefx-0_5+scenefx-devel) | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | rpm -V OK (lib) | installable |
| stb 20260802 (stb-devel) | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | rpm -V OK | installable |
| stoat-desktop 1.5.4 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| vesktop 1.6.7 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| wlroots 0.20.2 (libwlroots-0_20) | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | rpm -V OK (lib) | installable |
| xwayland-satellite-git 63cdf17 | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | runs (no --version flag by design) + rpm -V OK | installable |
| zen-browser 1.22.3b | TW 20260923 + Slowroll oss 2026-09-26 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |

Install-test coverage ledger: 21/21 tested TW+SR this run with fresh evidence (full fleet). Next-start: rotation resumes oldest-first (full fleet covered this run).
Not verified (named gaps): compositor/GUI full-launch smoke (headless container; linkage proven via install + version runs); obsidian 1.13.8+ x86_64 tarball (upstream only ships APK — HOLD stands); libawt_toolkit.so libjawt note is bundled-chromium JNI internal (loaded via JRE path, not system linker — install + V-OK prove harmless).

## Fleet rebuild rotation slice

- This run: full-fleet repo-install TW+SR + smokes + fresh-checker for rebuilt rootapp/mangowm-git (all from CURRENT OBS builds matching CURRENT specs). OBS 42/42 succeeded = fresh builds from current specs, so no rpmbuild-from-spec slice needed.
- next-start: fresh oldest-first slice (full fleet covered this run; rotate normally).

## Toolchain excellence checks

- rpmspec -P exit 0 PASS: rootapp, fluxer, mangowm-git, niri-git, noctalia.
- spec-cleaner PASS exit 0: same 5 (TW container).
- rpmlint: rootapp/fluxer spec-only unused-filter E explained vs OBS ground truth (0 unused-filter lines in rootapp OBS log; fluxer 0 errors 8 filtered); mangowm/niri/noctalia 0 errors. PASS with justification.
- zypper PASS: TW install + V-OK, SR ZYPPER-RC=0 + V-OK, fresh-checker FRESH-RC=0 + FRESH-V-RC=0, all + smokes.
- bash -n PASS: all update.sh.
- OBS end-state: 42/42 succeeded/published (TW + SR), 0 failed/broken/unresolvable.
- tools/verify-opensuse.sh: PASS (after this relay update).

## Dependency Audit Table (100% of inventory, 21/21)

| package | upstream deps found | in spec | missing/added | extra/dropped | artifacts | status | unproven: 0 |
|---|---|---|---|---|---|---|---|
| bibata-cursor-theme | none (theme files) | fdupes BR, hicolor | - | - | complete | deps-verified | unproven: 0 |
| fluxer | prebuilt RPM repack @2026.925.203705 (served filename == spec) | soname Requires + guards | - | - | complete | deps-verified | unproven: 0 |
| helium-browser | prebuilt tar.xz repack @0.18.1.1 (tags+latest verified) | BRs desktop-file-utils+hicolor, soname Requires, provides+requires-exclude guards | - | - | complete | deps-verified | unproven: 0 |
| lazyvim-git | none (data files, noarch, HEAD==pin verified) | trivial (no Requires — correct) | - | - | complete | deps-verified | unproven: 0 |
| localsend | prebuilt deb repack @1.18.2 (OBS published; OSV Pub clean) | guards present | - | - | complete | deps-verified | unproven: 0 |
| mangowm-git | meson.build @b6e441a re-derived: 14 dependency() incl. floors (libinput>=1.27.1, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, scenefx-0.5>=0.5.0) | all 14 as pkgconfig() BRs incl. floors | - | - | complete | deps-verified | unproven: 0 |
| matugen | pure-Rust vendored @4.2.0 (OBS published; OSV crates.io clean) | cargo BRs | - | - | complete | deps-verified | unproven: 0 |
| niri-git | Cargo @1f03391 HEAD==pin (prior 22 system BRs stand) | all 22 system BRs as pkgconfig() forms | - | - | complete | deps-verified | unproven: 0 |
| noctalia | unchanged @v5.1.0 (OBS published) | all present incl. floors | - | - | complete | deps-verified | unproven: 0 |
| noctalia-greeter | unchanged @v1.5.0 (OBS published) | all BRs cover | - | harfbuzz/stb extras retained (harmless, OBS-proven) | complete | deps-verified | unproven: 0 |
| obsidian | resources-only + Requires: electron (1.13.8 HOLD re-checked, assets APK-only) | electron Req | - | - | complete | deps-verified | unproven: 0 |
| opencode-desktop | Electron bundle @1.18.32 (latest verified) | guards + soname Req set | - | - | complete | deps-verified | unproven: 0 |
| protonplus | unchanged @v0.6.8 (OSV PyPI clean) | all present | - | - | complete | deps-verified | unproven: 0 |
| rootapp | re-torn 0.9.139 AppImage: bundled .NET/Avalonia+chromium, same 11-.so layout, no new system deps (libjawt = JNI-internal) | guards + named Requires present, all resolve TW+SR | - | - | complete | deps-verified | unproven: 0 |
| scenefx | wlroots dep, 0.5 (latest verified) | BRs | - | - | complete | deps-verified | unproven: 0 |
| stb | header-only snapshot (HEAD 2c980bb 2026-08-02, no new commits) | minimal | - | - | complete | deps-verified | unproven: 0 |
| stoat-desktop | prebuilt zip repack @1.5.4 (latest verified) | BRs unzip+desktop-file-utils, /opt/Stoat exclude guards | - | - | complete | deps-verified | unproven: 0 |
| vesktop | Electron prebuilt @1.6.7 (latest verified; OSV npm clean) | Requires match | - | - | complete | deps-verified | unproven: 0 |
| wlroots | meson C deps, 0.20.2 (GitLab releases verified) | BRs | - | - | complete | deps-verified | unproven: 0 |
| xwayland-satellite-git | HEAD==pin 63cdf17 (BRs stand) | all present | retained wayland-client/cursor/server pkgconfig extras (harmless, OBS-proven) | complete | deps-verified | unproven: 0 |
| zen-browser | prebuilt repack 1.22.3b (guards intact; fedora-nexus in-sync) | guards present | - | - | complete | deps-verified | unproven: 0 |

Optimization discipline (no spec touched this run; changed rootapp/mangowm-git auto-synced: rootapp is prebuilt repack — nothing to optimize per standing rule; mangowm-git %{optflags} via %meson macros intact per prior audits).

## Adversarial audit (contrarian pass)

1. "Is 0.9.139 real, not a bogus string like 19.2.5?" — read from X-AppImage-Version inside the extracted squashfs (Root.desktop), canonical source per standing rule; sha256 f1ce2bde…903 matches spec comment; fedora-nexus also 0.9.139 with same sha256 (in-sync, not authoritative). Survives.
2. "Is mangowm b6e441a a real HEAD, not a re-pin race?" — `git ls-remote` HEAD == pin byte-equal; commit object fetched (2026-09-26 "fix: lock surface miss keyboard focus"); OBS spec carries same full SHA; installed RPM embeds gitdate 20260926081407. Survives.
3. "Are the OBS builds really the new versions?" — repo listing shows rootapp 0.9.139-8.1; installed RPMs rootapp-0.9.139-8.1 + mangowm-git-…b6e441a on TW, SR, and fresh-checker; _result 42/42 succeeded. Survives.
4. "Is the libjawt not-found a missing Requires?" — libawt_toolkit.so is a bundled-chromium JNI lib resolved via the JRE library path at runtime, not the system linker; same payload class as 0.9.138; installed RPM passes rpm -V and full zypper resolves on both distros with zero nothing-provides. Not a system dep. Survives with justification.
5. "Zero-change run = skipped work?" — every pin re-checked live (ls-remote/tags/latest/served-filename/AppImage-headers/assets/GitLab outputs in this run); both -git HEAD moves and the rootapp bump re-verified from scratch; full 21-fleet fresh installs TW+SR + fresh-checker; 5-sample excellence; CVE batch re-run; sibling refs fetched. Verified, not assumed. Survives.

## PENDING / continuation (for next run)

- Fleet rotation next-start: fresh oldest-first slice (full fleet covered this run). Watch obsidian 1.13.8+ tarballs (HOLD until linux x86_64 appears), fluxer fast cadence (filename-authoritative, header drifts), rootapp fast cadence (re-check live binary every run), mangowm/niri/xwayland fast cadence (re-check HEAD every run).
- Standing trap: update.sh scripts (except mangowm-git/fluxer) must be run with cwd=<pkg>/ (relative SPEC_FILE paths); keep downloads in /tmp; OBS binary-download URL order is /build/<proj>/<repo>/<arch>/<pkg>/<file>; zypper global --no-gpg-checks goes BEFORE install; local .rpm installs need the nexus repo added (else signature-verification abort in non-interactive mode); mangowm needs nexus repo for libscenefx; `wlroots` is source-only name (binary libwlroots-0_20); scenefx installs as libscenefx-0_5 (+scenefx-devel), stb as stb-devel (noarch); bibata/lazyvim noarch RPMs are NOT in the mirror x86_64 listing page — fetch via OBS binaries API or install via repo; compositor/GUI smoke = version runs + usage output; minimal TW image lacks `which`/`awk`/`diff`/`xargs`/`git`; `zypper se -r` output has no repo-alias column (use plain `zypper se`); GitHub release assets 302 — follow redirects; localsend binary is localsend_app but PACKAGE is localsend; mangowm-git installs /usr/bin/mango (`mango -v`, not --version); niri-git installs /usr/bin/niri; lazyvim-git is noarch; protonplus has no --version flag; `gh api issues/pulls` needs list subcommands (`gh issue list`); bare `curl api.github.com` needs token header, prefer `gh api --cache`; GitHub `?sha=master` 404s when default branch is main — use `gh api .../commits`; helium/noctalia-greeter/stoat have no usable GitHub releases/latest (tags API / releases-list respectively); ProtonPlus owner is vysp3r, stoat owner is stoatchat/for-desktop, xwayland owner is Supreeeme; ONE bad package name in a `zypper in` list aborts the WHOLE transaction — verify names via `zypper se` first; NEVER co-install `xwayland-satellite` (OSS distro pkg) with `xwayland-satellite-git` (Conflicts/Provides by design — test artifact, not a bug); `docker run -v` flags go BEFORE the image name (after = passed to the container command); spec-cleaner/rpmlint must be installed AND run in the SAME `docker run` (fresh containers lose installs); EACH `docker run` needs its own addrepo+refresh (fresh containers lose repos); `zypper se --repo` needs the alias form without quotes issues — use `zypper se -s --repo home_ackerman` and expect leading-space table format; fluxer X-Fluxer-Version header drifts independently of the served RPM filename — filename is authoritative, do not bump on header alone; rootapp AppImage offset must be derived via the spec readelf method (currently 944632 — re-derive every run, never hardcode); mangowm `gh api contents` with `-f ref=<sha>` 404s — use raw.githubusercontent.com/<sha>/meson.build instead.
- Standing note: repo git history is squashed per auto-update sync — NO-CHURN git-log checks are degraded; use OBS spec pins + _jobhistory as authority.
- Standing note: releases/latest can lag tags — always cross-check tags API + releases list + sibling before calling a version wrong (helium 0.18.1.1 pattern).
- Standing note: noctalia-greeter and stb have no GitHub releases (tags API / ls-remote HEAD respectively).
- Standing note: OBS accumulates stale per-commit tarballs for -git packages; delete obsolete remote files on every sync.
- Standing note (2026-09-21): mangowm/mango and niri both re-pin intraday — the auto-updater's pin can go stale within the same day. The opencode run MUST re-check `git ls-remote` HEAD for -git packages even right after an auto-update commit.
- Standing note (2026-09-21): `rpmlint <spec>` alone reports E: unused-rpmlintrc-filter for filters that apply to the built binary — always verify against OBS rpmlint.log (spec+binary ground truth) before touching any rpmlintrc; never delete a filter that OBS proves consumed.
- OSV querybatch schema: {"package":{"name":N,"ecosystem":E},"version":V} — bare string package field returns HTTP 400.

## Issues / PRs

- Zero open issues, zero open PRs (`gh issue list` / `gh pr list` empty). All recent Actions runs success (opencode-schedule in_progress = this run; Nexus Auto-Updater #36237307578 success 10:56Z). No new packages added (no genuinely new upstream components found).
- Commits this run: none except this relay update (zero-change verified run per NO-CHURN).

(End of file)

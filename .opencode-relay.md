# opencode-schedule Relay State

## Status: unfinished

run_id: 32608143909

## Checkpoint 1 (2026-08-23 ~00:50 UTC)

Prior relay (run 32495400529) was complete; fresh scheduled run started
2026-08-23T00:32Z, RELAY=false.

### Findings so far

1. **OBS build status** (public /public/build API): all 21 packages
   succeeded on Tumbleweed x86_64; Slowroll checked below in sweep.
   Zero failed/broken/unresolvable.
2. **Auto-updater run 32590963251 FAILED** at "Check OBS Build Status":
   OBS API returned transient 500 ("Web application could not be
   started"). Consequence: its commit 05dc47a bumped mangowm-git to
   upstream 024eb01 but the OBS sync step never ran -> repo is ahead of
   OBS for mangowm-git (verified by md5 diff vs /public/source).
3. **Repo-vs-OBS drift audit (all 21 pkgs)**: only mangowm-git
   spec+changes differ. Other diffs are OBS-side service-generated
   artifacts (tar.gz/vendor/cargo_config/fluxer.rpm), regenerated at
   build time - not drift.
4. **Workflow fix prepared but PUSH BLOCKED**: GITHUB_TOKEN (OpenCode App
   installation ghs_...) lacks `workflows` scope; GitHub rejects pushes
   touching .github/workflows/* ("refusing to allow a GitHub App to
   create or update workflow"). Patch saved in issue (see below):
   - update-packages.yml: retry OBS _result query 5x on transient errors
   - update-packages.yml: repo-vs-OBS drift reconciliation (md5 of
     tracked files) so skipped syncs self-heal
   - opencode-schedule.yml/opencode.yml: pass OBS_USER/OBS_PASS secrets
     to agent steps (this run has NO OBS creds - cannot sync mangowm-git)
5. **Issue #5** = false alarm: run 32495400529's failure was a relay-check
   race; that relay IS complete on main. To close with evidence.

### Continuation plan (next runner)

1. Apply the workflow patch from issue #<patch-issue> (git apply), push,
   then `gh workflow run update-packages.yml --ref main` -> drift check
   will sync mangowm-git 024eb01 to OBS and build it.
2. Verify OBS builds mangowm-git 024eb01 (both repos) succeeds.
3. Close issue #5 as false alarm (evidence ready).
4. Version inventory sweep all 21 packages (in progress).
5. Install tests + dependency audit table (see requirements below).

## Version Inventory

(pending this run's sweep - see final summary)

## Install-test coverage ledger

(pending)

# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31432103271

## Verification Summary (Run 31432103271, scheduled pass)

Independent maintenance pass over all 17 packages in home:ackerman.

### Inventory (17/17 match repo dirs = README = OBS)
bibata-cursor-theme, fluxer, localsend, mangowm-git, matugen, niri-git,
noctalia, noctalia-greeter, obsidian, opencode-desktop, rootapp, scenefx, stb,
vesktop, wlroots, xwayland-satellite-git, zen-browser. No missing/extra
packages on either side; README table matches.

### Binaries that changed since the previous run (all re-verified + install-tested)
The 20:32 auto-updater (run 31429511028) bumped three packages; a prior opencode
run had bumped localsend to 1.18.0 at 22:26 (build finished during this run):
- niri-git 97c96a1 -> 464d45d (gitdate 20260810152026 = committer date verified
  via gh api; upstream main HEAD confirmed via git ls-remote)
- noctalia 5.0.0~beta7 -> 5.0.0~beta8 (tag v5.0.0-beta.8 confirmed; tarball URL 200)
- obsidian 1.13.4 -> 1.13.6 (v1.13.6 = releases/latest; tarball downloaded 129MB)
- localsend 1.17.0 -> 1.18.0 (v1.18.0 = latest tag; DEB URL 200; version.json
  INSIDE the DEB: {"version":"1.18.0","build_number":"60"}; DEB control
  Depends: libappindicator3-1|libayatana-appindicator3-1, gir1.2-appindicator*,
  libayatana-ido3-0.4-0, xdg-user-dirs)

OBS now carries all four new binaries and all 34 builds (17 pkgs x 2 repos =
openSUSE_Tumbleweed + openSUSE_Slowroll) report `succeeded`. Failure filter
`&code=failed&code=broken&code=unresolvable` => EMPTY.

### OBS vs local drift
All 17 spec files and all 17 .changes files content-MATCH OBS (md5 per package).
No re-sync needed this run.

### Version sweep (independent, this process) - 17/17 current
- bibata-cursor-theme 2.0.7 = v2.0.7 (gh api; source URL 200, 21MB)
- fluxer 2026.731.153836 = X-Fluxer-Version header (curl -sI api.fluxer.app, 200, 92MB)
- localsend 1.18.0 = v1.18.0 + version.json INSIDE DEB ("1.18.0", build 60)
- mangowm-git 8169bfc = main HEAD (git ls-remote)
- matugen 4.1.0 = v4.1.0 (gh api)
- niri-git 464d45d = main HEAD (git ls-remote, twice; committer date matches gitdate)
- noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api tags)
- noctalia-greeter 1.2.1 = tag v1.2.1 (gh api tags)
- obsidian 1.13.6 = v1.13.6 (gh api releases/latest)
- opencode-desktop 1.18.16 = v1.18.16 (gh api releases/latest; DEB URL 200, 123MB)
- rootapp 0.9.126: AppImage re-downloaded (386MB, sha256 0736926f...d910 == spec
  comment, byte-identical artifact) AND re-extracted via squashfs offset 944632:
  Root.desktop X-AppImage-Version=0.9.126 (independent version confirmation)
- scenefx 0.5 = tags 0.5 (gh api; source URL 200)
- stb 20260802 = master HEAD 2c980bb dated 2026-08-02T06:42:09Z -> 20260802 in
  UTC+8 (matches version scheme)
- vesktop 1.6.5 = v1.6.5 (gh api; RPM URL 200, 84MB)
- wlroots 0.20.2 = GitLab release 0.20.2 (releases API). Direct download served
  an Anubis anti-bot page for THIS runner's IP (403/HTML), but OBS downloaded it
  fine (build succeeded; _service download_files ran on OBS side).
- xwayland-satellite-git 3bc915f = main HEAD (git ls-remote)
- zen-browser 1.21.13b = releases/latest; tarball URL 200 (92MB)

### Source URLs / checksums
All Source0 URLs reachable (curl -sL 200) except wlroots direct download from
this runner (anti-bot; OBS-side download works - build succeeded). matugen/stb
sources are pipeline-uploaded tarballs (present on OBS). rootapp sha256 + internal
version verified. No checksums needed updating.

### No-churn
Isolated-copy runs of niri-git, mangowm-git, xwayland-satellite-git, localsend
update.sh all exit 0 with "already up to date" (pure commit-SHA / version
guards, no artifact-file dependency). No repeated auto-update commits for the
same SHA. No duplicate .changes entries.

### Spec validity / updater health
- `rpmspec -P` all 17 specs OK
- `bash -n` all 16 update.sh OK
- all 12 _service files parse as valid XML (python xml parser)
- GitHub Actions: 5 most recent "Nexus Auto-Updater (OBS Pipeline)" runs all
  success (31429511028, 31381491220, 31346029617, 31334029842, 31308210606).
  No failed runs, nothing to fix.

### Issues/PRs
`gh api issues?state=open` => 0 open; pulls => 0 open. Nothing to triage.

### Tumbleweed snapshot
Clean container /etc/os-release = 20260806 (current; snapshot 20260806 announced
on openSUSE forums 2026-08-08). GCC 16 is the TW default since snapshot 0726
(openSUSE News, Week 31 review) - all builds green on current snapshot. Slowroll
not container-tested (identical binaries ~5-10 days behind TW; TW proof per
policy).

### Install tests (fresh TW containers, snapshot 20260806)
NOTE: download.opensuse.org OSS mirror was flaky from this runner (timeouts +
404s on dependency RPMs during preloading, same infra flake family as previous
runs); OSS repo pointed at ftp.fau.de mirror for all tests. home:ackerman nexus
repo used via download.opensuse.org throughout.

CHANGED binaries, installed from LOCAL RPMs (nexus repo key imported via nexus
repo for signature verification), INSTALL_EXIT=0 for all four, rpm -V all rc=0:
- niri-git 26.04+git20260810152026.464d45d-2.1: `niri --version` => "niri 26.04 (464d45d)" rc=0
- noctalia 5.0.0~beta8-4.1: `noctalia --version` => "noctalia v5.0.0" rc=0
- obsidian 1.13.6-4.1: launches (openSUSE Electron bootstrap message) rc=0
- localsend 1.18.0-8.1: installs; ldd 0 not-found; binary launches to GUI init
  ("cannot open display" = headless Docker only, rc=0 after display init)

USER-PATH test from PUBLIC nexus repo (download.opensuse.org home:ackerman TW):
niri-git + obsidian + noctalia + localsend INSTALL_EXIT=0, all 4 rpm -q match
the exact new versions, `niri --version` = "niri 26.04 (464d45d)".

FRESH-CHECKER broader pass from public nexus repo (unchanged binaries re-checked):
zen-browser, mangowm-git, matugen, vesktop, xwayland-satellite-git all
INSTALL_EXIT=0; `zen-browser --version` => "Mozilla Zen 1.21.13b" rc=0;
`mango -v` => "mango 0.15.6(release)"; `matugen --version` => 4.1.0;
`xwayland-satellite -version` => 0.8.2 rc=0 (correct flag is single-dash
`-version`, verified in src/main.rs of commit 3bc915f; `--version`/`-V`/`--help`
panic by design - NOT a package bug). vesktop installs (Electron, needs display).

### Install-test coverage ledger (current binaries as of this run)
| package | last install-tested | result | smoke |
|---|---|---|---|
| bibata-cursor-theme | 2026-08-10 run 31383627057 (binary unchanged) | pass | n/a (icons) |
| fluxer | 2026-08-10 run 31383627057 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| localsend (1.18.0) | 2026-08-10 run 31432103271 | pass | launches; display needed |
| mangowm-git (8169bfc) | 2026-08-10 run 31432103271 | pass | mango 0.15.6(release) |
| matugen | 2026-08-10 run 31432103271 | pass | 4.1.0 |
| niri-git (464d45d) | 2026-08-10 run 31432103271 | pass | niri 26.04 (464d45d) |
| noctalia (5.0.0~beta8) | 2026-08-10 run 31432103271 | pass | v5.0.0 |
| noctalia-greeter | 2026-08-10 run 31383627057 (binary unchanged) | pass | --help usage |
| obsidian (1.13.6) | 2026-08-10 run 31432103271 | pass | electron bootstrap rc=0 |
| opencode-desktop (1.18.16) | 2026-08-10 run 31383627057 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| rootapp | 2026-08-10 run 31383627057 (binary unchanged) | pass | --version info:Program[0] |
| scenefx (libscenefx-0_5) | 2026-08-10 run 31383627057 (binary unchanged) | pass | n/a (lib, no smoke) |
| stb (stb-devel) | 2026-08-10 run 31383627057 (binary unchanged) | pass | n/a (header lib) |
| vesktop | 2026-08-10 run 31432103271 | pass | electron root-fatal (Docker-only) |
| wlroots (libwlroots-0_20) | 2026-08-10 run 31383627057 (binary unchanged) | pass | n/a (lib, no smoke) |
| xwayland-satellite-git | 2026-08-10 run 31432103271 | pass | -version => 0.8.2 |
| zen-browser (1.21.13b) | 2026-08-10 run 31432103271 | pass | Mozilla Zen 1.21.13b |

### Dependency audit (all 17, high confidence)
OBS `_builddepinfo` (TW/x86_64) for all 17 packages: 0 `<error>` entries.
Re-derived this run for the four changed binaries:

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (icons only) | none | none | deps-verified |
| fluxer | Electron bundled; ldd 0 not-found | soname Requires | none | deps-verified |
| localsend (1.18.0) | DEB control Depends: libappindicator3-1\|libayatana-appindicator3-1, gir1.2-appindicator*, libayatana-ido3-0.4-0, xdg-user-dirs; ldd of all libs: libayatana-appindicator3.so.1 + libayatana-ido3-0.4.so.0 + libayatana-indicator3.so.7 (transitive via appindicator) | soname Requires (appindicator3, ido3) + xdg-user-dirs | none (gir1.2 binding not needed by Flutter; indicator3 pulled transitively) | deps-verified |
| mangowm-git (8169bfc) | meson: libcjson,libdrm,libinput,libpcre2-8,pangocairo,pixman-1,scenefx-0.5,wayland-client,wayland-protocols,wayland-server,wlroots-0.20,xcb,xcb-icccm,xkbcommon | all mapped | none | deps-verified |
| matugen | Cargo vendored; no system deps beyond cargo | cargo-packaging,gcc-c++ | none | deps-verified |
| niri-git (464d45d) | niri.spec.rpkg (extracted): udev,gbm,xkbcommon,wayland,libinput,dbus,systemd,libseat,libdisplay-info,pipewire,pango,cairo-gobject,clang,mesa-libEGL + Requires mesa-dri-drivers,mesa-libEGL,libwayland-server,xwayland-satellite>=0.7 | all mapped (pkgconfig(egl) covers mesa-libEGL; wayland-client/server cover wayland-devel) | none | deps-verified |
| noctalia (5.0.0~beta8) | meson.build (extracted): sdbus-c++,wayland-client,wayland-protocols,wayland-egl,epoxy,egl,glesv2,freetype2,fontconfig,cairo,cairo-ft,pango,pangocairo,pangoft2,harfbuzz,librsvg-2.0,xkbcommon,glib,gobject,gio,libsecret,libsodium,polkit-agent,gobject-1,pipewire,wireplumber-0.5,libcurl,libqalculate,libxml-2.0,md4c,nlohmann_json,tomlplusplus,libical,jemalloc(optional),libwebp,libjxl,libjxl_threads,sndfile + pam via cc.find_library + stb headers | all in spec incl pkgconfig(pam) + pkgconfig(stb) | none | deps-verified |
| noctalia-greeter | meson: wayland,xkb,pango/cairo,glib,libinput,wlroots,epoxy,tomlplusplus,json,stb,webp,rsvg | all in spec | none | deps-verified |
| obsidian (1.13.6) | Electron bundled; AutoReqProv:no; Requires electron | electron,bash | none | deps-verified |
| opencode-desktop (1.18.16) | Electron bundled; ldd of ai.opencode.desktop 0 not-found | soname/named Requires | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn (offset 944632): Root.desktop X-AppImage-Version=0.9.126; .NET AOT + bundled libs | all soname/named | none | deps-verified |
| scenefx (0.5) | meson: egl,gbm,glesv2,libdrm,pixman-1,xkbcommon,wlroots-0.20,wayland-server | all in spec | none | deps-verified |
| stb | header-only; c++ test in %check | c++_compiler | none | deps-verified |
| vesktop | Electron bundled; app.asar 1.6.5 | soname Requires | none | deps-verified |
| wlroots (0.20.2) | meson: glslang,egl,gbm,glesv2,hwdata,libdrm,libpng,libseat,libinput,vulkan,wayland,xkbcommon,x11/xcb/xwayland,ffmpeg,libdisplay-info,libliftoff,lcms2 | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson: fontconfig,wayland,xcb,xcb-cursor,clang/llvm,cargo | all in spec | none | deps-verified |
| zen-browser (1.21.13b) | Firefox tarball; application.ini 1.21.13b; ldd 0 not-found | mozilla-nss/nspr,hunspell,hyphen | none | deps-verified |

Confidence: high for all 17 (_builddepinfo + artifact extraction + clean
container installs agree). Package-specific verification notes: localsend DEB
torn (control + data.tar.zst extracted, version.json read, ldd of every .so);
niri tarball torn (niri.spec.rpkg read); noctalia tarball torn (meson.build +
meson_options read); obsidian tarball listed; rootapp AppImage torn twice
(sha256 + X-AppImage-Version).

### Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts kept in /tmp only).
2. Every package touched this run install-tested in clean TW container incl.
   fresh-checker re-runs -> **PASS** (4 changed binaries tested from local RPMs
   AND from public repo; 5 more unchanged binaries re-checked in a fresh
   container; ledger updated above).
3. Install-test coverage ledger updated -> **PASS** (this file; all rows now
   reference current binaries).
4. Dependency audit 100% coverage (17/17) -> **PASS** (table above; 4 changed
   packages re-derived from NEW artifacts this run).
5. Claims backed by command output -> **PASS** (gh api, git ls-remote, curl -sL
   status/size, sha256sum, od/unsquashfs, ar/tar --zstd, version.json reads,
   rpmspec -P, bash -n, python XML parse, OBS _result/_builddepinfo XML,
   docker/zypper/rpm -V/ldd logs).
6. Named gaps -> see below.
7. Relay updated with run_id 31432103271 + status complete -> **PASS** (this
   commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64 proof per
  policy).
- Slowroll container installs (identical binaries ~5-10 days behind TW; TW proof
  per policy).
- wlroots Source0 direct download from this runner blocked by GitLab Anubis
  anti-bot (403 HTML page); OBS-side _service download verified working (build
  succeeded). If OBS ever loses the cached tarball and hits the same block,
  rebuild would fail - monitor in future runs. NOT an install-time issue.
- GUI apps' full graphical smoke (no display in Docker; launcher + binary
  execution verified instead).
- niri upstream is extremely active; pinned commit 464d45d may advance before
  the next auto-updater. Spec will be refreshed by the next schedule run; not a
  bug.
- One transient OSS-mirror flake family from download.opensuse.org on this
  runner (timeouts/404s on dependency RPMs during zypper preloading); worked
  around with ftp.fau.de mirror. Infra, not a package issue.

# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31932574049

## Run Summary (31932574049)

Autonomous maintenance pass over all 22 packages in home:ackerman.
Started 2026-08-16 ~06:55Z (opencode-schedule, relay=false). Prior
relay 31917877507 was complete. This run was a fresh checker over the
whole inventory plus a dependency fix on protonplus.

### Work done this run

1. **OBS build health (priority 1)** - All 44 builds (22 Tumbleweed +
   22 Slowroll) `succeeded`/`published`, 0 failed/broken/unresolvable.
   `_builddepinfo` (openSUSE_Tumbleweed/x86_64) re-fetched: 0 `<error>`
   blocks across all 22 packages. Auto-updater had bumped mangowm-git
   to 3cb7cb2 (0.16.1) at 06:40Z (commit dcc57cf); OBS already carried
   the new spec + tarball and rebuilt both repos green - no churn.

2. **Version inventory sweep 22/22 (priority 2)** - every package
   verified against upstream this run via gh api (releases/latest,
   tags, commits/HEAD) + curl (fluxer X-Fluxer-Version header, rootapp
   AppImage re-tear, wlroots GitLab tag page):
   - bibata-cursor-theme 2.0.7 = v2.0.7 (up-to-date)
   - fluxer 2026.815.214426 = X-Fluxer-Version header (up-to-date;
     re-torn the built RPM this run: app.asar package.json "0.0.8" is
     the dev placeholder, X-Fluxer-Version is canonical per upstream)
   - helium-browser 0.15.5.1 = tag 0.15.5.1 exists + asset 200
     (releases/latest API still shows 0.15.4.1; spec correct)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (up-to-date)
   - localsend 1.18.0 = held (v1.18.1 ships ONLY 4 Android APKs; guard
     holds, verified asset list this run)
   - ly 1.4.1 = codeberg v1.4.1 (up-to-date)
   - mangowm-git 3cb7cb2 = mango main HEAD (up-to-date; just auto-
     bumped, verified gitdate 20260816035347 == committer date)
   - matugen 4.1.0 = upstream v4.1.0 (up-to-date)
   - niri-git 6062844 = niri main HEAD, gitdate 20260814122556 ==
     committer date (up-to-date)
   - noctalia 5.0.0~beta8 = upstream v5.0.0-beta.8; local tarball
     sha256 a60e4ab8... == fresh GitHub download byte-identical
   - noctalia-greeter 1.2.1 = upstream v1.2.1; local tarball sha256
     1d1c4349... == fresh GitHub download byte-identical
   - obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
   - opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date)
   - protonplus 0.6.4 = upstream v0.6.4 (up-to-date)
   - rootapp 0.9.126 = AppImage re-torn fresh this run: sha256
     0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910
     == spec comment == fedora-nexus pin; X-AppImage-Version=0.9.126 +
     sq.version 0.9.126 from inside
   - scenefx 0.5 = wlrfx/scenefx tag 0.5 (up-to-date)
   - stb 20260802 = commit 2c980bb date 2026-08-02 (up-to-date)
   - stoat-desktop 1.4.2 = upstream v1.4.2 (up-to-date)
   - vesktop 1.6.5 = upstream v1.6.5 (up-to-date)
   - wlroots 0.20.2 = GitLab protected tag 0.20.2 + Fedora 44 ships
     0.20.2 (up-to-date)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD, gitdate
     20260808203058 == committer date (up-to-date)
   - zen-browser 1.21.14b = upstream 1.21.14b (up-to-date)
   All versioned Source0 URLs verified (200/302-GitHub-redirect) this
   run. OBS package list == 22 local dirs == 22 README rows (diff
   clean). All -git gitdates == upstream committer dates exactly.

3. **protonplus dependency fix (priority 2, deps-fixed)** - Deep audit
   of the v0.6.4 tarball: upstream meson.build requires
   `dependency('appstream')` and `dependency('cairo')` (every valac
   compile uses `--pkg appstream` and `-I/usr/include/cairo`), but the
   spec only declared `appstream-glib` and relied on TRANSITIVE
   resolution of AppStream-devel/cairo-devel. Latent break: if the
   transitive chain changes the build dies. Added explicit
   `BuildRequires: pkgconfig(appstream)` + `pkgconfig(cairo)`
   (alphabetized). Web-verified AppStream-devel provides
   pkgconfig(appstream) in Tumbleweed OSS (rpmfind/pkgdex). Added
   .changes entry. Commit aa522fc. Synced spec+changes to OBS,
   triggered rebuild -> SUCCEEDED in BOTH repos (binary
   protonplus-0.6.4-8.1). Fresh builddepinfo now shows AppStream-devel
   + cairo-devel resolved as direct pkgdeps. Install-tested clean in a
   fresh TW container: `protonplus version` = "ProtonPlus 0.6.4", rc=0.

4. **No-churn verification (mandatory)** - lazyvim-git and mangowm-git
   update.sh sandbox-tested BOTH paths this run: unchanged commit +
   tarball present -> "No update needed", exit 0, spec/.changes
   byte-identical; unchanged commit + tarball missing -> downloads,
   exit 0, spec/.changes untouched (no churn). All other update.sh
   scripts verified to contain version-based no-churn guards
   (already-up-to-date -> exit 0). No repeated .changes-only commits.

5. **Dependency audit 22/22 (ground truth)** - `_builddepinfo`
   re-fetched this run: ZERO `<error>` blocks. mangowm-git re-derived
   from its NEW artifact (3cb7cb2): meson deps (libdrm, xcb,
   xcb-icccm, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon,
   libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0,
   pixman-1, libcjson, pangocairo) all present in spec. noctalia/
   noctalia-greeter meson deps re-derived from fresh tarballs, all in
   spec. wlroots/scenefx deps resolved per builddepinfo. Electron
   packages use soname-based Requires (correct for openSUSE). All 22
   install clean from the public mirror - no "nothing provides".

6. **Install-test sweep (fresh TW containers, 22/22)** - ALL 22
   packages install-tested this run in FRESH Tumbleweed containers
   from the PUBLIC download.opensuse.org mirror (exact user flow):
   bibata-cursor-theme, fluxer, helium-browser, lazyvim-git, localsend,
   ly, mangowm-git (0.16.1, TWICE - the only binary changed since last
   relay), matugen, niri-git, noctalia, noctalia-greeter, obsidian,
   opencode-desktop, protonplus (0.6.4-8.1, the fixed build), rootapp
   (0.9.126), scenefx, stb, stoat-desktop, vesktop, wlroots,
   xwayland-satellite-git, zen-browser. rpm -V clean on all. Smoke
   tests rc=0: ly 1.4.1, mango 0.16.1(release), matugen 4.1.0,
   niri 26.04 (6062844), noctalia v5.0.0, protonplus 0.6.4,
   rootapp (binary launch), xwayland-satellite 0.8.2,
   noctalia-greeter helpers, zen-browser (sandbox EPERM is Docker-only
   rc=0). Electron apps show the documented running-as-root sandbox
   fatal (Docker-only), ldd 0 missing on all.

7. **GitHub Actions health (priority 3)** - recent auto-updater runs
   all `success`; last failure/cancel was opencode-schedule
   31917475941 (concurrency cancel). No workflow failures.

8. **Issues/PRs (priority 4/5)** - 0 open issues, 0 open PRs (gh api
   this run).

## Commits this run
- aa522fc: fix(autonomous): protonplus: add explicit BuildRequires
  pkgconfig(appstream) and pkgconfig(cairo)
- (this relay commit)

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp).
2. Every package touched this run install-tested in a fresh TW
   container -> **PASS**. protonplus (the fixed package) installed
   clean: rpm -V clean, `protonplus version` = 0.6.4 rc=0. mangowm-git
   (only binary changed since prior relay) installed TWICE from
   scratch (checker + fresh-checker): clean + mango 0.16.1 rc=0.
3. Install-test coverage ledger updated -> **PASS** (all 22 rows THIS
   RUN, table below).
4. Dependency audit 100% coverage (22/22) -> **PASS** (builddepinfo
   0 errors; protonplus fixed this run with direct deps; mangowm-git
   re-derived from its NEW artifact).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, unsquashfs+X-AppImage-Version, rpm2cpio+asar extract,
   OBS _result/_builddepinfo XML, docker/zypper/rpm logs, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 31932574049 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 22 packages install-tested THIS RUN in FRESH Tumbleweed
containers from the PUBLIC download.opensuse.org mirror (exact user
flow), plus the two fixed updaters sandbox-verified for no-churn.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | clean | n/a (icons) |
| fluxer | 2026.815.214426-4.1 | pass | clean | ldd /usr/lib64/fluxer/fluxer: 0 missing |
| helium-browser | 0.15.5.1-4.1 | pass | clean | electron root-fatal (Docker-only) |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass | clean | n/a (config package) |
| localsend | 1.18.0-15.1 | pass | clean | ldd 0 missing |
| ly | 1.4.1-8.1 | pass | clean | "Ly version 1.4.1" rc=0 |
| mangowm-git | 0.16.1+git20260816035347.3cb7cb2-2.1 | pass (x2) | clean | "mango 0.16.1(release)" rc=0 |
| matugen | 4.1.0-30.1 | pass | clean | "matugen 4.1.0" rc=0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | clean | "niri 26.04 (6062844)" rc=0 |
| noctalia | 5.0.0~beta8-4.1 | pass | clean | "noctalia v5.0.0" rc=0 |
| noctalia-greeter | 1.2.1-2.1 | pass | clean | greeter helpers rc=0 |
| obsidian | 1.13.7-6.1 | pass | clean | electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | clean | electron root-fatal (Docker-only) |
| protonplus | 0.6.4-8.1 | pass | clean | "ProtonPlus 0.6.4" rc=0 |
| rootapp | 0.9.126-2.1 | pass | clean | binary launch rc=0 |
| libscenefx-0_5 | 0.5-5.1 | pass | clean | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | clean | n/a (headers) |
| stoat-desktop | 1.4.2-10.1 | pass | clean | ldd 0 missing (3 binaries) |
| vesktop | 1.6.5-35.1 | pass | clean | ldd 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass | clean | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | clean | "0.8.2" rc=0 |
| zen-browser | 1.21.14b-6.1 | pass | clean | "Mozilla Zen 1.21.14b" rc=0 (sandbox EPERM Docker-only) |

## Dependency audit (all 22)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) re-fetched
this run covers all 22 packages with ZERO `<error>` entries.
protonplus FIXED this run (added pkgconfig(appstream) + pkgconfig(cairo),
now explicit direct deps, resolved as AppStream-devel + cairo-devel
in fresh builddepinfo). mangowm-git re-derived from its NEW artifact
3cb7cb2.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.815.214426) | X-Fluxer-Version header; ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| helium-browser (0.15.5.1) | Chromium bundled; AutoReqProv | shfmt, bubblewrap, electron0 | none | none | deps-verified |
| lazyvim-git (459a4c3) | config package, 0 deps | none (noarch lua files) | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd | soname Requires + xdg-user-dirs | none | none | deps-verified |
| ly (1.4.1) | zig deps vendored (cache) | VENDORED via ly-zig-cache.tar.gz | none | none | deps-verified |
| mangowm-git (3cb7cb2) | meson.build @3cb7cb2 (14 deps) | all present incl. wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 + builddepinfo | pkgconfig set (21) + cargo-packaging | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (fresh tarball) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (fresh tarball) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | meson.build appstream + cairo (direct) | pkgconfig(appstream), pkgconfig(cairo) | ADDED both | none | deps-FIXED |
| rootapp (0.9.126) | AppImage re-torn (sha == spec) | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| stb (20260802) | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.4.2) | Electron bundled | soname Requires; rpmlintrc filters | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 22. Independent public-mirror installs of
every package this run + clean builddepinfo is stronger evidence than
static analysis alone.

## OBS build status

All 44 builds (22 Tumbleweed + 22 Slowroll) succeeded/published.
protonplus rebuilt from the dependency fix (rev bump, binary
0.6.4-8.1) and verified green in both repos. mangowm-git (auto-
bumped to 3cb7cb2) already built green on OBS from the auto-updater.
No failed/broken/unresolvable.

## Not Verified / Gaps
- Tumbleweed snapshot current (20260814 per openSUSE forums); Slowroll
  lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only). stoat-desktop,
  protonplus, helium-browser, obsidian, opencode-desktop, vesktop,
  zen-browser are GUI apps - install-level proof only.
- fluxer internal version: app.asar package.json "0.0.8" is a dev
  placeholder; X-Fluxer-Version header (2026.815.214426) is the
  canonical version per the project's own update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only). Guard
  protects the repo; will bump when upstream publishes the desktop DEB.
- mangowm/niri/xwayland upstreams extremely active; pinned commits
  may advance before the next auto-updater - the guarded update.sh
  scripts will catch them.
- helium-browser releases/latest API reports 0.15.4.1 while tag
  0.15.5.1 exists with a downloadable asset (spec version 0.15.5.1 is
  correct per the tag list).
- lazyvim-git ships Lua config as plain files (no plugins vendored);
  installs clean and files verified, but end-to-end LazyVim startup
  requires its runtime-download on first launch (network dependent,
  not testable offline in a container).
# opencode-schedule Relay State

## Status: complete

run_id: 36061698094

## Run Summary (2026-09-24 ~21:28-22:30Z, opencode-schedule, RELAY=false)

Prior relay (36032033019) was `complete`; no unfinished pickup. Auto-updater (21:14Z) success, zero new commits since prior relay. OBS fleet at start AND end: 42/42 succeeded, 0 failed/broken/unresolvable. Zero open issues, zero open PRs. All Actions runs success. CVE batch 5/5 clean. ZERO upstream moves across all 21 packages (all pins == HEAD/tags/latest, rootapp binary unchanged, obsidian HOLD re-confirmed, fluxer filename-authoritative re-confirmed). Zero-change run per NO-CHURN: no spec bumps, no OBS syncs needed. Install-tested the 13-package rotation slice (8 oldest-first per next-start + P0 re-verify overlap) TW+SR in fresh containers incl. rpm -V + smokes. Only commit this run: this relay.

## Version inventory sweep (21/21 verified)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 releases/latest (ful1e5/Bibata_Cursor) | up-to-date |
| fluxer | 2026.924.13630 | served Content-Disposition filename Fluxer-2026.924.13630 (api.fluxer.app 302->200) | up-to-date |
| helium-browser | 0.18.1.1 | 0.18.1.1 tags[0] + releases/latest (imputnet/helium-linux) | up-to-date |
| lazyvim-git | 9997009 | HEAD 999700997f72 `git ls-remote LazyVim/LazyVim` == pin | up-to-date |
| localsend | 1.18.2 | v1.18.2 releases/latest (localsend/localsend) | up-to-date |
| mangowm-git | 06ca647 | HEAD 06ca6477c6a2 `git ls-remote mangowm/mango` == pin | up-to-date |
| matugen | 4.2.0 | v4.2.0 releases/latest (InioX/matugen) | up-to-date |
| niri-git | c3e2566 | HEAD c3e25664b36f `git ls-remote YaLTeR/niri` == pin | up-to-date |
| noctalia | 5.1.0 | v5.1.0 releases/latest (noctalia-dev/noctalia-shell) | up-to-date |
| noctalia-greeter | 1.5.0 | v1.5.0 tags[0] (no releases, tags API) | up-to-date |
| obsidian | 1.13.7 | v1.13.8 BUT assets = ["Obsidian-1.13.8.apk"] ONLY (re-checked via releases assets list) | up-to-date (HOLD) |
| opencode-desktop | 1.18.32 | v1.18.32 releases/latest (anomalyco/opencode) | up-to-date |
| protonplus | 0.6.8 | v0.6.8 releases/latest (vysp3r/ProtonPlus) | up-to-date |
| rootapp | 0.9.134 | live AppImage HTTP 200 Last-Modified Tue, 22 Sep 2026 20:17:53 GMT, content-length 441231864 == prior run (binary unchanged) | up-to-date |
| scenefx | 0.5 | 0.5 releases/latest (wlrfx/scenefx) | up-to-date |
| stb | 20260802 | master HEAD 2c980bb nothings/stb (no new commits) | up-to-date |
| stoat-desktop | 1.5.4 | v1.5.4 releases/latest (stoatchat/for-desktop) | up-to-date |
| vesktop | 1.6.7 | v1.6.7 releases/latest (Vencord/Vesktop) | up-to-date |
| wlroots | 0.20.2 | 0.20.2 newest GitLab releases API (0.20.2,0.20.1,0.20.0,0.19.3); 0.20.2 tarball URL 302-ok | up-to-date |
| xwayland-satellite-git | b83eab9 | HEAD b83eab900644 `git ls-remote Supreeeme/xwayland-satellite` == pin | up-to-date |
| zen-browser | 1.22.3b | 1.22.3b releases/latest (zen-browser/desktop); fedora-nexus spec Version 1.22.3b matches | up-to-date |

Inventory cross-checks: 21 local spec dirs == OBS 21 source packages; fleet _result 42/42 succeeded. bash -n PASS all update.sh; yaml.safe_load PASS opencode-schedule + update-packages workflows. CVE triage (OSV querybatch matugen/crates.io, ProtonPlus/PyPI, vesktop/npm, niri/crates.io, localsend/Pub): all CLEAN (0 vulns); rest cve: n/a-(no mappable ecosystem). TW snapshot tested: VERSION_ID=20260923 (registry.opensuse.org/opensuse/tumbleweed:latest pulled 2026-09-24). README package table covers all 21 dirs. Sibling fedora-nexus helium-browser spec now Version 0.18.1.1 == ours (in-sync).

## Upstream deltas (P0 eight + changed packages)

upstream-delta: niri-git c3e2566->c3e2566 | deps+/-: none | artifacts+/-: none | health: ok | notes: HEAD==pin, OBS succeeded both, installed TW+SR `niri 26.04 (c3e2566)` both
upstream-delta: xwayland-satellite-git b83eab9->b83eab9 | deps+/-: none | artifacts+/-: none | health: ok | notes: HEAD==pin, OBS succeeded both, installed TW+SR (`--version` unrecognized-arg = binary runs, expected, no such flag)
upstream-delta: noctalia 5.1.0->5.1.0 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: noctalia-greeter 1.5.0->1.5.0 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: mangowm-git 06ca647->06ca647 | deps+/-: none | artifacts+/-: none | health: ok | notes: HEAD==pin, OBS succeeded both, installed TW+SR `mango 0.17.3(release)` both
upstream-delta: zen-browser 1.22.3b->1.22.3b | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: helium-browser 0.18.1.1->0.18.1.1 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: lazyvim-git 9997009->9997009 | deps+/-: none | artifacts+/-: none | health: ok | notes: HEAD==pin, OBS succeeded both, installed TW+SR
upstream-delta: fluxer 2026.924.13630->2026.924.13630 | deps+/-: none (served filename == spec) | artifacts+/-: none | health: ok | notes: OBS succeeded both
upstream-delta: rootapp 0.9.134->0.9.134 | deps+/-: none (live binary unchanged per Last-Modified+length) | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR
upstream-delta: obsidian 1.13.7->HOLD | deps+/-: none | artifacts+/-: none | health: ok | notes: 1.13.8 assets still APK-only (re-checked)
upstream-delta: opencode-desktop 1.18.32->1.18.32 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both, installed TW+SR

## Sibling cross-references (this run)

sibling-ref: fedora-nexus zen-browser spec Version 1.22.3b | ours: 1.22.3b | delta: in-sync | kept-improvements: n/a
sibling-ref: fedora-nexus helium-browser spec Version 0.18.1.1 | ours: 0.18.1.1 | delta: in-sync | kept-improvements: __provides_exclude_from/__requires_exclude_from guards present in ours
sibling-ref: openSUSE Factory: n/a (no Factory counterparts for these stacks consulted; upstream-direct verification used throughout)

## DOCKER BATTLE TEST (fresh registry.opensuse.org/opensuse/tumbleweed:latest, VERSION_ID=20260923)

Rotation slice (13 pkgs: 8 oldest-first per next-start + P0 re-verify): bibata-cursor-theme, lazyvim-git, localsend, mangowm-git, xwayland-satellite-git, niri-git, noctalia, noctalia-greeter, opencode-desktop, rootapp, stoat-desktop, zen-browser, helium-browser — ALL installed clean via nexus repo TW + Slowroll-oss containers (EXIT=0 both), rpm -V clean both, smokes niri 26.04 (c3e2566) TW+SR+fresh, mango 0.17.3 TW+SR. Zero nothing-provides/Problem on both distros. Fresh-checker re-run: localsend+mangowm-git+niri-git in a second fresh TW container, EXIT=0 + VERIFY-OK + niri smoke. Remaining 8 packages (fluxer, matugen, obsidian, protonplus, scenefx, stb, vesktop, wlroots): binaries unchanged since prior run's passing TW+SR tests (local pins == OBS pins == upstream, OBS 42/42 succeeded). Repo self-check: `zypper se -t srcpackage -r home_ackerman` = 21 srcpackages via home:ackerman.repo path.
spec-cleaner exit 0 (TW container): bibata-cursor-theme, localsend, matugen, protonplus, vesktop (rotated 5-sample; prior run covered niri/mangowm/xwayland/noctalia/fluxer).
rpmlint: bibata/matugen/protonplus zero E; localsend 6x + vesktop 4x unused-rpmlintrc-filter E's are SPEC-ONLY false positives — OBS rpmlint.log ground truth: vesktop 0 errors/168 filtered (all consumed), localsend 4/6 consumed by binary (only arch-dependent-file-in-usr-share + explicit-lib-dependency unused even on OBS; harmless log E's, build succeeded+published; NO-CHURN, no filter touched per standing note).
rpmspec -P exit 0: all 5 sampled. bash -n PASS: all update.sh. yaml.safe_load PASS: workflows.
OBS end-state: 42/42 succeeded/published (TW + SR), 0 failed/broken/unresolvable.

## zypper install test table (real installs THIS RUN, fresh containers)

TW snapshot: registry.opensuse.org/opensuse/tumbleweed:latest pulled 2026-09-24 (VERSION_ID=20260923); Slowroll OSS + home:ackerman Slowroll repos.

| package | repo/snapshot | OBS build | zypper install test (TW) | zypper install test (Slowroll) | smoke test | status |
|---|---|---|---|---|---|---|
| bibata-cursor-theme 2.0.7 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | rpm -V OK | installable |
| lazyvim-git 9997009 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | rpm -V OK | installable |
| localsend 1.18.2 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| mangowm-git 06ca647 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | mango 0.17.3 TW+SR | installable |
| xwayland-satellite-git b83eab9 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | runs (no --version flag by design) + rpm -V OK | installable |
| niri-git c3e2566 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | niri 26.04 (c3e2566) TW+SR+fresh | installable |
| noctalia 5.1.0 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| noctalia-greeter 1.5.0 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| opencode-desktop 1.18.32 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| rootapp 0.9.134 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| stoat-desktop 1.5.4 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| zen-browser 1.22.3b | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| helium-browser 0.18.1.1 | TW 20260923 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |

Install-test coverage ledger: 13/21 tested TW+SR this run with fresh evidence (rotation slice + P0 re-verify); remaining 8/21 (fluxer, matugen, obsidian, protonplus, scenefx, stb, vesktop, wlroots) covered by prior run (36032033019) with unchanged binaries/pins since (verified: local pins == OBS pins == upstream). Full fleet covered, zero gaps. Next-start: rotation resumes oldest-first at fluxer, matugen, obsidian, protonplus, scenefx, stb, vesktop, wlroots (then re-rotate).
Not verified (named gaps): compositor/GUI full-launch smoke (headless container; linkage proven via install + version runs); obsidian 1.13.8+ x86_64 tarball (upstream only ships APK — HOLD stands).

## Fleet rebuild rotation slice

- This run: 13-package slice repo-install TW+SR + smokes + fresh-checker re-run (all from CURRENT OBS builds matching CURRENT specs). OBS 42/42 succeeded = fresh builds from current specs, so no rpmbuild-from-spec slice needed.
- next-start: fluxer, matugen, obsidian, protonplus, scenefx, stb, vesktop, wlroots (oldest-first; full fleet rotates within 3 runs)

## Toolchain excellence checks

- rpmspec -P exit 0: bibata, localsend, matugen, protonplus, vesktop (local + TW container).
- spec-cleaner exit 0: same 5 (TW container).
- rpmlint: bibata/matugen/protonplus zero E; localsend/vesktop spec-only unused-filter E's explained vs OBS ground truth (above); no filter touched.
- zypper: 13/13 slice TW+SR EXIT=0 + smokes + rpm -V; fresh-checker re-run EXIT=0.
- bash -n PASS: all update.sh. yaml.safe_load PASS: workflows.
- OBS end-state: 42/42 succeeded/published (TW + SR), 0 failed/broken/unresolvable.
- tools/verify-opensuse.sh: PASS (after this relay update).

## Dependency Audit Table (100% of inventory, 21/21)

| package | upstream deps found | in spec | missing/added | extra/dropped | artifacts | status | unproven: 0 |
|---|---|---|---|---|---|---|---|
| bibata-cursor-theme | none (theme files) | fdupes BR, hicolor | - | - | complete | deps-verified | unproven: 0 |
| fluxer | prebuilt RPM repack @2026.924.13630 (served filename == spec) | soname Requires + guards | - | - | complete | deps-verified | unproven: 0 |
| helium-browser | prebuilt tar.xz repack @0.18.1.1 (tags+latest verified) | BRs desktop-file-utils+hicolor, soname Requires, provides+requires-exclude guards | - | - | complete | deps-verified | unproven: 0 |
| lazyvim-git | none (data files, noarch, HEAD==pin verified) | trivial (no Requires — correct) | - | - | complete | deps-verified | unproven: 0 |
| localsend | prebuilt deb repack @1.18.2 (OBS published; OSV Pub clean) | guards present | - | - | complete | deps-verified | unproven: 0 |
| mangowm-git | HEAD==pin 06ca647 (no re-tear needed, unchanged) | all BRs incl. floors | - | - | complete | deps-verified | unproven: 0 |
| matugen | pure-Rust vendored @4.2.0 (OBS published; OSV crates.io clean) | cargo BRs | - | - | complete | deps-verified | unproven: 0 |
| niri-git | HEAD==pin c3e2566 (prior-run Cargo.toml derivation stands, unchanged) | all system BRs incl. pkgconfig() forms | - | - | complete | deps-verified | unproven: 0 |
| noctalia | unchanged @v5.1.0 (OBS published) | all present incl. floors | - | - | complete | deps-verified | unproven: 0 |
| noctalia-greeter | unchanged @v1.5.0 (OBS published) | all BRs cover | - | harfbuzz/stb extras retained (harmless, OBS-proven) | complete | deps-verified | unproven: 0 |
| obsidian | resources-only + Requires: electron (1.13.8 HOLD re-checked, assets APK-only) | electron Req | - | - | complete (1.13.8 HOLD) | deps-verified | unproven: 0 |
| opencode-desktop | Electron bundle @1.18.32 (latest verified) | guards + soname Req set | - | - | complete | deps-verified | unproven: 0 |
| protonplus | unchanged @v0.6.8 (OSV PyPI clean) | all present | - | - | complete | deps-verified | unproven: 0 |
| rootapp | bundled .NET/Avalonia AppImage @0.9.134 (live binary unchanged per Last-Modified+length) | guards present | - | - | complete | deps-verified | unproven: 0 |
| scenefx | wlroots dep, 0.5 (latest verified) | BRs | - | - | complete | deps-verified | unproven: 0 |
| stb | header-only snapshot (HEAD 2c980bb, no new commits) | minimal | - | - | complete | deps-verified | unproven: 0 |
| stoat-desktop | prebuilt zip repack @1.5.4 (latest verified) | BRs unzip+desktop-file-utils, /opt/Stoat exclude guards | - | - | complete | deps-verified | unproven: 0 |
| vesktop | Electron prebuilt @1.6.7 (latest verified; OSV npm clean) | Requires match | - | - | complete | deps-verified | unproven: 0 |
| wlroots | meson C deps, 0.20.2 (GitLab releases verified) | BRs | - | - | complete | deps-verified | unproven: 0 |
| xwayland-satellite-git | HEAD==pin b83eab9 (unchanged) | all BRs + Requires: xwayland | retained wayland-client/cursor/server pkgconfig extras (harmless, OBS-proven) | complete | deps-verified | unproven: 0 |
| zen-browser | prebuilt repack 1.22.3b (guards intact; fedora-nexus in-sync) | guards present | - | - | complete | deps-verified | unproven: 0 |

Optimization discipline (no source package re-pinned this run; %{optflags} via macros intact per prior audits; prebuilt packages recorded as nothing-to-optimize per standing rule).

## Adversarial audit (contrarian pass)

1. "Zero-change run = skipped work?" — every pin re-checked live (ls-remote/tags/latest/headers/assets, outputs pasted); 13 fresh installs TW+SR; 5-sample excellence rotated; CVE batch re-run. Verified, not assumed. Survives.
2. "Is obsidian 1.13.7 stale given v1.13.8?" — v1.13.8's ONLY asset is .apk (re-checked via releases assets list); no linux x86_64 tarball. HOLD stands. Survives.
3. "Is fluxer's served build newer than 13630?" — Content-Disposition filename still 13630 == spec; filename authoritative per standing note. Correct to hold. Survives.
4. "Do localsend/vesktop rpmlint E's mean broken specs?" — spec-only view; OBS ground truth (spec+binary) consumes the filters (vesktop 0 errors/168 filtered); builds succeeded+published and install clean TW+SR. Not broken. Survives.
5. "Slowroll verify loopctl — did batch1 really install on SR?" — first SR container split installs across two containers (verify-fail artifact of fresh containers, caught immediately); re-ran ONE SR container installing all 13 → 13/13 SR-VERIFY-OK + smokes. Survives.
6. "xwayland-satellite --version panics — broken binary?" — panics with arg-parser usage error = binary executes and parses args; it simply has no --version flag (same class as protonplus). Install + rpm -V + ldd-linkage proven. Survives.

## PENDING / continuation (for next run)

- Fleet rotation next-start: fluxer, matugen, obsidian, protonplus, scenefx, stb, vesktop, wlroots (oldest-first). Watch obsidian 1.13.8+ tarballs (HOLD until linux x86_64 appears), fluxer fast cadence (filename-authoritative), rootapp fast cadence, mangowm/niri/xwayland fast cadence (re-pin intraday — re-check HEAD every run).
- Standing trap: update.sh scripts (except mangowm-git/fluxer) must be run with cwd=<pkg>/ (relative SPEC_FILE paths); keep downloads in /tmp; OBS binary-download URL order is /build/<proj>/<repo>/<arch>/<pkg>/<file>; zypper global --no-gpg-checks goes BEFORE install; local .rpm installs need the nexus repo added (else signature-verification abort in non-interactive mode); mangowm needs nexus repo for libscenefx; `wlroots` is source-only name (binary libwlroots-0_20); scenefx installs as libscenefx-0_5 (+scenefx-devel), stb as stb-devel (noarch); bibata/lazyvim noarch RPMs are NOT in the mirror x86_64 listing page — fetch via OBS binaries API or install via repo; compositor/GUI smoke = version runs + usage output; minimal TW image lacks `which`/`awk`/`diff`/`xargs`/`git`; `zypper se -r` output has no repo-alias column (use plain `zypper se`); GitHub release assets 302 — follow redirects; localsend binary is localsend_app but PACKAGE is localsend; mangowm-git installs /usr/bin/mango (`mango -v`, not --version); niri-git installs /usr/bin/niri; lazyvim-git is noarch; protonplus has no --version flag; `gh api issues/pulls` needs list subcommands (`gh issue list`); bare `curl api.github.com` needs token header, prefer `gh api --cache`; GitHub `?sha=master` 404s when default branch is main — use `gh api .../commits`; helium/noctalia-greeter/stoat have no usable GitHub releases/latest (tags API / releases-list respectively); ProtonPlus owner is vysp3r, stoat owner is stoatchat/for-desktop, xwayland owner is Supreeeme; ONE bad package name in a `zypper in` list aborts the WHOLE transaction — verify names via `zypper se` first; NEVER co-install `xwayland-satellite` (OSS distro pkg) with `xwayland-satellite-git` (Conflicts/Provides by design — test artifact, not a bug); `docker run -v` flags go BEFORE the image name (after = passed to the container command); spec-cleaner/rpmlint must be installed AND run in the SAME `docker run` (fresh containers lose installs); EACH `docker run --rm` is a FRESH container — install + verify + smoke must happen in the SAME invocation (this run's split-container SR verify-fail artifact); xwayland-satellite has no --version flag (arg-parser panic = runs, not broken); niri-wm/niri == YaLTeR/niri HEAD, update.sh repo name valid.
- Standing note: repo git history is squashed per auto-update sync — NO-CHURN git-log checks are degraded; use OBS spec pins + _jobhistory as authority.
- Standing note: releases/latest can lag tags — always cross-check tags API + releases list + sibling before calling a version wrong (helium 0.18.1.1 pattern).
- Standing note: noctalia-greeter and stb have no GitHub releases (tags API / ls-remote HEAD respectively).
- Standing note: OBS accumulates stale per-commit tarballs for -git packages; delete obsolete remote files on every sync.
- Standing note: fluxer X-Fluxer-Version header drifts independently of the served RPM filename — filename is authoritative, do not bump on header alone.
- Standing note (2026-09-21): mangowm/mango and niri both re-pin intraday — the auto-updater's pin can go stale within the same day. The opencode run MUST re-check `git ls-remote` HEAD for -git packages even right after an auto-update commit.
- Standing note (2026-09-21): `rpmlint <spec>` alone reports E: unused-rpmlintrc-filter for filters that apply to the built binary — always verify against OBS rpmlint.log (spec+binary ground truth) before touching any rpmlintrc; never delete a filter that OBS proves consumed.
- OSV querybatch schema: {"package":{"name":N,"ecosystem":E},"version":V} — bare string package field returns HTTP 400.

## Issues / PRs

- Zero open issues, zero open PRs (`gh issue list` / `gh pr list` empty). All recent Actions runs success (opencode-schedule in_progress = this run; Nexus Auto-Updater success 21:14Z). No new packages added (no genuinely new upstream components found).
- Commits this run: this relay update only (zero spec churn — NO-CHURN; fully-verified run).

(End of file)

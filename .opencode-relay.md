# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31915790276

## Run Summary (31915790276)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started ~00:0xZ 2026-08-16 (opencode-schedule). Prior relay
31907409069 was complete for 17 packages, but the repo had since
grown to 21 dirs (protonplus + stoat-desktop added by commit
e1a37a4 "updated" but never synced to OBS) and ly was failing to
build on OBS.

### Work done this run

1. **ly OBS build failure FIXED (priority 1)** - Root cause: zig
   0.16's package manager fetches git deps (clap, zigini, termbox2,
   translate-c) during `zig build`; the OBS chroot is offline, so it
   failed with `unable to discover remote git server capabilities:
   NameServerFailure`. Fix: vendor a pre-populated zig global cache
   tarball `ly-zig-cache.tar.gz` (contains `ly-zig-cache/p/<hash>.
   tar.gz` canonical packages, byte-identical to reference recipe
   home:kei8933:ly) and pass `--global-cache-dir` in %build/%install.
   Spec: added `Source1: %{name}-zig-cache.tar.gz` + %prep extract.
   update.sh rewritten: pure version guard (exits 0 when
   current == latest), regenerates the cache via zig 0.16 only when
   pinned dep hashes change, deterministic tar
   (`--mtime='@0' --sort=name --owner=0 --group=0`, verified
   byte-identical across regens, sha256
   aad0e41cd58478c570c967714770d86c00cbe7df57685f0b5e8db079704bf99f).
   .changes entry added. Commit 906e9c0 -> rebased/pushed as 3ed8698.
   Full rpmbuild -bb verified in a Tumbleweed container; OBS rev 8
   (srcmd5 1708a9b481c33e7cec40009305aa7c80) rebuilt successfully in
   BOTH repos (build log: 0 occurrences of NameServerFailure; binary
   ly-1.4.1-8.1.x86_64.rpm, sha256
   379faf3c430c7e26041f5b817bd792c8e746ac775779c4056513c0177b642e65).

2. **protonplus + stoat-desktop SYNCED to OBS (priority 1)** - Both
   packages existed only in git (commit e1a37a4), missing from OBS.
   Created both OBS packages via `_meta` PUT, uploaded all files,
   deleted update.sh from OBS, triggered runservice+waitservice.
   - protonplus 0.6.4 (upstream ProtonPlus v0.6.4, published
     2026-08-13): built + succeeded in both repos; install-test
     clean (protonplus-0.6.4-6.1.x86_64, rpm -V OK, ldd 0 missing,
     binary launches with usage output).
   - stoat-desktop 1.4.2 (upstream Stoat v1.4.2, published
     2026-07-17): FIRST build FAILED on rpmlint (Badness 20036 >
     threshold 999): binary-or-shlib-defines-rpath, explicit-lib-
     dependency, filelist-forbidden-opt, permissions-setuid bits on
     chrome-sandbox. FIXED by adding `stoat-desktop-rpmlintrc`
     (filters matching the sibling Electron packages: rpath,
     explicit-lib-dependency, filelist-forbidden-opt, setuid/
     setgroups, gethostbyname). Commit 0176a8c -> rebased/pushed as
     dd195ad. Rebuild: stoat-desktop-1.4.2-10.1.x86_64 SUCCEEDED in
     both repos. Install-test clean (rpm -V OK, ldd 0 missing on
     stoat-desktop + libffmpeg.so + chrome-sandbox; Electron GUI,
     no graphical smoke under Docker).

3. **Version inventory sweep 21/21 (priority 2)** - every package
   verified against upstream via gh api (releases/latest, tags,
   commits/HEAD) + curl (fluxer X-Fluxer-Version header, rootapp
   AppImage re-tear, wlroots GitLab API) this run:
   - bibata-cursor-theme 2.0.7 = upstream v2.0.7 (up-to-date)
   - fluxer 2026.815.214426 = X-Fluxer-Version header (up-to-date;
     spec matches OBS binary 2026.815.214426-4.1)
   - helium-browser 0.15.5.1 = upstream tag 0.15.5.1 (latest tag;
     asset 200). NOTE: releases/latest API shows 0.15.4.1 but the
     tag 0.15.5.1 EXISTS and its asset downloads 200; spec is correct.
   - localsend 1.18.0 = held (v1.18.1 upstream release has ONLY 4
     Android APKs, no desktop DEB - re-verified; guard holds)
   - ly 1.4.1 = upstream (up-to-date, just fixed)
   - mangowm-git f5d1dbb = mangowm/mango main HEAD (up-to-date)
   - matugen 4.1.0 = upstream v4.1.0 (up-to-date)
   - niri-git 6062844 = niri-wm/niri main HEAD (up-to-date)
   - noctalia 5.0.0~beta8 = upstream v5.0.0-beta.8 (up-to-date)
   - noctalia-greeter 1.2.1 = upstream v1.2.1 (up-to-date)
   - obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
   - opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date;
     Source0 anomalyco/opencode resolves 200)
   - protonplus 0.6.4 = upstream v0.6.4 (up-to-date)
   - rootapp 0.9.126 = AppImage re-torn fresh this run
     (X-AppImage-Version=0.9.126, Comment "Root 0.9.126"; sha256
     0736926fde... == spec comment, byte-identical)
   - scenefx 0.5 = wlrfx/scenefx latest tag 0.5 (up-to-date)
   - stb 20260802 = commit 2c980bb date 2026-08-02 (up-to-date;
     fresh-downloaded stb_image.h diff == 0 vs local tarball)
   - stoat-desktop 1.4.2 = upstream Stoat v1.4.2 (up-to-date)
   - vesktop 1.6.5 = upstream v1.6.5 (up-to-date; asset
     vesktop-1.6.5.x86_64.rpm 200)
   - wlroots 0.20.2 = GitLab releases 0.20.2 (up-to-date; note
     GitLab /projects/12042/releases returned empty but
     wlroots%2Fwlroots path + tags show 0.20.2)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD
     (up-to-date)
   - zen-browser 1.21.14b = upstream 1.21.14b (up-to-date)
   All versioned Source0 URLs verified (200/302-GitHub-redirect).
   README table == 21 local dirs == 21 OBS packages (diff clean).
   All -git spec gitdates == upstream commit dates exactly.

4. **No-churn verification (mandatory)** - ly update.sh no-change
   path exits 0 with zero diff (pure version guard); cache regen
   produces byte-identical tarball. No repeated .changes-only commits
   for the same version/SHA. stoat-desktop/protonplus were genuinely
   NEW to OBS (required sync). Tracked tarballs byte-identical to
   fresh upstream downloads.

5. **Dependency audit 21/21 (ground truth)** - `_builddepinfo`
   (openSUSE_Tumbleweed/x86_64) ZERO `<error>` blocks. New packages
   this run: protonplus (Rust cargo, builds clean, install-tested),
   stoat-desktop (Electron, rpmlintrc added, install-tested). ly
   (zig) full rpmbuild + install-tested. fluxer re-verified vs its
   current X-Fluxer-Version. Every runtime Requires confirmed by the
   public-mirror install sweep (0 "nothing provides" anywhere).

6. **Install-test sweep (fresh TW containers)** - new/changed RPMs
   install-tested this run from OBS
   (openSUSE_Tumbleweed/x86_64):
   - ly-1.4.1-8.1: install RC=0, rpm -V OK, ldd 0 missing,
     `ly --version` = "Ly version 1.4.1". FRESH-CHECKER independent
     pass from the PUBLIC download.opensuse.org mirror (exact user
     flow): installed clean, rpm -q ly = ly-1.4.1-8.1.x86_64,
     "Ly version 1.4.1". (second fresh-checker pass also clean)
   - protonplus-0.6.4-6.1: install RC=0, rpm -V OK, ldd 0 missing,
     binary launches (usage output; no --version flag in this app).
   - stoat-desktop-1.4.2-10.1: install RC=0, rpm -V OK, ldd 0
     missing on stoat-desktop + libffmpeg.so + chrome-sandbox.
     Electron GUI, no graphical smoke under Docker (documented).
   - All 18 other packages unchanged since their prior install tests
     (17 from run 31907409069 + helium-browser 0.15.5.1 which was
     verified in an earlier cycle); all builds green in both repos.

7. **GitHub Actions health (priority 3)** - last auto-updater runs
   all `success` (incl. 31915417565). All specs pass `rpmspec -P`;
   update.sh scripts pass `bash -n`.

8. **Issues/PRs (priority 4/5)** - 0 open issues, 0 open PRs (checked
   via gh api this run).

## Commits this run
- 906e9c0 / 3ed8698: fix(autonomous): ly: vendor zig package-manager
  deps to fix offline OBS build
- 0176a8c / dd195ad: fix(autonomous): stoat-desktop: add rpmlintrc to
  pass OBS rpmlint (Electron RPATH/setuid filters)
- (this relay commit)

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp).
2. Every package touched this run install-tested in a fresh TW
   container -> **PASS**. ly (two independent fresh containers +
   public-mirror FRESH-CHECKER), protonplus, stoat-desktop all
   installed clean from OBS-built RPMs.
3. Install-test coverage ledger updated -> **PASS** (table below).
4. Dependency audit 100% coverage (21/21) -> **PASS** (builddepinfo
   0 errors; ly/protonplus/stoat-desktop derived from their NEW
   artifacts this run).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, readelf/unsquashfs+X-AppImage-Version, OBS _result/
   _builddepinfo/_log XML, docker/zypper/rpm logs, rpmspec -P,
   bash -n, tar --mtime reproducibility).
6. Named gaps -> see below.
7. Relay updated with run_id 31915790276 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 21 packages install-test clean in FRESH Tumbleweed containers.
New/changed this run (ly, protonplus, stoat-desktop) installed from
OBS binaries; the other 18 previously verified (their installs remain
valid - binaries unchanged since). FRESH-CHECKER pass installed ly
from the PUBLIC download.opensuse.org mirror (exact user flow).

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass (prior) | OK | n/a (icons) |
| fluxer | 2026.815.214426-4.1 | pass (prior) | OK | ldd /usr/lib64/fluxer/fluxer: 0 missing |
| helium-browser | 0.15.5.1-x.1 | pass (prior) | OK | electron root-fatal (Docker-only) |
| localsend | 1.18.0-15.1 | pass (prior) | OK | ldd /opt/localsend_app/localsend_app: 0 missing |
| ly | 1.4.1-8.1 | pass (THIS RUN) | OK | `ly --version` = "Ly version 1.4.1" (public-mirror fresh-checker: RC=0) |
| mangowm-git | 0.16.0+git20260815092240.f5d1dbb-1.1 | pass (prior) | OK | `mango -v` = "mango 0.16.0(release)", exit 0 |
| matugen | 4.1.0-30.1 | pass (prior) | OK | matugen 4.1.0, exit 0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass (prior) | OK | `niri --version` = "niri 26.04 (6062844)", exit 0 |
| noctalia | 5.0.0~beta8-4.1 | pass (prior) | OK | noctalia v5.0.0, exit 0 |
| noctalia-greeter | 1.2.1-2.1 | pass (prior) | OK | --help usage, exit 0 |
| obsidian | 1.13.7-6.1 | pass (prior) | OK | electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass (prior) | OK | electron root-fatal (Docker-only); ldd 0 missing |
| protonplus | 0.6.4-6.1 | pass (THIS RUN) | OK | binary launches (usage); ldd 0 missing |
| rootapp | 0.9.126-2.1 | pass (prior) | OK | `rootapp --version` exit 0 |
| libscenefx-0_5 | 0.5-5.1 | pass (prior) | OK | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass (prior) | OK | n/a (headers) |
| stoat-desktop | 1.4.2-10.1 | pass (THIS RUN) | OK | Electron GUI no-smoke; ldd 0 missing (3 binaries) |
| vesktop | 1.6.5-35.1 | pass (prior) | OK | electron root-fatal (Docker-only); ldd 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass (prior) | OK | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass (prior) | OK | /usr/bin/xwayland-satellite -version = 0.8.2, exit 0 |
| zen-browser | 1.21.14b-6.1 | pass (prior) | OK | `zen-browser --version` = "Mozilla Zen 1.21.14b", exit 0 |

## Dependency audit (all 21)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) covers
all 21 packages with ZERO `<error>` entries. ly (zig, vendored cache),
protonplus (cargo), stoat-desktop (Electron) re-derived from their NEW
artifacts this run; the other 18 unchanged since their last audit and
confirmed by the public-mirror install sweep.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.815.214426) | X-Fluxer-Version header; container/public-mirror ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| helium-browser (0.15.5.1) | Chromium bundled; AutoReqProv | shfmt, bubblewrap, electron0 | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd (unchanged) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| ly (1.4.1) | zig package-manager deps: clap, zigini, termbox2, translate-c (+ transitive aro, ini) | VENDORED via ly-zig-cache.tar.gz + --global-cache-dir; no network in chroot | none | none | deps-verified (NEW) |
| mangowm-git (f5d1dbb) | meson.build @f5d1dbb (unchanged) | all 13 mapped + wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored (unchanged) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 (unchanged) + builddepinfo | pkgconfig set (21) + cargo-packaging/clang/gcc-c++ | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control (unchanged) | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | Rust cargo deps (this run: clean build) | cargo-packaging, gcc-c++, pkgconfig(gtk4) etc. | none | none | deps-verified (NEW) |
| rootapp (0.9.126) | AppImage re-torn this run (sha 0736926f == spec), Root ELF ldd clean | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| stb (20260802) | header-only (unchanged) | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.4.2) | Electron bundled; RPATH/setuid flagged by rpmlint | soname Requires; rpmlintrc filters | none | none | deps-verified (NEW) |
| vesktop (1.6.5) | Electron bundled (unchanged) | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson (unchanged) | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs (unchanged) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 21. Independent public-mirror install of ly
plus clean builddepinfo + clean container builds of the 3 new/changed
packages is stronger evidence than static analysis alone.

## OBS build status

All 42 builds (21 Tumbleweed + 21 Slowroll) succeeded/published. ly
rebuilt from rev 8 (fixed zig vendoring); protonplus/stoat-desktop
synced to OBS for the first time this run; stoat-desktop rpmlintrc
fixed its first-build failure. No failed/broken/unresolvable.

## Not Verified / Gaps
- Tumbleweed snapshot current (checked /etc/os-release inside the
  test container); Slowroll lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only). stoat-desktop and
  protonplus are GUI apps - no graphical smoke, install-level proof
  only.
- fluxer internal version string: the RPM carries version
  2026.815.214426 per the X-Fluxer-Version header upstream marker
  (matches update.sh's source of truth); app.asar package.json says
  a dev version string, not the app release version. The
  X-Fluxer-Version header is the canonical version per the project's
  own update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only; DEB URL
  404). Guard protects the repo from a broken spec; will bump when
  upstream publishes the desktop DEB.
- mangowm/niri/xwayland upstreams extremely active; pinned commits
  may advance before the next auto-updater - the guarded update.sh
  scripts will catch them.
- helium-browser releases/latest API reports 0.15.4.1 while tag
  0.15.5.1 exists with a downloadable asset (spec version 0.15.5.1 is
  correct per the tag list; release may not be marked "latest").
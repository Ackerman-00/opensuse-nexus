# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31383627057

## Verification Summary (Run 31383627057, scheduled pass)

Full independent maintenance pass over all 17 packages in home:ackerman.

### Inventory (17/17 match repo dirs = README = OBS)
bibata-cursor-theme, fluxer, localsend, mangowm-git, matugen, niri-git,
noctalia, noctalia-greeter, obsidian, opencode-desktop, rootapp, scenefx, stb,
vesktop, wlroots, xwayland-satellite-git, zen-browser. No missing/extra
packages on either side; README table matches.

### Changed this run
**niri-git 59a1001 -> 97c96a1 (two bumps)**:
- Upstream niri main moved twice within the window since the last auto-updater
  (10:58 UTC): 59a1001 -> ab35348 (13:25 UTC) -> 97c96a1 (13:58 UTC). The 01:02
  auto-updater could not have caught these; detected via fresh `git ls-remote`
  + `gh api` + unauthenticated API (all three agreed).
- Commits: `6f39880` (ab35348) then `f98c178` (97c96a1) - the second bump was
  needed because HEAD moved again between the first bump and build completion.
- update.sh run in-tree: generated niri-97c96a1.tar.gz (tarball top dir =
  commit sha, verified in upstream git), vendor.tar.xz (xz -t OK), cargo_config.
  spec: %global commit 97c96a13829ec74c83e976c2ea34e7cc717b02ec, shortcommit
  97c96a1, gitdate 20260810135827 (= committer date 2026-08-10T13:58:27Z).
- OBS sync: deleted niri-59a1001.tar.gz and niri-ab35348.tar.gz, uploaded
  spec/changes/tarball/vendor.tar.xz/cargo_config, cmd=rebuild. OBS built
  niri-git-26.04+git20260810135827.97c96a1-5.1 on BOTH repos (succeeded).
- Install test of new binary in clean TW container: INSTALL_EXIT=0, rpm -V rc=0,
  `niri --version` => "niri 26.04 (97c96a1)".
- Dep re-derivation (new commit): tarball niri.spec.rpkg BR list unchanged from
  ab35348 (udev/gbm/xkbcommon/wayland/libinput/dbus/systemd/libseat/
  libdisplay-info/pipewire/pango/cairo-gobject/clang/mesa-libEGL) - all mapped
  in spec. No dep changes.

No other package changed this run. The 10:58 auto-updater (run 31381491220) had
already bumped mangowm-git 8169bfc, opencode-desktop 1.18.16, zen-browser
1.21.13b; those binaries were re-verified and install-tested this run (see below).

### OBS build status (at end of run)
All 17 packages x 2 repos (openSUSE_Tumbleweed + openSUSE_Slowroll) = 34
`succeeded`, result_state published. Failure filter
`&code=failed&code=broken&code=unresolvable` => EMPTY.

### OBS vs local drift
All 17 spec files and all 17 .changes files content-MATCH OBS. No re-sync
needed except niri-git (handled above).

### Version sweep (independent, this process) - 17/17 current
- bibata-cursor-theme 2.0.7 = v2.0.7
- fluxer 2026.731.153836 = X-Fluxer-Version header (curl -sI api.fluxer.app, 200)
- localsend 1.17.0 = v1.17.0 + version.json INSIDE .rpm repack ("1.17.0","build 58")
- mangowm-git 8169bfc = main HEAD (gh api + git ls-remote agree)
- matugen 4.1.0 = v4.1.0
- niri-git 97c96a1 = main HEAD (gh api + unauthenticated curl + git ls-remote)
- noctalia 5.0.0~beta7 = v5.0.0-beta.7
- noctalia-greeter 1.2.1 = v1.2.1 (tags)
- obsidian 1.13.4 = v1.13.4 + app.asar package.json (extracted)
- opencode-desktop 1.18.16 = v1.18.16 + app.asar package.json (extracted)
- rootapp 0.9.126: AppImage re-downloaded (386MB), extracted via squashfs
  offset 944632, Root.desktop X-AppImage-Version=0.9.126, sha256
  0736926f...d910 == spec comment (byte-identical artifact => prior dep audit
  still valid)
- scenefx 0.5 = releases/tags 0.5
- stb 20260802 = master HEAD 2c980bb (gitdate via UTC+8)
- vesktop 1.6.5 = v1.6.5 + app.asar package.json (extracted)
- wlroots 0.20.2 = GitLab release 0.20.2 (GET -L 200 via object storage)
- xwayland-satellite-git 3bc915f = main HEAD
- zen-browser 1.21.13b = release/latest + application.ini Version=1.21.13b
  (extracted from RPM)

### Source URLs / checksums
All Source0 URLs reachable (curl -sI/-L 200). matugen/stb sources are
pipeline-uploaded tarballs (present on OBS: matugen-4.1.0.tar.gz +
vendor.tar.xz, stb-20260802.tar.xz) - not direct URLs, expected.
wlroots GitLab downloads redirect to object storage (GET -L 200).
rootapp sha256 verified byte-for-byte against spec comment.

### No-churn
All 16 update.sh run in isolated copies -> exit 0 / up-to-date messages,
except niri-git which found a genuine new HEAD (handled). No repeated
auto-update commits for the same SHA. No duplicate .changes entries.

### Spec validity / updater health
All 17 specs pass `rpmspec -P`. All 16 update.sh pass `bash -n`.

### GitHub Actions health
Last auto-updater run 31381491220 success. No failed runs. No failures to fix.

### Issues/PRs
`gh api issues?state=open` => 0 open; pulls => 0 open. Nothing to triage.

### Tumbleweed snapshot
Clean container /etc/os-release = 20260806 (current). GCC 16 default per TW
July 2026 review; all builds green on current snapshot. Slowroll not
container-tested (identical binaries ~5-10 days behind TW; TW proof suffices per
policy).

### Install tests (fresh TW containers, snapshot 20260806)
All 17 installed together from local RPMs: INSTALL_EXIT=0, 0 dependency
problems, rpm -V all 17 rc=0, `zypper verify` => "all satisfied", all ldd 0
not-found, smoke tests rc=0 (mango 0.15.6; niri 26.04 (97c96a1); matugen 4.1.0;
noctalia v5.0.0; greeter usage; zen-browser sandbox EPERM is Docker-root-only;
Electron apps reach the root-sandbox fatal = launcher executes).

User-path test: niri-git + zen-browser + mangowm-git installed from the PUBLIC
nexus repo (download.opensuse.org home:ackerman TW) in a fresh container:
INSTALL_EXIT=0, niri 26.04 (97c96a1), mango 0.15.6. Repo metadata + gpg work.

NOTE (transient flake): one of four identical fresh-container 17-package
installs aborted once during OSS mirror preloading (mirror 404s seen in earlier
logs); the other three passed with the exact same command and RPMs. Treat as
infra flake, not a package issue.

Install-test coverage ledger (all current binaries):
| package | last install-tested | result | smoke |
|---|---|---|---|
| bibata-cursor-theme | 2026-08-10 run 31383627057 | pass | n/a (icons) |
| fluxer | 2026-08-10 run 31383627057 | pass | electron root-fatal (Docker-only) |
| localsend | 2026-08-10 run 31383627057 | pass | --version rc=0 |
| mangowm-git (8169bfc) | 2026-08-10 run 31383627057 | pass | mango -v rc=0 |
| matugen | 2026-08-10 run 31383627057 | pass | --version rc=0 |
| niri-git (97c96a1) | 2026-08-10 run 31383627057 | pass | niri 26.04 (97c96a1) |
| noctalia | 2026-08-10 run 31383627057 | pass | v5.0.0 |
| noctalia-greeter | 2026-08-10 run 31383627057 | pass | --help usage |
| obsidian | 2026-08-10 run 31383627057 | pass | electron root-fatal (Docker-only) |
| opencode-desktop (1.18.16) | 2026-08-10 run 31383627057 | pass | electron root-fatal (Docker-only) |
| rootapp | 2026-08-10 run 31383627057 | pass | --version info:Program[0] |
| scenefx (libscenefx-0_5) | 2026-08-10 run 31383627057 | pass | n/a (lib, no smoke) |
| stb (stb-devel) | 2026-08-10 run 31383627057 | pass | n/a (header lib) |
| vesktop | 2026-08-10 run 31383627057 | pass | electron root-fatal (Docker-only) |
| wlroots (libwlroots-0_20) | 2026-08-10 run 31383627057 | pass | n/a (lib, no smoke) |
| xwayland-satellite-git | 2026-08-10 run 31383627057 | pass | --version rc=0 |
| zen-browser (1.21.13b) | 2026-08-10 run 31383627057 | pass | zen --version rc=0 |

### Dependency audit (all 17, high confidence)
OBS `_builddepinfo` (TW/x86_64) for all 17 packages: 0 `<error>` entries.
Re-derived this run for changed packages:

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (icons only) | none | none | deps-verified |
| fluxer | Electron bundled; ldd 0 not-found | soname Requires | none | deps-verified |
| localsend | version.json inside; ldd 0 not-found | soname Requires (ayatana-appindicator3, ido3) | none | deps-verified |
| mangowm-git (8169bfc) | meson: libcjson,libdrm,libinput,libpcre2-8,pangocairo,pixman-1,scenefx-0.5,wayland-client,wayland-protocols,wayland-server,wlroots-0.20,xcb,xcb-icccm,xkbcommon | all 14 mapped | none | deps-verified |
| matugen | Cargo vendored; no system deps beyond cargo | cargo-packaging,gcc-c++ | none | deps-verified |
| niri-git (97c96a1) | niri.spec.rpkg: udev,gbm,xkbcommon,wayland,libinput,dbus,systemd,libseat,libdisplay-info,pipewire,pango,cairo-gobject,clang,mesa-libEGL | all mapped | none | deps-verified |
| noctalia | meson full list (wayland/egl/xkb/pango/glib/libsodium/sdbus-c++/pipewire/webp/jxl/sndfile/rsvg/qalculate/xml/md4c/tomlplusplus/ical/json/stb/jemalloc/pam/polkit) | all in spec | none | deps-verified |
| noctalia-greeter | meson: wayland,xkb,pango/cairo,glib,libinput,wlroots,epoxy,tomlplusplus,json,stb,webp,rsvg | all in spec | none | deps-verified |
| obsidian | Electron bundled; AutoReqProv:no; Requires electron | electron,bash | none | deps-verified |
| opencode-desktop (1.18.16) | Electron bundled; ldd of ai.opencode.desktop 0 not-found; sonames = gtk3/notify/nss3/Xss/Xtst/asound/cups/gbm/Xcomposite/Xdamage/xkbcommon/secret/at-spi/uuid/ripgrep | all soname/named | none | deps-verified |
| rootapp (0.9.126) | AppImage teared: .NET AOT + bundled libs; system sonames resolve (X11/Xtst/xkbcommon/wayland/gtk3/nss/nspr/alsa/notify/pulse/gbm/egl/vulkan/at-spi) | all soname/named | none | deps-verified |
| scenefx (0.5) | meson: egl,gbm,glesv2,libdrm,pixman-1,xkbcommon,wlroots-0.20,wayland-server | all in spec | none | deps-verified |
| stb | header-only; c++ test in %check | c++_compiler | none | deps-verified |
| vesktop | Electron bundled; app.asar 1.6.5; ldd 0 not-found | soname Requires | none | deps-verified |
| wlroots (0.20.2) | meson: glslang,egl,gbm,glesv2,hwdata,libdrm,libpng,libseat,libinput,vulkan,wayland,xkbcommon,x11/xcb/xwayland,ffmpeg,libdisplay-info,libliftoff,lcms2 | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson: fontconfig,wayland,xcb,xcb-cursor,clang/llvm,cargo | all in spec | none | deps-verified |
| zen-browser (1.21.13b) | Firefox tarball; application.ini 1.21.13b; ldd 0 not-found | mozilla-nss/nspr,hunspell,hyphen | none | deps-verified |

Confidence: high for all 17 (_builddepinfo + source/artifact extraction + clean
container installs agree).

### Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts kept in /tmp only).
2. Every package install-tested in clean TW container incl. fresh-checker
   re-runs -> **PASS** (17/17; niri-git re-tested for BOTH new binaries
   ab35348 and 97c96a1).
3. Install-test coverage ledger updated -> **PASS** (this file, all 17 rows,
   all current binaries).
4. Dependency audit 100% coverage (17/17) -> **PASS** (table above; mangowm/
   niri/opencode-desktop/zen re-derived this run for their new commits/
   versions).
5. Claims backed by command output -> **PASS** (gh api, git ls-remote,
   unauthenticated API, curl -sI/-L, sha256sum, unsquashfs, asar package.json
   reads, xz -t, docker/zypper/rpm -V/ldd logs, OBS _result XML, srcmd5).
6. Named gaps -> see below.
7. Relay updated with run_id 31383627057 + status complete -> **PASS** (this
   commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64 proof
  per policy).
- Slowroll container installs (identical binaries ~5-10 days behind TW; TW
  proof per policy).
- One transient fresh-container install abort during OSS mirror preloading
  (infra flake; 3 identical subsequent runs passed) - recorded, not a package
  issue.
- GUI apps' full graphical smoke (no display in Docker; launcher + binary
  execution verified instead).
- niri upstream is extremely active (2 commits within ~1.5h this run); the
  pinned commit may advance again before the next auto-updater. Spec will be
  refreshed by the next schedule run; not a bug.

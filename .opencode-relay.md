# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31558011652

## Run Summary (31558011652)

Independent maintenance pass over all 17 packages in home:ackerman.
Started 02:47Z. Found one upstream advancement (mango main moved
post-auto-updater) and verified everything else current.

1. **mangowm-git: de691ce (bumped by the first model attempt of this
   run, commit c1ee11c).** Upstream mango main advanced from 4a3bff4
   (2026-08-11T08:13Z) to de691ce (2026-08-12T02:07:23Z, "feat: add c
   flag to ignore bind conflict"), pushed after the 01:08Z auto-updater
   (which ran before the commit existed - its log confirms it found
   everything "already synced"/"already up to date"). Commit c1ee11c
   bumped spec + .changes (single "Nightly sync (Commit: de691ce)"
   entry, no churn). OBS rev 97 carries mango-de691ce.tar.gz + spec +
   changes; both repos built mangowm-git-0.15.6+git20260812020723.de691ce-3.1,
   `succeeded` (GPG signature 02:51:22Z post-upload; RPM "Build Date"
   02:09:20Z is SOURCE_DATE_EPOCH from the changelog, not the build
   time). Version 0.15.6+git20260812020723.de691ce, gitdate matches
   committer date.

2. **Version sweep: 17/17 current** (each verified against the true
   upstream this run - see table below). No wrong or stale versions.
   No source URLs 404. No package needed a version fix.

3. **No other changes needed.** No failed OBS builds, no broken
   workflows, no open issues, no open PRs, README accurate.

### Inventory (17/17 match repo dirs = README = OBS)
bibata-cursor-theme, fluxer, localsend, mangowm-git, matugen, niri-git,
noctalia, noctalia-greeter, obsidian, opencode-desktop, rootapp, scenefx, stb,
vesktop, wlroots, xwayland-satellite-git, zen-browser. No missing/extra
packages on either side; README table matches.

### OBS build status
All 34 builds (17 pkgs x 2 repos = openSUSE_Tumbleweed + openSUSE_Slowroll)
report `succeeded`/`published`. Failure filter
`&code=failed&code=broken&code=unresolvable` => EMPTY (checked twice this
run). mangowm-git builddepinfo resolves all spec BuildRequires (meson,
ninja, gcc-c++/gcc16-c++, systemd-rpm-macros, fdupes, wayland-protocols,
wayland-devel, libdrm-devel, libpixman-1-0-devel, libinput-devel,
libxkbcommon-devel, pango-devel, pcre2-devel, wlroots-devel, scenefx-devel,
libxcb-devel, xcb-util-wm-devel, cJSON-devel).

### Version sweep - 17/17 current (verified against upstream this run)
| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api releases/latest) | up-to-date |
| fluxer | 2026.810.212541 | X-Fluxer-Version header on Source0 (200, 92MB) | up-to-date |
| localsend | 1.18.0 | v1.18.0 (gh api; DEB URL 200) | up-to-date |
| mangowm-git | 0.15.6+git20260812020723.de691ce | de691ce = main HEAD (git ls-remote; commit msg/date match) | UPDATED (c1ee11c) |
| matugen | 4.1.0 | v4.1.0 (gh api; refs/tags/v4.1.0.tar.gz 200) | up-to-date |
| niri-git | 26.04+git20260811204258.fec411e | fec411e = main HEAD (git ls-remote); gitdate == committer date | up-to-date |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 (gh api tags+releases; URL 302) | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 (gh api tags) | up-to-date |
| obsidian | 1.13.6 | v1.13.6 (gh api releases/latest) | up-to-date |
| opencode-desktop | 1.18.16 | v1.18.16 (gh api; DEB URL 302) | up-to-date |
| rootapp | 0.9.126 | AppImage downloaded (386MB) sha256 0736926f... == spec comment; X-AppImage-Version=0.9.126 inside | up-to-date |
| scenefx | 0.5 | wlrfx/scenefx tag 0.5 (gh api; URL 302) | up-to-date |
| stb | 20260802 | nothings/stb master 2c980bb dated 2026-08-02T06:42:09Z | up-to-date |
| vesktop | 1.6.5 | v1.6.5 (gh api; RPM URL 302) | up-to-date |
| wlroots | 0.20.2 | GitLab releases API 0.20.2 newest | up-to-date |
| xwayland-satellite-git | 0.8.2+git...3bc915f | 3bc915f = main HEAD (git ls-remote) | up-to-date |
| zen-browser | 1.21.13b | 1.21.13b (gh api; tar.xz URL 302) | up-to-date |

### No-churn
Only mangowm changed since the last completed run (relay 31536365363).
mangowm-git .changes has exactly ONE entry for de691ce. No package was
re-synced to OBS that was already there; the auto-updater run 31552642955
(01:08Z) made no commits (its log shows all packages "already synced").
Git history: c1ee11c is the only commit after 8c2cc18.

### Spec validity / updater health
- `bash -n` all 16 update.sh OK
- `rpmspec -P` all 17 specs OK
- all 12 _service files parse as valid XML
- GitHub Actions: last 30 runs all success; no failed Nexus Auto-Updater
  runs. update-packages.yml reviewed: Phase 1/2/3 + Smart Sync logic sound
  (update.sh failures are non-fatal warnings; git-modified + failing
  packages are the sync set; new/empty OBS packages are re-created).

### Issues/PRs
`gh api issues?state=open` => 0 open; pulls => 0 open. Nothing to triage.

### Tumbleweed snapshot
Fresh container /etc/os-release = 20260809 (current as of this run;
web-searched forums/news confirm ~20260809 era, no newer announced).
Slowroll lags ~5-10 days (not container-tested; identical binaries, TW
proof per policy).

### Install tests (fresh TW 20260809 containers)
mangowm-git is the ONLY package whose binary changed since its last
install test (previous relay ledger 2026-08-11). Tested end-to-end in
3 separate fresh containers:
1. OBS-downloaded RPM + OSS+nexus repos: installed clean, `mango -v` =>
   "mango 0.15.6(release)" exit 0, ldd ALL RESOLVED.
2. Same, `rpm -V mangowm-git` => exit 0 (package name is mangowm-git,
   not mangowm).
3. Fresh-checker: full user path `zypper install mangowm-git` from the
   public mirror (download.opensuse.org home:/ackerman), which lists
   mangowm-git-0.15.6+git20260812020723.de691ce-3.1.x86_64.rpm
   (12-Aug-2026 02:51): installed clean (146 pkgs incl libwlroots-0_20,
   libscenefx-0_5, xwayland), `mango -v` exit 0, `rpm -V` exit 0.

The other 16 packages have unchanged binaries since their 2026-08-11
tests (ledger below); no re-test required by policy.

### Install-test coverage ledger (current binaries as of this run)
| package | last install-tested | result | smoke |
|---|---|---|---|
| bibata-cursor-theme (2.0.7) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (icons) |
| fluxer (2026.810.212541) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| localsend (1.18.0-15.1) | 2026-08-11 run 31536365363 (binary unchanged) | pass | launches; display needed |
| mangowm-git (0.15.6+git20260812020723.de691ce-3.1) | 2026-08-12 run 31558011652 (binary CHANGED de691ce) | pass | mango 0.15.6(release) |
| matugen (4.1.0) | 2026-08-11 run 31536365363 (binary unchanged) | pass | 4.1.0 |
| niri-git (fec411e) | 2026-08-11 run 31536365363 (binary unchanged) | pass | niri 26.04 (fec411e) |
| noctalia (5.0.0~beta8) | 2026-08-11 run 31536365363 (binary unchanged) | pass | v5.0.0 |
| noctalia-greeter (1.2.1) | 2026-08-11 run 31536365363 (binary unchanged) | pass | --help usage |
| obsidian (1.13.6) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| opencode-desktop (1.18.16) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| rootapp (0.9.126) | 2026-08-11 run 31536365363 (binary unchanged) | pass | --version info:Program[0] |
| scenefx (libscenefx-0_5) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (lib) |
| stb (stb-devel) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (header lib) |
| vesktop (1.6.5) | 2026-08-11 run 31536365363 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| wlroots (libwlroots-0_20) | 2026-08-11 run 31536365363 (binary unchanged) | pass | n/a (lib) |
| xwayland-satellite-git (3bc915f) | 2026-08-11 run 31536365363 (binary unchanged) | pass | -version => 0.8.2 |
| zen-browser (1.21.13b) | 2026-08-11 run 31536365363 (binary unchanged) | pass | Mozilla Zen 1.21.13b |

### Dependency audit (all 17, high confidence)
OBS `_builddepinfo` (TW/x86_64): zero `<error>` entries (the 9 "error"
grep hits are legit dep names: xcb-util-errors-devel, libxcb-errors0,
libgpg-error-devel). Only mangowm changed upstream since the last full
audit; it was RE-DERIVED this run:
- mangowm-git (de691ce): meson.build dependency() list = libdrm, xcb,
  xcb-icccm, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon,
  libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0,
  pixman-1, libcjson, pangocairo -> ALL 13 have matching BuildRequires
  in the spec; builddepinfo resolves them all. Deps unchanged from
  4a3bff4 (upstream compare: only docs/bindings/keys.md,
  src/config/parse_config.h, src/mango.c changed - no build files).
- rootapp (0.9.126): AppImage re-torn this run (unsquashfs). sha256
  byte-identical to the pinned spec comment; X-AppImage-Version=0.9.126.
  ldd of bundled Root binary + all libSkiaSharp.so/libuiohook.so/
  libonigwrap.so etc: 0 not-found deps. Spec's soname/named Requires
  unchanged from prior verified audit.

Remaining 15 packages: upstream versions/tags unchanged since their last
full artifact audits (previous run re-tore changed packages; this run
confirmed versions + source URLs + checksums). Prior dependency
derivations remain valid; specs build and install cleanly.

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (icons only) | none | none | deps-verified |
| fluxer (2026.810) | Electron bundled; ldd 0 not-found | soname Requires + named (all exist in OSS) | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd (libayatana-indicator3.so.7 etc) | 4 soname Requires + xdg-user-dirs | none (fixed prior run) | deps-verified |
| mangowm-git (de691ce) | meson 13 deps (re-derived this run) | all 13 mapped | none | deps-verified |
| matugen (4.1.0) | Cargo vendored; no system deps beyond cargo | cargo-packaging, gcc-c++ | none | deps-verified |
| niri-git (fec411e) | niri.spec.rpkg BR set + Requires | all mapped; vendor regenerated | none | deps-verified |
| noctalia (5.0.0~beta8) | meson.build full dep list | all in spec incl pkgconfig(pam)+pkgconfig(stb) | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| obsidian (1.13.6) | Electron bundled; AutoReqProv:no; Requires electron | electron, bash | none | deps-verified |
| opencode-desktop (1.18.16) | DEB control Depends; ldd 0 not-found | all mapped (named pkgs verified in OSS) | none | deps-verified |
| rootapp (0.9.126) | AppImage torn this run; ldd 0 not-found | all soname/named | none | deps-verified |
| scenefx (0.5) | meson deps (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| stb (20260802) | header-only; c++ test in %check | c++_compiler | none | deps-verified |
| vesktop (1.6.5) | Electron bundled; app.asar 1.6.5; ldd 0 not-found | soname Requires | none | deps-verified |
| wlroots (0.20.2) | meson deps (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson (prior audit, unchanged upstream) | all in spec | none | deps-verified |
| zen-browser (1.21.13b) | Firefox tarball; application.ini 1.21.13b | mozilla-nss/nspr, hunspell, hyphen | none | deps-verified |

Confidence: high for all 17 (_builddepinfo clean + artifacts verified +
clean container installs agree).

### Commits this run
- c1ee11c fix(autonomous): mangowm-git: bump to upstream main de691ce
  (made by the first model attempt of this run; verified + install-tested
  by this session; OBS already synced rev 97, both repos succeeded)
- (relay+run record commit)

### Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp only;
   verified clean after relay commit).
2. Every package touched this run install-tested in a clean TW container
   incl. fresh-checker re-runs -> **PASS** (mangowm-git de691ce: 3 fresh
   containers, incl. public-mirror user path).
3. Install-test coverage ledger updated -> **PASS** (this file; mangowm
   row references the de691ce binary).
4. Dependency audit 100% coverage (17/17) -> **PASS** (table above;
   mangowm re-derived from new artifact, rootapp re-torn).
5. Claims backed by command output -> **PASS** (gh api, git ls-remote,
   curl -sIL, sha256sum, readelf+unsquashfs, ldd, rpm -qp, rpmspec -P,
   bash -n, OBS _result/_builddepinfo/_history XML, docker/zypper/rpm -V
   logs).
6. Named gaps -> see below.
7. Relay updated with run_id 31558011652 + status complete -> **PASS**
   (this commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64
  proof per policy).
- Slowroll container installs (identical binaries ~5-10 days behind TW; TW
  proof per policy).
- GUI apps' full graphical smoke (no display in Docker; launcher + binary
  execution verified instead; electron apps show the documented
  running-as-root sandbox fatal, Docker-only). Only mangowm was binary-
  changed this run, so only its runtime smoke applied.
- wlroots 0.20.2 direct tarball download from this runner blocked by
  GitLab anti-bot (403 challenge); version verified via the GitLab
  releases API and OBS builds/publishes it fine.
- niri/mangowm upstreams are extremely active; pinned commits may advance
  before the next auto-updater. Specs will be refreshed by the next
  schedule run; not a bug.
- matugen/xwayland-satellite vendor tarballs not independently re-extracted
  this run (upstream commits unchanged since the last full audit and OBS
  offline builds succeed).
- The download.opensuse.org mirror occasionally serves metadata slightly
  ahead of RPM files during republish (transient 404 on re-fetch; retry
  passes). Known infra flake, not a repo bug.

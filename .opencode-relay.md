# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32322286814

## Run Summary (32322286814)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-20 ~01:47Z (opencode-schedule, relay=false). Prior
relay 32289540292 was complete. This run:

1. **OBS build check at start: zen-browser UNRESOLVABLE on both
   codestreams, all other 40 builds green.**
   `_result?view=status&code=failed&code=broken&code=unresolvable`
   showed zen-browser x2 unresolvable:
   "have choice for libnspr4.so()(64bit) needed by mozilla-nss-certs:
   mozilla-nspr zen-browser" (same for libplc4.so, libplds4.so).
   Root cause: the previous run's fix kept the bundled NSPR libs
   (libnspr4/plc4/plds4) in the tarball, which makes the built RPM
   auto-provide those sonames; mozilla-nss-certs needs them, and OBS's
   build solver could not pick between mozilla-nspr and the package
   itself. **Fix (commit 7e1d7c7)**: add `BuildRequires: mozilla-nspr`
   to pin the provider at build-dependency-resolution time. .changes
   entry added. Pushed to main, synced spec+changes to OBS
   (rev 197/198), triggered runservice. OBS rebuilt
   zen-browser-1.21.15b-19.1 on BOTH codestreams -> succeeded.
   Verified BOTH sides: before = unresolvable have-choice; after =
   all 42 builds green (`_result` = 42 succeeded, 0 failed). Build log
   shows mozilla-nspr-4.39-1.3 installed as build dep and clean finish.

2. **Auto-updater (01:15Z, run 32320360401) bumped helium-browser to
   0.15.6.1 and rootapp to 0.9.127 since the prior relay; both verified
   against upstream THIS RUN and install-tested (see below).**
   All updater runs (last 8) success; Actions health green.

3. **Version inventory sweep: all 21 packages up-to-date.** Evidence
   per package (see table below): gh api releases/latest or
   commits/HEAD (gitdates all match committer dates),
   fluxer X-Fluxer-Version header = 2026.819.105810 (HTTP 200),
   rootapp AppImage re-downloaded + torn apart this run (squashfs at
   ELF offset 944632, X-AppImage-Version = 0.9.127, sq.version
   <version>0.9.127, sha256 2c54d964... == spec comment), helium
   tarball downloaded + main ELF inspected (0.15.6.1, ldd 0 missing),
   stb commit date 2026-08-02T06:42:09Z -> 20260802 (UTC+8 formula),
   wlroots GitLab releases API = 0.20.2 newest. localsend held at
   1.18.0 (v1.18.1 assets still Android APKs only, re-verified this
   run). All GitHub source URLs return HTTP 200 (curl -sI -L).

4. **No other repo changes needed.** No other spec, .changes, update.sh
   or workflow file needed an edit. All 20 update.sh scripts pass
   `bash -n`. Local spec == OBS spec (md5) for all 21 packages;
   all _service files match; README package table == 21 dirs == 21 OBS
   packages (no discrepancy).

5. **Issues/PRs: none open** (`gh issue list` empty, `gh pr list`
   empty). Two historical closed issues (previous run failures) already
   closed. No new issues/PRs to triage this run.

6. **Dependency audit: 100% coverage (21/21).** Changed packages
   RE-DERIVED from new artifacts this run:
   - helium-browser 0.15.6.1: downloaded new tarball, main ELF NEEDED
     33 sonames incl. libnspr4/nss3/nssutil3/smime3 (explicit soname
     Requires in spec) + asound/gbm; ldd 0 missing; install RC=0.
     deps-verified.
   - rootapp 0.9.127: AppImage torn apart this run; bundled ELFs
     (Root, UpdateNix, libHarfBuzzSharp, libSkiaSharp) ldd 0 missing;
     Requires set covers runtime; install RC=0. deps-verified.
   - zen-browser 1.21.15b: BuildRequires mozilla-nspr added (fixes
     have-choice); bundled NSS/NSPr kept; install RC=0. deps-fixed.
   All other upstreams unchanged (version sweep above), prior audits
   carry forward. Ground truth: all 42 OBS builds green AND every one
   of the 21 packages install-tested in fresh TW containers this run.

7. **Install-verification sweep: ALL 21 PACKAGES install-tested in
   fresh Tumbleweed containers this run** (100% coverage, first time
   the full set has been swept in one run). Every package: `zypper
   install --no-recommends <rpm>` RC=0 + `rpm -V` clean. Smoke:
   zen-browser --version = "Mozilla Zen 1.21.15b"; helium --version =
   "Helium 0.15.6.1 (Chromium 151.0.7922.169)"; matugen --help works;
   rootapp Root binary present + sq.version 0.9.127. Fresh-checker
   pass re-run the three changed binaries from scratch (RC=0 + rpm -V
   clean + version strings) and the Slowroll-produced binaries of
   zen/helium/rootapp also install RC=0 in TW. (Slowroll container
   image not available on registry mirror; TW pass per policy.)
   A transient matugen signature-key abort (rc=3) on the first attempt
   was re-run cleanly (RC=0, rpm -V clean, --help works) - the
   container's fresh zypper gpg state, not a package defect.

8. **Validation:** bash -n on all 20 update.sh scripts (all OK),
   rpmspec -P on zen-browser.spec (parses), zen-browser OBS build log
   clean (mozilla-nspr build dep installed, 0 errors), rootapp sha256
   re-verified (2c54d964... == spec comment), all Source0 URLs
   reachable.

## Version inventory sweep (21/21, THIS RUN)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api releases/latest) | up-to-date |
| fluxer | 2026.819.105810 | X-Fluxer-Version header 2026.819.105810 (HTTP 200) | up-to-date |
| helium-browser | 0.15.6.1 | 0.15.6.1 (gh api releases; tag 0.15.6.1; tarball 200) | up-to-date (auto-update bump verified) |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3 | LazyVim main 459a4c3 (gitdate matches) | up-to-date |
| localsend | 1.18.0 | held (v1.18.1 = Android APKs only; re-verified assets) | up-to-date (hold) |
| mangowm-git | 0.16.1+git20260819162330.6441cfe | mango main 6441cfe (gitdate matches) | up-to-date |
| matugen | 4.2.0 | v4.2.0 (gh api) | up-to-date |
| niri-git | 26.04+git20260819083147.b0eb8ad | niri main b0eb8ad (gitdate matches) | up-to-date |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 (gh api) | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 (gh api tags) | up-to-date |
| obsidian | 1.13.7 | v1.13.7 (gh api) | up-to-date |
| opencode-desktop | 1.18.18 | v1.18.18 (gh api) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (gh api tags) | up-to-date |
| rootapp | 0.9.127 | X-AppImage-Version 0.9.127 (torn this run); sha 2c54d964 == spec | up-to-date (auto-update bump verified) |
| scenefx | 0.5 | tag 0.5 (gh api) | up-to-date |
| stb | 20260802 | master 2026-08-02T06:42:09Z -> UTC+8 20260802 | up-to-date |
| stoat-desktop | 1.5.3 | v1.5.3 (gh api releases/latest) | up-to-date |
| vesktop | 1.6.7 | v1.6.7 (gh api) | up-to-date |
| wlroots | 0.20.2 | GitLab releases 0.20.2 (newest 0.20.x) | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f | Supreeeme main 3bc915f (gitdate matches) | up-to-date |
| zen-browser | 1.21.15b | 1.21.15b (gh api; upstream version correct) | FIXED-WRONG-PACKAGING |

## Bugs found and fixed this run

- **zen-browser 1.21.15b OBS build unresolvable (fixed, commit
  7e1d7c7).** The previous run's NSS/NSPr bundling fix left the built
  RPM providing libnspr4/libplc4/libplds4 sonames, so mozilla-nss-certs'
  build-time dependency had two providers ("have choice: mozilla-nspr
  zen-browser"). Added `BuildRequires: mozilla-nspr` to pin the choice.
  OBS rebuilt 1.21.15b-19.1 on both codestreams (succeeded), install
  RC=0 in a fresh TW container, `zen-browser --version` = "Mozilla Zen
  1.21.15b". No churn: version unchanged, only build-time dep added.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (only the relay
   commit remains; all artifacts in /tmp/opencode).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (zen-browser fixed binary 1.21.15b-19.1 RC=0 + version
   string; helium 0.15.6.1 + rootapp 0.9.127 RC=0 + rpm -V clean;
   fresh-checker re-run + Slowroll binaries re-tested).
3. Install-test coverage ledger updated -> **PASS** (ledger below;
   ALL 21 packages tested in fresh TW containers this run).
4. Dependency audit 100% coverage (21/21) -> **PASS** (table below;
   changed packages re-derived from new artifacts this run, all 42 OBS
   builds green).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, unsquashfs, readelf, rpm, docker/zypper/rpm -V,
   rpmspec -P, bash -n, git log/diff/status, OBS _result + binary
   listing + downloads + build logs).
6. Named gaps -> see below.
7. Relay updated with run_id 32322286814 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: all 21 packages install-tested from the OBS-published
binaries (Tumbleweed repo) in FRESH Tumbleweed containers. Plus
fresh-checker re-run of the three binaries whose build changed this
run (zen-browser, helium-browser, rootapp) and their Slowroll-built
equivalents.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| zen-browser | 1.21.15b-19.1 (FIXED, both repos) | pass (fresh TW, RC=0; BEFORE = unresolvable have-choice, AFTER = green) | clean | `Mozilla Zen 1.21.15b` |
| helium-browser | 0.15.6.1-4.1 (both repos) | pass (fresh TW, RC=0) | clean | `Helium 0.15.6.1 (Chromium 151.0.7922.169)` |
| rootapp | 0.9.127-2.1 (both repos) | pass (fresh TW, RC=0) | clean | Root binary present; sq.version 0.9.127; ldd 0 missing |
| mangowm-git | 0.16.1+git20260819162330.6441cfe-1.1 | pass (fresh TW, RC=0) | clean | compositor; binary present |
| stoat-desktop | 1.5.3-2.1 | pass (fresh TW, RC=0) | clean | /opt/Stoat + launcher present (Electron GUI; no display) |
| niri-git | 26.04+git20260819083147.b0eb8ad-5.1 | pass (fresh TW, RC=0) | clean | compositor; binary present |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass (fresh TW, RC=0) | clean | binary present |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1 | pass (fresh TW, RC=0) | clean | config-only; binary present |
| vesktop | 1.6.7-2.1 | pass (fresh TW, RC=0) | clean | binary present (Electron GUI; no display) |
| localsend | 1.18.0-15.1 | pass (fresh TW, RC=0) | clean | binary present |
| opencode-desktop | 1.18.18-3.1 | pass (fresh TW, RC=0) | clean | binary present (GUI; no display) |
| obsidian | 1.13.7-6.1 | pass (fresh TW, RC=0) | clean | binary present (Electron GUI; no display) |
| fluxer | 2026.819.105810-3.1 | pass (fresh TW, RC=0) | clean | binary present (GUI; no display) |
| protonplus | 0.6.4-8.1 | pass (fresh TW, RC=0) | clean | binary present (GUI; no display) |
| noctalia | 5.0.0~beta8-4.2 | pass (fresh TW, RC=0) | clean | compositor; binary present |
| noctalia-greeter | 1.2.1-2.1 | pass (fresh TW, RC=0) | clean | binary present |
| matugen | 4.2.0-6.1 | pass (fresh TW, RC=0; 1st attempt rc=3 gpg-key transient, re-run RC=0) | clean | `matugen --help` works |
| bibata-cursor-theme | 2.0.7-4.1 | pass (fresh TW, RC=0) | clean | cursor theme files present |
| libscenefx-0_5 | 0.5-7.1 | pass (fresh TW, RC=0) | clean | library present |
| libwlroots-0_20 | 0.20.2-8.1 | pass (fresh TW, RC=0) | clean | library present |
| stb-devel | 20260802-1.1 | pass (fresh TW, RC=0) | clean | headers present |

Fresh-checker pass (independent re-run of changed binaries):
zen-browser 1.21.15b-19.1 / helium-browser 0.15.6.1-4.1 / rootapp
0.9.127-2.1 all RC=0 + rpm -V clean + version strings verified in a
fresh container. Slowroll-built copies of those three also RC=0 in TW.

## Dependency audit (all 21)

Ground truth this run: (1) all 42 OBS builds green; (2) every one of
the 21 packages installed in fresh TW containers; (3) local spec ==
OBS for all 21; (4) upstream unchanged for 18/21 since prior audits
(version sweep above); helium-browser + rootapp + zen-browser changed
and were RE-DERIVED this run.

RE-DERIVATION (changed packages):
- helium-browser 0.15.6.1 (prebuilt): downloaded new tarball; readelf
  NEEDED of main ELF = 33 sonames (X11 set, asound, atk/bridge/atspi,
  cairo, cups, dbus, gbm, glib set, nspr4/nss3/nssutil3/smime3,
  pango, udev, xcb, xkbcommon); ldd 0 missing; spec's explicit soname
  Requires (asound/gbm/nspr4/nss3/nssutil3/smime3) + rpm auto-deps
  cover the set. Install passed.
- rootapp 0.9.127 (prebuilt AppImage): torn apart this run (squashfs
  offset 944632); bundled ELFs (Root, UpdateNix, libHarfBuzzSharp.so,
  libSkiaSharp.so) ldd 0 missing; spec Requires (gtk3, mozilla-nss,
  alsa, libnotify, xdg-utils, at-spi2-core, hicolor-icon-theme,
  Mesa-libEGL1, libvulkan, mozilla-nspr, libpulse0, libgbm1,
  wl-clipboard, libxkbcommon0, wayland set, X11 set) covers runtime.
  Install passed.
- zen-browser 1.21.15b: BuildRequires mozilla-nspr added (fixes
  have-choice); bundled NSS provides NSS_3.126; Requires
  mozilla-nss-certs (libnssckbi.so), hunspell, hyphen + auto sonames
  (self-satisfied). Install passed.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer | ldd of main ELF (prior) | soname Requires | none | none | deps-verified |
| helium-browser (0.15.6.1, re-derived) | 33 NEEDED sonames this run | soname Requires + auto-deps | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real-ELF ldd 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git | meson.build (prior; unchanged 6441cfe) | pkgconfig set | none | none | deps-verified |
| matugen | Cargo.lock == vendor (prior) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git | Fedora spec.rpkg + _builddepinfo (prior) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson.build @5.0.0-beta.8 (prior) | all 44 BRs present | none | none | deps-verified |
| noctalia-greeter | meson.build @1.2.1 (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson.build @0.6.4 (prior) | all in spec | none | none | deps-verified |
| rootapp (0.9.127, re-derived) | AppImage ELFs ldd 0 missing this run | libXt6 + wl-clipboard etc. | none | none | deps-verified |
| scenefx | meson.build @0.5 (prior) | all in spec; provides libscenefx-0_5 | none | none | deps-verified |
| stb | header-only | c++_compiler; provides stb.pc | none | none | deps-verified |
| stoat-desktop | 27 NEEDED sonames (prior 1.5.3) | named Requires set | none | none | deps-verified |
| vesktop | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior); provides libwlroots-0_20 | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser (1.21.15b, re-derived) | bundled NSS stack (NSS_3.126) + NSPR sonames | fixed: BuildRequires mozilla-nspr added | added BuildRequires mozilla-nspr | none | deps-fixed |

Confidence: high for all 21 (upstream verified per-package this run,
all 42 OBS builds green, every package install-tested in fresh TW
containers, changed packages re-derived from new artifacts this run).

## Commits this run

1. `7e1d7c7` fix(autonomous): zen-browser: add BuildRequires
   mozilla-nspr for have-choice resolution - spec + .changes.
2. Relay update (this commit) - run_id 32322286814, status complete.

## OBS build status

All 42 builds (21 pkgs x Tumbleweed + Slowroll) green: 42 succeeded,
zero failed/broken/unresolvable codes in
`_result?view=status&code=failed&code=broken&code=unresolvable`.
zen-browser fixed rebuild 1.21.15b-19.1 on both codestreams.

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs: the registry.opensuse.org slowroll image
  is unavailable on the mirror (`name unknown`); per policy a TW pass
  proves enough (same binaries, only days lag), and the Slowroll-built
  binaries of zen/helium/rootapp were install-tested in TW (RC=0).
- GUI apps' full graphical smoke under Docker (no display; binary
  presence + rpm -V verified, CLI-capable apps run: zen-browser
  --version, helium --version, matugen --help).
- The wlroots GitLab download could not be fetched by local curl (Anubis
  anti-bot challenge on gitlab.freedesktop.org object storage); the
  version (0.20.2) was confirmed via the GitLab releases API and OBS
  builds wlroots green on both codestreams.
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- Dependency re-derivation for the 18 unchanged upstreams was carried
  forward from prior audits (upstream artifacts identical - verified by
  the version sweep showing no upstream change).
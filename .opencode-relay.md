# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31350283207

## Verification Summary (Run 31350283207, scheduled pass)

Independent full re-verification of everything on main at run start. The 20:21
auto-updater (run 31334029842) and 01:02 auto-updater (run 31346029617) had
already handled all upstream changes before this run started.

- **OBS builds**: `_result?view=status` queried (fresh) => 17 packages x 2 repos
  (openSUSE_Tumbleweed + openSUSE_Slowroll) = 34 `succeeded`, repo states
  `published`. Failure filter `&code=failed&code=broken&code=unresolvable` =>
  EMPTY (checked twice this run). Evidence: `_result` XML (34 statuses, all
  succeeded).
- **OBS spec drift**: full content diff of all 17 local spec files vs OBS
  `/<pkg>/<spec>` => all 17 MATCH. Same for all 17 `.changes` files => MATCH.
  No re-sync needed, no churn.
- **OBS vs local file lists**: no file missing on OBS; OBS extras are gitignored
  build sources (vendor.tar.xz, *.tar.gz, cargo_config) — expected.
- **Version sweep (independent, this process)** — 17/17 current, 0 changed:
  - bibata-cursor-theme 2.0.7 = v2.0.7 (`gh api ful1e5/Bibata_Cursor/...latest`)
  - fluxer 2026.731.153836 = X-Fluxer-Version header (curl -sI api.fluxer.app, 200)
  - localsend 1.17.0 = v1.17.0 + version.json INSIDE the .deb (1.17.0) + .desktop (1.17.0+58)
  - mangowm-git e8eb11e = main HEAD (`gh api mangowm/mango/commits/main`,
    committer 2026-08-09T22:47:47Z; gitdate 20260809224747 matches). NOTE: relay
    previously recorded 05ba346; the 01:02 UTC auto-updater bumped it to e8eb11e
    (OBS already carries e8eb11e-2.1, spec MATCH).
  - matugen 4.1.0 = v4.1.0 (`gh api InioX/matugen/...latest`)
  - niri-git 59a1001 = main HEAD (`gh api niri-wm/niri/commits/main`)
  - noctalia 5.0.0~beta7 = v5.0.0-beta.7 (`gh api noctalia-dev/noctalia/...latest`)
  - noctalia-greeter 1.2.1 = v1.2.1 (tags API)
  - obsidian 1.13.4 = v1.13.4 + app.asar package.json (extracted, 1.13.4)
  - opencode-desktop 1.18.15 = v1.18.15 + app.asar package.json (extracted, 1.18.15)
  - rootapp 0.9.126: AppImage downloaded (386MB), extracted via squashfs offset
    944632, sq.version XML = 0.9.126, Root.desktop X-AppImage-Version = 0.9.126,
    sha256 0736926f...d910 == spec comment.
  - scenefx 0.5 = tag 0.5 (releases exist; phase-2 updater works)
  - stb 20260802 = master HEAD 2c980bb (committer 2026-08-02T06:42:09Z; UTC+8 ->
    20260802)
  - vesktop 1.6.5 = v1.6.5 + app.asar package.json (extracted, 1.6.5)
  - wlroots 0.20.2 = GitLab release 0.20.2
  - xwayland-satellite-git 3bc915f = main HEAD (`gh api Supreeeme/.../commits/main`)
  - zen-browser 1.21.12b = release/latest + application.ini Version=1.21.12b
    (extracted, matches); zen-bin --version => "Mozilla Zen 1.21.12b"
- **Source URLs**: all reachable (curl -sI -L). Rootapp/zen/obsidian/GH releases
  200; fluxer 200; wlroots GitLab tarball 200 via API.
- **No-churn**: all 16 update.sh run in isolated copies -> exit 0, 0 churn
  messages. localsend "Already up to date (1.17.0)", rootapp pure sha256 guard
  "Package is up to date" -> all no-churn guards health.
- **Spec validity**: all 17 pass `rpmspec -P`; all 16 update.sh pass `bash -n`.
- **GitHub Actions health**: last 10 runs all success (auto-updater +
  opencode-schedule). No failures to fix.
- **Issues/PRs**: `gh api issues?state=open` => 0 open; pulls => 0 open.
- **Tumbleweed snapshot**: clean container /etc/os-release = 20260806.

### Fresh-container install tests (this process, clean TW snapshot 20260806)

Two independent fresh containers (OSS + home:ackerman nexus repos):

1. Full 17-package install (`zypper install --no-recommends` all 17 from nexus
   repo): INSTALL_EXIT=0, 0 dependency conflicts.
2. All 17 `rpm -V` after install: rc=0 (no missing/modified files).
3. `zypper verify`: "Dependencies of all installed packages are satisfied."
4. ldd on all 10+ binaries: 0 "not found".
5. Smoke tests (rc=0): mango 0.15.6(release); niri 26.04 (59a1001); matugen
   4.1.0; noctalia v5.0.0; noctalia-greeter --help usage; rootapp --version
   "info: Program[0]"; zen-browser "Mozilla Zen 1.21.12b" (sandbox EPERM is
   Docker-root-only); Electron apps (vesktop/opencode-desktop/obsidian/fluxer)
   reach the "running as root without --no-sandbox" Electron fatal => launcher
   executes correctly; rc=0 (Electron root issue is Docker-only, not a package
   bug).

   Install-test coverage ledger (all installable):
   mangowm-git binary changed (05ba346 -> e8eb11e) since last test => RE-TESTED
   fresh this run: INSTALL_EXIT=0, rpm -V rc=0, `mango -v` rc=0. Ledger
   otherwise unchanged (all 17 covered in previous runs at same binaries).

### Dependency audit (this process)

OBS `_builddepinfo` (TW/x86_64, all 17 packages) fetched => 3055 pkgdeps, zero
`<error>` entries. meson.build dependency extraction for mangowm (new commit),
noctalia, noctalia-greeter, niri (upstream niri.spec.rpkg), scenefx:

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (icons only) | none | none | deps-verified |
| fluxer | Electron bundled; sonames resolve | soname Requires | none | deps-verified |
| localsend | binary needs libayatana-appindicator3.so.1 (confirmed by ldd/loading) | soname Requires | none | deps-verified |
| mangowm-git (e8eb11e) | meson: libdrm, xcb, xcb-icccm, wayland-server>=1.23.1, wlroots-0.20, xkbcommon, libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5, pixman-1, libcjson, pangocairo | all | none | deps-verified |
| matugen | Cargo (vendored); no system deps beyond cargo | cargo-packaging, gcc-c++ | none | deps-verified |
| niri-git (59a1001) | upstream niri.spec.rpkg: udev, gbm, xkbcommon, wayland, libinput, dbus-1, systemd, libseat, libdisplay-info, pipewire, pango, cairo-gobject, clang, EGL | all mapped | none | deps-verified |
| noctalia (5.0.0~beta7) | meson.build full list incl wayland/egl/xkb/pango/glib/libsodium/sdbus-c++/pipewire/webp/jxl/sndfile/rsvg/qalculate/xml/md4c/tomlplusplus/ical/json/stb/jemalloc/pam/polkit | all in spec | none | deps-verified |
| noctalia-greeter (1.2.1) | meson.build: wayland, xkb, pango/cairo, glib, libinput, wlroots, epoxy, tomlplusplus, json, stb, webp, rsvg | all in spec | none | deps-verified |
| obsidian | Electron bundled; AutoReqProv:no; Requires electron | electron, bash | none | deps-verified |
| opencode-desktop (1.18.15) | Electron bundled; sonames resolve (gtk3, notify, nss3, Xss, Xtst, asound, cups, gbm, Xcomposite, Xdamage, xkbcommon, secret, at-spi, uuid, ripgrep) | all soname/named | none | deps-verified |
| rootapp (0.9.126) | AppImage teared: .NET AOT Root + bundled libs (SkiaSharp, HarfBuzz, onigwrap, uiohook); system sonames resolve to Requires list | all soname/named | none | deps-verified |
| scenefx (0.5) | meson: egl, gbm, glesv2, libdrm>=2.4.129, pixman-1, xkbcommon, wlroots-0.20, wayland-server | all in spec | none | deps-verified |
| stb (20260802) | header-only; c++ test in %check | c++_compiler | none | deps-verified |
| vesktop (1.6.5) | Electron bundled (RPM repack); sonames resolve | soname Requires | none | deps-verified |
| wlroots (0.20.2) | meson/subprojects: glslang, egl, gbm, glesv2, hwdata, libdrm, libpng, libseat, libinput, vulkan, wayland, xkbcommon, x11/xcb/xwayland, ffmpeg, libdisplay-info, libliftoff, lcms2 | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson: fontconfig, wayland-client/cursor/server, xcb, xcb-cursor, clang/llvm, cargo | all in spec | none | deps-verified |
| zen-browser (1.21.12b) | Firefox-based tarball; mozilla-nss/nspr, hunspell, hyphen | all named | none | deps-verified |

Confidence: high for all 17 (builddepinfo + meson/Cargo/asar extraction +
clean-container installs agree).

### Changed this run

**NONE.** All 17 packages current upstream; builds succeeded; no issues/PRs.
Only work product is this verification + relay update.

### Pre-completion gate

1. `git status --porcelain` empty -> **PASS** (artifacts kept in /tmp only).
2. Every package install-tested in clean Tumbleweed container -> **PASS**
   (17/17, INSTALL_EXIT=0; mangowm re-tested fresh this run because its binary
   changed).
3. Install-test coverage ledger updated -> **PASS** (this file, all 17 rows).
4. Dependency audit covers 100% (17/17) -> **PASS** (table above).
5. Every claim backed by command output -> **PASS** (OBS _result XML, gh api,
   curl -sI, sha256sum, unsquashfs/sq.version, npx asar package.json reads,
   docker/zypper/rpm -V/ldd logs).
6. Named gaps: GUI smoke tests run without display (Docker root, not package
   bugs); aarch64 not container-tested (same toolchain, TW x86_64 proof per
   policy); Slowroll not container-tested (identical binaries, ~5-10 day lag,
   per policy TW proof suffices); fluxer internal version 0.0.8 is the app's
   dev placeholder — authoritative version is the X-Fluxer-Version header
   (2026.731.153836) which matches spec.
7. Relay updated with run_id 31350283207 + status complete -> **PASS** (this
   commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64 proof per
  policy).
- Slowroll container installs (identical binaries, ~5-10 day lag; TW proof per
  policy).
- Full upstream source extraction for every package — mangowm/noctalia/greeter/
  niri/scenefx verified via downloaded sources + meson; all binary packages
  verified by tearing the actual artifacts (AppImage, .deb, .rpm, tar.xz) and
  reading versions from inside + ldd.
- New-package watch: no unrelated new compositor/gaming binary warranted adding
  without a concrete upstream signal this run; all existing 17 are current.
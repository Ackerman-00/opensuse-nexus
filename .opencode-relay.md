# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32057328402

## Run Summary (32057328402)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-17 ~18:53Z (opencode-schedule, relay=false). Prior
relay 32032902608 was complete. The auto-updater run 32055957658
(18:39-18:57Z) bumped ONLY matugen 4.1.0 -> 4.2.0 (upstream released
v4.2.0 at 18:04:03Z this day - a genuine new release, not churn), so
this run: (1) re-verified the matugen bump end-to-end at the new
commit (tarball md5, vendor vs Cargo.lock, OBS build, install,
smoke), (2) fixed a genuine optimization-discipline violation in
matugen.spec (`%global debug_package %{nil}` on a source-built Rust
package), and (3) ran an INDEPENDENT FRESH-CHECKER install re-test of
matugen from the public nexus repo.

### What this run verified (evidence-backed)

1. OBS build health: all 42 builds (21 packages x Tumbleweed +
   Slowroll) `succeeded`/`published`; 0 failed/broken/unresolvable.
   _builddepinfo (TW): all 21 packages present, 0 <error> blocks.
2. matugen 4.2.0 verified: gh api releases v4.2.0 2026-08-17T18:04:03Z
   == spec Version 4.2.0; OBS matugen-4.2.0.tar.gz md5
   (bcecff1fe20852f299a9c935748ab3cb) == freshly downloaded upstream
   tarball md5; vendor.tar.xz (381 crate dirs) == Cargo.lock (382
   names incl. root crate matugen) - exact match; OBS spec/changes ==
   local. Rebuild after my fix succeeded on BOTH codestreams
   (published). Binary matugen-4.2.0-6.1.x86_64 installed cleanly in
   a FRESH TW container from the public nexus repo (INSTALL-RC=0,
   rpm -V clean, `matugen --version` -> "matugen 4.2.0" rc=0, ldd 0
   missing) - TWICE (main test + independent fresh-checker re-run).
3. FIXED: matugen.spec dropped `%global debug_package %{nil}`
   (source-built Rust package was killing debuginfo - rule (e)
   violation; all other 8 packages carrying it are prebuilt/
   repackaged binaries where it is justified). commit 179524c.
   .changes entry added. Verified: rebuilt + published both
   codestreams; rpmlint clean except unstripped warning that ALSO
   affects niri-git (which never had the flag) - it is OBS project
   behavior (no _config => debuginfo not enabled project-wide,
   RUSTFLAGS -C strip=none), NOT a regression from this change.
4. Version inventory sweep 21/21 (STEP B), all up-to-date:
   - bibata-cursor-theme 2.0.7 = tag v2.0.7 (gh api)
   - fluxer 2026.816.204155 = X-Fluxer-Version header from the stable
     API, verified last relay (32032902608, this morning); API
     unreachable from this runner today (connection timeout,
     DNS-only IPv6) - see gaps
   - helium-browser 0.15.5.1 = tag 0.15.5.1 (gh api)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (gh api; gitdate
     20260602133554 == committer date; base_version 16.0.0 == tag
     v16.0.0)
   - localsend 1.18.0 = held (v1.18.1 assets re-verified = 4 Android
     APKs ONLY, no desktop DEB -> correct hold)
   - mangowm-git 7b0b39f = mango main HEAD (gh api)
   - matugen 4.2.0 = tag v4.2.0 (this run, see above)
   - niri-git 6062844 = niri main HEAD (gh api)
   - noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api
     noctalia-dev/noctalia)
   - noctalia-greeter 1.2.1 = tag v1.2.1 (tags API
     noctalia-dev/noctalia-greeter)
   - obsidian 1.13.7 = tag v1.13.7 (gh api) AND obsidian.asar binary
     contains "1.13.7" (grep -ao); OBS tarball md5 == upstream
   - opencode-desktop 1.18.18 = tag v1.18.18 (gh api
     anomalyco/opencode)
   - protonplus 0.6.4 = tag v0.6.4 (gh api vysp3r/ProtonPlus)
   - rootapp 0.9.126 = X-AppImage-Version=0.9.126 READ from the
     actual AppImage (unsquashfs at ELF section-header offset 944632,
     Root.desktop); sha256 downloaded AppImage
     0736926f...d910 == spec pin == fedora-nexus == OBS
   - scenefx 0.5 = release 0.5 (gh api wlrfx/scenefx)
   - stb 20260802 = master commit 2c980bb dated 2026-08-02 (gh api;
     TZ=Etc/GMT-8 -> 20260802)
   - stoat-desktop 1.5.1 = tag v1.5.1 (gh api stoatchat/for-desktop)
   - vesktop 1.6.5 = tag v1.6.5 (gh api Vencord/Vesktop)
   - wlroots 0.20.2 = git ls-remote tags (0.20.2 newest 0.20.x;
     gitlab API not reachable this run, ls-remote is ground truth)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD (gh api)
   - zen-browser 1.21.14b = tag 1.21.14b (gh api)
   All Source0 URLs verified 200 (curl -sI) except fluxer API
   (unreachable from runner - environmental). Git-pinned tarballs
   verified: OBS tarball md5 == upstream md5 for niri-git, mangowm,
   xwayland-satellite, lazyvim (all YES). README (21 rows) == local
   dirs (21) == OBS packages (21).
5. Dependency deep audit: matugen Cargo.lock @4.2.0 fully re-derived
   (382 lock entries, vendor has 381 = all except root crate).
   All other packages unchanged since last full audit (relay
   32032902608); their binaries are byte-identical (OBS binary list
   matches ledger, no upstream change since). _builddepinfo 0 errors,
   all 21 packages present.
6. Optimization discipline: niri-git/xwayland-satellite explicitly
   export CFLAGS="%{optflags} -ffat-lto-objects"; mangowm/scenefx/
   wlroots/noctalia/noctalia-greeter/protonplus use %meson (propagates
   %optflags); lazyvim/bibata config/icon-only; stb header-only.
   No -march/-O3/LTO escapes. matugen debuginfo fixed (see 3).
7. update.sh health: `bash -n` PASS on all 19 update.sh scripts.
   No-churn guards verified (pure version / commit-SHA / sha256
   comparisons). matugen update.sh correctly bumped to 4.2.0 (guard
   is version-only; tarballs gitignored).
8. Actions health: last auto-updater run 32055957658 SUCCESS
   (completed 18:57Z; only matugen touched); 10/10 recent runs
   success. 0 open issues, 0 open PRs.
9. Tumbleweed snapshot: TW OSS container pulled today (tw:latest);
   Slowroll builds green. GCC 16 toolchain: all 42 builds succeed.
   No Leap 16 involvement.

### Bugs found and fixed this run

- matugen.spec: `%global debug_package %{nil}` removed (source-built
  Rust package - rule (e) violation). commit 179524c. Rebuilt,
  published, install-tested twice.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (artifacts in
   /tmp/opencode, not the repo tree).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (matugen 4.2.0-6.1 tested twice in fresh containers from
   the public nexus repo, main + independent fresh-checker).
3. Install-test coverage ledger updated -> **PASS** (ledger below).
   matugen's binary changed (4.1.0->4.2.0 + debuginfo rebuild) and
   was re-tested; no other binary changed since last test.
4. Dependency audit 100% coverage (21/21) -> **PASS** (matugen
   re-derived @4.2.0; others unchanged, binaries byte-identical;
   builddepinfo 0 errors).
5. Claims backed by command output -> **PASS** (gh api, git
   ls-remote, curl -sI, sha256sum/md5sum, docker/zypper/rpm/ldd
   logs, OBS _result/_builddepinfo/_binarylist XML, bash -n, readelf/
   unsquashfs for rootapp).
6. Named gaps -> see below.
7. Relay updated with run_id 32057328402 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 21 packages install-tested in prior run 32032902608 (fresh TW
containers, public nexus repo). THIS RUN: matugen's binary changed
(4.1.0 -> 4.2.0 + debuginfo rebuild) so ONLY matugen required
re-testing - done, twice:

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| matugen | 4.2.0-6.1.x86_64 | pass | clean | "matugen 4.2.0" rc=0; ldd 0 missing |
| all other 20 | unchanged since 32032902608 ledger | (re-tested last run) | clean | (see last ledger) |

Last full sweep (run 32032902608): bibata-cursor-theme 2.0.7 pass,
fluxer 2026.816.204155 pass, helium-browser 0.15.5.1 pass (launches),
lazyvim-git 459a4c3 pass, localsend 1.18.0 pass, mangowm-git 7b0b39f
pass, niri-git 6062844 pass, noctalia 5.0.0~beta8 pass,
noctalia-greeter 1.2.1 pass, obsidian 1.13.7 pass, opencode-desktop
1.18.18 pass, protonplus 0.6.4 pass, rootapp 0.9.126 pass
(X-AppImage-Version=0.9.126, AppRun launches), libscenefx-0_5 0.5
pass, stb-devel 20260802 pass, stoat-desktop 1.5.1 pass, vesktop
1.6.5 pass, libwlroots-0_20 0.20.2 pass, xwayland-satellite-git
3bc915f pass, zen-browser 1.21.14b pass.

## Dependency audit (all 21)

Ground truth this run: (1) `_builddepinfo` (TW) covers all 21
packages, ZERO <error> blocks; (2) matugen Cargo.lock @4.2.0 fully
re-derived against vendor tarball (exact match); (3) all other
packages unchanged upstream since last full audit (binaries
byte-identical).

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer | real-ELF ldd 0 missing (prior audit) | soname Requires | none | none | deps-verified |
| helium-browser | ldd real ELF 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real ELF 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git | meson.build @7b0b39f (14 deps, prior audit) | all present | none | none | deps-verified |
| matugen (4.2.0) | Cargo.lock @4.2.0: 382 crates, vendor 381 | cargo-packaging, gcc-c++ (vendored) | none | none | deps-verified |
| niri-git | Cargo.toml @6062844 (prior) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson deps (prior) | all in spec | none | none | deps-verified |
| noctalia-greeter | meson deps (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson appstream + cairo (prior) | pkgconfig(appstream), pkgconfig(cairo) | none | none | deps-verified |
| rootapp | ldd libuiohook.so 0 missing (prior) | libXt6 | none | none | deps-verified |
| scenefx | meson.build (prior) | all in spec | none | none | deps-verified |
| stb | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop | real ELF 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| vesktop | real ELF 0 missing (prior) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior) | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser | libxul/libtool deps; NSS symlinked (prior) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for matugen (fresh-container install x2 + exact
vendor-vs-lock match this run). high for the other 20 (unchanged
since last full fresh-container audit 6h ago; binaries
byte-identical).

## Commits this run

- 179524c fix(autonomous): matugen: enable debuginfo (drop
  debug_package nil on source-built Rust package)

(the auto-updater commit c809711 bumped matugen to 4.2.0 - legitimate
upstream move, not churn)

## OBS build status

All 42 builds succeeded/published (Tumbleweed + Slowroll). matugen
rebuilt after the debuginfo fix (both codestreams published).

## Not Verified / Gaps
- fluxer version: stable API (api.fluxer.app) unreachable from this
  runner all run (HEAD/GET both time out; DNS resolves IPv6-only,
  connection stalls). Version 2026.816.204155 = X-Fluxer-Version
  header verified by prior relay this morning; OBS build green; the
  running auto-updater also failed to fetch it (warning, no churn).
  If a newer stable was cut between 12:43Z and now, it is unverified
  - next run should retry the API before assuming fluxer is current.
- wlroots version: gitlab.freedesktop.org API unreachable this run;
  `git ls-remote --tags` (0.20.2 newest 0.20.x) is ground truth.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified in prior runs).
- obsidian/opencode-desktop/vesktop/etc. binary artifacts are fetched
  by OBS `download_files` service at build time (not stored in OBS
  source); verified via URL 200 + upstream release assets instead.
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
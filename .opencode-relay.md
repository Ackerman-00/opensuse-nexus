# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31948429168

## Run Summary (31948429168)

Autonomous maintenance pass over all 22 packages in home:ackerman.
Started 2026-08-16 ~12:56Z (opencode-schedule, relay=false). Prior
relay 31932574049 was complete. This run acted as a fresh checker over
the whole inventory and re-verified the package whose binary changed
since the last relay (mangowm-git, auto-bumped by the updater at
12:40Z).

### Work done this run

1. **OBS build health (priority 1)** - All 44 builds (22 Tumbleweed +
   22 Slowroll) `succeeded`/`published`, 0 failed/broken/unresolvable
   (queried _result?view=status this run). The auto-updater (run
   31947644643, 12:39Z) had bumped mangowm-git to c5dd504 (0.16.1);
   OBS already carried the new spec + tarball (mango-c5dd504.tar.gz)
   and both repos built green - no churn, no re-sync needed.
   `_builddepinfo` (openSUSE_Tumbleweed/x86_64) re-fetched this run:
   0 `<error>` blocks, all 22 packages present.

2. **Version inventory sweep 22/22 (priority 2)** - every package
   verified against upstream this run via gh api (releases/latest,
   tags, commits/HEAD), git ls-remote (codeberg ly), GitLab API
   (wlroots), X-Fluxer-Version header (fluxer), and AppImage re-tear
   (rootapp):
   - bibata-cursor-theme 2.0.7 = tag v2.0.7 (up-to-date)
   - fluxer 2026.815.214426 = X-Fluxer-Version header this run
     (up-to-date; rpm -q confirms installed NVR)
   - helium-browser 0.15.5.1 = tag 0.15.5.1 exists + asset 200
     (releases/latest now reports 0.15.5.1 too; tarball HTTP 200)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (up-to-date)
   - localsend 1.18.0 = held (v1.18.1 ships ONLY 4 Android APKs;
     re-verified asset list this run, correct hold)
   - ly 1.4.1 = codeberg tag v1.4.1 (up-to-date)
   - mangowm-git c5dd504 = mango main HEAD (up-to-date; auto-bumped
     this cycle, gitdate 20260816114232 == committer date 11:42:32Z)
   - matugen 4.1.0 = upstream v4.1.0 (up-to-date)
   - niri-git 6062844 = niri main HEAD, gitdate 20260814122556 ==
     committer date (up-to-date)
   - noctalia 5.0.0~beta8 = tag v5.0.0-beta.8; local tarball sha256
     a60e4ab8... == fresh GitHub download byte-identical
   - noctalia-greeter 1.2.1 = tag v1.2.1; local tarball sha256
     1d1c4349... == fresh GitHub download byte-identical
   - obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
   - opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date)
   - protonplus 0.6.4 = tag v0.6.4 (up-to-date)
   - rootapp 0.9.126 = AppImage re-torn fresh this run: sha256
     0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910
     == spec comment; sq.version <version>0.9.126</version> +
     X-AppImage-Version=0.9.126 from inside
   - scenefx 0.5 = wlrfx/scenefx tag 0.5 (up-to-date)
   - stb 20260802 = commit 2c980bb date 2026-08-02 (up-to-date;
     local tarball tree byte-identical to GitHub archive of 2c980bb)
   - stoat-desktop 1.4.2 = tag v1.4.2 + zip HTTP 200 (up-to-date)
   - vesktop 1.6.5 = upstream v1.6.5 (up-to-date)
   - wlroots 0.20.2 = GitLab release 0.20.2 (up-to-date)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD, gitdate
     20260808203058 == committer date (up-to-date)
   - zen-browser 1.21.14b = tag 1.21.14b + tarball HTTP 200
     (up-to-date)
   All versioned Source0 URLs verified (200/302-GitHub-redirect) this
   run. OBS package list == 22 local dirs == 22 README rows (diff
   clean). All -git gitdates == upstream committer dates exactly.

3. **Dependency audit (ground truth)** - `_builddepinfo` re-fetched
   this run: ZERO `<error>` blocks across all 22 packages. mangowm-git
   re-derived from its NEW artifact (c5dd504, the only package whose
   artifact changed since the last relay): meson.build requires
   libdrm, xcb, xcb-icccm, wayland-server>=1.23.1, wlroots-0.20,
   xkbcommon, libinput>=1.27.1, wayland-client, libpcre2-8,
   scenefx-0.5>=0.5.0, pixman-1, libcjson, pangocairo - ALL present in
   the spec's BuildRequires. builddepinfo confirms meson, ninja,
   wayland-protocols-devel, libdrm-devel, libpixman-1-0-devel,
   libinput-devel, libxkbcommon-devel, pango-devel, pcre2-devel,
   wlroots-devel, scenefx-devel, libxcb-devel, xcb-util-wm-devel,
   cJSON-devel all resolved for mangowm-git. No dependency gaps.

4. **No-churn verification (mandatory)** - mangowm-git OBS spec +
   .changes byte-identical to local (diff clean this run) - the
   auto-updater synced once, OBS build green, nothing to re-push.
   All other 21 packages unchanged since last relay (verified version
   sweep above). All update.sh scripts pass `bash -n` (21 scripts).
   No repeated .changes-only commits observed.

5. **GitHub Actions health (priority 3)** - recent runs all `success`;
   last failure/cancel was opencode-schedule 31917475941 (concurrency
   cancel, 00:32Z). Auto-updater 31947644643 (12:39Z) success, touched
   only mangowm-git. No workflow failures.

6. **Issues/PRs (priority 4/5)** - 0 open issues, 0 open PRs (gh api
   this run).

7. **Spec validation** - `rpmspec -P` on all 22 specs: 21 clean; the
   protonplus "FAIL" is a false positive (`%files lang -f
   com.vysp3r.ProtonPlus.lang` requires the build-generated .lang file
   from %find_lang - standard OBS pattern; OBS build succeeded).

8. **Tumbleweed snapshot (discover every run)** - current snapshot
   20260812 (review of week 2026/33, GCC 16.2.0, Mesa 26.2.0,
   Plasma 6.7.4). Slowroll lags ~5-10 days. All packages built with
   the current toolchain on both codestreams (OBS green).

## Commits this run
- (relay commit) - no package changes this run; the repo was fully
  current and green. All 22 packages verified up-to-date against
  upstream, OBS green, install-clean.

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp).
2. Every package touched this run install-tested in a fresh TW
   container -> **PASS**. The only binary changed since the last relay
   (mangowm-git c5dd504) was install-tested twice from scratch
   (first attempt exposed the libscenefx-0.5 dependency which the
   nexus repo provides; retested WITH both oss+nexus repos = exact
   user flow -> clean). Plus a fresh-checker pass re-installed all 22
   packages from the PUBLIC mirror in fresh containers.
3. Install-test coverage ledger updated -> **PASS** (all 22 rows THIS
   RUN, table below).
4. Dependency audit 100% coverage (22/22) -> **PASS** (builddepinfo
   0 errors; mangowm-git re-derived from its NEW artifact c5dd504;
   all deps present in spec).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, unsquashfs+sq.version+X-AppImage-Version, rpm2cpio,
   OBS _result/_builddepinfo XML, docker/zypper/rpm logs, bash -n,
   rpmspec -P).
6. Named gaps -> see below.
7. Relay updated with run_id 31948429168 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 22 packages install-tested THIS RUN in FRESH Tumbleweed
containers from the PUBLIC download.opensuse.org mirror (exact user
flow: OSS + home:ackerman repos). mangowm-git (the only binary
changed since the last relay) additionally tested via direct RPM
download.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | clean | n/a (icons) |
| fluxer | 2026.815.214426-4.1 | pass | clean | ldd /usr/lib64/fluxer/fluxer: 0 missing; electron root-fatal (Docker-only) |
| helium-browser | 0.15.5.1-4.1 | pass | clean | electron root-fatal (Docker-only) |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass | clean | n/a (config package) |
| localsend | 1.18.0-15.1 | pass | clean | ldd 0 missing |
| ly | 1.4.1-8.1 | pass | clean | "Ly version 1.4.1" rc=0 |
| mangowm-git | 0.16.1+git20260816114232.c5dd504-1.1 | pass (x2) | clean | mango launches rc=0 (compositor needs display); ldd 0 missing |
| matugen | 4.1.0-30.1 | pass | clean | "matugen 4.1.0" rc=0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | clean | "niri 26.04 (6062844)" rc=0 |
| noctalia | 5.0.0~beta8-4.1 | pass | clean | "noctalia v5.0.0" rc=0 |
| noctalia-greeter | 1.2.1-2.1 | pass | clean | greeter helpers rc=0 |
| obsidian | 1.13.7-6.1 | pass | clean | electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | clean | electron root-fatal (Docker-only) |
| protonplus | 0.6.4-8.1 | pass | clean | "ProtonPlus 0.6.4" rc=0 |
| rootapp | 0.9.126-2.1 | pass | clean | binary launch rc=0 |
| libscenefx-0_5 | 0.5-5.1 | pass | clean | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | clean | n/a (headers) |
| stoat-desktop | 1.4.2-10.1 | pass | clean | ldd 0 missing |
| vesktop | 1.6.5-35.1 | pass | clean | ldd 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass | clean | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | clean | binary launches rc=0 |
| zen-browser | 1.21.14b-6.1 | pass | clean | "zen-bin" launches rc=0 (sandbox EPERM Docker-only) |

## Dependency audit (all 22)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) re-fetched
this run covers all 22 packages with ZERO `<error>` entries.
mangowm-git re-derived from its NEW artifact c5dd504 (only artifact
changed since last relay).

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.815.214426) | X-Fluxer-Version header; ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| helium-browser (0.15.5.1) | Chromium bundled; AutoReqProv | shfmt, bubblewrap, electron0 | none | none | deps-verified |
| lazyvim-git (459a4c3) | config package, 0 deps | none (noarch lua files) | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd | soname Requires + xdg-user-dirs | none | none | deps-verified |
| ly (1.4.1) | zig deps vendored (cache) | VENDORED via ly-zig-cache.tar.gz | none | none | deps-verified |
| mangowm-git (c5dd504) | meson.build @c5dd504 (13 deps) | all present incl. wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 + builddepinfo | pkgconfig set (21) + cargo-packaging | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (fresh tarball) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (fresh tarball) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | meson.build appstream + cairo (direct) | pkgconfig(appstream), pkgconfig(cairo) | none (added last run) | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn (sha == spec) | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| stb (20260802) | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.4.2) | Electron bundled | soname Requires; rpmlintrc filters | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 22. Independent public-mirror installs of
every package this run + clean builddepinfo is stronger evidence than
static analysis alone.

## OBS build status

All 44 builds (22 Tumbleweed + 22 Slowroll) succeeded/published.
mangowm-git (auto-bumped to c5dd504 at 12:40Z) already built green on
OBS from the auto-updater - verified this run. No failed/broken/
unresolvable. Nothing to rebuild.

## Not Verified / Gaps
- Tumbleweed snapshot current 20260812 (review of week 2026/33);
  Slowroll lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only). helium-browser,
  obsidian, opencode-desktop, stoat-desktop, vesktop, zen-browser,
  fluxer are GUI apps - install-level proof only.
- fluxer internal version: app.asar package.json is a dev placeholder;
  X-Fluxer-Version header (2026.815.214426) is the canonical version
  per the project's own update mechanism (rpm -q confirms the
  packaged NVR matches the header).
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only). Guard
  protects the repo; will bump when upstream publishes the desktop DEB.
- mangowm/niri/xwayland upstreams extremely active; pinned commits
  may advance before the next auto-updater - the guarded update.sh
  scripts will catch them.
- lazyvim-git ships Lua config as plain files (no plugins vendored);
  installs clean and files verified, but end-to-end LazyVim startup
  requires its runtime-download on first launch (network dependent,
  not testable offline in a container).
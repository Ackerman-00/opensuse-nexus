# opencode-schedule Relay State

## Status: complete

run_id: 35956912983

## Run Summary (2026-09-24 ~04:44-05:15Z, opencode-schedule, RELAY=false)

Prior relay (35922588385) was `complete`; no unfinished pickup. Auto-updater (03:32Z) success. OBS fleet end-state: 42/42 succeeded, 0 failed/broken/unresolvable (TW + SR). Zero open issues, zero open PRs. All Actions runs success. CVE batch 5/5 clean. Three real upstream moves since prior relay, all already correctly applied by auto-updater and verified this run: fluxer 2026.922.193957->2026.924.13630, mangowm-git 516c21a->d6b87bd, xwayland-satellite-git add2795->5274bdc (each verified real via upstream API + OBS pins match local + OBS builds succeeded). Install-tested the 9-package rotation slice (all changed + oldest-first) TW+SR in fresh containers incl. rpm -V + smokes. Commits this run: this relay only; zero spec churn.

## Version inventory sweep (21/21 verified)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 releases/latest (ful1e5/Bibata_Cursor) | up-to-date |
| fluxer | 2026.924.13630 | served filename Fluxer-2026.924.13630-linux-x86_64.rpm (api.fluxer.app stable endpoint 302->200; X-Fluxer-Version header 2026.924.14020 drifts per standing note, filename authoritative) | up-to-date (bumped by auto-updater, verified real) |
| helium-browser | 0.18.1.1 | 0.18.1.1 tags[0] + releases-list[0] (imputnet/helium-linux) | up-to-date |
| lazyvim-git | 9997009 | HEAD 999700997f72 `git ls-remote LazyVim/LazyVim` == pin | up-to-date |
| localsend | 1.18.2 | v1.18.2 releases/latest (localsend/localsend) | up-to-date |
| mangowm-git | d6b87bd | HEAD d6b87bdd4600 `git ls-remote mangowm/mango` == pin | up-to-date (bumped by auto-updater, verified real) |
| matugen | 4.2.0 | v4.2.0 releases/latest (InioX/matugen) | up-to-date |
| niri-git | 5f4469b | HEAD 5f4469b6a992 `git ls-remote YaLTeR/niri` == pin | up-to-date |
| noctalia | 5.1.0 | v5.1.0 releases/latest (noctalia-dev/noctalia-shell) | up-to-date |
| noctalia-greeter | 1.5.0 | v1.5.0 tags newest (no releases, tags API) | up-to-date |
| obsidian | 1.13.7 | v1.13.8 BUT assets = ["Obsidian-1.13.8.apk"] ONLY (re-checked via releases list) | up-to-date (HOLD) |
| opencode-desktop | 1.18.32 | v1.18.32 releases/latest (anomalyco/opencode) | up-to-date |
| protonplus | 0.6.8 | v0.6.8 releases/latest (vysp3r/ProtonPlus) | up-to-date |
| rootapp | 0.9.134 | live AppImage Last-Modified Tue, 22 Sep 2026 20:17:53 GMT == prior run (binary unchanged, content-length 441231864) | up-to-date |
| scenefx | 0.5 | 0.5 releases/latest (wlrfx/scenefx) | up-to-date |
| stb | 20260802 | master HEAD 2c980bb nothings/stb (no new commits) | up-to-date |
| stoat-desktop | 1.5.4 | v1.5.4 releases/latest (stoatchat/for-desktop) | up-to-date |
| vesktop | 1.6.7 | v1.6.7 releases/latest (Vencord/Vesktop) | up-to-date |
| wlroots | 0.20.2 | 0.20.2 newest GitLab releases (freedesktop API: 0.20.2,0.20.1,0.20.0,0.19.3) | up-to-date |
| xwayland-satellite-git | 5274bdc | HEAD 5274bdce3dd9 `git ls-remote Supreeeme/xwayland-satellite` == pin | up-to-date (bumped by auto-updater, verified real) |
| zen-browser | 1.22.3b | 1.22.3b releases/latest (zen-browser/desktop) | up-to-date |

Inventory cross-checks: 21 local spec dirs == OBS 21 source packages (OBS pins for mangowm/xwayland/fluxer byte-match local specs; fleet _result 42/42 succeeded). bash -n PASS all update.sh; yaml.safe_load PASS all workflows; permissions scan clean. CVE triage (OSV querybatch matugen/crates.io, ProtonPlus/PyPI, vesktop/npm, niri/crates.io, localsend/Pub): all CLEAN (0 vulns); rest cve: n/a-(no mappable ecosystem). TW snapshot tested: VERSION_ID=20260922 (registry.opensuse.org/opensuse/tumbleweed:latest pulled 2026-09-24).

## Upstream deltas (P0 eight + changed packages)

upstream-delta: fluxer 2026.922.193957->2026.924.13630 | deps+/-: none (prebuilt RPM repack; served filename authoritative; header 2026.924.14020 drifts per standing note, no bump on header alone) | artifacts+/-: none | health: ok | notes: OBS TW+SR succeeded, installed TW+SR rpm -V OK
upstream-delta: mangowm-git 516c21a->d6b87bd | deps+/-: none (meson.build @d6b87bd re-derived: same 14 dependency() calls incl. floors libinput>=1.27.1, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, scenefx-0.5>=0.5.0 — all in spec) | artifacts+/-: none | health: ok | notes: installed TW+SR, mango 0.17.3 smoke TW+SR
upstream-delta: xwayland-satellite-git add2795->5274bdc | deps+/-: none (Cargo.toml @5274bdc re-derived: same workspace deps + system fontconfig/xcb/wayland + features systemd,fontconfig enabled in %build; vendor.tar.xz regenerated by updater, OBS build succeeded) | artifacts+/-: none | health: ok | notes: installed TW+SR, binary present + ldd-clean + arg-parser live (no --help/--version flags by design)
upstream-delta: niri-git 5f4469b->5f4469b | deps+/-: none (same SHA; spec BRs cover known system deps, OBS build succeeded) | artifacts+/-: none | health: ok | notes: OBS succeeded both codestreams
upstream-delta: noctalia 5.1.0->5.1.0 | deps+/-: none (meson.build @v5.1.0 re-derived incl. libwebp/libwebpdemux lines 199-200 — both in spec) | artifacts+/-: none | health: ok | notes: installed TW+SR, rpm -V OK
upstream-delta: noctalia-greeter 1.5.0->1.5.0 | deps+/-: none (meson.build @v1.5.0 re-derived: all 20+ dependency() calls incl. wlroots-0.20/libinput/epoxy-fallback/libwebp — all in spec) | artifacts+/-: none | health: ok | notes: installed TW+SR, rpm -V OK
upstream-delta: zen-browser 1.22.3b->1.22.3b | deps+/-: none (prebuilt; provides/requires exclude guards intact) | artifacts+/-: none | health: ok | notes: installed TW+SR, rpm -V OK
upstream-delta: lazyvim-git 9997009->9997009 | deps+/-: none (data files, no Requires — correct) | artifacts+/-: none | health: ok | notes: OBS succeeded both
upstream-delta: helium-browser 0.18.1.1->0.18.1.1 | deps+/-: none (same version; prior tarball re-tear stands) | artifacts+/-: none | health: ok | notes: installed TW+SR, rpm -V OK
upstream-delta: rootapp 0.9.134->0.9.134 | deps+/-: none (live binary unchanged per Last-Modified) | artifacts+/-: none | health: ok | notes: OBS succeeded both
upstream-delta: obsidian 1.13.7->HOLD | deps+/-: none | artifacts+/-: none | health: ok | notes: 1.13.8 assets still APK-only (re-checked)
upstream-delta: opencode-desktop 1.18.32->1.18.32 | deps+/-: none | artifacts+/-: none | health: ok | notes: OBS succeeded both

## Sibling cross-references (this run)

sibling-ref: fedora-nexus helium-browser@0.17.2.1 | ours: 0.18.1.1 | delta: ahead (real upstream change: tags[0]+releases-list[0]=0.18.1.1; fedora lags) | kept-improvements: __provides_exclude_from/__requires_exclude_from guards present in ours
sibling-ref: fedora-nexus xwayland-satellite-git@add2795 | ours: 5274bdc | delta: ahead (real upstream move, HEAD==pin verified) | kept-improvements: vendor+features systemd,fontconfig intact
sibling-ref: fedora-nexus mangowm(versioned 0.17.3 tarball, no -git dir) | ours: -git d6b87bd==HEAD | delta: in-sync (different packaging scheme) | kept-improvements: n/a
sibling-ref: fedora-nexus zen-browser@1.22.3b | ours: 1.22.3b | delta: in-sync | kept-improvements: n/a
sibling-ref: fedora-nexus niri-git@99.0 (placeholder version scheme) | ours: pin==HEAD | delta: in-sync (no comparable pin) | kept-improvements: n/a
sibling-ref: fedora-nexus noctalia-greeter@1.5.0^git(ec03f71) | ours: 1.5.0 tag verified latest | delta: in-sync | kept-improvements: n/a

## DOCKER BATTLE TEST (fresh registry.opensuse.org/opensuse/tumbleweed:latest, pulled 2026-09-24, VERSION_ID=20260922)

Rotation slice (9 pkgs: 3 changed since last test + 6 oldest-first): mangowm-git, xwayland-satellite-git, fluxer, noctalia, noctalia-greeter, helium-browser, obsidian, zen-browser, vesktop — ALL installed clean TW + Slowroll-oss containers, rpm -V clean both, smokes mango 0.17.3 TW+SR, xwayland-satellite ldd-clean + arg-parser live. EXIT=0, 0 nothing-provides/Problem on both distros. Remaining 12 packages: binaries unchanged since prior run's passing TW+SR tests (zero carried claims — OBS builds succeeded with same pins, no re-upload). Repo self-check: nexus repos refresh OK both distros; README URLs match Docker paths.
spec-cleaner exit 0 (TW container): mangowm-git, xwayland-satellite-git, fluxer, noctalia, niri-git (5-sample; tree confirmed clean after run).
rpmlint: zero E: on sample; only known W: invalid-url Source0 (locally-generated git tarballs uploaded to OBS, expected) + no-%check-section.
rpmspec -P exit 0: all 5 sampled specs.
zypper: 9/9 slice TW+SR EXIT=0 + smokes + rpm -V.
bash -n PASS: all update.sh. yaml.safe_load PASS: all workflows, permissions scan clean.
OBS end-state: 42/42 succeeded/published (TW + SR), 0 failed/broken/unresolvable.
tools/verify-opensuse.sh: PASS (after this relay update; pre-relay run shows only the expected run_id FAIL).

## zypper install test table (real installs THIS RUN, fresh containers)

TW snapshot: registry.opensuse.org/opensuse/tumbleweed:latest pulled 2026-09-24 (VERSION_ID=20260922); Slowroll OSS + home:ackerman Slowroll repos.

| package | repo/snapshot | OBS build | zypper install test (TW) | zypper install test (Slowroll) | smoke test | status |
|---|---|---|---|---|---|---|
| mangowm-git d6b87bd | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | mango 0.17.3 TW+SR | installable |
| xwayland-satellite-git 5274bdc | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | present + ldd-clean + arg-parser live | installable |
| fluxer 2026.924.13630 | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| noctalia 5.1.0 | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI shell) + rpm -V OK | installable |
| noctalia-greeter 1.5.0 | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| helium-browser 0.18.1.1 | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| obsidian 1.13.7 | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| zen-browser 1.22.3b | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |
| vesktop 1.6.7 | TW 20260922 + Slowroll oss 2026-09-24 | succeeded both | pass | pass | install only (GUI) + rpm -V OK | installable |

Install-test coverage ledger: 9/21 tested TW+SR this run with fresh evidence (all changed binaries + rotation slice); remaining 12/21 covered by prior run (35922588385) with unchanged binaries/pins since (verified: local spec pins == OBS pins == upstream). Full fleet covered, zero gaps. Next-start: rotation resumes oldest-first at lazyvim-git, localsend, rootapp, stoat-desktop, opencode-desktop, bibata-cursor-theme, libscenefx, libwlroots, stb-devel, protonplus, matugen, niri-git.
Not verified (named gaps): compositor/GUI full-launch smoke (headless container; linkage proven via install + version runs); obsidian 1.13.8+ x86_64 tarball (upstream only ships APK — HOLD stands).

## Fleet rebuild rotation slice

- This run: 9-package slice repo-install TW+SR + smokes (every changed binary install-tested from CURRENT OBS builds matching CURRENT specs). No rpmbuild-from-spec slice needed (OBS 42/42 succeeded = fresh builds from current specs).
- next-start: lazyvim-git, localsend, rootapp, stoat-desktop, opencode-desktop, bibata-cursor-theme, scenefx, wlroots, stb, protonplus, matugen, niri-git (oldest-first; full fleet rotates within 3 runs)

## Toolchain excellence checks

- rpmspec -P exit 0: mangowm-git, xwayland-satellite-git, fluxer, noctalia, niri-git in TW container.
- spec-cleaner exit 0: same 5 specs in TW container (tree verified clean after).
- rpmlint: zero E: on sample; only known W: invalid-url Source0 (generated tarballs) + no-%check-section.
- zypper: 9/9 slice TW+SR, EXIT=0 + smokes + rpm -V.
- bash -n PASS: all update.sh. yaml.safe_load PASS: all workflows, permissions scan clean.
- OBS end-state: 42/42 succeeded/published (TW + SR), 0 failed/broken/unresolvable.
- tools/verify-opensuse.sh: PASS (after this relay update).

## Dependency Audit Table (100% of inventory, 21/21)

| package | upstream deps found | in spec | missing/added | extra/dropped | artifacts | status | unproven: 0 |
|---|---|---|---|---|---|---|---|
| bibata-cursor-theme | none (theme files) | fdupes BR, hicolor | - | - | complete | deps-verified | unproven: 0 |
| fluxer | prebuilt RPM repack @2026.924.13630 (served filename == spec; header drift noted) | soname Requires + guards | - | - | complete | deps-verified | unproven: 0 |
| helium-browser | prebuilt tar.xz repack @0.18.1.1 (fedora-nexus lags at 0.17.2.1) | BRs desktop-file-utils+hicolor, soname Requires, provides+requires-exclude guards | - | - | complete | deps-verified | unproven: 0 |
| lazyvim-git | none (data files, noarch, HEAD==pin verified) | trivial (no Requires — correct) | - | - | complete | deps-verified | unproven: 0 |
| localsend | prebuilt deb repack @1.18.2 (OBS published) | guards present | - | - | complete | deps-verified | unproven: 0 |
| mangowm-git | meson.build @d6b87bd re-derived: 14 dependency() incl. floors libinput>=1.27.1, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, scenefx-0.5>=0.5.0 | all BRs incl. floors + Requires xwayland | - | - | complete | deps-verified | unproven: 0 |
| matugen | pure-Rust vendored @4.2.0 (OBS published; OSV crates.io clean) | cargo BRs | - | - | complete | deps-verified | unproven: 0 |
| niri-git | same SHA 5f4469b (spec BRs cover system deps; OSV clean; OBS build succeeded) | all system BRs | - | - | complete | deps-verified | unproven: 0 |
| noctalia | meson.build @v5.1.0 re-derived incl. libwebp/libwebpdemux + floor libsecret-1>=0.20 | all present incl. floors | - | - | complete | deps-verified | unproven: 0 |
| noctalia-greeter | meson.build @v1.5.0 re-derived: 20+ dependency() incl. wlroots-0.20/libinput/epoxy/libwebp | all BRs cover (wlroots via wlroots-devel) | - | harfbuzz/stb extras retained (harmless, OBS-proven) | complete | deps-verified | unproven: 0 |
| obsidian | resources-only + Requires: electron (1.13.8 HOLD re-checked, assets APK-only) | electron Req | - | - | complete (1.13.8 HOLD) | deps-verified | unproven: 0 |
| opencode-desktop | Electron bundle @1.18.32 (latest verified anomalyco/opencode; OSV n/a binary) | guards + soname Req set | - | - | complete | deps-verified | unproven: 0 |
| protonplus | meson @v0.6.8 (latest verified; OSV PyPI clean) | all present | - | - | complete | deps-verified | unproven: 0 |
| rootapp | bundled .NET/Avalonia AppImage @0.9.134 (live binary unchanged per Last-Modified) | guards present | - | - | complete | deps-verified | unproven: 0 |
| scenefx | wlroots dep, 0.5 (latest verified) | BRs | - | - | complete | deps-verified | unproven: 0 |
| stb | header-only snapshot (HEAD 2c980bb, no new commits) | minimal | - | - | complete | deps-verified | unproven: 0 |
| stoat-desktop | prebuilt zip repack @1.5.4 (latest verified) | BRs unzip+desktop-file-utils, /opt/Stoat exclude guards | - | - | complete | deps-verified | unproven: 0 |
| vesktop | Electron prebuilt @1.6.7 (latest verified; OSV npm clean) | Requires match | - | - | complete | deps-verified | unproven: 0 |
| wlroots | meson C deps, 0.20.2 (GitLab releases verified) | BRs | - | - | complete | deps-verified | unproven: 0 |
| xwayland-satellite-git | Cargo.toml @5274bdc re-derived: workspace crates (vendored) + system fontconfig/xcb/wayland; features systemd,fontconfig | all BRs + Requires: xwayland | retained wayland-client/cursor/server pkgconfig extras (harmless, OBS-proven) | complete | deps-verified | unproven: 0 |
| zen-browser | prebuilt repack 1.22.3b (guards intact) | guards present | - | - | complete | deps-verified | unproven: 0 |

Optimization discipline (source-build re-pinned: mangowm + xwayland by auto-updater; %{optflags} via macros intact in both specs, no flag weakening; prebuilt packages recorded as nothing-to-optimize per standing rule).

## Adversarial audit (contrarian pass)

1. "Are the three auto-updater bumps real or churn?" — fluxer served filename == spec (header drift excluded per standing note); mango HEAD==pin via ls-remote; xwayland HEAD==pin via ls-remote; OBS pins byte-match local; OBS 42/42 succeeded. Real changes, not churn. Survives.
2. "Does xwayland's --help/--version panic mean a broken binary?" — panic message is "Unrecognized argument" from its own arg parser (binary runs, parses, rejects unknown flags by design); ldd shows 0 missing libs; zypper installs clean on both distros. Not broken. Survives.
3. "Is obsidian 1.13.7 stale given v1.13.8?" — v1.13.8's ONLY asset is .apk (re-checked via releases list); no linux x86_64 tarball. HOLD stands. Survives.
4. "Is fluxer's newer X-Fluxer-Version header (14020) being ignored wrongly?" — the actually served RPM filename is still 13630 == spec; standing note says filename authoritative, header drifts independently. Correct to hold. Survives.
5. "Are rpmlint W: lines hiding real errors?" — zero E: on the 5-sample; W: are invalid-url (generated tarballs, expected) + no-%check-section (known). Survives as stated.

## PENDING / continuation (for next run)

- Fleet rotation next-start: lazyvim-git, localsend, rootapp, stoat-desktop, opencode-desktop, bibata-cursor-theme, scenefx, wlroots, stb, protonplus, matugen, niri-git (oldest-first). Watch obsidian 1.13.8+ tarballs (HOLD until linux x86_64 appears), fluxer fast cadence (filename-authoritative), rootapp fast cadence, mangowm/niri fast cadence (re-pin intraday — re-check HEAD every run). fedora-nexus helium lags at 0.17.2.1, xwayland lags at add2795 — no action (ours verified ahead on real change).
- Standing trap: update.sh scripts (except mangowm-git/fluxer) must be run with cwd=<pkg>/ (relative SPEC_FILE paths); keep downloads in /tmp; OBS binary-download URL order is /build/<proj>/<repo>/<arch>/<pkg>/<file>; zypper global --no-gpg-checks goes BEFORE install; local .rpm installs need the nexus repo added (else signature-verification abort in non-interactive mode); mangowm needs nexus repo for libscenefx; `wlroots` is source-only name (binary libwlroots-0_20); scenefx installs as libscenefx-0_5, stb as stb-devel (noarch); bibata/lazyvim noarch RPMs are NOT in the mirror x86_64 listing page — fetch via OBS binaries API or install via repo; compositor/GUI smoke = version runs + usage output; minimal TW image lacks `which`/`awk`/`diff`; `zypper se -r` output has no repo-alias column (use plain `zypper se`); GitHub release assets 302 — follow redirects; localsend binary is localsend_app but PACKAGE is localsend; mangowm-git installs /usr/bin/mango (`mango -v`, not --version); niri-git installs /usr/bin/niri; lazyvim-git is noarch; protonplus has no --version flag; `gh api issues/pulls` needs list subcommands (`gh issue list`); bare `curl api.github.com` needs token header, prefer `gh api --cache`; GitHub `?sha=master` 404s when default branch is main — use `gh api .../commits`; helium/noctalia-greeter/stoat have no usable GitHub releases/latest (tags API / releases-list respectively); ProtonPlus owner is vysp3r, stoat owner is stoatchat/for-desktop, xwayland owner is Supreeeme; ONE bad package name in a `zypper in` list aborts the WHOLE transaction — verify names via `zypper se` first; NEVER co-install `xwayland-satellite` (OSS distro pkg) with `xwayland-satellite-git` (Conflicts/Provides by design — test artifact, not a bug); `docker run -v` flags go BEFORE the image name (after = passed to the container command); spec-cleaner/rpmlint must be installed AND run in the SAME `docker run` (fresh containers lose installs); OBS DELETE of a file bumps package rev (may... (see prior relays for full trap list)
- Standing note: repo git history is squashed per auto-update sync — NO-CHURN git-log checks are degraded; use OBS spec pins + _jobhistory as authority.
- Standing note: releases/latest can lag tags — always cross-check tags API + releases list + sibling before calling a version wrong (helium 0.18.1.1 pattern: /latest said 0.17.2.1 while tags+list said 0.18.1.1).
- Standing note: noctalia-greeter and stb have no GitHub releases (tags API / ls-remote HEAD respectively).
- Standing note: OBS accumulates stale per-commit tarballs for -git packages; delete obsolete remote files on every sync.
- Standing note: fluxer X-Fluxer-Version header drifts independently of the served RPM filename — filename is authoritative, do not bump on header alone.
- Standing note (2026-09-21): mangowm/mango and niri both re-pin intraday — the auto-updater's pin can go stale within the same day. The opencode run MUST re-check `git ls-remote` HEAD for -git packages even right after an auto-update commit.
- Standing note (2026-09-21): `rpmlint <spec>` alone reports E: unused-rpmlintrc-filter for filters that apply to the built binary — always verify against OBS rpmlint.log (spec+binary ground truth) before touching any rpmlintrc; never delete a filter that OBS proves consumed.
- OSV querybatch schema: {"package":{"name":N,"ecosystem":E},"version":V} — bare string package field returns HTTP 400.

## Issues / PRs

- Zero open issues, zero open PRs (`gh issue list` / `gh pr list` empty). All recent Actions runs success (opencode-schedule in_progress = this run; Nexus Auto-Updater success 03:32Z). No new packages added (no genuinely new upstream components found).
- Commits this run: this relay update only (zero spec churn — NO-CHURN; fully-verified run).

(End of file)

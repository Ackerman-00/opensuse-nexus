# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31917877507

## Run Summary (31917877507)

Autonomous maintenance pass over all 22 packages in home:ackerman.
Started ~00:4xZ 2026-08-16 (opencode-schedule). Prior relay
31915790276 was complete for 21 packages, but lazyvim-git had since
been added to git (commit b1d8d70 "updated" by the repo owner) and
was failing to build on OBS (missing Source0 tarball).

### Work done this run

1. **lazyvim-git OBS build failure FIXED (priority 1)** - Root cause:
   the Source0 tarball `lazyvim-%{shortcommit}.tar.gz` is gitignored
   (`lazyvim-git/.gitignore` contains `*.tar.gz`), and the package was
   added to git with update.sh exiting 0 (spec commit == upstream HEAD)
   so the tarball was never downloaded or uploaded to OBS -> OBS build
   failed with missing Source0. Upstream verified: LazyVim main HEAD =
   spec commit `459a4c3b1059671e766a46c7cc223827dc67e3d0`, and
   `M.version = "16.0.0"` matches the spec base_version. Downloaded the
   tarball (sha256
   2fd229074312202af37c9559be9c915d7111d3e764eea88b6ba1d30818ecb2ee),
   uploaded to OBS as rev 3 (Source0), triggered rebuild ->
   SUCCEEDED in BOTH repos (binary
   lazyvim-git-16.0.0+git20260602133554.459a4c3-3.1.noarch.rpm).
   update.sh hardened: download the source tarball even when the
   commit is unchanged if it is missing locally (exit 0 without
   editing spec/.changes). Commit 6aebb0c (pushed). Sandbox-tested
   both paths: unchanged+artifact-present -> exit 0 no churn;
   unchanged+artifact-missing -> downloads, exit 0, spec/.changes
   untouched (no churn).

2. **mangowm-git updater hardened (priority 2, same root-cause class)**
   - mangowm-git/update.sh has the same gitignored-Source0 flaw
   (mango-<shortcommit>.tar.gz is gitignored). Applied the identical
   fix: download the tarball even when the commit is unchanged if it
   is missing locally, exit 0 without spec/.changes edits. Sandbox-
   tested both paths (no churn). Commit d44d859 -> pushed (rebase).
   niri-git and xwayland-satellite-git were evaluated for the same
   change but REVERTED: the mandate forbids update.sh guards from
   requiring local artifact files (tarballs, vendor.tar.xz,
   cargo_config) to exist in CI checkouts, and both packages already
   have their artifacts present on OBS (niri-6062844.tar.gz,
   vendor.tar.xz, cargo_config; xwayland-satellite-3bc915f.tar.gz) -
   regenerating the full cargo vendor on every unchanged run would add
   minutes of churn to the 6h auto-updater. Their pure commit-SHA
   guards are correct as-is.

3. **Version inventory sweep 22/22 (priority 2)** - every package
   verified against upstream via gh api (releases/latest, tags,
   commits/HEAD) + curl (fluxer X-Fluxer-Version header, rootapp
   AppImage re-tear, wlroots GitLab API, stb fresh-download diff) this
   run:
   - bibata-cursor-theme 2.0.7 = upstream v2.0.7 (up-to-date)
   - fluxer 2026.815.214426 = X-Fluxer-Version header (up-to-date)
   - helium-browser 0.15.5.1 = tag 0.15.5.1 exists, asset 200
     (releases/latest API shows 0.15.4.1 but the newer tag exists;
     spec correct)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (up-to-date, just fixed)
   - localsend 1.18.0 = held (v1.18.1 ships ONLY 4 Android APKs, no
     desktop DEB; guard holds)
   - ly 1.4.1 = codeberg v1.4.1 (up-to-date)
   - mangowm-git f5d1dbb = mangowm/mango main HEAD (up-to-date)
   - matugen 4.1.0 = upstream v4.1.0 (up-to-date)
   - niri-git 6062844 = niri-wm/niri main HEAD (up-to-date)
   - noctalia 5.0.0~beta8 = upstream v5.0.0-beta.8 (up-to-date)
   - noctalia-greeter 1.2.1 = upstream v1.2.1 (up-to-date)
   - obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
   - opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date)
   - protonplus 0.6.4 = upstream v0.6.4 (up-to-date)
   - rootapp 0.9.126 = AppImage re-torn fresh this run
     (X-AppImage-Version=0.9.126; sha256
     0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910
     == spec comment, byte-identical)
   - scenefx 0.5 = wlrfx/scenefx latest tag 0.5 (up-to-date)
   - stb 20260802 = commit 2c980bb date 2026-08-02 (up-to-date;
     fresh-downloaded stb_image.h diff == 0 across the whole tree)
   - stoat-desktop 1.4.2 = upstream Stoat v1.4.2 (up-to-date)
   - vesktop 1.6.5 = upstream v1.6.5 (up-to-date)
   - wlroots 0.20.2 = GitLab releases 0.20.2 (up-to-date)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD
     (up-to-date)
   - zen-browser 1.21.14b = upstream 1.21.14b (up-to-date)
   All versioned Source0 URLs verified (200/302-GitHub-redirect).
   README table == 22 local dirs == 22 OBS packages (diff clean).
   All -git spec gitdates == upstream commit dates exactly.

4. **No-churn verification (mandatory)** - lazyvim-git and
   mangowm-git update.sh no-change paths exit 0 with zero diff (pure
   commit-SHA guard + conditional tarball download, never a spec/
   .changes edit on an unchanged commit). Sandbox-run both scripts
   with artifact present (exit 0, no change) and artifact absent
   (download + exit 0, spec/.changes byte-identical). No repeated
   .changes-only commits for the same version/SHA.

5. **Dependency audit 22/22 (ground truth)** - `_builddepinfo`
   (openSUSE_Tumbleweed/x86_64) ZERO `<error>` blocks. lazyvim-git
   added this run: noarch config package, 0 build/runtime deps,
   builddepinfo + install sweep confirm nothing missing. All other
   packages re-confirmed via builddepinfo + the public-mirror install
   sweep (0 "nothing provides" anywhere). Electron packages use
   soname-based Requires (correct for openSUSE).

6. **Install-test sweep (fresh TW containers)** - ALL 22 packages
   install-tested clean in fresh Tumbleweed containers from the PUBLIC
   download.opensuse.org mirror (exact user flow) this run:
   lazyvim-git (184 files, rpm -V OK, verified 3 ways incl. OSS+
   rpm -ihv), rootapp (0.9.126, `--version` rc=0), opencode-desktop
   (rpm -V OK), vesktop, obsidian (transient mirror 404 first attempt,
   clean retry), zen-browser, localsend, helium-browser, stoat-desktop,
   fluxer, protonplus, ly, mangowm-git, matugen, niri-git, noctalia,
   noctalia-greeter, xwayland-satellite-git, libscenefx-0_5,
   libwlroots-0_20, stb-devel, bibata-cursor-theme. Smoke tests rc=0:
   ly 1.4.1, mango 0.16.0, matugen 4.1.0, niri 26.04 (6062844),
   noctalia v5.0.0, noctalia-greeter usage, xwayland-satellite 0.8.2.
   zen-browser sandbox EPERM is Docker-only (rc=0). Electron apps show
   the documented running-as-root sandbox fatal (Docker-only).

7. **GitHub Actions health (priority 3)** - last auto-updater runs
   all `success`; the only failure/cancel was opencode-schedule
   31917475941 (cancelled = normal concurrency cancel). All specs pass
   `rpmspec -P` (protonplus shows a LOCAL rpmspec quirk - rpm 4.18.2
   lacks the openSUSE `%lang_package` macro - but the spec is correct:
   OBS builds succeeded/published in both repos and the binary
   installs clean); all update.sh pass `bash -n`.

8. **Issues/PRs (priority 4/5)** - 0 open issues, 0 open PRs (checked
   via gh api this run).

## Commits this run
- 6aebb0c: fix(autonomous): lazyvim-git: download gitignored Source0
  tarball so OBS never loses it
- d44d859: fix(autonomous): mangowm-git: ensure Source0 tarball
  downloaded when OBS loses it
- (this relay commit)

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp).
2. Every package touched this run install-tested in a fresh TW
   container -> **PASS**. lazyvim-git installed clean 3 ways (fresh TW
   + public mirror + OSS/rpm -ihv); mangowm-git re-verified
   (mango 0.16.0 exit 0); updater sandbox tests both scripts exit 0.
3. Install-test coverage ledger updated -> **PASS** (table below; all
   22 rows THIS RUN).
4. Dependency audit 100% coverage (22/22) -> **PASS** (builddepinfo 0
   errors; lazyvim-git derived from its NEW artifact this run).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, unsquashfs+X-AppImage-Version, OBS _result/
   _builddepinfo XML, docker/zypper/rpm logs, rpmspec -P, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 31917877507 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 22 packages install-tested THIS RUN in FRESH Tumbleweed
containers from the PUBLIC download.opensuse.org mirror (exact user
flow), plus the two fixed updaters sandbox-verified for no-churn.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | OK | n/a (icons) |
| fluxer | 2026.815.214426-4.1 | pass | OK | ldd /usr/lib64/fluxer/fluxer: 0 missing |
| helium-browser | 0.15.5.1-x.1 | pass | OK | electron root-fatal (Docker-only) |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass (3 ways) | OK (184 files) | n/a (config package) |
| localsend | 1.18.0-15.1 | pass | OK | ldd /opt/localsend_app/localsend_app: 0 missing |
| ly | 1.4.1-8.1 | pass | OK | `ly --version` = "Ly version 1.4.1" |
| mangowm-git | 0.16.0+git20260815092240.f5d1dbb-1.1 | pass | OK | `mango -v` = "mango 0.16.0(release)", exit 0 |
| matugen | 4.1.0-30.1 | pass | OK | matugen 4.1.0, exit 0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | OK | `niri --version` = "niri 26.04 (6062844)", exit 0 |
| noctalia | 5.0.0~beta8-4.1 | pass | OK | noctalia v5.0.0, exit 0 |
| noctalia-greeter | 1.2.1-2.1 | pass | OK | --help usage, exit 0 |
| obsidian | 1.13.7-6.1 | pass | OK | electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | OK | electron root-fatal (Docker-only); ldd 0 missing |
| protonplus | 0.6.4-6.1 | pass | OK | binary launches (usage); ldd 0 missing |
| rootapp | 0.9.126-2.1 | pass | OK | `rootapp --version` exit 0 |
| libscenefx-0_5 | 0.5-5.1 | pass | OK | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | OK | n/a (headers) |
| stoat-desktop | 1.4.2-10.1 | pass | OK | Electron GUI no-smoke; ldd 0 missing (3 binaries) |
| vesktop | 1.6.5-35.1 | pass | OK | electron root-fatal (Docker-only); ldd 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass | OK | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | OK | /usr/bin/xwayland-satellite -version = 0.8.2, exit 0 |
| zen-browser | 1.21.14b-6.1 | pass | OK | `zen-browser --version` = "Mozilla Zen 1.21.14b", exit 0 |

## Dependency audit (all 22)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) covers
all 22 packages with ZERO `<error>` entries. lazyvim-git is the new
package this run (noarch config, 0 build deps, 0 runtime deps);
everything else re-confirmed via builddepinfo + public-mirror install.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.815.214426) | X-Fluxer-Version header; public-mirror ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| helium-browser (0.15.5.1) | Chromium bundled; AutoReqProv | shfmt, bubblewrap, electron0 | none | none | deps-verified |
| lazyvim-git (459a4c3) | config package, 0 deps | none (noarch lua files) | none | none | deps-verified (NEW) |
| localsend (1.18.0) | DEB control + ldd | soname Requires + xdg-user-dirs | none | none | deps-verified |
| ly (1.4.1) | zig deps vendored (cache) | VENDORED via ly-zig-cache.tar.gz | none | none | deps-verified |
| mangowm-git (f5d1dbb) | meson.build @f5d1dbb | all 13 mapped + wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 + builddepinfo | pkgconfig set (21) + cargo-packaging/clang/gcc-c++ | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | Rust cargo deps (clean build) | cargo-packaging, gcc-c++, pkgconfig(gtk4) etc. | none | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn (sha 0736926f == spec) | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build | all in spec | none | none | deps-verified |
| stb (20260802) | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.4.2) | Electron bundled; RPATH/setuid flagged | soname Requires; rpmlintrc filters | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build | all in spec | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 22. Independent public-mirror installs of
every package + clean builddepinfo is stronger evidence than static
analysis alone.

## OBS build status

All 44 builds (22 Tumbleweed + 22 Slowroll) succeeded/published.
lazyvim-git rebuilt from rev 3 (uploaded the missing Source0 tarball);
protonplus re-verified green in both repos (local rpmspec quirk is a
tooling artifact, not a spec issue). No failed/broken/unresolvable.

## Not Verified / Gaps
- Tumbleweed snapshot current (checked /etc/os-release inside the
  test container); Slowroll lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only). stoat-desktop,
  protonplus, helium-browser, obsidian, opencode-desktop, vesktop,
  zen-browser are GUI apps - install-level proof only.
- fluxer internal version string: the RPM carries version
  2026.815.214426 per the X-Fluxer-Version header upstream marker;
  app.asar package.json says a dev version string, not the app
  release version. The X-Fluxer-Version header is the canonical
  version per the project's own update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only; DEB URL
  404). Guard protects the repo from a broken spec; will bump when
  upstream publishes the desktop DEB.
- mangowm/niri/xwayland upstreams extremely active; pinned commits
  may advance before the next auto-updater - the guarded update.sh
  scripts will catch them.
- helium-browser releases/latest API reports 0.15.4.1 while tag
  0.15.5.1 exists with a downloadable asset (spec version 0.15.5.1 is
  correct per the tag list; release may not be marked "latest").
- lazyvim-git ships Lua config as plain files (no plugins vendored);
  installs clean and 184 files verified, but end-to-end LazyVim
  startup requires its runtime-download on first launch (network
  dependent, not testable offline in a container).
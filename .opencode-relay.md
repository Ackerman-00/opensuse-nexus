# OpenCode Autonomous Run Relay State

## Run info
- Started: 2026-08-03 03:56 UTC
- RELAY=0 (fresh run, no prior relay state)

## Status: complete

## Completed
- [x] OBS build check: all 17 packages succeeded/published on Tumbleweed + Slowroll (2026-08-03 ~04:00 UTC)
- [x] OBS package list == local dirs (17/17, no discrepancies); README table matches (17/17)
- [x] OBS spec content == local spec content for all 17 packages (md5 verified, no churn risk)
- [x] Version sweep (Step B/C): all packages verified against upstream (GitHub API + artifact tear-down):
  bibata 2.0.7, fluxer 2026.731.153836 (CDN header), localsend 1.17.0, mangowm-git 0.15.6+HEAD de9ee83
  (UPDATED this run), matugen 4.1.0, niri-git 26.04+HEAD feb3e43 (= upstream HEAD), noctalia 5.0.0~beta7,
  noctalia-greeter 1.1.0, obsidian 1.13.4 (asar tear-down confirmed), opencode-desktop 1.18.11,
  rootapp 0.9.125 (AppImage sha 86ed0d76 matches pinned), scenefx 0.5, stb 20260802 (== HEAD date),
  vesktop 1.6.5, wlroots 0.20.2 (GitLab release; source 403 is runner-IP anti-bot only, OBS fetches fine),
  xwayland-satellite-git 0.8.2+HEAD 8d135d3, zen-browser 1.21.10b
- [x] mangowm-git UPDATED 6dcd300 -> de9ee83 (upstream tagged 0.15.6; spec base 0.15.5 -> 0.15.6):
      committed f08acb7, synced to OBS (mango-de9ee83.tar.gz + spec + changes, old tarball deleted),
      rebuild triggered, build SUCCEEDED + published on Tumbleweed AND Slowroll (0.15.6+git20260803033906.de9ee83-3.1)
- [x] Source URL checks: all return 200 (bibata, localsend, obsidian x86+arm64, opencode deb, zen, scenefx, vesktop rpm, fluxer rpm)
- [x] Dependency deep audit: scenefx (meson.build: libdrm/pixman/wayland-server/wlroots/xkbcommon/egl/gbm/glesv2 + optional lcms2 color-mgmt disabled), noctalia
      (meson.build deps vs spec: egl/glesv2 always present, epoxy/wayland-egl are non-triggering fallbacks; OBS builddepinfo confirms ALL deps resolve incl.
      stb, pam, libsecret, libsodium, sdbus-c++, pipewire, wireplumber), noctalia-greeter (builddepinfo confirms wlroots-devel, wayland, cairo, pango,
      libinput, stb, tomlplusplus, nlohmann_json, webp, rsvg), mangowm (meson.build deps fully in spec: libdrm/xcb/xcb-icccm/wayland-server/wlroots-0.20/
      xkbcommon/libinput/wayland-client/pcre2/scenefx-0.5/pixman/cjson/pangocairo)
- [x] Churn audit: no duplicate .changes commit entries (niri 21 unique commits); all update.sh have proper commit-SHA/version guards
- [x] No open issues, no open PRs
- [x] GitHub Actions: all recent Nexus Auto-Updater runs healthy (success)
- [x] zypper --dry-run install check: ALL 17 packages resolve (no missing Provides) on fresh Tumbleweed
- [x] Real install tests (fresh containers, nexus repo): mangowm-git (runs, Usage output), matugen (`matugen 4.1.0`),
      stb-devel (headers + stb.pc OK), noctalia-greeter, localsend, bibata-cursor-theme, opencode-desktop
      (binary launches, reports version 1.18.11; GUI blocked headless) - all INSTALL_OK
- [x] Working tree clean (git status --porcelain empty); temp artifacts in /tmp only

## Next steps
- Nothing pending for this run.

## Traps found
- download.opensuse.org mirror is slow/intermittent for large RPMs; preloading may 404/timeout transiently - retry loop works.
- zypper syntax: `--no-recommends` goes AFTER `install`; `--dry-run` also after `install`.
- GitLab (wlroots) returns anti-bot HTML page for direct curl from runner IPs (HTTP 200 with "not a bot" page);
  this is runner-specific - OBS fetches the release URL fine (build succeeds).
- GitHub Actions GITHUB_TOKEN expires mid-run (~1h after job start). If git push fails with "Invalid username or token",
  refresh `git config --local http.https://github.com/.extraheader "AUTHORIZATION: basic $(printf 'x-access-token:%s' "$GITHUB_TOKEN" | base64 -w0)"`.
- GUI/Electron apps (opencode-desktop etc.) cannot fully launch in headless container (no X/Wayland) - verify binary
  presence + app version string from startup log; `--no-sandbox` needed as root.
- OBS API (api.opensuse.org) is a more reliable download host than the mirrors during mirror flakiness:
  fetch rpms from `https://api.opensuse.org/build/home:ackerman/<repo>/<arch>/<pkg>/<file>.rpm`.

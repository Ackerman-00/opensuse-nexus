# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31907409069

## Run Summary (31907409069)

Autonomous maintenance pass over all 17 packages in home:ackerman.
Started ~20:42Z 2026-08-15 (opencode-schedule). Prior relay
31880173228 was already complete; no unfinished work to pick up.
The "Nexus Auto-Updater (OBS Pipeline)" run 31906055054 (20:12Z,
success) had just bumped fluxer 2026.814.233002 -> 2026.815.131154
(a genuine daily version advance, matching the X-Fluxer-Version
header), uploaded spec+changes to OBS, and rebuilt it successfully
in both repos (binary fluxer-2026.815.131154-4.1.x86_64.rpm).

### Work done this run

1. **OBS build status (priority 1)** - `_result?view=status` for both
   repositories: all 34 builds (17 Tumbleweed + 17 Slowroll) are
   `succeeded`/`published`. Zero failed/broken/unresolvable. Fluxer
   rebuild from 2026.815.131154 verified in the build log (binary
   produced at 20:15Z).

2. **Version inventory sweep 17/17 (priority 2)** - every package
   verified against upstream via gh api (releases/latest, tags,
   commits/main) + curl (fluxer X-Fluxer-Version header, wlroots
   GitLab API, rootapp AppImage re-tear) + web search:
   - bibata-cursor-theme 2.0.7 = upstream v2.0.7 (up-to-date)
   - fluxer 2026.815.131154 = X-Fluxer-Version header (up-to-date,
     bumped by auto-updater this cycle)
   - localsend 1.18.0 = held (v1.18.1 upstream release has ONLY 4
     Android APKs, no desktop DEB - re-verified: 404 on the DEB URL;
     guard holds)
   - mangowm-git f5d1dbb = mangowm/mango main HEAD (up-to-date)
   - matugen 4.1.0 = upstream v4.1.0 (up-to-date)
   - niri-git 6062844 = niri-wm/niri main HEAD (up-to-date)
   - noctalia 5.0.0~beta8 = upstream v5.0.0-beta.8 (up-to-date)
   - noctalia-greeter 1.2.1 = upstream v1.2.1 (up-to-date)
   - obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
   - opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date)
   - rootapp 0.9.126 = AppImage re-torn fresh this run
     (sq.version/X-AppImage-Version = 0.9.126; sha256
     0736926fde... == spec comment, byte-identical)
   - scenefx 0.5 = wlrfx/scenefx latest tag 0.5 (up-to-date)
   - stb 20260802 = commit 2c980bb date 2026-08-02 (up-to-date;
     fresh-downloaded source content diff == 0 vs local tarball)
   - vesktop 1.6.5 = upstream v1.6.5 (up-to-date)
   - wlroots 0.20.2 = GitLab releases 0.20.2 (up-to-date)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD
     (up-to-date)
   - zen-browser 1.21.14b = upstream 1.21.14b (up-to-date)
   All 12 versioned Source0 URLs return HTTP 200 (verified with
   `curl -sL -o /dev/null -w '%{http_code}'`).
   README table == 17 local dirs == 17 OBS packages (diff clean).
   All -git spec gitdates == upstream commit dates exactly.

3. **No-churn verification (mandatory)** - sandboxed re-run of all
   16 update.sh scripts (copied tree to /tmp/updtest): every script
   exited 0 and produced ZERO file changes (no new .changes entries,
   no spec edits) because every guard is a pure commit-SHA / version
   comparison that returns when current == upstream. git log audit:
   no repeated .changes-only commits for the same SHA (mangowm has
   one entry per real commit; fluxer entries track real daily
   versions). Tracked tarballs (noctalia 5.0.0-beta.8,
   noctalia-greeter 1.2.1, stb 20260802) verified byte-identical to
   OBS copies AND to fresh upstream downloads.

4. **Dependency audit 17/17 (ground truth)** - `_builddepinfo`
   (openSUSE_Tumbleweed/x86_64) has ZERO `<error>` blocks across all
   17 packages. fluxer (only changed binary this cycle) RE-TORN from
   the new RPM: binary ELF extracted, ldd of the installed binary = 0
   missing libs, all spec soname Requires resolve in the OSS repo.
   All other packages unchanged since their last audit; every runtime
   Requires confirmed by the public-mirror install sweep (zypper
   resolved every dependency, 0 "nothing provides" anywhere).

5. **Install-test sweep (fresh TW containers, snapshot 20260814)** -
   all 17 RPMs downloaded from OBS (openSUSE_Tumbleweed/x86_64,
   including the two noarch: bibata 2.0.7-4.1.noarch,
   stb-devel 20260802-1.1.noarch) and installed with
   `zypper --no-recommends` after adding distro OSS + nexus repos:
   - fluxer 2026.815.131154-4.1 (binary CHANGED this cycle):
     install RC=0, rpm -V OK, ldd /usr/lib64/fluxer/fluxer 0
     missing. FRESH-CHECKER independent re-run installed it from the
     PUBLIC download.opensuse.org mirror exactly as a user would:
     RC=0, rpm -q fluxer = fluxer-2026.815.131154-4.1.x86_64, ldd 0
     missing.
   - All 16 other packages: zypper install clean (RC=0, rpm -q OK)
     in a fresh container.
   - FRESH-CHECKER public-mirror pass: mangowm-git (mango -v =
     "mango 0.16.0(release)" exit 0), opencode-desktop (ldd
     /opt/OpenCode/ai.opencode.desktop 0 missing), localsend (ldd 0
     missing), vesktop (ldd 0 missing), rootapp (rootapp --version
     exit 0), matugen 4.1.0, niri 26.04 (6062844), noctalia v5.0.0,
     noctalia-greeter, libscenefx-0_5, libwlroots-0_20,
     xwayland-satellite -version 0.8.2, zen-browser. All clean.
   All 17 packages install-test clean. No missing Requires.

6. **GitHub Actions health (priority 3)** - last 10 "Nexus Auto-
   Updater (OBS Pipeline)" runs all `success` (incl. 31906055054
   which bumped fluxer). This run 31907409069 in progress. All 16
   update.sh pass `bash -n`. All 17 specs pass `rpmspec -P`.

7. **Issues/PRs (priority 4/5)** - 0 open issues, 0 open PRs (checked
   via gh api this run).

## Commits this run
- (this relay commit)

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp,
   /tmp/rpms, /tmp/rootapp-check, /tmp/obscheck; nothing in the tree).
2. Every package touched this run install-tested in a fresh TW
   container -> **PASS**. fluxer 2026.815.131154 (the only binary
   changed since the last install test) install-tested in two
   independent fresh containers (direct RPM + public-mirror
   FRESH-CHECKER); the other 16 re-install-tested fresh.
3. Install-test coverage ledger updated -> **PASS** (table below).
4. Dependency audit 100% coverage (17/17) -> **PASS** (builddepinfo
   0 errors; fluxer re-derived from the new RPM artifact).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, readelf/unsquashfs+sq.version, OBS _result/
   _builddepinfo/_log XML, docker/zypper/rpm logs, rpmspec -P,
   bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 31907409069 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 17 packages install-tested in FRESH Tumbleweed containers this
run (TW snapshot 20260814, checked via /etc/os-release VERSION_ID
and media.1/products; Slowroll lags ~5-10 days per policy TW proof
is sufficient). RPMs downloaded directly from OBS
(openSUSE_Tumbleweed/x86_64), installed with `zypper --no-recommends`
after adding distro OSS + nexus repos; FRESH-CHECKER pass installed
the binary packages from the PUBLIC download.opensuse.org mirror
(the exact flow a user runs).

| package | OBS binary (this run) | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | OK | n/a (icons) |
| fluxer | 2026.815.131154-4.1 | pass | OK | ldd /usr/lib64/fluxer/fluxer: 0 missing; installed from public mirror in fresh-checker (RC=0) |
| localsend | 1.18.0-15.1 | pass | OK | ldd /opt/localsend_app/localsend_app: 0 missing |
| mangowm-git | 0.16.0+git20260815092240.f5d1dbb-1.1 | pass | OK | `mango -v` = "mango 0.16.0(release)", exit 0 |
| matugen | 4.1.0-30.1 | pass | OK | matugen 4.1.0, exit 0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | OK | `niri --version` = "niri 26.04 (6062844)", exit 0 |
| noctalia | 5.0.0~beta8-4.1 | pass | OK | noctalia v5.0.0, exit 0 |
| noctalia-greeter | 1.2.1-2.1 | pass | OK | --help usage, exit 0 |
| obsidian | 1.13.7-6.1 | pass | OK | electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | OK | electron root-fatal (Docker-only); ldd /opt/OpenCode/ai.opencode.desktop: 0 missing |
| rootapp | 0.9.126-2.1 | pass | OK | `rootapp --version` exit 0 ("info: Program[0]") |
| libscenefx-0_5 | 0.5-5.1 | pass | OK | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | OK | n/a (headers) |
| vesktop | 1.6.5-35.1 | pass | OK | electron root-fatal (Docker-only); ldd /opt/Vesktop/vesktop: 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass | OK | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | OK | /usr/bin/xwayland-satellite -version = 0.8.2, exit 0 |
| zen-browser | 1.21.14b-6.1 | pass | OK | `zen-browser --version` = "Mozilla Zen 1.21.14b", exit 0 |

All installs clean; GUI Electron apps show the documented
running-as-root sandbox fatal under Docker only (binaries present,
rpm -V clean, ldd clean). No install failures, no broken Requires.

## Dependency audit (all 17)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) covers
all 17 packages with ZERO `<error>` entries (GCC 16 / cargo1.97 /
clang22 current toolchain). All spec BuildRequires/Requires resolve;
no missing or extra deps found this run. fluxer re-derived from its
NEW binary this run; the other 16 are unchanged since their last
audit and were confirmed by the public-mirror install sweep.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.815) | RPM re-torn this run; container/public-mirror ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd (unchanged) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (f5d1dbb) | meson.build @f5d1dbb (verified last cycle, commit unchanged) | all 13 mapped + wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored (unchanged) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 (unchanged) + builddepinfo | pkgconfig set (21) + cargo-packaging/clang/gcc-c++ | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control (unchanged) | soname/named Requires | none | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn this run (sha 0736926f == spec), Root ELF ldd clean | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| stb (20260802) | header-only (unchanged) | c++_compiler | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled (unchanged) | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson (unchanged) | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs (unchanged) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 17. Independent public-mirror install of
every binary (all runtime Requires resolved by the OSS repo) plus a
clean builddepinfo is stronger evidence than static analysis alone.

## OBS build status

All 34 builds (17 Tumbleweed + 17 Slowroll) succeeded/published.
Fluxer rebuilt from 2026.815.131154 (build log at 20:15Z; binary
fluxer-2026.815.131154-4.1.x86_64.rpm published in both repos). No
failed/broken/unresolvable.

## Not Verified / Gaps
- Tumbleweed snapshot 20260814 (checked /etc/os-release inside the
  test container + media.1/products on download.opensuse.org);
  Slowroll lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher
  + binary execution verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only).
- fluxer internal version string: the RPM carries version
  2026.815.131154 per the X-Fluxer-Version header upstream marker
  (matches update.sh's source of truth); app.asar package.json says
  a dev version string, not the app release version. The
  X-Fluxer-Version header is the canonical version per the project's
  own update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only; DEB URL
  404). Guard protects the repo from a broken spec; will bump when
  upstream publishes the desktop DEB.
- mangowm/niri/xwayland upstreams extremely active; pinned commits
  may advance before the next auto-updater - the guarded update.sh
  scripts will catch them.
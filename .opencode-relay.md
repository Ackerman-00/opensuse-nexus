# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31485227878

## Verification Summary (Run 31485227878, scheduled fresh-checker pass)

Independent maintenance pass over all 17 packages in home:ackerman.
Started 11:07Z; the 10:42Z auto-updater (run 31483363061, success) had
already bumped mangowm-git to 4a3bff4 and niri-git to 9474d05 and synced
both to OBS. This run verified that work, then caught and fixed a new
niri upstream commit.

### Inventory (17/17 match repo dirs = README = OBS)
bibata-cursor-theme, fluxer, localsend, mangowm-git, matugen, niri-git,
noctalia, noctalia-greeter, obsidian, opencode-desktop, rootapp, scenefx, stb,
vesktop, wlroots, xwayland-satellite-git, zen-browser. No missing/extra
packages on either side; README table matches.

### OBS build status
All 34 builds (17 pkgs x 2 repos = openSUSE_Tumbleweed + openSUSE_Slowroll)
report `succeeded`/`published`. Failure filter
`&code=failed&code=broken&code=unresolvable` => EMPTY (checked twice during
the run, before and after the niri-git rebuild).

### Version sweep (independent, this run) - 17/17 current
- bibata-cursor-theme 2.0.7 = v2.0.7 (gh api releases/latest; source URL 200)
- fluxer 2026.731.153836 = X-Fluxer-Version header (curl -sIL, 200, 92MB)
- localsend 1.18.0 = v1.18.0 (gh api releases/latest; DEB URL 200)
- mangowm-git 4a3bff4 = upstream main HEAD (git ls-remote mangowm/mango, twice);
  gitdate 20260811081334 == upstream committer date 2026-08-11T08:13:34Z
- matugen 4.1.0 = v4.1.0 (gh api releases/latest)
- niri-git 0add05d = upstream main HEAD (git ls-remote niri-wm/niri + YaLTeR/niri);
  gitdate 20260811113951 == committer date 2026-08-11T11:39:51Z. NOTE: the 10:42Z
  auto-updater pinned 9474d05; upstream pushed 0add05d at 11:39Z (post-autoupdater).
  This run bumped the spec to 0add05d, regenerated vendor.tar.xz + cargo_config,
  committed 1ed699b, synced to OBS (revs 68-72, deleted obsolete
  niri-9474d05.tar.gz), triggered rebuild: TW + Slowroll both built
  26.04+git20260811113951.0add05d-6.1 and are `published`.
- noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api tags); tarball URL 200
- noctalia-greeter 1.2.1 = tag v1.2.1 (gh api tags); tarball URL 200
- obsidian 1.13.6 = v1.13.6 (gh api releases/latest); AppImage URL 200
- opencode-desktop 1.18.16 = v1.18.16 (gh api releases/latest); DEB URL 200
- rootapp 0.9.126: AppImage downloaded (386MB, sha256 0736926f...d910 == spec
  comment, byte-identical) and extracted via squashfs offset 944632:
  Root.desktop X-AppImage-Version=0.9.126 (independent version confirmation)
- scenefx 0.5 = tag 0.5 (gh api releases/latest); tarball URL 200
- stb 20260802 = master HEAD 2c980bb dated 2026-08-02T06:42:09Z (gh api)
- vesktop 1.6.5 = v1.6.5 (gh api releases/latest); RPM URL 200
- wlroots 0.20.2 = latest GitLab release (releases API: 0.20.2, 0.20.1, 0.20.0).
  Direct tarball download from this runner is intermittently blocked by GitLab
  anti-bot (403 challenge page, also observed for the uploads domain); OBS
  builds it fine and the published binary is current. Not a repo bug.
- xwayland-satellite-git 3bc915f = main HEAD (git ls-remote Supreeeme/
  xwayland-satellite); gitdate == committer date 2026-08-08T20:30:58Z.
  NOTE: YaLTeR/xwayland-satellite is abandoned; spec URL points to the current
  upstream Supreeeme repo and is correct.
- zen-browser 1.21.13b = releases/latest; tarball URL 200

### No-churn
All 16 update.sh run in isolation with GITHUB_TOKEN: all EXIT=0
("already up to date"), working tree unchanged. All -git updaters use pure
commit-SHA guards. mangowm/niri .changes have exactly one entry per distinct
commit (grep + uniq audit clean). OBS build history shows exactly ONE build
entry per commit for mangowm (4a3bff4) and niri (0add05d) - no duplicate
rebuilds. No churn introduced.

### Spec validity / updater health
- `rpmspec -P` all 17 specs OK (incl. niri-git post-bump)
- `bash -n` all 16 update.sh OK
- all 12 _service files parse as valid XML
- GitHub Actions: auto-updater run 31483363061 (10:42Z) success; all 5 most
  recent Nexus Auto-Updater + opencode-schedule runs green. No failed runs.

### Issues/PRs
`gh api issues?state=open` => 0 open; pulls => 0 open. Nothing to triage.

### Tumbleweed snapshot
Clean container /etc/os-release = 20260809 (current). Web confirms rolling
cadence: GCC 16 landed as TW default in snapshot 0726, python311 module
packages dropped in 0703, Review of the week 2026/31 = snapshots 0723-0729.
Slowroll lags ~5-10 days (not container-tested; identical binaries, TW proof
per policy).

### Install tests (fresh TW 20260809 containers)
Public nexus repo (download.opensuse.org home:ackerman TW) plus local
OBS-downloaded RPMs. rpm -q confirms ALL 17 packages present at correct
versions (17/17). Smoke (rc=0):
- mango -v => "mango 0.15.6(release)" (mangowm-git 4a3bff4)
- niri --version => "niri 26.04 (0add05d)" (niri-git 0add05d, local RPM AND public repo)
- matugen --version => "matugen 4.1.0"
- noctalia --version => "noctalia v5.0.0"
- xwayland-satellite -version => "0.8.2"
- zen-browser --version => "Mozilla Zen 1.21.13b" (sandbox userns EPERM is Docker-only)
Electron apps (fluxer, obsidian, vesktop, opencode-desktop, localsend) and
GUI/libs (bibata, scenefx, stb, wlroots, noctalia-greeter, rootapp) installed
cleanly; graphical smoke skipped (headless Docker) per policy.
NOTE: download.opensuse.org OSS mirror intermittently 404s base dependency
RPMs during zypper preloading (libgtk, libharfbuzz, libXfixes, ...), causing
zypper exit 4/106 in some runs - NEVER on nexus repo files, NEVER preventing
the transaction from completing. Same flake family as previous runs'
documented notes. Install tests PASSED regardless.

### Install-test coverage ledger (current binaries as of this run)
| package | last install-tested | result | smoke |
|---|---|---|---|
| bibata-cursor-theme | 2026-08-11 run 31485227878 (binary unchanged) | pass | n/a (icons) |
| fluxer | 2026-08-11 run 31485227878 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| localsend (1.18.0) | 2026-08-11 run 31485227878 (binary unchanged) | pass | launches; display needed |
| mangowm-git (4a3bff4) | 2026-08-11 run 31485227878 (binary CHANGED) | pass | mango 0.15.6(release) |
| matugen | 2026-08-11 run 31485227878 (binary unchanged) | pass | 4.1.0 |
| niri-git (0add05d) | 2026-08-11 run 31485227878 (binary CHANGED x2) | pass | niri 26.04 (0add05d) |
| noctalia (5.0.0~beta8) | 2026-08-11 run 31485227878 (binary unchanged) | pass | v5.0.0 |
| noctalia-greeter | 2026-08-11 run 31485227878 (binary unchanged) | pass | --help usage |
| obsidian (1.13.6) | 2026-08-11 run 31485227878 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| opencode-desktop (1.18.16) | 2026-08-11 run 31485227878 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| rootapp (0.9.126) | 2026-08-11 run 31485227878 (binary unchanged) | pass | --version info:Program[0] |
| scenefx (libscenefx-0_5) | 2026-08-11 run 31485227878 (binary unchanged) | pass | n/a (lib, no smoke) |
| stb (stb-devel) | 2026-08-11 run 31485227878 (binary unchanged) | pass | n/a (header lib) |
| vesktop | 2026-08-11 run 31485227878 (binary unchanged) | pass | electron root-fatal (Docker-only) |
| wlroots (libwlroots-0_20) | 2026-08-11 run 31485227878 (binary unchanged) | pass | n/a (lib, no smoke) |
| xwayland-satellite-git (3bc915f) | 2026-08-11 run 31485227878 (binary unchanged) | pass | -version => 0.8.2 |
| zen-browser (1.21.13b) | 2026-08-11 run 31485227878 (binary unchanged) | pass | Mozilla Zen 1.21.13b |

### Dependency audit (all 17, high confidence)
OBS `_builddepinfo` (TW/x86_64) for all 17 packages: 0 `<error>` entries.
mangowm-git (4a3bff4) and niri-git (0add05d) changed since the last audit;
deps re-derived from the NEW tarballs:
- mangowm: meson.build `dependency(` => libcjson, libdrm, libinput, libpcre2-8,
  pangocairo, pixman-1, scenefx-0.5, wayland-client, wayland-server,
  wlroots-0.20, xcb, xcb-icccm, xkbcommon, xwayland - ALL mapped in spec.
- niri: upstream niri.spec.rpkg BuildRequires (cargo-rpm-macros, pkgconfig(udev),
  gbm, xkbcommon, wayland-devel, libinput, dbus-1, systemd, libseat,
  libdisplay-info, pipewire-devel, pango-devel, cairo-gobject-devel, clang,
  mesa-libEGL) + Requires (mesa-dri-drivers, mesa-libEGL, libwayland-server,
  xwayland-satellite >= 0.7) - all covered by openSUSE-mapped BuildRequires/
  Requires in the spec. vendor.tar.xz regenerated from the NEW Cargo.lock by
  update.sh (cargo vendor); OBS offline build with --locked succeeded, so the
  vendored crate set matches Cargo.lock.
All other packages upstream-unchanged since the last audit; binary artifacts
were torn in previous runs and this run re-verified the upstream version tags.

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (icons only) | none | none | deps-verified |
| fluxer | Electron bundled; ldd 0 not-found | soname Requires | none | deps-verified |
| localsend (1.18.0) | DEB control Depends; ldd 0 not-found | soname Requires + xdg-user-dirs | none | deps-verified |
| mangowm-git (4a3bff4) | meson: libcjson,libdrm,libinput,libpcre2-8,pangocairo,pixman-1,scenefx-0.5,wayland-client,wayland-server,wlroots-0.20,xcb,xcb-icccm,xkbcommon,xwayland | all mapped | none | deps-verified |
| matugen | Cargo vendored; no system deps beyond cargo | cargo-packaging,gcc-c++ | none | deps-verified |
| niri-git (0add05d) | niri.spec.rpkg BR set + Requires mesa-dri-drivers,mesa-libEGL,libwayland-server,xwayland-satellite>=0.7 | all mapped | none | deps-verified |
| noctalia (5.0.0~beta8) | meson.build full dep list | all in spec incl pkgconfig(pam)+pkgconfig(stb) | none | deps-verified |
| noctalia-greeter | meson: wayland,xkb,pango/cairo,glib,libinput,wlroots,epoxy,tomlplusplus,json,stb,webp,rsvg | all in spec | none | deps-verified |
| obsidian (1.13.6) | Electron bundled; AutoReqProv:no; Requires electron | electron,bash | none | deps-verified |
| opencode-desktop (1.18.16) | DEB control Depends: libgtk-3-0, libnotify4, libnss3, libxss1, libxtst6, xdg-utils, libatspi2.0-0, libuuid1, libsecret-1-0; ldd 0 not-found | all mapped (incl libsecret-1-0, ripgrep) | none | deps-verified |
| rootapp (0.9.126) | AppImage torn (offset 944632): Root.desktop X-AppImage-Version=0.9.126 | all soname/named | none | deps-verified |
| scenefx (0.5) | meson: egl,gbm,glesv2,libdrm,pixman-1,xkbcommon,wlroots-0.20,wayland-server | all in spec | none | deps-verified |
| stb | header-only; c++ test in %check | c++_compiler | none | deps-verified |
| vesktop (1.6.5) | Electron bundled; app.asar package.json 1.6.5 | soname Requires | none | deps-verified |
| wlroots (0.20.2) | meson: glslang,egl,gbm,glesv2,hwdata,libdrm,libpng,libseat,libinput,vulkan,wayland,xkbcommon,x11/xcb/xwayland,ffmpeg,libdisplay-info,libliftoff,lcms2 | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson: fontconfig,wayland,xcb,xcb-cursor,clang/llvm,cargo | all in spec | none | deps-verified |
| zen-browser (1.21.13b) | Firefox tarball; application.ini 1.21.13b; ldd 0 not-found | mozilla-nss/nspr,hunspell,hyphen | none | deps-verified |

Confidence: high for all 17 (_builddepinfo + artifact checks + clean container
installs agree).

### Commits this run
- 1ed699b fix(autonomous): niri-git: bump to upstream main 0add05d (new commit
  11:39Z, post-autoupdater) - full run record in commit body.

### Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts kept in /tmp only).
2. Every package touched this run install-tested in a clean TW container incl.
   fresh-checker re-runs -> **PASS** (niri-git 0add05d tested from local RPM
   AND public repo; mangowm-git re-tested; full 17-package public-repo sweep
   confirms rpm -q 17/17).
3. Install-test coverage ledger updated -> **PASS** (this file; rows reference
   current binaries).
4. Dependency audit 100% coverage (17/17) -> **PASS** (table above; mangowm +
   niri re-derived from NEW artifacts; all others upstream-unchanged and
   cross-checked against _builddepinfo).
5. Claims backed by command output -> **PASS** (gh api, git ls-remote,
   curl -sIL status/size, sha256sum, readelf/unsquashfs, rpm -qip, rpmspec -P,
   bash -n, python XML parse, OBS _result/_history/_builddepinfo XML,
   docker/zypper/rpm -V logs).
6. Named gaps -> see below.
7. Relay updated with run_id 31485227878 + status complete -> **PASS** (this
   commit).

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain, TW x86_64 proof per
  policy).
- Slowroll container installs (identical binaries ~5-10 days behind TW; TW proof
  per policy).
- GUI apps' full graphical smoke (no display in Docker; launcher + binary
  execution verified instead).
- wlroots 0.20.2 direct tarball download from this runner blocked by GitLab
  anti-bot (403 challenge); version verified via the GitLab releases API and
  OBS builds/publishes it fine.
- niri upstream is extremely active; pinned commit 0add05d may advance before
  the next auto-updater. Spec will be refreshed by the next schedule run; not a
  bug.
- matugen/niri/xwayland-satellite vendor tarballs not independently re-extracted
  this run beyond niri (niri vendor.tar.xz regenerated from the NEW Cargo.lock;
  matugen/xwayland-satellite upstream commits unchanged since the last full
  audit and OBS offline builds succeed).
- The download.opensuse.org OSS-mirror 404 flake family (base dep RPMs during
  zypper preloading) persists on this runner; it does not affect the nexus
  repo or final install state.

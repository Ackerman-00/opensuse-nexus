# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32089425191

## Run Summary (32089425191)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-18 ~01:46Z (opencode-schedule, relay=false). Prior
relay 32077009014 was complete. The auto-updater run 32087532022
(01:15Z) bumped fluxer to 2026.818.4659 (daily stable build,
X-Fluxer-Version header) - a genuine new upstream release, not churn
(previous 2026.817.195019). This run: verified that bump end-to-end
(upstream header, OBS sync + build + publish on BOTH codestreams,
install + smoke in clean TW containers x2 including fresh-checker),
ran the full 21-package version inventory sweep, dependency audit,
and install-tested ALL 21 packages in clean Tumbleweed containers
from the public nexus repo.

### What this run verified (evidence-backed)

1. Auto-updater bump verified:
   - fluxer 2026.818.4659: curl -sI on the exact Source0 URL
     (api.fluxer.app/dl/desktop/stable/linux/x64/latest/rpm) returned
     `x-fluxer-version: 2026.818.4659` == spec Version. OBS binary
     fluxer-2026.818.4659-4.1.x86_64.rpm built + published on BOTH
     codestreams. The updater's .changes entry is the only one for
     this version (no churn).
2. OBS build health: all 42 builds (21 packages x Tumbleweed +
   Slowroll) `succeeded`/`published`; 0 failed/broken/unresolvable.
   `_builddepinfo` (TW): all 21 packages present, 0 <error> blocks.
3. Full version inventory sweep 21/21 (STEP B), all up-to-date:
   - bibata-cursor-theme 2.0.7 = tag v2.0.7 (gh api)
   - fluxer 2026.818.4659 = X-Fluxer-Version header (this run)
   - helium-browser 0.15.5.1 = tag 0.15.5.1 (gh api)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (gh api; gitdate
     20260602133554 == committer date; base_version 16.0.0 ==
     M.version in lua/lazyvim/config/init.lua @ commit AND tag
     v16.0.0)
   - localsend 1.18.0 = held (v1.18.1 assets re-verified THIS RUN =
     4 Android APKs ONLY, no desktop DEB -> hold still correct)
   - mangowm-git 7b0b39f = mango main HEAD (gh api; gitdate
     20260817092559 == committer date)
   - matugen 4.2.0 = tag v4.2.0 (gh api)
   - niri-git 6062844 = niri main HEAD (gh api; gitdate
     20260814122556 == committer date)
   - noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api
     noctalia-dev/noctalia)
   - noctalia-greeter 1.2.1 = tag v1.2.1 (tags API
     noctalia-dev/noctalia-greeter)
   - obsidian 1.13.7 = tag v1.13.7 (gh api obsidianmd/obsidian-releases)
   - opencode-desktop 1.18.18 = tag v1.18.18 (gh api
     anomalyco/opencode)
   - protonplus 0.6.4 = tag v0.6.4 (gh api vysp3r/ProtonPlus)
   - rootapp 0.9.126 = X-AppImage-Version=0.9.126 READ from the
     actual AppImage THIS RUN (unsquashfs at ELF section-header
     offset 944632, Root.desktop); sha256 downloaded AppImage
     0736926f...d910 == spec pin
   - scenefx 0.5 = release 0.5 (gh api wlrfx/scenefx)
   - stb 20260802 = master commit 2c980bb dated 2026-08-02 (gh api;
     TZ=Etc/GMT-8 -> 20260802)
   - stoat-desktop 1.5.1 = tag v1.5.1 (gh api stoatchat/for-desktop)
   - vesktop 1.6.6 = tag v1.6.6 (gh api Vencord/Vesktop) + internal
     version read from app.asar THIS RUN (name: vesktop,
     version: 1.6.6)
   - wlroots 0.20.2 = git ls-remote --tags (0.20.2 newest 0.20.x)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD (gh api;
     gitdate 20260808203058 == committer date)
   - zen-browser 1.21.14b = tag 1.21.14b (gh api)
   Git-pinned tarball md5s verified THIS RUN: OBS == upstream for all
   4 (niri-git, mangowm, xwayland-satellite, lazyvim) - exact match.
   Source0 URLs verified THIS RUN: curl -sL -> 200 for all 15 direct
   source URLs (bibata, helium, matugen, noctalia, noctalia-greeter,
   protonplus, scenefx, stoat, vesktop, zen, obsidian, opencode-desktop,
   localsend, wlroots, fluxer). README (21 rows) == local dirs (21) ==
   OBS packages (21). ALL 21 spec files md5-match between local and OBS
   (adversarial audit - no drift).
4. Dependency deep audit: fluxer binary changed this run so it was
   RE-TORN (rpm2cpio + cpio, ldd of the real ELFs): main ELF
   /usr/lib64/fluxer/fluxer ldd 0 missing, all bundled .so 0 missing;
   readelf NEEDED sonames all covered by spec Requires. vesktop 1.6.6
   also re-torn this run: main ELF ldd 0 missing. Noctalia meson.build
   @5.0.0-beta.8 (extracted THIS RUN) requires 30 deps - ALL present
   in spec BRs (44 total, verified full list this run). Noctalia-
   greeter meson.build deps all in spec. mango meson.build deps all in
   spec. scenefx meson deps all in spec. protonplus meson deps all in
   spec (gio-unix-2.0.pc confirmed provided by glib2-devel in TW
   container). matugen Cargo.lock (382 crates) == vendor.tar.xz (382
   crate dirs) THIS RUN. rootapp AppImage ELFs (Root, libuiohook.so,
   libSkiaSharp.so, libHarfBuzzSharp.so, libonigwrap.so) ldd 0 missing
   THIS RUN. _builddepinfo 0 errors, all 21 present. OBS ground truth
   confirms every spec dep resolves.
5. Optimization discipline: no regressions - fluxer + vesktop are
   prebuilt Electron RPMs (immutable ELFs, "prebuilt, nothing to
   optimize"). No source-built package was touched this run.
6. update.sh health: `bash -n` PASS on all 20 update.sh scripts.
   No-churn guards verified (pure version / commit-SHA comparisons in
   all scripts; fluxer bumped 2026.817.195019 -> 2026.818.4659 via
   X-Fluxer-Version). .changes files show exactly one entry per
   version/commit (no duplicate entries).
7. Actions health: last auto-updater 32087532022 SUCCESS (only fluxer
   touched, 2 files); prior auto-updater 32076924274 SUCCESS; 10/10
   recent opencode-schedule + auto-updater runs success. 0 open issues,
   0 open PRs (verified this run).
8. Tumbleweed snapshot: current snapshots ~20260812 (week 33 review,
   news.opensuse.org 2026-08-15); TW OSS container pulled fresh today.
   Slowroll builds green. GCC 16 toolchain: all 42 builds succeed.
   No Leap 16 involvement.

### Bugs found and fixed this run

None required fixing. The fluxer bump (2026.817.195019 ->
2026.818.4659) was verified correct; no stale versions, no missing
deps, no install failures, no spec/OBS drift. Pre-existing non-blocking
rpmlint style findings on fluxer (explicit-lib-dependency
libXtst6/libuuid1, branding-requires-unversioned hicolor-icon-theme)
and vesktop (branding-requires-unversioned) were NOT introduced by the
bump (Requires lists unchanged since the prior version); builds publish
and installs pass, so they were left as-is per the no-churn rule.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (artifacts in
   /tmp/opencode, not the repo tree).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (fluxer tested twice from clean containers including the
   independent fresh-checker pass; no other package's binary changed
   this run).
3. Install-test coverage ledger updated -> **PASS** (ledger below -
   ALL 21 packages install-tested from the public nexus repo this
   run).
4. Dependency audit 100% coverage (21/21) -> **PASS** (fluxer +
   vesktop re-derived @current versions via real-ELF ldd + readelf;
   noctalia/noctalia-greeter/mango/scenefx/protonplus meson deps
   verified against extracted upstream; matugen Cargo.lock==vendor;
   rootapp AppImage ELFs ldd'd; builddepinfo 0 errors).
5. Claims backed by command output -> **PASS** (gh api, git
   ls-remote, curl -sI/-sL, sha256sum/md5sum, docker/zypper/rpm/ldd/
   readelf logs, OBS _result/_builddepinfo/_binarylist XML, bash -n,
   unsquashfs for rootapp).
6. Named gaps -> see below.
7. Relay updated with run_id 32089425191 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: the auto-updater bumped fluxer (2026.818.4659), so its
binary changed and required re-testing (done - local RPM + public repo
x2, fresh-checker pass). In addition, ALL 21 packages were install-
tested this run from clean TW containers using the public nexus repo
(independent of OBS "succeeded" state - proves zypper resolution and
rpm -V integrity):

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass (public repo) | clean (rc=0) | n/a (theme) |
| fluxer | 2026.818.4659-4.1.x86_64 | pass (local + public repo x2) | clean (rc=0) | PROCESS_ALIVE x2; ldd 0 missing |
| helium-browser | 0.15.5.1-7.1.x86_64 | pass | clean | (GUI; launches prior) |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass | clean | n/a (config) |
| localsend | 1.18.0-15.1.x86_64 | pass | clean | (GUI) |
| mangowm-git | 0.16.1+git20260817092559.7b0b39f-1.1.x86_64 | pass | clean | n/a (compositor) |
| matugen | 4.2.0-6.1.x86_64 | pass | clean | --help exit 0 |
| niri-git | 26.04+git20260814122556.6062844-4.1.x86_64 | pass | clean | n/a (compositor) |
| noctalia | 5.0.0~beta8-4.1.x86_64 | pass | clean | (GUI shell) |
| noctalia-greeter | 1.2.1-2.1.x86_64 | pass | clean | (GUI) |
| obsidian | 1.13.7-6.1.x86_64 | pass | clean | (GUI) |
| opencode-desktop | 1.18.18-3.1.x86_64 | pass | clean | (GUI) |
| protonplus | 0.6.4-8.1.x86_64 | pass | clean | (GUI) |
| rootapp | 0.9.126-5.1.x86_64 | pass | clean | (GUI; AppRun launches prior) |
| libscenefx-0_5 | 0.5-7.1.x86_64 | pass | clean | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | clean | n/a (headers) |
| stoat-desktop | 1.5.1-2.1.x86_64 | pass | clean | (GUI) |
| vesktop | 1.6.6-2.1.x86_64 | pass | clean | (GUI; internal version 1.6.6 verified) |
| libwlroots-0_20 | 0.20.2-8.1.x86_64 | pass | clean | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1.x86_64 | pass | clean | n/a (compositor helper) |
| zen-browser | 1.21.14b-6.1.x86_64 | pass | clean | (GUI) |

## Dependency audit (all 21)

Ground truth this run: (1) `_builddepinfo` (TW) covers all 21
packages, ZERO <error> blocks; (2) fluxer + vesktop re-torn from the
NEW binaries (rpm2cpio + cpio, real-ELF ldd + readelf NEEDED vs spec
Requires) - both 0 missing, spec Requires resolve in clean TW
container; (3) noctalia + noctalia-greeter + mango + scenefx +
protonplus meson.builds extracted THIS RUN, deps fully reconciled
with spec BRs (all present, no gaps); (4) matugen Cargo.lock (382)
== vendor.tar.xz (382 crate dirs); (5) rootapp AppImage ELFs ldd'd 0
missing; (6) local spec == OBS for all 21 (md5).

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer (2026.818.4659) | real-ELF ldd 0 missing + readelf NEEDED (re-torn this run) | soname Requires | none | none | deps-verified |
| helium-browser | ldd real ELF 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real ELF 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git | meson.build @7b0b39f (verified this run) | all present | none | none | deps-verified |
| matugen (4.2.0) | Cargo.lock 382 crates, vendor 382 (verified this run) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git | Cargo.toml @6062844 (prior) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson.build @5.0.0-beta.8 30 deps (verified this run) | all 44 BRs present | none | none | deps-verified |
| noctalia-greeter | meson.build @1.2.1 deps (verified this run) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson.build @0.6.4 deps (verified this run; gio-unix via glib2-devel confirmed) | all in spec | none | none | deps-verified |
| rootapp | AppImage ELFs ldd 0 missing (this run) | libXt6 | none | none | deps-verified |
| scenefx | meson.build @0.5 (verified this run) | all in spec | none | none | deps-verified |
| stb | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop | real ELF 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| vesktop (1.6.6) | real-ELF ldd 0 missing (re-torn this run) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior) | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser | libxul/libtool deps; NSS symlinked (prior) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for fluxer + vesktop (fresh-container install x2 +
real-ELF re-tear this run). high for all others (upstream unchanged
since prior audits; binaries byte-identical; local==OBS md5; all
install-tested this run).

## Commits this run

No source commits needed - the only change this cycle was the
auto-updater's fluxer bump (2026.818.4659) in the pipeline's own
commit 8794975, verified correct and OBS-synced by the pipeline
itself. This relay commit is the only commit from this run.

## OBS build status

All 42 builds succeeded/published (Tumbleweed + Slowroll). fluxer
2026.818.4659 (the package re-built this run) published on both
codestreams after the auto-updater sync.

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified in prior runs and fluxer/vesktop
  this run).
- obsidian/opencode-desktop/vesktop/etc. binary artifacts are fetched
  by OBS `download_files` service at build time (not stored in OBS
  source); verified via URL 200 + upstream release assets instead.
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- Pre-existing non-blocking rpmlint style findings on fluxer and
  vesktop (explicit-lib-dependency, branding-requires-unversioned)
  were left as-is - not introduced by these bumps and not functional.
- helium-browser/obsidian/stoat/etc. GUI smoke was not re-run in
  Docker this run (no X server); their binaries are unchanged since
  the last launch-verified install test.

# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32032902608

## Run Summary (32032902608)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-17 ~13:02Z (opencode-schedule, relay=false). Prior
relay 32004791309 was complete. The auto-updater run 32031317586 at
12:43Z bumped ONLY mangowm-git (c5dd504 -> 7b0b39f, a genuine new
upstream commit), so this run: (1) verified the mangowm bump
end-to-end (upstream HEAD match, tarball md5, OBS sync, build,
install, deps re-derivation at the new commit), and (2) ran an
INDEPENDENT FRESH-CHECKER full install sweep of all 21 packages in
fresh Tumbleweed containers from the public nexus repo (real user
flow).

### What this run verified (evidence-backed)

1. OBS build health: all 42 builds (21 packages x Tumbleweed +
   Slowroll) `succeeded`/`published`; 0 failed/broken/unresolvable.
   _builddepinfo (TW): all 21 packages present, 0 <error> blocks.
2. mangowm-git 7b0b39f verified: commit == mangowm/mango main HEAD
   (gh api 7b0b39f | 2026-08-17T09:25:59Z == %global gitdate
   20260817092559); tarball md5 on OBS (a08cd4f5...) == locally
   downloaded upstream tarball md5; both TW + Slowroll builds
   succeeded; binary mangowm-git-0.16.1+git20260817092559.7b0b39f-1.1
   installed cleanly in a fresh TW container (INSTALL-RC=0, rpm -V
   clean, ldd 0 missing, `mango -v` -> "mango 0.16.1(release)",
   `mmsg -h` -> usage, rc=0). meson.build @7b0b39f deps (14:
   libdrm/xcb/xcb-icccm/wayland-server>=1.23.1/wlroots-0.20>=0.20.0/
   xkbcommon/libinput>=1.27.1/wayland-client/libpcre2-8/scenefx-0.5
   >=0.5.0/pixman-1/libcjson/pangocairo) ALL present in spec; OBS
   _builddepinfo confirms resolution. Artifacts complete: mango,
   mmsg, man1/mmsg.1.gz, wayland-sessions/mango.desktop,
   xdg-desktop-portal/mango-portals.conf, /etc/mango/config.conf,
   LICENSE, README.md.
3. Version inventory sweep 21/21 (STEP B) - all up-to-date, no
   churn:
   - bibata-cursor-theme 2.0.7 = tag v2.0.7 (gh api)
   - fluxer 2026.816.204155 = X-Fluxer-Version header this run
   - helium-browser 0.15.5.1 = tag 0.15.5.1 (gh api imputnet/
     helium-linux)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (gh api; gitdate
     matches committer date)
   - localsend 1.18.0 = held (v1.18.1 asset list re-verified = 4
     Android APKs ONLY, no desktop DEB -> correct hold)
   - mangowm-git 7b0b39f = mango main HEAD (this run, see above)
   - matugen 4.1.0 = tag v4.1.0 (gh api)
   - niri-git 6062844 = niri main HEAD (gh api; gitdate matches)
   - noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api
     noctalia-dev/noctalia)
   - noctalia-greeter 1.2.1 = tag v1.2.1 (tags API
     noctalia-dev/noctalia-greeter)
   - obsidian 1.13.7 = tag v1.13.7 (gh api)
   - opencode-desktop 1.18.18 = tag v1.18.18 (gh api
     anomalyco/opencode)
   - protonplus 0.6.4 = tag v0.6.4 (gh api vysp3r/ProtonPlus)
   - rootapp 0.9.126 = X-AppImage-Version=0.9.126 read from the
     installed /opt/rootapp/Root.desktop inside the fresh container
   - scenefx 0.5 = release 0.5 (gh api wlrfx/scenefx)
   - stb 20260802 = master commit 2c980bb dated 2026-08-02 (gh api;
     TZ=Etc/GMT-8 -> 20260802)
   - stoat-desktop 1.5.1 = tag v1.5.1 (gh api stoatchat/for-desktop)
   - vesktop 1.6.5 = tag v1.6.5 (gh api)
   - wlroots 0.20.2 = git ls-remote tags 0.20.2 is the newest
     0.20.x (gitlab API blocked/404 this run; ls-remote is ground
     truth; 0.20.2 == latest)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD (gh api;
     gitdate matches)
   - zen-browser 1.21.14b = tag 1.21.14b (gh api); installed
     application.ini Version=1.21.14b
   All versioned Source0 URLs verified 200 (curl -sI). README (21
   rows) == local dirs (21) == OBS packages (21).
4. Dependency deep audit: mangowm meson.build @7b0b39f fully
   re-derived (all 14 deps in spec, builddepinfo confirms). All
   other packages unchanged since last full audit (relay
   32004791309) - their binaries are byte-identical (OBS binary
   list matches ledger). real-ELF ldd 0 missing everywhere tested
   (mangowm, fluxer, localsend, helium, rootapp libuiohook,
   obsidian, opencode-desktop, stoat, vesktop, zen libxul).
5. Install-test sweep: ALL 21 packages installed from the PUBLIC
   nexus repo in FRESH TW containers (exact user flow: OSS +
   home:ackerman, --gpg-auto-import-keys). Every `rpm -V` clean.
   Smoke results in ledger below. mangowm-git tested BOTH via the
   repo (0.16.1) and local RPM; the rest via the repo.
6. Spec validation: all 21 local specs byte-identical to OBS
   (SPEC-MATCH) and all 21 .changes byte-identical to OBS
   (CHG-MATCH). OBS file lists match local dirs except gitignored
   artifacts (tarballs, vendor.tar.xz, cargo_config, _service
   outputs) which exist only on OBS - expected.
7. update.sh health: `bash -n` PASS on all 20 update.sh scripts.
   No-churn guards verified (pure commit-SHA / version / sha256
   comparisons; tarball refresh is artifact-only and exits 0).
   localsend 404-guard correctly stays on 1.18.0.
8. Actions health: 10/10 recent "Nexus Auto-Updater (OBS Pipeline)"
   runs success (32031317586 latest, 12:43Z). 0 open issues, 0 open
   PRs.
9. Tumbleweed snapshot: TW OSS repomd revision date 2026-08-16;
   Slowroll 2026-08-03 (~13 day lag). All builds green on both
   codestreams. No Leap 16. No full rebuild wave breakage observed
   (GCC 16 toolchain, all 42 builds succeed).

### Bugs found and fixed this run

None. mangowm-git's bump was a legitimate upstream move handled by
the auto-updater; no manual fix required. Repo fully healthy.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (artifacts in
   /tmp/opencode, not the repo tree).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (mangowm-git re-verified at the new commit; plus the
   full 21-package fresh sweep below as independent checker).
3. Install-test coverage ledger updated -> **PASS** (21/21 rows this
   run, ledger below). mangowm-git's binary changed (new commit) and
   was re-tested; no other binary changed since the last test.
4. Dependency audit 100% coverage (21/21) -> **PASS** (mangowm
   re-derived @7b0b39f; others unchanged, binaries byte-identical;
   builddepinfo 0 errors).
5. Claims backed by command output -> **PASS** (gh api, git
   ls-remote, curl -sI, sha256sum/md5sum, docker/zypper/rpm/ldd
   logs, OBS _result/_builddepinfo/_binarylist XML, bash -n).
6. Named gaps -> see below.
7. Relay updated with run_id 32032902608 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 21 packages install-tested THIS RUN in FRESH Tumbleweed
containers from the PUBLIC download.opensuse.org nexus repo (exact
user flow: OSS + home:ackerman repos, --gpg-auto-import-keys).

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | clean | icons present |
| fluxer | 2026.816.204155-4.1 | pass | clean | ldd /usr/lib64/fluxer/fluxer 0 missing; X-Fluxer-Version header == version |
| helium-browser | 0.15.5.1-7.1 | pass | clean | **launches: "Helium 0.15.5.1 (Chromium 151.0.7922.137)" rc=0**; ldd real ELF 0 missing |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass | clean | /usr/share/lazyvim present |
| localsend | 1.18.0-15.1 | pass | clean | ldd 0 missing |
| mangowm-git | 0.16.1+git20260817092559.7b0b39f-1.1 | pass | clean | "mango 0.16.1(release)" rc=0; mmsg usage rc=0 |
| matugen | 4.1.0-30.1 | pass | clean | "matugen 4.1.0" rc=0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | clean | "niri 26.04 (6062844)" rc=0 |
| noctalia | 5.0.0~beta8-4.1 | pass | clean | "noctalia v5.0.0" rc=0 |
| noctalia-greeter | 1.2.1-2.1 | pass | clean | "noctalia-greeter 1.2.1" rc=0 |
| obsidian | 1.13.7-6.1 | pass | clean | ldd 0 missing; electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | clean | ldd /opt/OpenCode/ai.opencode.desktop 0 missing |
| protonplus | 0.6.4-8.1 | pass | clean | /usr/bin/protonplus present; --version unsupported flag (upstream) |
| rootapp | 0.9.126-5.1 | pass | clean | **AppRun launches rc=0 (info: Starting application); X-AppImage-Version=0.9.126; libuiohook.so 0 missing** |
| libscenefx-0_5 | 0.5-7.1 | pass | clean | lib present (libscenefx-0.5.so) |
| stb-devel | 20260802-1.1.noarch | pass | clean | /usr/include/stb/*.h present (21 headers) |
| stoat-desktop | 1.5.1-2.1 | pass | clean | ldd /opt/Stoat/stoat-desktop 0 missing |
| vesktop | 1.6.5-35.1 | pass | clean | ldd /opt/Vesktop/vesktop 0 missing |
| libwlroots-0_20 | 0.20.2-8.1 | pass | clean | lib present (libwlroots-0.20.so) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | clean | "Starting xwayland-satellite version 0.8.2" then display panic (headless container only) |
| zen-browser | 1.21.14b-6.1 | pass | clean | application.ini Version=1.21.14b; sandbox EPERM Docker-only |

## Dependency audit (all 21)

Ground truth this run: (1) `_builddepinfo` (TW) covers all 21
packages, ZERO <error> blocks; (2) REAL-ELF ldd of bundled binaries
- 0 missing everywhere.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer (2026.816.204155) | real-ELF ldd 0 missing | soname Requires | none | none | deps-verified |
| helium-browser (0.15.5.1) | ldd real ELF 0 missing | soname Requires | none | none | deps-verified |
| lazyvim-git (459a4c3) | config only | none | none | none | deps-verified |
| localsend (1.18.0) | real ELF 0 missing | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (7b0b39f) | meson.build @7b0b39f (14 deps) | all present | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 + builddepinfo | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron via system pkg | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | real-ELF ldd 0 missing | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | meson appstream + cairo | pkgconfig(appstream), pkgconfig(cairo) | none | none | deps-verified |
| rootapp (0.9.126) | ldd libuiohook.so 0 missing (libXt6) | libXt6 | none | none | deps-verified |
| scenefx (0.5) | meson.build | all in spec | none | none | deps-verified |
| stb (20260802) | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.5.1) | real ELF 0 missing | soname/named Requires | none | none | deps-verified |
| vesktop (1.6.5) | real ELF 0 missing | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build | all in spec | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | libxul/libtool deps; NSS symlinked | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 21 (fresh-container install + real-ELF ldd
this run + clean builddepinfo + meson/Cargo re-derivation for the
source-built packages).

## Commits this run

None required (repo fully healthy - mangowm bump was legitimate
auto-updater work, everything else current). Relay state commit
only.

## OBS build status

All 42 builds succeeded/published (Tumbleweed + Slowroll). No
rebuilds triggered this run (nothing broken or changed by me).

## Not Verified / Gaps
- Tumbleweed snapshot ~20260816 (OSS repomd revision date);
  Slowroll lags ~13 days (2026-08-03). TW proof per policy.
- wlroots release-API version check: gitlab.freedesktop.org API
  returned 404/"not responding" this run; used `git ls-remote
  --tags` (0.20.2 == newest 0.20.x) as ground truth instead.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified). Electron apps (fluxer, helium,
  obsidian, opencode-desktop, stoat, vesktop, zen) running as root
  hit the sandbox/namespace EPERM which is Docker-only and
  documented; helium+rootapp launch evidence captured.
- helium /opt/helium/libqt{5,6}_shim.so need Qt5/Qt6 - OPTIONAL
  dlopen'd shims, not required for launch (verified). Not hard
  Requires to avoid forcing Qt5/Qt6 install.
- localsend intentionally held at 1.18.0 (v1.18.1 has no desktop
  DEB). Guard protects the repo.
- lazyvim-git ships Lua config as plain files; end-to-end LazyVim
  startup requires runtime-download on first launch (network
  dependent, not testable offline).
- fluxer internal version: X-Fluxer-Version header (2026.816.204155)
  is canonical per the project's update mechanism.

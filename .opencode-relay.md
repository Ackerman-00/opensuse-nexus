# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32289540292

## Run Summary (32289540292)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-19 ~18:49Z (opencode-schedule, relay=false). Prior
relay 32256104161 was complete. This run:

1. **OBS build check: all 42 builds green after fix.** At start all
   builds succeeded except zen-browser 1.21.15b which FAILED to install
   (see #2). After the fix, `_result?view=status` = 40 succeeded + 2
   finished/succeeded (zen-browser on both repos), zero
   failed/broken/unresolvable.
2. **CRITICAL BUG FOUND + FIXED: zen-browser 1.21.15b was NOT
   installable.** The auto-updater bumped zen-browser 1.21.14b -> 1.21.15b
   (commit c7b82a0). The new build required `libnss3.so(NSS_3.126)(64bit)`
   (from bundled libxul.so -> symbol PK11_CreatePrivateKeyFromTemplate)
   which openSUSE Tumbleweed's system mozilla-nss 3.125 does not provide
   (max NSS_3.117). The old spec deleted the bundled NSS stack and
   symlinked system libs, so zypper failed with "nothing provides
   libnss3.so(NSS_3.126)(64bit)" -> RC=4, verified in a clean TW
   container against the OBS-published 1.21.15b-6.1 binary.
   **Fix (commit c98049d)**: keep the bundled NSS/NSPr stack (the tarball
   ships libnss3/nspr4/ssl3/smime3/nssutil3/softokn3/freeblpriv3 which
   provide the NSS_3.126 version node); symlink only the root CA certs
   module libnssckbi.so (not bundled) from mozilla-nss-certs. Dropped the
   now-unneeded `Requires/BR: mozilla-nss` + `mozilla-nspr`.
   **Verified BOTH sides of the symptom**: before = RC=4 "nothing
   provides libnss3.so(NSS_3.126)(64bit)"; after = local rpmbuild in a
   clean TW container + OBS build zen-browser-1.21.15b-9.1 both install
   with RC=0, `rpm -V` clean, `zen-browser --version` = "Mozilla Zen
   1.21.15b". Self-contained: the fixed RPM provides
   libnss3.so(NSS_3.126)(64bit) from its bundled libnss3.so.
3. **Auto-updater bumps verified (commit c7b82a0, 18:38Z).** It bumped
   mangowm-git to commit 6441cfe (gitdate 20260819162330) and
   stoat-desktop to 1.5.3, plus zen (above). All verified against
   upstream THIS RUN:
   - mangowm: `gh api repos/mangowm/mango/commits/main` = 6441cfe,
     committer date 2026-08-19T16:23:30Z == gitdate 20260819162330.
   - stoat-desktop: `gh api repos/stoatchat/for-desktop/releases/latest`
     = v1.5.3.
   - zen-browser: `gh api repos/zen-browser/desktop/releases/latest` =
     1.21.15b (upstream correct; only the packaging was broken).
   - Local spec == OBS spec (md5) for all three after the auto-update;
     OBS had already built them green. No churn - nothing re-pushed for
     mangowm/stoat.
4. **Version inventory sweep: all 21 packages up-to-date** (see table
   below). Evidence per package: gh api releases/latest or commits/main
   (gitdates all match committer dates), fluxer X-Fluxer-Version header
   = 2026.819.105810 (HTTP 200), rootapp AppImage re-downloaded + torn
   apart this run (squashfs at ELF offset 944632, X-AppImage-Version =
   0.9.126, sha256 0736926f... == spec comment), stb commit date
   2026-08-02T06:42:09Z -> 20260802 (UTC+8 formula), wlroots GitLab
   releases = 0.20.2 newest 0.20.x. localsend held at 1.18.0 (v1.18.1
   assets still Android APKs only, re-verified this run). All 12 GitHub
   source URLs + noctalia/noctalia-greeter return HTTP 200. GitLab
   wlroots download is anti-bot (Anubis) blocked for local curl, but
   OBS builds wlroots green on both repos (source fetched by OBS).
5. **No other repo changes needed this run.** No other spec, .changes,
   update.sh, or workflow file needed an edit. All 20 update.sh scripts
   pass `bash -n`.
6. **GitHub Actions health: healthy.** Latest "Nexus Auto-Updater (OBS
   Pipeline)" run (2026-08-19T18:36Z) succeeded; prior updater runs
   12:47Z + 06:43Z succeeded. The one failed opencode-schedule run
   (32140255891, 08-18 13:04Z) was relayed and recovered by the chain
   (subsequent runs succeeded). All recent opencode-schedule runs
   succeeded.
7. **Issues/PRs: none open** (`gh issue list` and `gh pr list` empty).
8. **Dependency audit: 100% coverage (21/21)**. Changed packages
   RE-DERIVED from new artifacts this run:
   - mangowm-git 6441cfe: extracted the new tarball; meson.build deps
     (libdrm, xcb, xcb-icccm, wayland-server>=1.23.1, wlroots-0.20>=0.20,
     xkbcommon, libinput>=1.27.1, wayland-client, libpcre2-8,
     scenefx-0.5>=0.5, pixman-1, libcjson, pangocairo) all present in
     spec as pkgconfig() BuildRequires. deps-verified.
   - stoat-desktop 1.5.3: unzipped the new release; ldd/readelf of the
     main binary (Electron 43.4.0) -> all 27 NEEDED sonames present in
     the container after clean install (verified one-by-one with find);
     spec Requires set covers the runtime (gtk3 pulls glib/pango/cairo/
     atk, mozilla-nss pulls nspr, at-spi2-core pulls the bridge).
     deps-verified.
   - zen-browser 1.21.15b: fixed (bundled NSS). Deps self-contained.
     deps-fixed.
   All other upstreams unchanged (version sweep above), prior audits
   carry forward. Ground truth: all 42 OBS builds green AND the
   changed-binary install tests below passed in fresh TW containers.
9. **Install-verification sweep (fresh checker pass):**
   - zen-browser 1.21.15b: BEFORE the fix, the published 1.21.15b-6.1
     binary FAILED install (RC=4, "nothing provides
     libnss3.so(NSS_3.126)"). AFTER the fix, the OBS-built
     1.21.15b-9.1 binary installs in a FRESH TW container (RC=0, rpm -V
     clean) and `zen-browser --version` = "Mozilla Zen 1.21.15b"
     (sandbox EPERM is docker, not a package bug). Both sides captured.
   - mangowm-git 6441cfe (OBS binary, fresh container): RC=0, rpm -V
     clean; `mango -v` present (compositor; headless smoke limited).
   - stoat-desktop 1.5.3 (OBS binary, fresh container): RC=0, rpm -V
     clean; /opt/Stoat + /usr/bin/stoat-desktop launcher present
     (Electron GUI; no display in container).
10. **Validation:** bash -n on all 20 update.sh scripts (all OK),
    rpmspec -P on the fixed zen-browser.spec (parses), local rpmbuild
    of zen-browser in a clean TW container succeeded, rootapp AppImage
    re-torn + sha256 re-verified (0736926f... == spec comment).

### Version inventory sweep (21/21, THIS RUN)

| package | packaged version | upstream latest | status |
|---|---|---|---|
| bibata-cursor-theme | 2.0.7 | v2.0.7 (gh api releases/latest) | up-to-date |
| fluxer | 2026.819.105810 | X-Fluxer-Version header 2026.819.105810 | up-to-date |
| helium-browser | 0.15.5.1 | 0.15.5.1 (gh api releases/latest) | up-to-date |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3 | LazyVim main 459a4c3 (gitdate matches) | up-to-date |
| localsend | 1.18.0 | held (v1.18.1 = Android APKs only; re-verified assets) | up-to-date (hold) |
| mangowm-git | 0.16.1+git20260819162330.6441cfe | mango main 6441cfe (gitdate matches) | up-to-date (auto-update bump verified) |
| matugen | 4.2.0 | v4.2.0 (gh api) | up-to-date |
| niri-git | 26.04+git20260819083147.b0eb8ad | niri main b0eb8ad (gitdate matches) | up-to-date |
| noctalia | 5.0.0~beta8 | v5.0.0-beta.8 (gh api) | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 (tag list; newest tag) | up-to-date |
| obsidian | 1.13.7 | v1.13.7 (gh api) | up-to-date |
| opencode-desktop | 1.18.18 | v1.18.18 (gh api) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (gh api) | up-to-date |
| rootapp | 0.9.126 | X-AppImage-Version 0.9.126 (torn this run); sha 0736926f == spec | up-to-date |
| scenefx | 0.5 | tag 0.5 (gh api) | up-to-date |
| stb | 20260802 | master 2026-08-02T06:42:09Z -> UTC+8 20260802 | up-to-date |
| stoat-desktop | 1.5.3 | v1.5.3 (gh api releases/latest) | up-to-date (auto-update bump verified) |
| vesktop | 1.6.7 | v1.6.7 (gh api) | up-to-date |
| wlroots | 0.20.2 | GitLab releases 0.20.2 (newest 0.20.x) | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f | Supreeeme main 3bc915f (gitdate matches) | up-to-date |
| zen-browser | 1.21.15b | 1.21.15b (gh api; upstream version correct) | FIXED-WRONG-PACKAGING |

## Bugs found and fixed this run

- **zen-browser 1.21.15b install failure (fixed, commit c98049d).**
  Auto-update bump to 1.21.15b shipped a build whose bundled libs need
  the NSS_3.126 symbol node, but the spec deleted the bundled NSS and
  symlinked system mozilla-nss (max NSS_3.117), making zypper fail with
  "nothing provides libnss3.so(NSS_3.126)(64bit)". Fixed the spec to
  keep the bundled NSS/NSPr and symlink only libnssckbi.so from
  mozilla-nss-certs. OBS rebuilt: zen-browser-1.21.15b-9.1 on both
  codestreams, install-tested RC=0 in a fresh TW container, runs
  "Mozilla Zen 1.21.15b". NOTE: the auto-updater's .changes entry for
  zen ("Update to v1.21.15b") stands; the packaging fix has its own
  entry added.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (only the relay
   commit remains; all artifacts in /tmp/opencode).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (zen-browser 1.21.15b fixed binary: BEFORE RC=4 + AFTER
   RC=0 both captured; mangowm-git 6441cfe + stoat-desktop 1.5.3
   installed RC=0 in fresh containers; all three were the binaries that
   changed since the last test).
3. Install-test coverage ledger updated -> **PASS** (ledger below;
   the three binaries changed since the last test - zen-browser,
   mangowm-git, stoat-desktop - were all re-tested this run).
4. Dependency audit 100% coverage (21/21) -> **PASS** (table below;
   changed packages re-derived from new artifacts this run, all 42 OBS
   builds green).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, unsquashfs, objdump/readelf, rpm -qp, rpm2cpio+cpio,
   docker/zypper/rpm -V, rpmspec -P, bash -n, git log/diff/status,
   OBS _result + binary listing + downloads, rpmbuild in container).
6. Named gaps -> see below.
7. Relay updated with run_id 32289540292 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: binaries changed since last test for zen-browser
(1.21.14b -> 1.21.15b, via auto-updater), mangowm-git (813acaf ->
6441cfe), stoat-desktop (1.5.2 -> 1.5.3). All three re-tested in
FRESH Tumbleweed containers from the OBS-published binaries.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| zen-browser | 1.21.15b-9.1 (FIXED) | pass (fresh TW, RC=0; old 6.1 was RC=4 "nothing provides NSS_3.126" - both captured) | clean | `Mozilla Zen 1.21.15b` |
| mangowm-git | 0.16.1+git20260819162330.6441cfe-1.1 | pass (fresh TW, RC=0) | clean | `mango -v` present (compositor; headless smoke limited) |
| stoat-desktop | 1.5.3-2.1 | pass (fresh TW, RC=0) | clean | /opt/Stoat + /usr/bin/stoat-desktop present (Electron GUI; no display) |
| 18 remaining runtime pkgs | current published | carried forward (unchanged since last green test; version sweep confirms no upstream change) | clean | carried forward |
| scenefx, wlroots, stb | current published | n/a (library/devel packages) | n/a | consumed as BuildRequires; all 42 builds green |

## Dependency audit (all 21)

Ground truth this run: (1) all 42 OBS builds green; (2) the three
changed binaries installed in fresh TW containers (zen fixed binary
RC=0, mangowm RC=0, stoat RC=0); (3) local spec == OBS for the changed
packages; (4) upstream unchanged for 18/21 since prior audits (version
sweep above); mangowm-git + stoat-desktop + zen-browser changed and
were RE-DERIVED this run.

RE-DERIVATION (changed packages):
- mangowm-git 6441cfe (source): extracted the new commit tarball;
  meson.build dependency() calls (libdrm, xcb, xcb-icccm,
  wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon,
  libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0,
  pixman-1, libcjson, pangocairo) - all 13 present as pkgconfig()
  BuildRequires in the spec (build tools meson/ninja/gcc-c++/
  systemd-rpm-macros/fdupes/pkg-config also present). Install passed.
- stoat-desktop 1.5.3 (prebuilt): unzipped the new release; readelf
  NEEDED of the main binary = 27 sonames (incl. libffmpeg bundled);
  every one present after a clean install in a fresh TW container
  (verified one-by-one). Spec Requires (gtk3, mozilla-nss,
  at-spi2-core, libcups2, dbus-1, libsystemd0, X11 set, libgbm1,
  libasound2, libsecret-1-0, xdg-utils) covers runtime. Install passed.
- zen-browser 1.21.15b: fixed - bundled NSS provides NSS_3.126;
  Requires now mozilla-nss-certs (libnssckbi.so), hunspell, hyphen +
  auto sonames (self-satisfied). Install passed.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer | ldd of main ELF (prior) | soname Requires | none | none | deps-verified |
| helium-browser | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real-ELF ldd 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (6441cfe, re-derived) | meson.build this run (13 deps) | pkgconfig set (all 13) | none | none | deps-verified |
| matugen | Cargo.lock == vendor (prior) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git | Fedora spec.rpkg + _builddepinfo (prior) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson.build @5.0.0-beta.8 (prior) | all 44 BRs present | none | none | deps-verified |
| noctalia-greeter | meson.build @1.2.1 (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson.build @0.6.4 (prior) | all in spec | none | none | deps-verified |
| rootapp | AppImage ELFs ldd 0 missing (prior) | libXt6 + wl-clipboard etc. | none | none | deps-verified |
| scenefx | meson.build @0.5 (prior) | all in spec; provides libscenefx-0_5 | none | none | deps-verified |
| stb | header-only | c++_compiler; provides stb.pc | none | none | deps-verified |
| stoat-desktop (1.5.3, re-derived) | 27 NEEDED sonames this run | named Requires set | none | none | deps-verified |
| vesktop | real-ELF ldd 0 missing (prior) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior); provides libwlroots-0_20 | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser (1.21.15b, re-derived) | bundled NSS stack (needs NSS_3.126) | fixed: bundled NSS kept; mozilla-nss-certs, hunspell, hyphen | dropped mozilla-nss/mozilla-nspr (now bundled) | none | deps-fixed |

Confidence: high for all 21 (upstream verified per-package this run,
all 42 OBS builds green after the zen fix, changed binaries installed
in fresh TW containers, mangowm/stoat/zen deps re-derived from the new
artifacts this run).

## Commits this run

1. `c98049d` fix(autonomous): zen-browser: keep bundled NSS stack
   (NSS_3.126 not in system mozilla-nss) - spec + .changes.
2. Relay update (this commit) - run_id 32289540292, status complete.

## OBS build status

All 42 builds (21 pkgs x Tumbleweed + Slowroll) green: 40 succeeded +
2 finished/succeeded (zen-browser fixed rebuild 1.21.15b-9.1 on both
codestreams). Zero failed/broken/unresolvable codes in
`_result?view=status&code=failed&code=broken&code=unresolvable`.

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; binary
  presence + rpm -V verified, CLI-capable apps run: zen-browser
  --version works; stoat/others are Electron GUI with no headless
  CLI).
- The wlroots GitLab download could not be fetched by local curl (Anubis
  anti-bot challenge on gitlab.freedesktop.org object storage); the
  version (0.20.2) was confirmed via the GitLab releases API and OBS
  builds wlroots green on both codestreams, so OBS fetches it fine.
- The download.opensuse.org mirror for home:ackerman had not yet
  propagated the fixed zen-browser-1.21.15b-9.1 build at end of run
  (still serving 6.1); OBS API confirms 9.1 published. Normal OBS
  mirror lag, resolves automatically.
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- Dependency re-derivation for the 18 unchanged upstreams was carried
  forward from prior audits (upstream artifacts identical - verified by
  the version sweep showing no upstream change).
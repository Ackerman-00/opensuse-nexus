# opencode-schedule Relay State

## Status: COMPLETE

run_id: 32077009014

## Run Summary (32077009014)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-17 ~22:39Z (opencode-schedule, relay=false). Prior
relay 32057328402 was complete. The auto-updater run 32076924274
(22:38-22:39Z) bumped TWO packages to legitimate new upstream
releases: fluxer 2026.816.204155 -> 2026.817.195019 (daily stable
build, X-Fluxer-Version header) and vesktop 1.6.5 -> 1.6.6 (upstream
v1.6.6 released 2026-08-17T19:55:02Z). Both bumps were genuine - not
churn. This run: verified both bumps end-to-end (upstream tag/header,
Source0 URL 200, OBS sync, build, install, smoke), ran the full
21-package version inventory sweep, dependency audit, and an
independent FRESH-CHECKER install re-test of both changed binaries
from the public nexus repo.

### What this run verified (evidence-backed)

1. Auto-updater bumps verified:
   - vesktop 1.6.6: gh api `releases/latest` tag_name=v1.6.6,
     published 2026-08-17T19:55:02Z; asset
     vesktop-1.6.6.x86_64.rpm (91.7MB) URL HEAD 302->200; OBS spec
     Version == 1.6.6 == local. OBS binary
     vesktop-1.6.6-2.1.x86_64.rpm built + published on BOTH
     codestreams.
   - fluxer 2026.817.195019: curl -sI on the exact Source0 URL
     (api.fluxer.app/dl/desktop/stable/linux/x64/latest/rpm) returned
     `x-fluxer-version: 2026.817.195019` == spec Version == local.
     OBS binary fluxer-2026.817.195019-4.1.x86_64.rpm built +
     published on BOTH codestreams. (fluxer API was reachable this
     run, resolving the prior run's gap.)
2. OBS build health: all 42 builds (21 packages x Tumbleweed +
   Slowroll) `succeeded`/`published`; 0 failed/broken/unresolvable.
   _builddepinfo (TW): all 21 packages present, 0 <error> blocks.
3. Full version inventory sweep 21/21 (STEP B), all up-to-date:
   - bibata-cursor-theme 2.0.7 = tag v2.0.7 (gh api)
   - fluxer 2026.817.195019 = X-Fluxer-Version header (this run)
   - helium-browser 0.15.5.1 = tag 0.15.5.1 (gh api)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (gh api; gitdate
     20260602133554 == committer date; base_version 16.0.0 == tag
     v16.0.0)
   - localsend 1.18.0 = held (v1.18.1 assets re-verified THIS RUN =
     4 Android APKs ONLY, no desktop DEB -> hold still correct)
   - mangowm-git 7b0b39f = mango main HEAD (gh api; gitdate
     20260817092559 == committer date)
   - matugen 4.2.0 = tag v4.2.0 (gh api)
   - niri-git 6062844 = niri main HEAD (gh api; gitdate
     20260814122556 == committer date)
   - noctalia 5.0.0~beta8 = tag v5.0.0-beta.8 (gh api
     noctalia-dev/noctalia)
   - noctalia-greeter 1.2.1 = tag v1.2.1 (tags API
     noctalia-dev/noctalia-greeter)
   - obsidian 1.13.7 = tag v1.13.7 (gh api obsidianmd/obsidian-releases)
   - opencode-desktop 1.18.18 = tag v1.18.18 (gh api
     anomalyco/opencode)
   - protonplus 0.6.4 = tag v0.6.4 (gh api vysp3r/ProtonPlus)
   - rootapp 0.9.126 = X-AppImage-Version=0.9.126 READ from the
     actual AppImage THIS RUN (unsquashfs at ELF section-header
     offset 944632, Root.desktop); sha256 downloaded AppImage
     0736926f...d910 == spec pin
   - scenefx 0.5 = release 0.5 (gh api wlrfx/scenefx)
   - stb 20260802 = master commit 2c980bb dated 2026-08-02 (gh api;
     TZ=Etc/GMT-8 -> 20260802)
   - stoat-desktop 1.5.1 = tag v1.5.1 (gh api stoatchat/for-desktop)
   - vesktop 1.6.6 = tag v1.6.6 (gh api Vencord/Vesktop)
   - wlroots 0.20.2 = git ls-remote --tags (0.20.2 newest 0.20.x)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD (gh api;
     gitdate 20260808203058 == committer date)
   - zen-browser 1.21.14b = tag 1.21.14b (gh api)
   Git-pinned tarball md5s verified THIS RUN: OBS == upstream for all
   4 (niri-git, mangowm, xwayland-satellite, lazyvim) - exact match.
   README (21 rows) == local dirs (21) == OBS packages (21).
   ALL 21 spec + changes files md5-match between local and OBS
   (adversarial audit - no drift).
4. Dependency deep audit: fluxer + vesktop binaries changed this
   run so they were RE-TORN (rpm2cpio + cpio, ldd of the real ELFs):
   fluxer main ELF /usr/lib64/fluxer/fluxer: ldd 0 missing; vesktop
   main ELF /opt/Vesktop/vesktop: ldd 0 missing. Requires lists in
   both specs unchanged by the bump (version-only commits), all
   resolve in a clean TW container. _builddepinfo 0 errors, all 21
   present. All other packages unchanged since last full audit
   (relay 32032902608); binaries byte-identical.
5. Optimization discipline: no regressions - both bumps were
   version-only edits to prebuilt Electron RPMs (immutable ELFs,
   "prebuilt, nothing to optimize"). matugen debuginfo fix from last
   run still present in spec (verified).
6. update.sh health: `bash -n` PASS on all 19 update.sh scripts.
   No-churn guards verified (pure version / commit-SHA / sha256
   comparisons). fluxer update.sh correctly bumped 2026.816.204155 ->
   2026.817.195019 (X-Fluxer-Version). vesktop update.sh bumped
   1.6.5 -> 1.6.6 (release tag).
7. Actions health: last auto-updater run 32076924274 SUCCESS
   (all steps green, only fluxer + vesktop touched); prior auto-updater
   32055957658 SUCCESS; 10/10 recent runs success. 0 open issues,
   0 open PRs (verified this run).
8. Tumbleweed snapshot: current snapshots ~20260812 (week 33 review,
   news.opensuse.org); TW OSS container pulled fresh today.
   Slowroll builds green. GCC 16 toolchain: all 42 builds succeed.
   No Leap 16 involvement.

### Bugs found and fixed this run

None required fixing. Both auto-updater bumps were verified correct;
no stale versions, no missing deps, no install failures, no spec/OBS
drift. Pre-existing non-blocking rpmlint style findings on fluxer
(explicit-lib-dependency libXtst6/libuuid1, branding-requires-
unversioned hicolor-icon-theme) and vesktop (branding-requires-
unversioned) were NOT introduced by these bumps (Requires lists
unchanged since the prior version); builds publish and installs pass,
so they were left as-is per the no-churn rule.

## Pre-completion gate

1. `git status --porcelain` empty at end -> **PASS** (artifacts in
   /tmp/opencode, not the repo tree).
2. Every package touched install-tested in a clean TW container ->
   **PASS** (fluxer + vesktop tested in fresh containers from the
   public nexus repo, main + independent fresh-checker, twice each;
   the two packages the auto-updater bumped this run).
3. Install-test coverage ledger updated -> **PASS** (ledger below).
   Only fluxer + vesktop binaries changed since last test and both
   were re-tested; no other binary changed.
4. Dependency audit 100% coverage (21/21) -> **PASS** (fluxer +
   vesktop re-derived @new versions via real-ELF ldd; others
   unchanged, binaries byte-identical; builddepinfo 0 errors).
5. Claims backed by command output -> **PASS** (gh api, git
   ls-remote, curl -sI, md5sum, docker/zypper/rpm/ldd logs, OBS
   _result/_builddepinfo/_binarylist XML, bash -n, unsquashfs for
   rootapp).
6. Named gaps -> see below.
7. Relay updated with run_id 32077009014 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

THIS RUN: the auto-updater bumped fluxer (2026.817.195019) and
vesktop (1.6.6), so their binaries changed and BOTH required
re-testing. Tested from clean TW containers using the public nexus
repo (independent fresh-checker pass), plus local RPM install tests:

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| fluxer | 2026.817.195019-4.1.x86_64 | pass (local + public repo) | clean (rc=0) | PROCESS_ALIVE; ldd 0 missing |
| vesktop | 1.6.6-2.1.x86_64 | pass (local + public repo) | clean (rc=0) | PROCESS_ALIVE; ldd 0 missing |
| all other 19 | unchanged since 32057328402 ledger | (re-tested prior runs) | clean | (see last ledgers) |

Last full sweep (run 32032902608): bibata-cursor-theme 2.0.7 pass,
fluxer 2026.816.204155 pass, helium-browser 0.15.5.1 pass (launches),
lazyvim-git 459a4c3 pass, localsend 1.18.0 pass, mangowm-git 7b0b39f
pass, niri-git 6062844 pass, noctalia 5.0.0~beta8 pass,
noctalia-greeter 1.2.1 pass, obsidian 1.13.7 pass, opencode-desktop
1.18.18 pass, protonplus 0.6.4 pass, rootapp 0.9.126 pass
(X-AppImage-Version=0.9.126, AppRun launches), libscenefx-0_5 0.5
pass, stb-devel 20260802 pass, stoat-desktop 1.5.1 pass, vesktop
1.6.5 pass, libwlroots-0_20 0.20.2 pass, xwayland-satellite-git
3bc915f pass, zen-browser 1.21.14b pass.

## Dependency audit (all 21)

Ground truth this run: (1) `_builddepinfo` (TW) covers all 21
packages, ZERO <error> blocks; (2) fluxer + vesktop re-torn from the
NEW binaries (rpm2cpio + cpio, real-ELF ldd) - both 0 missing, spec
Requires resolve in clean TW container; (3) all other packages
unchanged upstream since last full audit (binaries byte-identical);
(4) local spec/changes == OBS for all 21 (md5).

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | icons only | none | none | none | deps-verified |
| fluxer (2026.817.195019) | real-ELF ldd 0 missing (re-torn this run) | soname Requires | none | none | deps-verified |
| helium-browser | ldd real ELF 0 missing (prior) | soname Requires | none | none | deps-verified |
| lazyvim-git | config only | none | none | none | deps-verified |
| localsend | real ELF 0 missing (prior) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git | meson.build @7b0b39f (prior audit) | all present | none | none | deps-verified |
| matugen (4.2.0) | Cargo.lock @4.2.0: 382 crates, vendor 381 (prior) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git | Cargo.toml @6062844 (prior) | pkgconfig set + cargo-packaging | none | none | deps-verified |
| noctalia | meson deps (prior) | all in spec | none | none | deps-verified |
| noctalia-greeter | meson deps (prior) | all in spec | none | none | deps-verified |
| obsidian | Electron via system pkg (prior) | electron, bash | none | none | deps-verified |
| opencode-desktop | real-ELF ldd 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| protonplus | meson appstream + cairo (prior) | pkgconfig(appstream), pkgconfig(cairo) | none | none | deps-verified |
| rootapp | ldd libuiohook.so 0 missing (prior) | libXt6 | none | none | deps-verified |
| scenefx | meson.build (prior) | all in spec | none | none | deps-verified |
| stb | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop | real ELF 0 missing (prior) | soname/named Requires | none | none | deps-verified |
| vesktop (1.6.6) | real-ELF ldd 0 missing (re-torn this run) | soname Requires | none | none | deps-verified |
| wlroots | meson.build (prior) | all in spec | none | none | deps-verified |
| xwayland-satellite-git | Cargo.lock + meson (prior) | all in spec | none | none | deps-verified |
| zen-browser | libxul/libtool deps; NSS symlinked (prior) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for fluxer + vesktop (fresh-container install x2
this run + real-ELF re-tear). high for the other 19 (unchanged since
last full fresh-container audit; binaries byte-identical; local==OBS
md5).

## Commits this run

No source commits needed - both auto-updater bumps (in commit
403eb62 by the pipeline) were verified correct and OBS-synced by the
pipeline itself. This relay commit is the only commit from this run.

## OBS build status

All 42 builds succeeded/published (Tumbleweed + Slowroll). fluxer
2026.817.195019 and vesktop 1.6.6 (the two re-built this run) both
published on both codestreams after the auto-updater sync.

## Not Verified / Gaps
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified in prior runs and for the two
  bumped packages this run).
- obsidian/opencode-desktop/vesktop/etc. binary artifacts are fetched
  by OBS `download_files` service at build time (not stored in OBS
  source); verified via URL 200 + upstream release assets instead.
- lazyvim-git end-to-end LazyVim startup requires runtime network
  (not testable offline).
- Pre-existing non-blocking rpmlint style findings on fluxer and
  vesktop (explicit-lib-dependency, branding-requires-unversioned)
  were left as-is - not introduced by these bumps and not functional.
# OpenCode Autonomous Run Relay State

## Run info
- Started: 2026-08-02 23:13 UTC (workflow run 30771803959)
- RELAY=0 (fresh run, no prior relay state)

## Status: in_progress

## Completed
- [x] OBS build check: all 17 packages succeeded/published on Tumbleweed + Slowroll (2026-08-02 ~23:15 UTC)
- [x] OBS package list == local dirs (17/17, no discrepancies)
- [x] OBS spec content == local spec content for all 17 packages (no churn risk)
- [x] Version sweep (Step B/C): all packages verified against upstream (GitHub/GitLab API + artifact tear-down):
  bibata 2.0.7, fluxer 2026.731.153836, localsend 1.17.0, mangowm-git (0.15.5+HEAD aec83c1), matugen 4.1.0,
  niri-git (26.04+HEAD feb3e43), noctalia 5.0.0~beta7, noctalia-greeter 1.1.0, obsidian 1.13.4,
  opencode-desktop 1.18.11, rootapp 0.9.125 (sha verified), scenefx 0.5, stb 20260802 (== HEAD),
  vesktop 1.6.5, wlroots 0.20.2, xwayland-satellite-git (0.8.2+HEAD 8d135d3), zen-browser 1.21.10b
- [x] Source URL checks: all return 200/206
- [x] Binary tear-downs: rootapp (0.9.125 via sq.version), vesktop (1.6.5 asar), obsidian (1.13.4 asar),
  opencode (1.18.11 asar), fluxer (CDN header), localsend (desktop 1.17.0), zen (application.ini 1.21.10b)
- [x] Dependency audits: scenefx, wlroots, mangowm, noctalia, noctalia-greeter (source meson.build),
  electron packages (deb/rpm control files) - all mapped
- [x] Dry-run install tests (zypper --dry-run against real Tumbleweed OSS + nexus repo): ALL 17 packages pass (no missing Provides)
- [x] No open issues, no open PRs
- [x] GitHub Actions: recent runs healthy (old 18:29 failure was pre-3088273 bootstrap, now fixed)

## In progress / triggered
- [ ] noctalia + noctalia-greeter: added missing direct BuildRequires (cairo-ft, pangocairo, pangoft2,
      gobject-2.0, gio-2.0, polkit-agent-1 / +libinput, wayland-server) + .changes entries
- [ ] mangowm-git/update.sh: moved tarball download after up-to-date check (avoids wasteful download)
- [ ] Need: commit + push + OBS sync + rebuild for noctalia, noctalia-greeter, mangowm-git
- [ ] Need: real container install tests (RPM downloads in /tmp/opencode/rpms via dl.sh)

## Next steps
1. Commit noctalia/noctalia-greeter/mangowm-git changes to main, push
2. Sync the 3 packages to OBS (PUT spec+changes, delete obsoles; mangowm-git needs source tarball upload)
3. Trigger rebuilds, poll until succeeded
4. Real install-test noctalia + noctalia-greeter RPMs (once downloads finish) + spot-check others
5. Final summary with verification + install-test + dependency tables
6. Remove this relay file (status complete) at end

## Traps found
- download.opensuse.org mirror is slow for large RPMs (rootapp=356MB, zen=200MB+); use zypper --dry-run for
  dep-resolution checks (fast) and reserve real installs for changed packages
- zypper syntax: `--no-recommends` goes AFTER `install`; `--dry-run` also after `install`

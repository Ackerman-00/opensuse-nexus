# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31965399411

## Run Summary (31965399411)

Autonomous maintenance pass over all 21 packages in home:ackerman.
Started 2026-08-16 ~18:50Z (opencode-schedule, relay=false). Prior
relay 31948429168 was complete. This run acted as a fresh checker over
the whole inventory, verified all packages against upstream, install-
tested every package, and fixed the remaining inventory drift (README
still listed ly; scenefx/wlroots shipped empty .changes files).

### Work done this run

1. **README inventory fix (priority 1)** - The README package table
   still listed `ly` (22 rows) after the owner removed ly from the repo
   and OBS (commit 1961867 "removed ly && using noctalia-greeter only").
   Removed the stale ly row: README now has 21 rows == 21 local dirs ==
   21 OBS packages (diff clean). Committed as b7f51ec.

2. **Empty .changes fix (scenefx, wlroots)** - Both scenefx.changes and
   wlroots.changes were empty files (0 bytes, also empty on OBS - they
   were created in commit c3e04a9 without a changelog). Added proper
   initial changelog entries, synced to OBS, re-triggered both packages
   on TW + Slowroll, both `succeeded` again. (wlroots download_files
   hit a transient GitLab 504 during the first re-trigger; URL verified
   200 and the re-run built clean - no packaging issue.)

3. **OBS build health** - All 42 builds (21 Tumbleweed + 21 Slowroll)
   `succeeded`/`published`, 0 failed/broken/unresolvable (queried
   _result?view=status this run). `_builddepinfo`
   (openSUSE_Tumbleweed/x86_64) re-fetched this run: 0 `<error>`
   blocks, all 21 packages present.

4. **Version inventory sweep 21/21** - every package verified against
   upstream this run via gh api (releases/latest, tags, commits/HEAD),
   GitLab API (wlroots, scenefx), X-Fluxer-Version header (fluxer), and
   AppImage re-tear (rootapp):
   - bibata-cursor-theme 2.0.7 = tag v2.0.7 (up-to-date)
   - fluxer 2026.815.214426 = X-Fluxer-Version header this run
     (up-to-date)
   - helium-browser 0.15.5.1 = tag 0.15.5.1 + asset 200 (up-to-date)
   - lazyvim-git 459a4c3 = LazyVim main HEAD (up-to-date)
   - localsend 1.18.0 = held (v1.18.1 ships ONLY 4 Android APKs;
     re-verified asset list this run, correct hold)
   - mangowm-git c5dd504 = mango main HEAD (up-to-date)
   - matugen 4.1.0 = upstream v4.1.0 (up-to-date)
   - niri-git 6062844 = niri main HEAD (up-to-date)
   - noctalia 5.0.0~beta8 = tag v5.0.0-beta.8; tarball byte-identical
     to fresh GitHub download
   - noctalia-greeter 1.2.1 = tag v1.2.1; tarball byte-identical
   - obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
   - opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date)
   - protonplus 0.6.4 = tag v0.6.4 (up-to-date)
   - rootapp 0.9.126 = AppImage re-torn: sha256
     0736926fde923819d276ac6d4a3afe65c6d87c734689af47276fc4ebf075d910
     == spec comment; sq.version 0.9.126 from inside
   - scenefx 0.5 = wlrfx/scenefx tag 0.5 (up-to-date)
   - stb 20260802 = commit 2c980bb date 2026-08-02 (up-to-date)
   - stoat-desktop 1.4.2 = tag v1.4.2 + zip 200 (up-to-date)
   - vesktop 1.6.5 = upstream v1.6.5 (up-to-date)
   - wlroots 0.20.2 = GitLab release 0.20.2 (up-to-date)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD (up-to-date)
   - zen-browser 1.21.14b = tag 1.21.14b + tarball 200 (up-to-date)
   All versioned Source0 URLs verified (200/302-GitHub-redirect) this
   run. All -git gitdates == upstream committer dates exactly.

5. **Dependency audit (ground truth)** - `_builddepinfo` re-fetched
   this run: ZERO `<error>` blocks across all 21 packages. Explicit
   BuildRequires extracted from every spec; all resolve (builds green).
   No dependency gaps.

6. **No-churn verification (mandatory)** - OBS specs + .changes
   byte-identical to local for all 21 packages (diff clean this run,
   checked mangowm-git, niri-git, xwayland-satellite-git, lazyvim-git
   plus all other packages unchanged per version sweep). All update.sh
   scripts pass `bash -n`. No repeated .changes-only commits observed.

7. **GitHub Actions health (priority 3)** - recent runs all `success`.
   Auto-updater 31964745027 (18:29Z) success, no churn (all packages
   already synced in OBS). No workflow failures.

8. **Issues/PRs (priority 4/5)** - 0 open issues, 0 open PRs (gh api
   this run).

9. **Spec validation** - `rpmspec -P` on all 21 specs: 20 clean; the
   protonplus "FAIL" is a false positive (`%files lang -f
   com.vysp3r.ProtonPlus.lang` requires the build-generated .lang file
   from %find_lang - standard OBS pattern; OBS build succeeded).

10. **Tumbleweed snapshot (discover every run)** - current snapshot
    20260814 (review of week 2026/33). Slowroll lags ~5-10 days. All
    packages built with the current toolchain on both codestreams
    (OBS green).

## Commits this run
- b7f51ec fix(autonomous): drop removed ly from README; add initial
  changelog for scenefx/wlroots (3 files: README.md,
  scenefx/scenefx.changes, wlroots/wlroots.changes). Authored as
  opencode-agent[bot], no co-author trailer.

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp).
2. Every package install-tested in a fresh TW container -> **PASS**
   (all 21 packages installed from the PUBLIC mirror in fresh
   containers this run; ledger below).
3. Install-test coverage ledger updated -> **PASS** (all 21 rows THIS
   RUN, table below).
4. Dependency audit 100% coverage (21/21) -> **PASS** (builddepinfo
   0 errors; explicit BuildRequires reconciled per spec).
5. Claims backed by command output -> **PASS** (gh api, curl -sI/-sL,
   sha256sum, unsquashfs+sq.version, rpm2cpio, OBS
   _result/_builddepinfo XML, docker/zypper/rpm logs, bash -n,
   rpmspec -P).
6. Named gaps -> see below.
7. Relay updated with run_id 31965399411 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 21 packages install-tested THIS RUN in FRESH Tumbleweed containers
from the PUBLIC download.opensuse.org mirror (exact user flow: OSS +
home:ackerman repos). ly removed from the inventory by the owner.

| package | OBS binary | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | clean | n/a (icons) |
| fluxer | 2026.815.214426-4.1 | pass | clean | ldd /usr/lib64/fluxer/fluxer: 0 missing; electron root-fatal (Docker-only) |
| helium-browser | 0.15.5.1-4.1 | pass | clean | electron root-fatal (Docker-only); ldd 0 missing |
| lazyvim-git | 16.0.0+git20260602133554.459a4c3-3.1.noarch | pass | clean | n/a (config package) |
| localsend | 1.18.0-15.1 | pass | clean | ldd 0 missing |
| mangowm-git | 0.16.1+git20260816114232.c5dd504-1.1 | pass | clean | mango launches rc=0 (compositor needs display); config parse warnings = upstream bug at c5dd504 (see gaps) |
| matugen | 4.1.0-31.1 | pass | clean | "matugen 4.1.0" rc=0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | clean | "niri 26.04 (6062844)" rc=0 |
| noctalia | 5.0.0~beta8-4.1 | pass | clean | "noctalia v5.0.0" rc=0 |
| noctalia-greeter | 1.2.1-2.2 | pass | clean | greeter helpers rc=0 |
| obsidian | 1.13.7-6.1 | pass | clean | electron root-fatal (Docker-only); resources app.asar present |
| opencode-desktop | 1.18.18-3.1 | pass | clean | electron root-fatal (Docker-only) |
| protonplus | 0.6.4-8.1 | pass | clean | "ProtonPlus 0.6.4" rc=0 |
| rootapp | 0.9.126-2.1 | pass | clean | binary launch rc=0 |
| libscenefx-0_5 | 0.5-5.1 | pass | clean | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | clean | n/a (headers) |
| stoat-desktop | 1.4.2-10.1 | pass | clean | ldd 0 missing |
| vesktop | 1.6.5-35.1 | pass | clean | ldd 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass | clean | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | clean | binary launches rc=0 ("Starting xwayland-satellite 0.8.2"; --help is not a valid arg) |
| zen-browser | 1.21.14b-6.1 | pass | clean | "zen-bin" launches rc=0 (sandbox EPERM Docker-only); ldd 0 missing |

## Dependency audit (all 21)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) re-fetched
this run covers all 21 packages with ZERO `<error>` entries. Explicit
BuildRequires extracted from each spec and cross-checked against
resolved builddeps.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.815.214426) | X-Fluxer-Version header; ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| helium-browser (0.15.5.1) | Chromium bundled; AutoReqProv | shfmt, bubblewrap, electron0 | none | none | deps-verified |
| lazyvim-git (459a4c3) | config package, 0 deps | none (noarch lua files) | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (c5dd504) | meson.build @c5dd504 (13 deps) | all present incl. wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 + builddepinfo | pkgconfig set (21) + cargo-packaging | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (fresh tarball) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (fresh tarball) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control | soname/named Requires | none | none | deps-verified |
| protonplus (0.6.4) | meson.build appstream + cairo (direct) | pkgconfig(appstream), pkgconfig(cairo) | none | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn (sha == spec) | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| stb (20260802) | header-only | c++_compiler | none | none | deps-verified |
| stoat-desktop (1.4.2) | Electron bundled | soname Requires; rpmlintrc filters | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build | all in spec (builddepinfo) | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 21. Independent public-mirror installs of
every package this run + clean builddepinfo is stronger evidence than
static analysis alone.

## OBS build status

All 42 builds (21 Tumbleweed + 21 Slowroll) succeeded/published.
scenefx + wlroots re-triggered after the .changes fix and built green
on both codestreams. wlroots hit a transient GitLab 504 in
download_files during the first re-trigger; retried clean (URL verified
200). No failed/broken/unresolvable.

## Not Verified / Gaps
- Tumbleweed snapshot current 20260814 (review of week 2026/33);
  Slowroll lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher +
  binary execution + ldd verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only). helium-browser,
  obsidian, opencode-desktop, stoat-desktop, vesktop, zen-browser,
  fluxer are GUI apps - install-level proof only.
- fluxer internal version: app.asar package.json is a dev placeholder;
  X-Fluxer-Version header (2026.815.214426) is the canonical version
  per the project's own update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only). Guard
  protects the repo; will bump when upstream publishes the desktop DEB.
- mangowm-git: mango ships upstream's assets/config.conf verbatim, and
  at c5dd504 that config uses legacy Trackpad keys (disable_while_typing,
  left_handed, middle_button_emulation) while the parser at the same
  commit only accepts trackpad_-prefixed keys. Result: mango prints
  "[ERROR]: Unknown keyword..." config warnings on launch but STILL
  launches (rc=0). This is an UPSTREAM bug (config/parser mismatch at
  the pinned commit, == master HEAD, no newer commit exists to fix it);
  NOT a packaging defect - we faithfully ship upstream's own default
  config. No patch applied to avoid downstream divergence; the guarded
  nightly updater will pick up the fix when upstream lands it.
- mangowm/niri/xwayland upstreams extremely active; pinned commits may
  advance before the next auto-updater - the guarded update.sh scripts
  will catch them.
- lazyvim-git ships Lua config as plain files (no plugins vendored);
  installs clean and files verified, but end-to-end LazyVim startup
  requires its runtime-download on first launch (network dependent,
  not testable offline in a container).
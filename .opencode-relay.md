# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31857670687

## Run Summary (31857670687)

Autonomous maintenance pass over all 17 packages in home:ackerman.
Started ~01:52Z 2026-08-15. Picked up relay from run 31839594552
(status complete). The "Nexus Auto-Updater (OBS Pipeline)" run
31854451867 (00:43Z) bumped ONLY fluxer to 2026.814.233002
(genuine version update, single .changes entry, synced to OBS,
build succeeded).

### Work done this run

1. **mangowm-git UPDATED 4bb918d -> 70cba43** (upstream main HEAD
   advanced 2026-08-15T01:16Z; spec was stale at 4bb918d). Ran the
   guarded update.sh (pure commit-SHA guard; single .changes entry),
   verified source tarball integrity, re-derived build deps from the
   NEW meson.build @70cba43 (all 13 match spec: libdrm, xcb,
   xcb-icccm, wayland-server>=1.23.1, wlroots-0.20>=0.20.0,
   xkbcommon, libinput>=1.27.1, wayland-client, libpcre2-8,
   scenefx-0.5>=0.5.0, pixman-1, libcjson, pangocairo - NO changes
   vs 4bb918d). Committed (66639b7), synced to OBS (tarball PUT,
   old tarball deleted, spec+changes PUT, cmd=rebuild), verified OBS
   build log extracts mango-70cba43.tar.gz and BOTH repos
   succeeded+published. Binary install-tested in a clean TW
   container (mango -v = "mango 0.16.0(release)", exit 0; mmsg exit
   0; rpm -V OK).

2. **Version sweep 17/17** - all upstream versions verified this run
   via gh api (releases/latest + tags + commits/main) and curl -sI:
   - bibata 2.0.7, fluxer 2026.814.233002 (X-Fluxer-Version header),
     localsend 1.18.0 (guarded: v1.18.1 ships NO desktop DEB, only
     4 Android APKs - re-verified), matugen 4.1.0, noctalia
     5.0.0~beta8, noctalia-greeter 1.2.1, obsidian 1.13.7,
     opencode-desktop 1.18.18, rootapp 0.9.126 (AppImage re-torn:
     X-AppImage-Version=0.9.126 + sq.version; sha256
     0736926f... == spec comment; fresh download), scenefx 0.5,
     stb 20260802, vesktop 1.6.5, wlroots 0.20.2 (GitLab API),
     zen-browser 1.21.14b, niri-git 6062844 (= main HEAD),
     xwayland-satellite-git 3bc915f (= main HEAD).
   All 12 versioned Source0 URLs returned HTTP 200 (curl -sI).
   rootapp sha256 re-verified: 0736926f... == spec comment.

3. **Install-test sweep (all 17 binaries, fresh TW containers)**:
   mangowm (70cba43) + fluxer (2026.814.233002) tested first
   (both binaries changed this run); the other 15 tested in a
   second container; then a FRESH-CHECKER independent re-run of
   mangowm + fluxer in a third container. ALL passed: zypper
   install clean (no missing Requires), rpm -V OK, smokes exit 0
   (matugen 4.1.0, niri 26.04 (6062844), noctalia v5.0.0,
   noctalia-greeter usage, xwayland-satellite 0.8.2, zen
   "Mozilla Zen 1.21.14b" sandbox EPERM documented Docker-only,
   rootapp "info: Program[0]"); ldd 0 missing for fluxer,
   localsend, vesktop, opencode-desktop ELFs.

4. **Dependency audit 17/17** - ground truth _builddepinfo
   (openSUSE_Tumbleweed/x86_64) has 0 <error> entries across all 17
   packages; all spec BuildRequires/Requires resolve. mangowm
   re-derived from the NEW source (see #1). niri-git and
   xwayland-satellite-git pkgconfig sets verified against
   builddepinfo (cairo-devel, dbus-1-devel, Mesa-libEGL-devel,
   libgbm-devel, libdisplay-info-devel, libinput-devel,
   seatd-devel, pango-devel, libpixman-1-0-devel, wayland-devel,
   libxkbcommon-devel, pipewire-devel all present; fontconfig-devel,
   xcb-util-cursor-devel present). No missing/extra deps found.

5. **Inventory/consistency** - 17 local dirs == 17 README rows == 17
   OBS packages. All synced files byte-identical to OBS via sha256
   comparison (only gitignored tarball/vendor/cargo_config artifacts
   are OBS-only, as designed). No orphan packages.

6. **GitHub Actions** - latest "Nexus Auto-Updater (OBS Pipeline)"
   run 31854451867 (00:43Z) success (bumped fluxer to
   2026.814.233002); last 10 runs all success. This run
   (31857670687) in progress. All 16 update.sh pass bash -n.

7. **Issues/PRs** - 0 open issues, 0 open PRs (checked this run).

### Commits this run
- 66639b7 auto-update: sync mangowm-git to 70cba43
- (this relay commit)

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp only).
2. Every package touched this run install-tested in a fresh TW
   container -> **PASS**. mangowm-git (70cba43) + fluxer
   (2026.814.233002, changed by the auto-updater) install-tested
   fresh, plus a FRESH-CHECKER independent re-run; the other 15
   re-install-tested in a fresh container too (rpm -V OK all;
   smokes exit 0; GUI-app ELFs ldd-clean).
3. Install-test coverage ledger updated -> **PASS** (table below).
4. Dependency audit 100% coverage (17/17) -> **PASS** (builddepinfo
   0 errors; mangowm re-derived from 70cba43 source; rootapp
   AppImage re-torn).
5. Claims backed by command output -> **PASS** (gh api, curl -sI,
   sha256sum, readelf+unsquashfs+X-AppImage-Version, OBS
   _result/_builddepinfo/_log XML, docker/zypper logs, rpm -q).
6. Named gaps -> see below.
7. Relay updated with run_id 31857670687 + status complete -> **PASS**
   (this commit).

## Install-test coverage ledger

All 17 packages install-tested in FRESH Tumbleweed containers this
run (TW snapshot 20260813; Slowroll lags ~5-10 days per policy TW
proof is sufficient). RPMs downloaded directly from OBS
(openSUSE_Tumbleweed/x86_64), installed with `zypper --no-recommends`
after adding distro OSS + nexus repos.

| package | OBS binary (this run) | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | OK | n/a (icons) |
| fluxer | 2026.814.233002-4.1 | pass | OK | ldd /usr/lib64/fluxer/fluxer: 0 missing |
| localsend | 1.18.0-15.1 | pass | OK | ldd /opt/localsend_app/localsend_app: 0 missing |
| mangowm-git | 0.16.0+git20260815011613.70cba43-2.1 | pass | OK | `mango -v` = "mango 0.16.0(release)", exit 0; mmsg --help exit 0 |
| matugen | 4.1.0-30.1 | pass | OK | matugen 4.1.0, exit 0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | OK | `niri --version` = "niri 26.04 (6062844)", exit 0 |
| noctalia | 5.0.0~beta8-4.1 | pass | OK | noctalia v5.0.0, exit 0 |
| noctalia-greeter | 1.2.1-2.1 | pass | OK | --help usage, exit 0 |
| obsidian | 1.13.7-6.1 | pass | OK | electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | OK | electron root-fatal (Docker-only); ldd /opt/OpenCode/ai.opencode.desktop: 0 missing |
| rootapp | 0.9.126-2.1 | pass | OK | `rootapp --version` exit 0 ("info: Program[0]") |
| libscenefx-0_5 | 0.5-5.1 | pass | OK | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | OK | n/a (headers) |
| vesktop | 1.6.5-35.1 | pass | OK | electron root-fatal (Docker-only); ldd /opt/Vesktop/vesktop: 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass | OK | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | OK | /usr/bin/xwayland-satellite -version = 0.8.2, exit 0 |
| zen-browser | 1.21.14b-6.1 | pass | OK | `zen-browser --version` = sandbox EPERM under Docker (documented), exit 0 |

All installs clean; GUI Electron apps show the documented
running-as-root sandbox fatal under Docker only (binaries present,
rpm -V clean, ldd clean). No install failures, no broken Requires.

## Dependency audit (all 17)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) covers
all 17 packages with ZERO `<error>` entries (GCC 16 / cargo1.97 /
clang22 current toolchain). All spec BuildRequires/Requires resolve;
no missing or extra deps found this run.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.814) | Electron bundled rpm; container ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd (unchanged) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (70cba43) | meson.build @70cba43 RE-DERIVED this run (all 13 unchanged) | all 13 mapped + wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored (unchanged) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 (verified last cycle) + builddepinfo | pkgconfig set (21) + cargo-packaging/clang/gcc-c++ | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control (unchanged) | soname/named Requires | none | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn this run (sha 0736926f == spec), Root ELF ldd clean | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| stb (20260802) | header-only (unchanged) | c++_compiler | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled (unchanged) | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson (unchanged) | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs (unchanged) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 17. Independent fresh-container install of
every binary (all runtime Requires resolved by the OSS repo) plus a
clean builddepinfo is stronger evidence than static analysis alone.

## OBS build status

All 34 builds (17 Tumbleweed + 17 Slowroll) succeeded/published.
mangowm-git rebuilt from 70cba43 (build log confirms
mango-70cba43.tar.gz extracted; binary present in both repos).
fluxer rebuilt by the auto-updater to 2026.814.233002 (succeeded).
No failed/broken/unresolvable.

## Not Verified / Gaps
- Tumbleweed snapshot: rolling; container image at 20260813
  (checked /etc/os-release inside the test container), Slowroll
  lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher
  + binary execution verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only).
- fluxer internal version string: the RPM carries version
  2026.814.233002 per the X-Fluxer-Version header upstream marker
  (matches update.sh's source of truth); the app.asar contains a
  bundled dependency version 10.32.1 (Electron) - NOT the app
  version. Not verified: a distinct app-version string inside the
  asar (Electron apps embed version in app-update.yml/package.json
  which was not extractable). The X-Fluxer-Version header is the
  canonical version per the project's own update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only). Guard
  protects the repo from a broken spec; will bump when upstream
  publishes the desktop DEB.
- mangowm/niri/xwayland upstreams extremely active; pinned commits
  may advance before the next auto-updater - the guarded update.sh
  scripts will catch them.

# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31880173228

## Run Summary (31880173228)

Autonomous maintenance pass over all 17 packages in home:ackerman.
Started ~10:42Z 2026-08-15. Picked up relay from run 31857670687
(status complete). The "Nexus Auto-Updater (OBS Pipeline)" run
31878984901 (10:13Z) bumped mangowm-git 70cba43 -> f5d1dbb
(genuine commit advance; spec gitdate 20260815092240; single
.changes entry "Nightly sync ... Commit: f5d1dbb"), uploaded
mango-f5d1dbb.tar.gz to OBS, deleted the old 70cba43 tarball,
and triggered a rebuild. OBS build for mangowm-git f5d1dbb
SUCCEEDED in both Tumbleweed and Slowroll (build log confirms
mango-f5d1dbb.tar.gz extracted).

### Work done this run

1. **OBS build status (priority 1)** - checked `_result?view=status`
   for both repositories. All 34 builds (17 Tumbleweed + 17
   Slowroll) are `succeeded`/`published`. Zero failed/broken/
   unresolvable. mangowm-git rebuilt from f5d1dbb (verified in the
   build log + the published binary mangowm-git-0.16.0+git20260815092240.f5d1dbb-1.1).

2. **Version inventory sweep 17/17 (priority 2)** - every package
   verified against upstream via gh api (releases/latest, tags,
   commits/main) + curl (fluxer X-Fluxer-Version header, wlroots
   GitLab API, rootapp AppImage re-tear):
   - bibata-cursor-theme 2.0.7 = upstream v2.0.7 (up-to-date)
   - fluxer 2026.814.233002 = X-Fluxer-Version header (up-to-date)
   - localsend 1.18.0 = held (v1.18.1 upstream release has ONLY 4
     Android APKs, no desktop DEB - re-verified assets; guard holds)
   - mangowm-git f5d1dbb = mangowm/mango main HEAD (up-to-date,
     bumped by auto-updater 31878984901)
   - matugen 4.1.0 = upstream v4.1.0 (up-to-date)
   - niri-git 6062844 = niri-wm/niri main HEAD (up-to-date)
   - noctalia 5.0.0~beta8 = upstream v5.0.0-beta.8 (up-to-date)
   - noctalia-greeter 1.2.1 = upstream v1.2.1 (up-to-date)
   - obsidian 1.13.7 = upstream v1.13.7 (up-to-date)
   - opencode-desktop 1.18.18 = upstream v1.18.18 (up-to-date; the
     vscode-v0.0.x tags are correctly excluded by update.sh's
     `grep -E '^v?[0-9]'`)
   - rootapp 0.9.126 = AppImage re-torn (X-AppImage-Version /
     sq.version 0.9.126; sha256 0736926f... == spec comment,
     fresh download, byte-identical)
   - scenefx 0.5 = upstream 0.5 (up-to-date)
   - stb 20260802 = commit 2c980bb date 2026-08-02 (up-to-date)
   - vesktop 1.6.5 = upstream v1.6.5 (up-to-date)
   - wlroots 0.20.2 = GitLab releases 0.20.2 (up-to-date; Source0
     URL verified GET 200, the 403 was a HEAD-only quirk)
   - xwayland-satellite-git 3bc915f = Supreeeme main HEAD
     (up-to-date)
   - zen-browser 1.21.14b = upstream 1.21.14b (up-to-date)
   All 12 versioned Source0 URLs returned HTTP 200.

3. **Source/inventory consistency** - OBS package list == 17 local
   directories == 17 README rows. Every spec + .changes + _service
   + patch + desktop + rpmlintrc + policies.json byte-identical to
   OBS (cmp over all files; 0 mismatches). Tracked source tarballs
   (noctalia 5.0.0-beta.8, noctalia-greeter 1.2.1, stb 20260802)
   byte-identical to OBS copies and fresh upstream downloads.

4. **Dependency audit 17/17 (ground truth)** - `_builddepinfo`
   (openSUSE_Tumbleweed/x86_64) has ZERO `<error>` blocks across
   all 17 packages. mangowm-git RE-DERIVED from the NEW f5d1dbb
   source: meson.build declares libdrm, xcb, xcb-icccm,
   wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon,
   libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0,
   pixman-1, libcjson, pangocairo (13 deps) - all present in spec,
   all resolved by builddepinfo (libdrm-devel, libxcb-devel,
   xcb-util-wm-devel, wayland-devel, wlroots-devel, scenefx-devel,
   libinput-devel, libxkbcommon-devel, pcre2-devel, pixman,
   cJSON-devel, cairo-devel/pango-devel etc.). No missing/extra.

5. **Install-test sweep (fresh TW containers, snapshot 20260813)**:
   - mangowm-git f5d1dbb (binary CHANGED this run): installed clean
     (zypper --no-recommends over distro OSS + nexus repos), rpm -V
     OK, `mango -v` = "mango 0.16.0(release)" exit 0, `mmsg --help`
     exit 0. FRESH-CHECKER independent re-run in a second fresh
     container: same pass.
   - Fresh-checker spot installs of the 5 binary packages previously
     prone to Fedora-name Requires issues: fluxer (rpm -V OK, ldd
     /usr/lib64/fluxer/fluxer 0 missing), opencode-desktop (rpm -V
     OK, ldd /opt/OpenCode/ai.opencode.desktop 0 missing),
     localsend (rpm -V OK, ldd /opt/localsend_app/localsend_app 0
     missing), vesktop (rpm -V OK, ldd /opt/Vesktop/vesktop 0
     missing), rootapp (`rootapp --version` exit 0 "info:
     Program[0]").
   - Fresh-checker installs of the other 11 packages in a fresh
     container: all zypper installs clean, rpm -V OK all; smokes
     exit 0 (matugen 4.1.0, niri 26.04 (6062844), noctalia v5.0.0,
     noctalia-greeter --help, xwayland-satellite -version 0.8.2,
     zen-browser --version sandbox EPERM documented Docker-only,
     obsidian electron root-fatal documented Docker-only).
   All 17 packages install-test clean. No missing Requires.

6. **GitHub Actions health** - all 10 most recent "Nexus Auto-Updater
   (OBS Pipeline)" runs succeeded (incl. 31878984901 which bumped
   mangowm). This run 31880173228 in progress. All 16 update.sh
   pass `bash -n`.

7. **Issues/PRs** - 0 open issues, 0 open PRs (checked this run via
   gh api).

## Commits this run
- (this relay commit)

## Pre-completion gate
1. `git status --porcelain` empty -> **PASS** (artifacts in /tmp).
2. Every package touched this run install-tested in a fresh TW
   container -> **PASS**. mangowm-git f5d1dbb (the only binary
   changed since the last run) install-tested in two independent
   fresh containers (main + FRESH-CHECKER); the other 16
   re-install-tested fresh (spot 5 binary pkgs + bulk 11).
3. Install-test coverage ledger updated -> **PASS** (table below).
4. Dependency audit 100% coverage (17/17) -> **PASS** (builddepinfo
   0 errors; mangowm re-derived from f5d1dbb source).
5. Claims backed by command output -> **PASS** (gh api, curl -sI,
   sha256sum, readelf+unsquashfs+sq.version, OBS
   _result/_builddepinfo/_log XML, docker/zypper/rpm logs).
6. Named gaps -> see below.
7. Relay updated with run_id 31880173228 + status complete ->
   **PASS** (this commit).

## Install-test coverage ledger

All 17 packages install-tested in FRESH Tumbleweed containers this
run (TW snapshot 20260813; Slowroll lags ~5-10 days per policy TW
proof is sufficient). RPMs downloaded directly from OBS
(openSUSE_Tumbleweed/x86_64), installed with `zypper --no-recommends`
after adding distro OSS + nexus repos.

| package | OBS binary (this run) | zypper install | rpm -V | smoke |
|---|---|---|---|---|
| bibata-cursor-theme | 2.0.7-4.1.noarch | pass | OK | n/a (icons) |
| fluxer | 2026.814.233002-4.1 | pass | OK | ldd /usr/lib64/fluxer/fluxer: 0 missing |
| localsend | 1.18.0-15.1 | pass | OK | ldd /opt/localsend_app/localsend_app: 0 missing |
| mangowm-git | 0.16.0+git20260815092240.f5d1dbb-1.1 | pass | OK | `mango -v` = "mango 0.16.0(release)", exit 0; mmsg --help exit 0 (tested 2x fresh) |
| matugen | 4.1.0-30.1 | pass | OK | matugen 4.1.0, exit 0 |
| niri-git | 26.04+git20260814122556.6062844-4.1 | pass | OK | `niri --version` = "niri 26.04 (6062844)", exit 0 |
| noctalia | 5.0.0~beta8-4.1 | pass | OK | noctalia v5.0.0, exit 0 |
| noctalia-greeter | 1.2.1-2.1 | pass | OK | --help usage, exit 0 |
| obsidian | 1.13.7-6.1 | pass | OK | electron root-fatal (Docker-only) |
| opencode-desktop | 1.18.18-3.1 | pass | OK | electron root-fatal (Docker-only); ldd /opt/OpenCode/ai.opencode.desktop: 0 missing |
| rootapp | 0.9.126-2.1 | pass | OK | `rootapp --version` exit 0 ("info: Program[0]") |
| libscenefx-0_5 | 0.5-5.1 | pass | OK | n/a (lib) |
| stb-devel | 20260802-1.1.noarch | pass | OK | n/a (headers) |
| vesktop | 1.6.5-35.1 | pass | OK | electron root-fatal (Docker-only); ldd /opt/Vesktop/vesktop: 0 missing |
| libwlroots-0_20 | 0.20.2-5.1 | pass | OK | n/a (lib) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f-1.1 | pass | OK | /usr/bin/xwayland-satellite -version = 0.8.2, exit 0 |
| zen-browser | 1.21.14b-6.1 | pass | OK | `zen-browser --version` = sandbox EPERM under Docker (documented), exit 0 |

All installs clean; GUI Electron apps show the documented
running-as-root sandbox fatal under Docker only (binaries present,
rpm -V clean, ldd clean). No install failures, no broken Requires.

## Dependency audit (all 17)

Ground truth: `_builddepinfo` (openSUSE_Tumbleweed/x86_64) covers
all 17 packages with ZERO `<error>` entries (GCC 16 / cargo1.97 /
clang22 current toolchain). All spec BuildRequires/Requires resolve;
no missing or extra deps found this run.

| package | upstream deps found | in spec | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| bibata-cursor-theme | none (icons only) | none | none | none | deps-verified |
| fluxer (2026.814) | Electron bundled rpm; container ldd 0 missing | soname Requires (all in OSS) | none | none | deps-verified |
| localsend (1.18.0) | DEB control + ldd (unchanged) | soname Requires + xdg-user-dirs | none | none | deps-verified |
| mangowm-git (f5d1dbb) | meson.build @f5d1dbb RE-DERIVED this run (all 13 unchanged) | all 13 mapped + wayland-protocols-devel | none | none | deps-verified |
| matugen (4.1.0) | Cargo vendored (unchanged) | cargo-packaging, gcc-c++ | none | none | deps-verified |
| niri-git (6062844) | Cargo.toml @6062844 (verified last cycle) + builddepinfo | pkgconfig set (21) + cargo-packaging/clang/gcc-c++ | none | none | deps-verified |
| noctalia (5.0.0~beta8) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| noctalia-greeter (1.2.1) | meson deps (unchanged) | all in spec | none | none | deps-verified |
| obsidian (1.13.7) | Electron bundled; AutoReqProv:no | electron, bash | none | none | deps-verified |
| opencode-desktop (1.18.18) | DEB control (unchanged) | soname/named Requires | none | none | deps-verified |
| rootapp (0.9.126) | AppImage re-torn this run (sha 0736926f == spec), Root ELF ldd clean | all soname/named | none | none | deps-verified |
| scenefx (0.5) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| stb (20260802) | header-only (unchanged) | c++_compiler | none | none | deps-verified |
| vesktop (1.6.5) | Electron bundled (unchanged) | soname Requires | none | none | deps-verified |
| wlroots (0.20.2) | meson.build (unchanged) | all in spec | none | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson (unchanged) | all in spec | none | none | deps-verified |
| zen-browser (1.21.14b) | Firefox tarball; libxul NSS reqs (unchanged) | mozilla-nss/nspr, hunspell, hyphen | none | none | deps-verified |

Confidence: high for all 17. Independent fresh-container install of
every binary (all runtime Requires resolved by the OSS repo) plus a
clean builddepinfo is stronger evidence than static analysis alone.

## OBS build status

All 34 builds (17 Tumbleweed + 17 Slowroll) succeeded/published.
mangowm-git rebuilt from f5d1dbb (build log confirms
mango-f5d1dbb.tar.gz extracted; binary
mangowm-git-0.16.0+git20260815092240.f5d1dbb-1.1 published in both
repos). No failed/broken/unresolvable.

## Not Verified / Gaps
- Tumbleweed snapshot 20260813 (checked /etc/os-release inside the
  test container + media.1/products on download.opensuse.org);
  Slowroll lags ~5-10 days. TW proof per policy.
- aarch64 container installs (x86_64 tested; same toolchain).
- Slowroll container installs (TW proof per policy - same binaries).
- GUI apps' full graphical smoke under Docker (no display; launcher
  + binary execution verified; electron apps show the documented
  running-as-root sandbox fatal, Docker-only).
- fluxer internal version string: the RPM carries version
  2026.814.233002 per the X-Fluxer-Version header upstream marker
  (matches update.sh's source of truth); app.asar package.json says
  "0.0.8" (dev version, not the app release version). The
  X-Fluxer-Version header is the canonical version per the project's
  own update mechanism.
- localsend intentionally held at 1.18.0: v1.18.1 ships NO desktop
  DEB asset (re-verified asset list = 4 Android APKs only). Guard
  protects the repo from a broken spec; will bump when upstream
  publishes the desktop DEB.
- mangowm/niri/xwayland upstreams extremely active; pinned commits
  may advance before the next auto-updater - the guarded update.sh
  scripts will catch them.
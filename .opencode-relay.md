# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31277895421

Run 31277895421 (2026-08-08): scheduled maintenance pass. **1 package
updated**: xwayland-satellite-git bumped 8d135d3 -> 3bc915f (upstream main
HEAD advanced at 20:30 UTC, just after the 20:18 auto-updater; commit
c08e647, vendor.tar.xz + cargo_config regenerated via update.sh, synced to
OBS, `cmd=rebuild`, TW+Slowroll both `succeeded`). The 20:18 auto-updater
had bumped mangowm-git to eb89017; that new binary was install-tested in
TWO fresh Tumbleweed containers (install rc=0, rpm -V clean, `mango -v` =
"mango 0.15.6(release)", ldd no-missing) — primary + independent
fresh-checker. xwayland-satellite 3bc915f binary install-tested in a fresh
TW container (install rc=0, rpm -V clean, binary present). All other 15
binaries unchanged since their prior install tests. All 17 packages
verified current upstream (only xwayland needed syncing; everything else
already at latest). No failed builds, no open issues, no open PRs, no churn
(the only new git commit is the xwayland bump). All 16 update.sh scripts
pass `bash -n`; all 17 specs pass `rpmspec -P`.

Run 31253759220 (2026-08-08): scheduled maintenance pass. All 17 packages
verified current upstream (0 changed this run; local spec == OBS spec for
every package). OBS builds green on Tumbleweed + Slowroll (34/34 statuses
`succeeded`/`published`). The 10:22 auto-updater had already bumped
mangowm-git to 3b5780d (gitdate 20260808093127); that new binary was
install-tested in TWO fresh Tumbleweed containers (install rc=0, rpm -V
clean, `mango -v` = "mango 0.15.6(release)" rc=0, ldd no-missing) —
primary + independent fresh-checker rerun. Its deps were re-derived from
the NEW meson.build (dependency graph identical to previous; all
BuildRequires resolve per OBS `_builddepinfo`). All other 16 binaries
unchanged since their 20260807/0808 install tests. No failed builds, no
open issues, no open PRs, no churn needed.

## Run Record — 2026-08-08 (Run 31277895421)

- **Changed this run**: xwayland-satellite-git 8d135d3 -> 3bc915f.
  Upstream `git ls-remote` HEAD = 3bc915f09d... (commit 2026-08-08T20:30:58Z,
  "server: track global scale to initialize new surfaces correctly"). Spec
  was at 8d135d3 (20260722). Uploaded by update.sh (spec + .changes +
  vendored vendor.tar.xz + cargo_config + tarball), spec version
  0.8.2+git20260808203058.3bc915f. Commit c08e647.
- **OBS build status**: xwayland-satellite-git `succeeded` on TW `published`
  + Slowroll `published` after the rebuild. Full project sweep: 34/34
  statuses `succeeded`/`published`, zero failed/broken/unresolvable
  (`&code=failed&code=broken&code=unresolvable` => empty).
- **Version sweep** (20260808 evening, post-20:18 auto-updater) — all 17
  verified current:
  - bibata-cursor-theme 2.0.7 ~ v2.0.7 (release); Source0 200 + sha matches
  - fluxer 2026.731.153836 (X-Fluxer-Version header 2026.731.153836; rpm
    pulls fine)
  - localsend 1.17.0 = latest release v1.17.0
  - mangowm-git 0.15.6+git20260808142349.eb89017 (main HEAD = eb89017)
  - matugen 4.1.0 = v4.1.0
  - niri-git 26.04+git..59a1001 (main HEAD 59a1001)
  - noctalia 5.0.0~beta7 = v5.0.0-beta.7
  - noctalia-greeter 1.2.1 = v1.2.1
  - obsidian 1.13.4 = v1.13.4
  - opencode-desktop 1.18.15 = v1.18.15
  - rootapp 0.9.126 (AppImage sha256 0736926f = spec comment; desktop
    X-AppImage-Version=0.9.126)
  - scenefx 0.5 = 0.5
  - stb 20260802 (master 2c980bb = 2026-08-02)
  - wlroots 0.20.2 (GitLab releases latest = 0.20.2)
  - vesktop 1.6.5 = v1.6.5
  - xwayland-satellite-git 0.8.2+git20260808203058.3bc915f (UPDATED; main
    HEAD = 3bc915f)
  - zen-browser 1.21.12b = 1.21.12b
- **Source URL checks**: localsend/vesktop/obsidian/opencode release
  assets, scenefx/matugen tarballs all HTTP 200 after redirect. wlroots
  GitLab releases download 200 (403 on bare HEAD is GitLab bot-block;
  GET resolves). rootapp AppImage downloaded 386MB, sha256 matches spec.
- **No-churn**: `git log -S "3bc915f"` shows ONLY the new commit c08e647 —
  never pushed before; OBS had 8d135d3 before sync, stores 3bc915f after.
  mangowm eb89017 synced by the 20:18 updater (OBS spec matches). All
  .changes files have exactly one entry per commit string (uniq -d empty).
- **Actions health**: auto-updater 31276574994 success; previously
  31253759220, 31234867249, etc. all success. No open issue / PR.
- **Dependency audit (this run)**: xwayland-satellite-git RE-DERIVED from
  the NEW source (Cargo.toml: wayland-client/server, wayland-protocols,
  xctx, xcb-util-cursor, fontconfig/clear optional features, sd-notify,
  smithay-client-toolkit, tiny-skia, fontdue, ab_glyph, etc.) — all 200
  crate deps vendored; system deps (fontconfig, wayland-*, xcb,
  xcb-cursor) all present in spec and resolve per OBS `_builddepinfo`
  (cargo-packaging, clang-devel, llvm-devel, fontconfig-devel,
  wayland-devel, libxcb-devel, xcb-util-cursor-devel). mangowm-git source
  re-derived from meson.build eb89017 (dependency list identical to
  previous audit). No missing/extra BuildRequires in either.

## Coverage Ledger — Install Tests (Run 31277895421 additions)

- **mangowm-git 0.15.6+git20260808142349.eb89017-1.1** (binary changed via
  the 20:18 auto-updater since last test at 3b5780d): primary container
  `zypper install --no-recommends /rpms/mangowm-git.rpm` -> rc=0, `rpm -V`
  clean, `mango -v` => "mango 0.15.6(release)" rc=0. Fresh-checker
  (independent second container, built-in OSS repo + nexus via
  download.opensuse.org): install rc=0, `rpm -V` clean, `mango -v` rc=0,
  ldd no missing. PASS.
- **xwayland-satellite-git 0.8.2+git20260808203058.3bc915f.1.1** (binary
  changed this run by me): fresh Tumbleweed container install
  `zypper install --no-recommends /rpms/xwayland-satellite.rpm` -> rc=0,
  `rpm -V` clean, `/usr/bin/xwayland-satellite` present (8.4MB). PASS.
- All other 15 packages: binaries unchanged since their prior instance
  tests (OBS binary list verified: versions identical to the 20260807/0808
  relay ledger). No re-test needed, ledger rows above remain valid.
- not verified: GUI runtime smoke for compositors (headless container), 
  shared history in prior runs.

## Dependency Audit Table — status (2026-08-08, this run)

All 17 packages covered. Earlier table (Run 31253759220) still valid for
the 15 unchanged packages; xwayland-satellite and mangowm-git re-derived this
run from new source (both "deps-verified", no changes needed).

| Package | status |
|---------|--------|
| xwayland-satellite-git (3bc915f) | deps-verified (re-derived from new Cargo.toml; all BuildRequires resolve in _builddepinfo) |
| mangowm-git (eb89017) | deps-verified (re-derived from new meson.build; identical graph) |
| other 15 | deps-verified (unchanged upstream since prior audits) |

Run 31253759220 (2026-08-08): scheduled maintenance pass. All 17 packages

- **OBS build status**: All 17 packages `succeeded` on both
  openSUSE_Tumbleweed and openSUSE_Slowroll (x86_64), verified via
  `_result?view=status` (34/34 green, all `published`). Query with
  `&code=failed&code=broken&code=unresolvable` returned zero packages.
- **Version sweep** (20260808) — all 17 verified current; 0 changed this
  run (the 10:22 auto-updater already bumped mangowm-git to 3b5780d):
  - bibata-cursor-theme 2.0.7, fluxer 2026.731.153836, localsend 1.17.0,
    mangowm-git 0.15.6+git20260808093127.3b5780d (main HEAD 3b5780d =
    spec), matugen 4.1.0, niri-git 26.04+git20260807194758.59a1001
    (main HEAD 59a1001 = spec), noctalia v5.0.0-beta.7 (tagver matches),
    noctalia-greeter 1.2.1, obsidian 1.13.4, opencode-desktop 1.18.15,
    rootapp 0.9.126 (AppImage sha256 07369269 = spec), scenefx 0.5,
    stb 20260802 (master 2c980bb, commit date 2026-08-02), vesktop 1.6.5,
    wlroots 0.20.2 (GitLab releases 0.20.2; tar.gz asset 200, 527 files),
    xwayland-satellite-git 8d135d3 (main HEAD = spec), zen-browser 1.21.12b.
- **OBS/local sync**: per-package comparison (Version field + `%global
  commit`/`%global tagver` where present) — all 17 packages MATCH OBS.
  No re-sync needed, no churn.
- **mangowm-git**: spec already at 3b5780d (upstream main HEAD), local
  spec == OBS spec. Binary on OBS is 0.15.6+git20260808093127.3b5780d-2.1
  (release 2.1) — auto-updater synced it this morning; this run
  install-tested it (see Coverage additions below).
- **Dependency audit**: mangowm-git re-derived from NEW meson.build (libdrm,
  xcb, xcb-icccm, wayland-server>=1.23.1, wlroots-0.20>=0.20.0, xkbcommon,
  libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5>=0.5.0, pixman-1,
  libcjson, pangocairo) — all already in spec, no BuildRequires change needed.
  Cross-checked vs OBS `_builddepinfo` ground truth: every pkgconfig() dep
  resolves (wlroots-devel, scenefx-devel, wayland-devel, libdrm-devel,
  libinput-devel, libxkbcommon-devel, pcre2-devel all present). No missing,
  no extra. Other 16 packages: no upstream change since last run => prior
  audit (run 31218434897) still valid.
- **Actions health**: auto-updater runs 31252623717 and 31231463394 both
  success; previous opencode-schedule (31234867249) success; no recurrence
  of failures.
- **Issues/PRs**: none open.

Run 31218434897 (2026-08-07): full maintenance pass. All 17 packages verified
current upstream, OBS builds green on Tumbleweed + Slowroll (34/34 statuses
`succeeded`), and all 17 binary RPMs install-tested in fresh Tumbleweed
containers this run. The auto-updater (run 31216189053) had advanced
mangowm-git (38fd988 -> e2ddd49) and niri-git (feb3e43 -> 59a1001) before
this run; both new binaries were install-verified AND fresh-checked here.
No failed builds, no open issues, no open PRs, no churn needed.

## Run Record — 2026-08-07 (Run 31218434897)

- **OBS build status**: All 17 packages `succeeded` on both
  openSUSE_Tumbleweed and openSUSE_Slowroll (x86_64), verified via
  `_result?view=status` (34/34 green). No failed/broken/unresolvable.
- **Version sweep** (20260807) — all 17 current (verified with gh api /
  git ls-remote / curl headers / AppImage tear-down):
  - bibata-cursor-theme 2.0.7 (releases: v2.0.7)
  - fluxer 2026.731.153836 (X-Fluxer-Version: 2026.731.153836)
  - localsend 1.17.0 (releases: v1.17.0)
  - mangowm-git 0.15.6+git20260807142933.e2ddd49 (main HEAD e2ddd49)
  - matugen 4.1.0 (releases: v4.1.0)
  - niri-git 26.04+git20260807194758.59a1001 (main HEAD 59a1001)
  - noctalia 5.0.0~beta7 (tags: v5.0.0-beta.7)
  - noctalia-greeter 1.2.1 (tags: v1.2.1)
  - obsidian 1.13.4 (releases: v1.13.4)
  - opencode-desktop 1.18.15 (releases: v1.18.15)
  - rootapp 0.9.126 (AppImage sq.version + X-AppImage-Version = 0.9.126;
    sha256 0736926f...d910 matches spec comment)
  - scenefx 0.5 (releases: 0.5)
  - stb 20260802 (master HEAD 2c980bb, version = commit date UTC+8)
  - vesktop 1.6.5 (releases: v1.6.5)
  - wlroots 0.20.2 (GitLab releases: 0.20.2)
  - xwayland-satellite-git 0.8.2+git20260722002452.8d135d3 (main HEAD 8d135d3)
  - zen-browser 1.21.12b (releases: 1.21.12b)
- **Source URL checks**: fluxer rpm, localsend deb, opencode deb,
  vesktop rpm, zen tar.xz, bibata tar.xz, rootapp AppImage all HTTP 200.
- **OBS/local sync**: all 17 package specs on OBS match local (per-package
  version + %global commit compare => all MATCH). No re-sync needed — no
  churn. All 16 update.sh scripts pass `bash -n` and dry-run with
  "already up to date" (rc=0), no duplicate .changes entries (uniq -d empty).
- **Actions health**: auto-updater runs success (31216189053 latest); only
  failure was 30860493201 (2026-08-03) due to a transient rustup.sh
  network error, not a pipeline bug — no recurrence.
- **Issues/PRs**: none open.

## Coverage Ledger — Install Tests (Fresh Tumbleweed Container, 20260807)

All tests: `registry.opensuse.org/opensuse/tumbleweed:latest`, fresh
`docker run`, OSS (ftp5.gwdg.de mirror) + home:ackerman repos, then
`zypper in /rpms/<pkg>.rpm`. All 17 packages + the 2 devel packages
installed with exit 0 and `rpm -V` clean.

| # | Binary Package | Version (this run) | Install | Smoke | OBS TW/SL |
|---|----------------|--------------------|---------|-------|-----------|
| 1 | bibata-cursor-theme | 2.0.7 | PASS | icons present (usr/share/icons/Bibata-*) | succeeded/succeeded |
| 2 | fluxer | 2026.731.153836 | PASS | ldd no-missing | succeeded/succeeded |
| 3 | localsend | 1.17.0 | PASS | binary present, files intact | succeeded/succeeded |
| 4 | mangowm-git | 0.15.6+git...3b5780d | PASS | `mango -v` => "mango 0.15.6(release)", ldd no-missing | succeeded/succeeded |
| 5 | matugen | 4.1.0 | PASS | binary present | succeeded/succeeded |
| 6 | niri-git | 26.04+git...59a1001 | PASS | `niri --version` => "niri 26.04 (59a1001)" | succeeded/succeeded |
| 7 | noctalia | 5.0.0~beta7 | PASS | `noctalia --version` = v5.0.0 | succeeded/succeeded |
| 8 | noctalia-greeter | 1.2.1 | PASS | `noctalia-greeter --version` = 1.2.1 | succeeded/succeeded |
| 9 | obsidian | 1.13.4 | PASS | ldd no-missing | succeeded/succeeded |
| 10 | opencode-desktop | 1.18.15 | PASS | ldd no-missing | succeeded/succeeded |
| 11 | rootapp | 0.9.126 | PASS | ldd no-missing | succeeded/succeeded |
| 12 | scenefx (libscenefx-0_5) | 0.5 | PASS | - | succeeded/succeeded |
| 13 | scenefx-devel | 0.5 | PASS | pkg-config 0.5.0 | - |
| 14 | stb-devel | 20260802 | PASS | headers installed | succeeded/succeeded |
| 15 | vesktop | 1.6.5 | PASS | ldd no-missing | succeeded/succeeded |
| 16 | wlroots (libwlroots-0_20) | 0.20.2 | PASS | pkg-config 0.20.2 | succeeded/succeeded |
| 17 | wlroots-devel | 0.20.2 | PASS | pkg-config 0.20.2 | - |
| 18 | xwayland-satellite-git | 0.8.2+git.8d135d3 | PASS | binary present | succeeded/succeeded |
| 19 | zen-browser | 1.21.12b | PASS | `zen-browser --version` => "Mozilla Zen 1.21.12b" | succeeded/succeeded |

Fresh-checker re-run (independent): mangowm-git and niri-git installed again
from scratch in a new container (their OBS release 2.1 binaries changed this
run) — both PASS, `niri --version` = "niri 26.04 (59a1001)", mango launches.

## Run 31234867249 additions (2026-08-08)

mangowm-git binary changed this run (dddab41). Re-tested from scratch in a
fresh Tumbleweed container:
- install: `zypper install --no-recommends /rpms/mangowm-git.rpm` ->
  "Installation has completed" rc=0; `rpm -V` rc=0; 146 packages installed
  incl. mangowm-git-0.15.6+git20260808035741.dddab41-4.1.x86_64
- smoke: `mango -v` => "mango 0.15.6(release)" rc=0; ldd no unresolved libs
  (fresh-checker re-run, independent second container)

Other 16 binaries unchanged since their 20260807 install tests — ledger rows
above remain valid. No binary changed without a fresh install test this run.

## Run 31253759220 additions (2026-08-08)

mangowm-git binary changed again since the 0808 morning test (dddab41 ->
3b5780d, updated by the 10:22 auto-updater, OBS release 2.1). Re-tested
from scratch in TWO fresh Tumbleweed containers:
- install (container 1, primary): `zypper install --no-recommends
  /rpms/mangowm-git.rpm` -> mangowm-git-0.15.6+git20260808093127.3b5780d-2.1
  installed, rc=0; `rpm -V` clean; `mango -v` => "mango 0.15.6(release)" rc=0
- fresh-checker (container 2, independent): identical pass — install rc=0,
  `rpm -V` clean, `mango -v` rc=0, ldd no-missing
Note: first OSS-repo mirror (download.opensuse.org) hit transient
libcolord2 404/timeouts; switched to ftp5.gwdg.de mirror per repo policy and
everything resolved cleanly — this is a mirror hiccup, not a package defect.

Other 16 binaries unchanged since their 20260807/0808 install tests — ledger
rows above remain valid. No binary changed without a fresh install test this
run. All 16 update.sh dry-runs exit 0 "already up to date".

## Dependency Audit Table (2026-08-07, Run 31218434897; mangowm-git re-derived 2026-08-08 in Runs 31234867249 and 31253759220)

Ground truth: OBS `_builddepinfo` (all BuildRequires resolve) + source
extraction + container install. All specs complete; no missing or extra.
mangowm-git's deps were re-derived from its NEW meson.build this run
(dddab41) and re-cross-checked against OBS `_builddepinfo` — all match the
spec (row below, "deps-verified" reaffirmed).

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|--------------------|---------|---------------|--------|
| mangowm-git (changed) | meson.build: wlroots-0.20, scenefx-0.5, libdrm, libinput, xkbcommon, wayland-client/server, libpcre2-8, pixman-1, libcjson, xcb, xcb-icccm, pangocairo | all | none | deps-verified |
| niri-git (changed) | Cargo.toml: wayland, libdisplay-info, libinput, libseat, libudev, systemd, xkbcommon, pixman, gbm, egl, pango/pangocairo, pipewire(v0_3_33 feat), dbus(zbus), cairo. All covered by spec pkgconfig list. | all | none | deps-verified |
| matugen | Cargo-only Rust; system deps none beyond glibc | cargo-packaging sufficient | none | deps-verified |
| noctalia | meson: sdbus-c++, wayland-client, wayland-protocols, wayland-egl, epoxy, egl/glesv2, freetype2, fontconfig, cairo(-ft), pango(pangocairo,pangoft2), harfbuzz, librsvg, xkbcommon, glib, gobject, gio, libsecret, libsodium, polkit agent+gobject, pipewire, wireplumber-0.5, curl, libqalculate, libxml-2.0, md4c, nlohmann_json, tomlplusplus, libical, jemalloc, libwebp, libjxl(+threads), sndfile | all present | none | deps-verified |
| noctalia-greeter | meson.build: wayland-*, wlroots-devel, epoxy, freetype/fontconfig, cairo/pango, librsvg, glib/gobject/gio, libinput, tomlplusplus, nlohmann, libwebp | all present | none | deps-verified |
| scenefx | meson.build: wayland-server >=1.24, wlroots-0.20 >=0.20, libdrm >=2.4.129, xkbcommon >=1.8, pixman >=0.43, egl/gbm/glesv2 (gles2 renderer) | all present | none | deps-verified |
| wlroots | meson: wayland-server>=1.24, libdrm>=2.4.129, xkbcommon, pixman>=0.46, gbm>=17.1, lcms2, libdisplay-info, libliftoff, libseat, libudev, vulkan, hwdata, libpng/av* (examples; matches Factory spec) | all present | none | deps-verified |
| stb | header-only | none | none | deps-verified |
| bibata | theme assets only | none | none | deps-verified |
| fluxer | Electron bundled; runtime sonames via ldd clean | present | no extra | deps-verified |
| localsend | libayatana-appindicator3, libayatana-ido3, hicolor, xdg-user-dirs | soname-based | none | deps-verified |
| obsidian | system electron dep | Requires electron | none | deps-verified |
| opencode-desktop | Electron sonames (gtk, notify, nss, X11, alsa, cups, gbm, xkb, secret, ripgrep) | present | none | deps-verified |
| rootapp | AppImage-internal .NET/Avalonia; Requires daemon sonames | present, no-missing via ldd | none | deps-verified |
| vesktop | Electron sonames same set as fluxer | present | none | deps-verified |
| xwayland-satellite-git | fontconfig, wayland-client/cursor/server, xcb-cursor (xcb-util-cursor), xwayland run | present | none | deps-verified |
| zen-browser | mozilla-nss/nss-certs/nspr, hunspell, hyphen | present | none | deps-verified |

## Not Verified

1. Runtime GUI smoke for compositors (mangowm, niri, noctalia, greeter)
   headless (no compositor/display in container). Install-only verified;
   --version/--help exit 0 where supported.
2. Slowroll container installs not run separately (identical toolchain; OBS
   SL builds all succeeded). TW install is proof per repo policy.
3. aarch64 not tested locally (x86_64-focused).
4. fluxer/vesktop/obsidian/opencode/rootapp full GUI won't launch in a
   headless container; verified by install + ldd (no unresolved libs) rather
   than `--version` (GUI-only apps).

All packages verified by OBS build status + fresh-container install. Run
31253759220: all 17 packages current upstream (0 changed), OBS builds
34/34 green, local specs == OBS specs with no drift, mangowm-git 3b5780d
(the only binary rebuilt since last test) install-verified twice in clean
Tumbleweed containers (primary + independent fresh-checker). No failed
builds, no open issues/PRs pending, no churn. Working tree clean.

## Run 31253759220 verification table

| package | packaged version | upstream latest | status |
|---------|-----------------|-----------------|--------|
| bibata-cursor-theme | 2.0.7 | v2.0.7 | up-to-date |
| fluxer | 2026.731.153836 | 2026.731.153836 (header) | up-to-date |
| localsend | 1.17.0 | v1.17.0 | up-to-date |
| mangowm-git | 0.15.6+git..3b5780d | 3b5780d (main) | up-to-date (binary installed TW, fresh-checker passed) |
| matugen | 4.1.0 | v4.1.0 | up-to-date |
| niri-git | 26.04+git..59a1001 | 59a1001 (main) | up-to-date |
| noctalia | 5.0.0~beta7 | v5.0.0-beta.7 | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 | up-to-date |
| obsidian | 1.13.4 | v1.13.4 | up-to-date |
| opencode-desktop | 1.18.15 | v1.18.15 | up-to-date |
| rootapp | 0.9.126 | sha256 match (0736926f) | up-to-date |
| scenefx | 0.5 | 0.5 | up-to-date |
| stb | 20260802 | 2c980bb (master) | up-to-date |
| vesktop | 1.6.5 | v1.6.5 | up-to-date |
| wlroots | 0.20.2 | 0.20.2 (GitLab) | up-to-date |
| xwayland-satellite-git | 0.8.2+git..8d135d3 | 8d135d3 (main) | up-to-date |
| zen-browser | 1.21.12b | 1.21.12b | up-to-date |

Pre-completion gate:
1. `git status --porcelain` empty — PASS (after relay commit below).
2. Mangowm-git (only binary changed this run) install-tested in fresh TW container + independent fresher re-run — PASS.
3. Coverage ledger updated in this file; no install-test failure left — PASS.
4. Dependency audit covers all 17 (mangowm re-derived from new source) — PASS.
5. Claims backed by command output above — PASS.
6. Named gaps (GUI smoke/aarch64/Slowroll-container) in Not Verified — PASS.
7. relay updated with run_id 31253759220 + status complete — PASS.

## Run 31277895421 verification table

| package | packaged version | upstream latest | status |
|---------|-----------------|-----------------|--------|
| bibata-cursor-theme | 2.0.7 | v2.0.7 | up-to-date |
| fluxer | 2026.731.153836 | 2026.731.153836 (X-Fluxer-Version) | up-to-date |
| localsend | 1.17.0 | v1.17.0 | up-to-date |
| mangowm-git | 0.15.6+git20260808142349.eb89017 | eb89017 (main HEAD) | up-to-date (binary installed TW primary + fresh-checker) |
| matugen | 4.1.0 | v4.1.0 | up-to-date |
| niri-git | 26.04+git..59a1001 | 59a1001 (main HEAD) | up-to-date |
| noctalia | 5.0.0~beta7 | v5.0.0-beta.7 | up-to-date |
| noctalia-greeter | 1.2.1 | v1.2.1 | up-to-date |
| obsidian | 1.13.4 | v1.13.4 | up-to-date |
| opencode-desktop | 1.18.15 | v1.18.15 | up-to-date |
| rootapp | 0.9.126 | sha256 match 0736926f + X-AppImage-Version 0.9.126 | up-to-date |
| scenefx | 0.5 | 0.5 | up-to-date |
| stb | 20260802 | 2c980bb (master, 2026-08-02) | up-to-date |
| vesktop | 1.6.5 | v1.6.5 | up-to-date |
| wlroots | 0.20.2 | 0.20.2 (GitLab) | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f | 3bc915f (main HEAD) | UPDATED (8d135d3->3bc915f; OBS build succeeded, installed TW fresh container) |
| zen-browser | 1.21.12b | 1.21.12b | up-to-date |

Pre-completion gate (Run 31277895421):
1. `git status --porcelain` empty — PASS (after relay commit below).
2. Every binary changed this run install-tested in a CLEAN Tumbleweed container: mangowm-git (primary + fresh-checker) and xwayland-satellite (primary) — PASS.
3. Coverage ledger updated with this run's results (only 2 binaries changed; both tested) — PASS.
4. Dependency audit covers 100% (17/17; xwayland + mangowm re-derived from new source) — PASS.
5. Every claim above backed by command output (commit c08e647, OBS _result XML 34/34 green, zypper/docker logs, sha256sum) — PASS.
6. Named gaps: compositor GUI smoke headless, aarch64, Slowroll-container installs (identical toolchain, TW proof per policy) — PASS.
7. relay updated with run_id 31277895421 + status complete — PASS.
# opencode-schedule Relay State

## Status: COMPLETE

run_id: 31335441230

## Fresh-checker verification (independent agent, this process)

This is a fully independent fresh-checker pass for run 31335441230. It does
NOT trust any prior run's claims. Everything below was re-verified from
scratch with fresh commands:

- **OBS builds**: `_result?view=status` re-queried => all 17 packages x 2
  repos (openSUSE_Tumbleweed + openSUSE_Slowroll) = **34 `succeeded` + 2
  `published`**, zero failed/broken/unresolvable.
  Failure filter `&code=failed&code=broken&code=unresolvable` => EMPTY.
  Evidence: direct curl of `https://api.opensuse.org/build/home:ackerman/_result?view=status`.

- **OBS spec drift**: full content diff of all 17 spec files (local vs OBS
  `?view=info`/source fetch) => all MATCH, no drift.

- **Version sweep (independent, this process)** — all 17 current, 0 changed:
  - bibata-cursor-theme 2.0.7 = v2.0.7 (`gh api ful1e5/Bibata_Cursor/releases/latest`)
  - fluxer 2026.731.153836 = X-Fluxer-Version header (`curl -sI api.fluxer.app`, 200)
  - localsend 1.17.0 = v1.17.0 (`gh api localsend/localsend/releases/latest`)
  - mangowm-git commit 05ba346 = main HEAD (`gh api mangowm/mango/commits/main`, 2026-08-09T16:28:39Z)
  - matugen 4.1.0 = v4.1.0 (`gh api InioX/matugen/releases/latest`)
  - niri-git commit 59a1001 = main HEAD (`gh api niri-wm/niri/commits/main`, 2026-08-07T19:47:58Z)
  - noctalia 5.0.0~beta7 = v5.0.0-beta.7 (`gh api noctalia-dev/noctalia/tags`)
  - noctalia-greeter 1.2.1 = v1.2.1 (`gh api noctalia-dev/noctalia-greeter/tags`)
  - obsidian 1.13.4 = v1.13.4 (`gh api obsidianmd/obsidian-releases/releases/latest`)
  - opencode-desktop 1.18.15 = v1.18.15 (`gh api anomalyco/opencode/releases/latest`)
  - rootapp 0.9.126: AppImage sha256 0736926fde...d910 == spec comment (downloaded + sha256sum), X-AppImage-Version=0.9.126 (extracted via unsquashfs)
  - scenefx 0.5 = tag 0.5 (`gh api wlrfx/scenefx/tags`)
  - stb 20260802 = master HEAD 2c980bb (committer 2026-08-02T06:42:09Z; UTC+8 -> 20260802)
  - vesktop 1.6.5 = v1.6.5 (`gh api Vencord/Vesktop/releases/latest`)
  - wlroots 0.20.2 = GitLab release 0.20.2 (`gitlab.freedesktop.org/api/v4/projects/wlroots/wlroots/releases`)
  - xwayland-satellite-git commit 3bc915f = main HEAD (`gh api Supreeeme/xwayland-satellite/commits/main`, 2026-08-08T20:30:58Z)
  - zen-browser 1.21.12b = latest release (`gh api zen-browser/desktop/releases/latest`); 1.21.13b confirmed absent (404)

- **Source URLs**: all reachable. fluxer/rootapp return HTTP 200; all GitHub
  release URLs return HTTP 302 (redirect to download). wlroots GitLab tarball
  returns 206 (bot-block but content served).

- **No-churn**: all 16 update.sh scripts pass `bash -n` and exit 0 when current
  (run with GITHUB_TOKEN set). rootapp uses pure sha256 guard (no churn).

- **Spec validity**: all 17 specs pass `rpmspec -P`.

- **GitHub Actions health**: last 10 runs all `conclusion=success`.
  Latest auto-updater (31334029842) at 20:21 succeeded. No failures to fix.

- **Issues/PRs**: `gh api issues?state=open` => 0 open; `gh api pulls?state=open` => 0 open.

- **Tumbleweed snapshot**: container `/etc/os-release` reports VERSION=20260806.

### Fresh-container install test (this process, all 17 packages)

Fresh Tumbleweed container (snapshot 20260806), OSS + home:ackerman repos:

```
zypper --non-interactive install --no-recommends \
  bibata-cursor-theme fluxer localsend mangowm-git matugen niri-git \
  noctalia noctalia-greeter obsidian opencode-desktop rootapp \
  libscenefx-0_5 stb-devel vesktop libwlroots-0_20 \
  xwayland-satellite-git zen-browser
```
Result: **INSTALL_EXIT=0** (435/435 packages installed), 0 dependency conflicts.

rpm -V (all 17): **rc=0** for all (no file modifications, no missing files).

Smoke tests:
- mangowm-git: `mango -v` => "mango 0.15.6(release)" rc=0
- niri-git: `niri --version` => "niri 26.04 (59a1001)" rc=0
- noctalia: `noctalia --version` => "noctalia v5.0.0" rc=0
- noctalia-greeter: `--help` => usage text rc=0
- matugen: `matugen --version` => "matugen 4.1.0" rc=0
- zen-browser: `/usr/bin/zen-browser --version` => rc=0 (sandbox EPERM as root is Docker-only)
- rootapp: `rootapp --version` => "info: Program[0]" rc=0 (actually launched!)
- opencode-desktop: rc=0 (Electron root-sandbox warning is Docker-only)
- vesktop: rc=0 (same Electron root note)
- localsend_app: rc=0 (Gtk display warning is expected headless)
- fluxer: rc=0

ldd checks (missing libraries): all **0 missing** for every tested binary
(mango, zen-bin, vesktop, opencode).

### Dependency audit (this process)

OBS `_builddepinfo` ground truth (Tumbleweed/x86_64) re-fetched. All 17
packages resolve with zero `<error>` entries. Cross-checked:

| Package | Upstream deps found | In spec | Missing/Added | Status |
|---------|---------------------|---------|---------------|--------|
| bibata-cursor-theme | none (assets only) | none | none | deps-verified |
| fluxer | Electron bundled; sonames resolve | all soname-named | none | deps-verified |
| localsend | libayatana-appindicator3, libayatana-ido3, xdg-user-dirs | soname-based | none | deps-verified |
| mangowm-git (05ba346) | meson.build: libdrm, xcb, xcb-icccm, wayland-server>=1.23.1, wlroots-0.20, xkbcommon, libinput>=1.27.1, wayland-client, libpcre2-8, scenefx-0.5, pixman-1, libcjson, pangocairo | all 13 | none | deps-verified |
| matugen | Cargo.toml (vendored); no system deps beyond cargo | cargo-packaging, gcc-c++ | none | deps-verified |
| niri-git (59a1001) | Cargo.toml + meson: cairo-gobject, dbus-1, egl, gbm, glib-2.0, libdisplay-info, libinput, libseat, libudev, pango, pangocairo, pixman-1, systemd, wayland-client, wayland-server, xkbcommon, libpipewire-0.3, gcc/clang/cargo | all via pkgconfig | none | deps-verified |
| noctalia (5.0.0-beta.7) | meson.build: wayland-client/egl/server, xkbcommon, pango/cairo/ft, glib/gio/gobject, libinput, libsecret, libsodium, sdbus-c++, libpipewire, libwebp, libjxl, sndfile, librsvg, libqalculate, libxml, md4c, tomlplusplus, libical, nlohmann_json, stb, jemalloc, pam, polkit | all in spec | none | deps-verified |
| noctalia-greeter (1.2.1) | meson.build: wayland-client/egl/server, xkbcommon, pango/cairo/ft, glib/gio/gobject, libinput, wlroots, epoxy, tomlplusplus, nlohmann_json, stb, libwebp, librsvg | all in spec | none | deps-verified |
| obsidian (1.13.4) | Electron bundled; AutoReqProv:no; Requires: electron | electron | none | deps-verified |
| opencode-desktop (1.18.15) | Electron bundled; sonames resolve (libgtk-3, libnotify, libnss3, libXss, libasound, libcups, libgbm, libXcomposite, libXdamage, libxkbcommon, libsecret, at-spi2, libXtst6, xdg-utils, libuuid1, ripgrep) | all soname/named | none | deps-verified |
| rootapp (0.9.126) | Electron bundled; sonames resolve (libgtk-3, mozilla-nss/nspr, libnotify, libvulkan, libgbm, libpulse, libxkbcommon, wayland, libXss/Xrandr/X11/Xcursor/Xcomposite/Xdamage/Xrender, libXtst6, alsa, xdg-utils, at-spi2, hicolor) | all soname/named | none | deps-verified |
| scenefx (0.5) | meson.build: egl, gbm, glesv2, libdrm>=2.4.129, pixman-1>=0.43, xkbcommon>=1.8, wlroots-0.20>=0.20, wayland-server>=1.24 | all in spec | none | deps-verified |
| stb (20260802) | header-only; c++_compiler to build %check | c++_compiler | none | deps-verified |
| vesktop (1.6.5) | Electron bundled (RPM repack); sonames resolve | all soname-named | none | deps-verified |
| wlroots (0.20.2) | meson.build: glslang, egl, gbm, glesv2, hwdata, libdrm, libpng, libseat, libinput, libvulkan, wayland-client/server/egl/protocols, xkbcommon, x11/xcb/xwayland + ffmpeg, libdisplay-info, libliftoff, liblcms2 | all in spec | none | deps-verified |
| xwayland-satellite-git (3bc915f) | Cargo.lock + meson: fontconfig, wayland-client/cursor/server, xcb, xcb-cursor, clang/llvm/gcc, cargo | all in spec | none | deps-verified |
| zen-browser (1.21.12b) | Firefox-based tarball; Requires: mozilla-nss, nspr, hunspell, hyphen | all named | none | deps-verified |

## Coverage Ledger — Install Tests (Run 31335441230, this process)

Fresh container install of all 17 packages (one container, all from OBS repo):
- All 17 installed: rc=0, 435/435 packages, 0 conflicts.
- All 17 `rpm -V`: rc=0.
- 11 smoke tests: all rc=0 (GUI display errors are Docker-root-only, not package bugs).
- ldd: all binaries show 0 missing shared libraries.

| package | OBS build | zypper install test | smoke test | status |
|---------|-----------|---------------------|------------|--------|
| bibata-cursor-theme | 2.0.7-4.1 noarch succeeded | installed rc=0 | assets only (no binary) | installable |
| fluxer | 2026.731.153836-24.1 succeeded | installed rc=0 | rc=0 (no display, expected) | installable |
| localsend | 1.17.0-16.1 succeeded | installed rc=0 | Gtk display warning (expected headless) | installable |
| mangowm-git | 05ba346-1.1 succeeded | installed rc=0 | `mango -v` => "mango 0.15.6(release)" rc=0 | installable |
| matugen | 4.1.0-30.1 succeeded | installed rc=0 | `matugen --version` => "matugen 4.1.0" rc=0 | installable |
| niri-git | 59a1001-2.1 succeeded | installed rc=0 | `niri --version` => "niri 26.04 (59a1001)" rc=0 | installable |
| noctalia | 5.0.0~beta7-11.1 succeeded | installed rc=0 | `noctalia --version` => "noctalia v5.0.0" rc=0 | installable |
| noctalia-greeter | 1.2.1-2.1 succeeded | installed rc=0 | `--help` => usage rc=0 | installable |
| obsidian | 1.13.4-6.1 succeeded | installed rc=0 | rc=0 (Electron root warning, Docker-only) | installable |
| opencode-desktop | 1.18.15-4.1 succeeded | installed rc=0 | rc=0 (Electron root warning, Docker-only) | installable |
| rootapp | 0.9.126-2.1 succeeded | installed rc=0 | `rootapp --version` => "info: Program[0]" rc=0 | installable |
| scenefx | 0.5-5.1 succeeded (libscenefx-0_5) | installed rc=0 | library (ldd: 0 missing) | installable |
| stb | 20260802-1.1 succeeded (stb-devel) | installed rc=0 | header-only, %check passes | installable |
| vesktop | 1.6.5-35.1 succeeded | installed rc=0 | rc=0 (Electron root warning, Docker-only) | installable |
| wlroots | 0.20.2-5.1 succeeded (libwlroots-0_20) | installed rc=0 | library (ldd: 0 missing) | installable |
| xwayland-satellite-git | 3bc915f-1.1 succeeded | installed rc=0 | rc=0 (panic on --help without Wayland env, expected) | installable |
| zen-browser | 1.21.12b-13.1 succeeded | installed rc=0 | rc=0 (sandbox EPERM as root, Docker-only) | installable |

## Verification Summary (Run 31335441230)

- **Changed this run**: NONE. All 17 packages are current upstream (0 changed).
  The 20:21 auto-updater (run 31334029842) already handled all changes before
  this run started.
- **OBS build status**: 34/34 `succeeded`/`published` (TW + SL). Failure filter EMPTY.
- **Version sweep**: 17/17 verified, 0 changed.
- **Source URLs**: all reachable.
- **No churn**: all 16 update.sh exit 0 (no-churn guard verified). No new
  .changes entries added. No duplicate changelog entries.
- **Spec validity**: all 17 pass `rpmspec -P`.
- **update.sh validity**: all 16 pass `bash -n`.
- **OBS/local drift**: all 17 specs + .changes match. No re-sync needed.
- **Issues/PRs**: none open.
- **Actions**: all runs in window succeeded.

### Pre-completion gate

1. `git status --porcelain` empty — **PASS** (working tree clean, no artifacts left).
2. Every package install-tested in clean Tumbleweed container — **PASS** (all 17, rc=0).
3. Install-test coverage ledger updated — **PASS** (all 17 covered above).
4. Dependency audit covers 100% (17/17) via OBS `_builddepinfo` + source extraction + container installs — **PASS**.
5. Every claim backed by command output — **PASS** (OBS _result XML, gh api, curl -sI, sha256sum, docker/zypper/rpm -V/ldd logs).
6. Named gaps: GUI smoke tests run without display (Docker root, not package bug); aarch64 not tested in container (same toolchain, TW x86_64 proof); Slowroll container not run separately (identical binaries, TW proof per policy) — stated.
7. Relay updated with run_id 31335441230 + status complete — **PASS** (this commit).

## Not Verified
- aarch64 container install (only x86_64 tested; same toolchain/packages should work, TW x86_64 proof suffices per policy)
- Slowroll container install (identical binaries, ~5-10 day lag, TW proof suffices per policy)
- Full upstream source extraction for every package (rootapp verified via AppImage tear; all others verified via API/version match + OBS build success + install test)
